T040 — Feedback layer: Toaster + 8 enumerated success toasts + inline failure surfacing (F401)
Independent re-verify on branch t040-toast-feedback tip 8450320, baseSha 7012e3b.
SCOPE (excl .ailoop): playwright.config(registration), App, ApplicationCard, LayoutEditor, LibraryToolbar,
  hooks/queries, queries/useApplications, toasts.spec(new). All ⊆ declared (LibraryToolbar added [0093]).
  EntryEditor/ProfileEditor/LibraryView/SettingsView declared-but-untouched (toasts live in shared hooks). CLEAN.
FAST BASELINE: check 0 · build 0 · lint 0 (255) · vitest 1077/1077.
WHAT: <Toaster/> mounted ONCE in App (sonner default = non-modal, auto-dismiss, no duration:Infinity).
  8 success toasts at correct call-sites: create/duplicate/delete (useApplications hook onSuccess),
  entry-save/profile-save (hooks/queries onSuccess), layout-save (LayoutEditor.handleSave call-site — NOT
  the shared debounced useUpdateSettings), import/export (LibraryToolbar call-sites). Inline (non-toast)
  failures via flagVoice pattern: duplicate/delete (application-card-error), invalid import (import-error).
GAMING READ: CLEAN (red-team #15). toasts.spec = 8 SEPARATE per-mutation tests, each asserts toast count 0
  before, exactly 1 after matched by message, then 0 (auto-dismiss) — individual, not a shared loop.
  2 failure contrasts assert REAL server error (duplicate 404, invalid import 400 via response.status()) +
  inline error visible + toast count 0. Toasts can't be firing spuriously (each test starts at count 0).
Builder self-ran toasts.spec (applications) 10/10. Full playwright (incl cohesion non-modality re-run) at P3 gate.
