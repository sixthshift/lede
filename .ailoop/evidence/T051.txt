T051 — Library per-row Edit + identity line + toolbar (OQ4/F502/F509). Re-verify branch t051-library-edit tip 53b5373, baseSha 1e1eafb.
SCOPE (excl .ailoop): EntryCard, LibraryToolbar, LibraryView, SectionAccordion(+[0130]), helpers/workspace, library-crud.spec. cohesion.spec declared-defensively-but-untouched (helper rewrite fixed it). All ⊆ declared. CLEAN.
BASELINE: check 0 · build 0 · lint 0 · vitest 1077/1077.
GAMING CLEAN: LibraryView dropdown GONE (0 refs to "Choose entry to edit"/"Edit selected"/editTargetId). EntryCard gains onEdit + Pencil Edit button beside Delete + F509 identity line ("Company · Role · Period" from META_FIELDS; suppressed for label sections + verbatim-fact echoes — structurally justified, not test-chasing). SectionAccordion threads onEdit per row (mirrors onDelete, minimal). LibraryToolbar Import/Export → outline buttons (behavior/toasts intact). Delete stays two-step (T016). library-crud.spec migrated: Edit-selected→per-row; +3 tests (dropdown gone count===0, per-row edit loads correct data with two-row CONTRAST, identity-line render). helpers/workspace openEditFor rewritten to per-row Edit → fixed cohesion:253 with no cohesion edit. library-crud 23/23 (chromium), cohesion 10/10 (applications). library.test/library-filter.test unit tests kept green via identity-line de-dup logic (not edited). Full playwright at P4 gate.
