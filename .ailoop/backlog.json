{
  "$doc": [
    "Loop driver for Lede v2 (provider-agnostic/BYOK/auth/Drizzle spec).",
    "Phase 0 detailed (it IS the whole risk, §24); Phases 1-4 coarse epics,",
    "decomposed as Phase 0 teaches the shape. Ready/batches/breaches come from",
    "schedule.mjs, never by eye. files is a CONTRACT (verifier fails undeclared",
    "touches; package.json + bun.lock allowlisted). Since nothing type-checks",
    "without T001's tsconfig, every ticket transitively depends on T001.",
    "Acceptance hardened by the intake red-team (ledger [v2-004]) — contrast",
    "checks, not existence checks. Two structural holes closed: (A) fixture",
    "provenance via the shared eval-core T016 + the Phase-0 live-reproduces-",
    "fixtures gate; (B) tag-scoring via the tag-shuffle invariance control."
  ],
  "project": "lede",
  "caps": {
    "maxAttempts": 3,
    "thrash": 2,
    "chunk": 30
  },
  "tickets": [
    {
      "id": "T001",
      "title": "Replace Bun scaffold with Fastify + Vite/React skeleton (bun toolchain)",
      "status": "done",
      "depends_on": [],
      "files": [
        "tsconfig.json",
        "tsconfig.server.json",
        "vite.config.ts",
        "index.html",
        ".env.example",
        "src/server/index.ts",
        "src/client/main.tsx",
        "src/client/App.tsx",
        "src/client/styles/app.css",
        "src/client/styles/print.css",
        "src/main.ts",
        "src/routes/health.ts"
      ],
      "origin": "spec §3, §21, §24 Phase 0",
      "context": "Establish the §21 layout on the LOCKED stack (§3.1) but with the BUN toolchain (oracle.md 'Toolchain'): Fastify 4 API + Vite/React 18 SPA, one process. DELETE the existing Bun.serve scaffold (src/main.ts, src/routes/health.ts) — replaced, do not keep Bun.serve. Set up: tsconfig (client, ESM, react-jsx, path alias @shared/* → src/shared/*, include src/**), tsconfig.server.json, vite.config.ts (react plugin, @shared alias, dev proxy /api → http://localhost:8787), index.html mounting src/client/main.tsx (bare App shell), src/client/styles/{app.css,print.css} stubs. src/server/index.ts: minimal Fastify server booting on PORT (default 8787, §17) serving GET /api/health → {ok:true} (in prod also serves dist/ static — TODO stub fine). package.json scripts (bun): dev (concurrently dev:api+dev:web), dev:api (bun --watch src/server/index.ts), dev:web (vite), build (vite build), check (tsc --noEmit both configs), start (bun src/server/index.ts), test (vitest run). Deps per §20 (fastify, react, react-dom, vite, @vitejs/plugin-react, vitest, concurrently, typescript). Locked: PORT 8787, Fastify not Bun.serve, ESM, @shared.",
      "acceptance": "- `bun install` succeeds; `bun run check` passes; `bun run build` emits dist/index.html\n- server boots (`bun run start`); `curl localhost:8787/api/health` served by the running Fastify instance → `{\"ok\":true}`\n- src/main.ts & src/routes/health.ts DO NOT exist; `grep -r 'Bun.serve' src` → no hits; src/server/index.ts imports fastify\n- meta-check: tsconfig include covers src/**, so a deliberate type error in src FAILS `bun run check`\n- an `@shared/*` import resolves under BOTH check and build; vite proxies /api → :8787\n- pure scaffold: no behavior tests required (stated)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-02): `bun run check`→exit 0; `bun run build`→dist/index.html + assets, exit 0; `bun run test`→'No test files found, exiting with code 0' (passWithNoTests, no dummy test); server boot `curl localhost:8787/api/health`→{\"ok\":true}. Scope: 14 touched paths all ⊆ declared ∪ {package.json,bun.lock}; no drift. Gaming read: test script = 'vitest run --passWithNoTests' (real green-on-empty), tsconfig include=['src/**/*'] (meta-check holds), src/server/index.ts imports fastify, no Bun.serve. Needed one re-dispatch to make the test runner green-on-empty."
    },
    {
      "id": "T002",
      "title": "Shared domain types (Entry, EntryMeta, TailorDecision, TailoredResume)",
      "status": "done",
      "depends_on": [
        "T001"
      ],
      "files": [
        "src/shared/types.ts"
      ],
      "origin": "spec §4.1, §5",
      "context": "Author src/shared/types.ts EXACTLY per §4.1 and §5: Section (10-member union), EntryMeta (discriminated union on `section`), Entry (id, section, meta, facts=FACT-LOCK, tags, framings?, sortKey), Profile (§4.2 shape), ProviderId ('anthropic'|'openai'|'google'|'openai-compatible'), JDSignals, TailorDecision (the MODEL's flat output — signals, summary, items[{entryId,text,rank,leadRationale?}], cut[]), TailoredItem/Group/Section/Resume (SERVER-assembled), Layout = { section: Section|'summary'; enabled: boolean }[]. Imported by both server and client via @shared. Locked (oracle): model returns judgment only; facts are the fact-lock; tags never for scoring.",
      "acceptance": "- `bun run check` passes\n- types match §4.1/§5 field-for-field (EntryMeta discriminated on section; TailorDecision flat with per-item rank; TailoredResume is the assembled shape)\n- types-only ticket: no runtime tests required (stated)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-02): `bun run check`→exit 0; `bun run test`→exit 0 (green-on-empty, no test file added). Scope: only src/shared/types.ts touched. Correctness read: file matches spec §4.1/§5 field-for-field — EntryMeta is a discriminated union on `section` (10 variants), TailorDecision is FLAT (signals/summary/items[]{entryId,text,rank,leadRationale?}/cut[]), TailoredResume is the nested assembled shape (sections→groups→items); Profile/Layout/ProviderId/JDSignals all exported. No runtime code, no gaming."
    },
    {
      "id": "T003",
      "title": "Section registry (SECTIONS)",
      "status": "done",
      "depends_on": [
        "T002"
      ],
      "files": [
        "src/shared/sections.ts"
      ],
      "origin": "spec §4.3",
      "context": "Author src/shared/sections.ts: the SECTIONS: Record<Section, {label; rephrase:'full'|'light'|'none'; order:'recency'|'relevance'|'manual'; groupBy?}> registry VERBATIM per §4.3 (experience/project full+recency+groupBy; education light+recency+groupBy; award/cert/publication recency; reference manual; skill none+relevance+groupBy category; interest relevance; language manual). Export SECTION_VALUES (the 10 keys). SINGLE SOURCE (§17) for rephrase/order/group — consumed by prompt, assemble, render — must be exact.",
      "acceptance": "- `bun run check` passes; `bun run test` green (config-only, test-exempt per baseline — nothing behavioral to test; consumed by T008 assemble tests later)\n- correctness (coordinator diff-read vs §4.3): SECTIONS has ALL 10 sections, each with the exact rephrase/order/groupBy triple, and SECTION_VALUES exports the 10 keys\n- do NOT add a test file (not in declared files)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (fan-out workflow Verify+Gate, re-confirmed by coordinator on merged tree): check/build/test green. src/shared/sections.ts has all 10 sections with exact §4.3 rephrase/order/groupBy triples; SECTION_VALUES exports 10 keys. Diff-read confirms verbatim §4.3. Config-only, test-exempt."
    },
    {
      "id": "T004",
      "title": "Providers registry (PROVIDERS, resolveModel, providerOptionsFor)",
      "status": "done",
      "depends_on": [
        "T002"
      ],
      "files": [
        "src/shared/providers.ts",
        "test/providers.test.ts"
      ],
      "origin": "spec §6.1, §8",
      "context": "Author src/shared/providers.ts per §6.1: PROVIDERS registry mapping each ProviderId → { label, models[], default }. resolveModel({provider,model,apiKey,baseURL}) builds an AI SDK LanguageModel via createAnthropic/createOpenAI/createGoogleGenerativeAI (openai-compatible = createOpenAI+baseURL). providerOptionsFor(provider) returns provider-specific extras (Anthropic: thinking/effort + prompt-cache control; sane defaults elsewhere). Deps: ai, @ai-sdk/anthropic, @ai-sdk/openai, @ai-sdk/google. Default Anthropic claude-opus-4-8; include a Google model. PHASE-0 EVAL MODEL = gemini-2.5-flash (ledger [v2-005]: key tested live — flash works incl. structured output; gemini-2.5-pro is 429 quota-blocked/no-billing). The Google factory ALSO reads GOOGLE_GENERATIVE_AI_API_KEY from env — Phase 0 relies on that.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- parametrized over ALL 4 ProviderIds: each returned model's provider/modelId matches the REQUESTED provider (dispatch correctness — not just non-throw)\n- the passed apiKey is forwarded to the factory (not dropped); google resolves via GOOGLE_GENERATIVE_AI_API_KEY env alone\n- providerOptionsFor('anthropic') includes cache-control/thinking keys; other providers return defined sane defaults",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator): check/build green; test/providers.test.ts 14/14. resolveModel dispatch correct for all 4 providers (modelId exact; provider strings distinct incl. openai vs openai-compatible); apiKey forwarded (verified via AI SDK header closure, no network); google resolves via GOOGLE_GENERATIVE_AI_API_KEY env; providerOptionsFor('anthropic') has cacheControl+thinking. Deps ai@7/@ai-sdk/* added. No tag-scoring."
    },
    {
      "id": "T005",
      "title": "zod schemas: TailorDecisionZ + entry/meta validation",
      "status": "done",
      "depends_on": [
        "T002",
        "T003"
      ],
      "files": [
        "src/shared/schema.ts",
        "test/schema.test.ts"
      ],
      "origin": "spec §4.4, §5, §17",
      "context": "Author src/shared/schema.ts. Critical: TailorDecisionZ — hand-written zod for the model's flat TailorDecision (§5), passed straight to generateObject. Also entryMetaZ (STRICT discriminatedUnion on 'section', one object per §4.1 — reject foreign fields) and entryInput (plain zod for Phase 0: id?, section enum, meta=entryMetaZ, facts 1-300 x≤12, tags ≤8 x1-40, framings? ≤6 x1-200, sortKey int; superRefine meta.section===section + per-section facts arity per §17). drizzle-zod derivation deferred to Phase 1 (no Drizzle yet). Locked: TailorDecisionZ hand-written, never table-derived.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Rejection table (each a distinct assert): TailorDecisionZ REJECTS empty entryId, non-numeric rank, missing signals.hardRequirements, a cut entry missing reason; ACCEPTS one fully-populated valid decision\n- Strict meta: canonical experience meta PASSES; same object + foreign field (school) REJECTED; missing role REJECTED\n- Facts arity: skill with 2 facts REJECTED (label sections exactly 1); experience with 0 facts REJECTED (narrative ≥1); certification with [] facts PASSES; meta.section !== section REJECTED",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+Gate, re-confirmed on merged tree): check/build green; test/schema.test.ts 13/13. TailorDecisionZ rejects empty entryId / non-numeric rank / missing signals.hardRequirements / cut missing reason, accepts a valid decision; entryMetaZ strict (foreign 'school' field rejected, missing role rejected); facts arity (skill!=2, experience>=1, cert [] ok, meta.section mismatch rejected). zod dep added. Hand-written, not table-derived."
    },
    {
      "id": "T006",
      "title": "Phase 0 seed entries (SEED_ENTRIES)",
      "status": "done",
      "depends_on": [
        "T002"
      ],
      "files": [
        "src/server/seed.ts"
      ],
      "origin": "spec §22",
      "context": "Author src/server/seed.ts exporting SEED_ENTRIES: Entry[] VERBATIM from §22 — the three Cloudcase experience entries (cloudcase-rules-engine sortKey 202101, cloudcase-frontend-rewrite 202501, cloudcase-platform-sdk 202503). All tagged platform-arch ON PURPOSE (forces discrimination on FACTS not tags) and all ONE job (same company/role/period → one group, so every flip is the group's leading item). Copy facts/tags/framings/sortKey EXACTLY — do NOT edit facts to inject target keywords (that would make the flip trivial keyword-lookup and void the Tier-0 premise). NOTE the secondary-tag hole (sdk/frontend/delivery map 1:1 to targets) is neutralized downstream by the tag-shuffle control (T014/T015), not by the seed.",
      "acceptance": "- `bun run check` passes; `bun run test` green (data-only, test-exempt per baseline — verified by diff-read; consumed by T013/T014 later)\n- correctness (coordinator diff-read vs §22): SEED_ENTRIES deep-equals the §22 literal — the 3 ids (cloudcase-rules-engine/-frontend-rewrite/-platform-sdk), meta, facts element-for-element, tags, framings, sortKey\n- all three share company/role/period (one group); every entry's tags include platform-arch; sortKeys exactly 202101/202501/202503 (the OLDEST, rules-engine, can still lead — proving rank not recency); facts NOT edited to inject target keywords\n- do NOT add a test file (not in declared files)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator diff-read): check/build/test green. src/server/seed.ts SEED_ENTRIES = §22 literal element-for-element (3 ids, sortKeys 202101/202501/202503, all one Cloudcase group, all tags incl platform-arch, facts NOT keyword-injected, framings only on rules-engine). Data-only, test-exempt."
    },
    {
      "id": "T007",
      "title": "System prompt + renderLibrary (the product IP)",
      "status": "done",
      "depends_on": [
        "T002",
        "T003"
      ],
      "files": [
        "src/server/tailor/prompt.ts",
        "test/prompt.test.ts"
      ],
      "origin": "spec §6.1, §6.2",
      "context": "Author src/server/tailor/prompt.ts — THE product IP (§6.2). SYSTEM_PROMPT (frozen; never interpolate JD/timestamp) encodes: judge from FACTS not tags; the fact-lock (never invent, never strengthen — 'contributed to' ≠ 'led'); the rephrase policy full/light/none GENERATED from SECTIONS (so prompt/code never drift); rank-within-group = the lede; a leadRationale on each group's leading item tied to a NAMED JD signal; return ONLY the flat TailorDecision with every number grounded in facts. renderLibrary(entries) serializes deterministically (by section, then sortKey desc, then id; stable key order) and FOREGROUNDS facts (tags not presented as a scorable field). This prompt is what makes the §22 lede flip — if the eval (T014) doesn't flip sharply, THIS file is the bug, not the code.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Derivation not literal: the policy block is produced by a pure fn(SECTIONS); a test passing a SECTIONS copy with one section's rephrase FLIPPED asserts the rendered block reflects the flipped value (a hardcoded string fails this)\n- Order-independence: SCRAMBLED input entries → output equals the canonical section→sortKey-desc→id ordering, and two different input orderings yield BYTE-IDENTICAL output\n- Facts foregrounded: renderLibrary(e) output contains every string in e.facts; tags are not framed as 'match these'\n- SYSTEM_PROMPT contains no JD/timestamp interpolation",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator): check/build green; test/prompt.test.ts 9/9. SYSTEM_PROMPT frozen (no JD/timestamp interpolation), encodes facts-not-tags + fact-lock ('contributed to'≠'led') + rank=lede + leadRationale-names-signal. Rephrase policy DERIVED from SECTIONS via pure fn (flip test: changing a section's rephrase in a SECTIONS copy changes the rendered block — not a hardcoded literal). renderLibrary deterministic (section→sortKey desc→id, byte-identical across input orderings), facts foregrounded, tags labeled 'never a relevance signal'. NOTE: real proof of the flip is the live eval T014."
    },
    {
      "id": "T008",
      "title": "assemble() — deterministic structure from the flat decision",
      "status": "done",
      "depends_on": [
        "T002",
        "T003"
      ],
      "files": [
        "src/server/tailor/assemble.ts",
        "test/assemble.test.ts"
      ],
      "origin": "spec §5, §4.3",
      "context": "Author src/server/tailor/assemble.ts: assemble(decision, entries, layout, sections): TailoredResume. Server owns ALL structure (§5): group by registry groupBy; group/flat order by registry `order` (recency = MAX member sortKey desc, server-computed; relevance = ascending min member rank; manual = sortKey); items-WITHIN-group ALWAYS by rank (lowest = lede); coerce text for rephrase:'none' to facts verbatim (empty-facts → text:''); filter to sections enabled in layout. The model NEVER controls structure. Keyless-testable — the regression net's core.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Max-member contrast: group A sortKeys [202101,202503] supplied in that order, group B [202501] → A ordered before B (A's key = max = 202503, distinguishing max from min/first)\n- Coercion is an OVERRIDE not pass-through: a rephrase:'none' entry fed decision text:\"FABRICATED\" → output text is the verbatim fact, not \"FABRICATED\"; empty-facts entry → text:\"\"\n- Section order follows LAYOUT: layout orders B before A while entries inserted A before B → output sections [B,A]; an enabled:false section present in entries is absent\n- items within a group order by rank (lowest first); flipping rank flips the leading item",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator): check/build green; test/assemble.test.ts 7/7. Server owns structure: recency groups by MAX member sortKey desc (contrast test distinguishes max from min/first); rephrase:'none' text COERCED to facts verbatim (decision text 'FABRICATED' overridden; empty-facts→''); section order follows LAYOUT (permuted) + disabled sections dropped; items within group by rank (flip test). Deterministic core of the keyless regression net."
    },
    {
      "id": "T009",
      "title": "validateNoFabrication + extractNumbers/hasNumberToken",
      "status": "done",
      "depends_on": [
        "T002"
      ],
      "files": [
        "src/server/tailor/validate.ts",
        "test/validate.test.ts"
      ],
      "origin": "spec §6.3, §23",
      "context": "Author src/server/tailor/validate.ts per §6.3: validateNoFabrication(resume, entries, baseSummary?) throws FabricationError on (a) an item entryId not a real entry, (b) a number in item.text not present in THAT item's OWN entry facts (per-entry scoped, not pooled), (c) a summary number not traceable to a kept entry's facts OR baseSummary. extractNumbers/hasNumberToken compare WHOLE number tokens INCLUDING attached %/k/x/units (so 30k≠30, 50%≠50), separators normalized (1,000==1000), NOT substrings ('1' ≠ inside '2021'). Mechanical only (§23) — NO LLM-checks-LLM pass, ever.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Token semantics (one assert per pair): text \"30k\" PASSES only if facts contain \"30k\", THROWS if facts contain only \"30\"; \"50%\" does NOT match a fact of \"50\"; \"1,000\" matches a fact of \"1000\"\n- Per-entry scoping: an item whose number is present in a DIFFERENT entry's facts but absent from its OWN entry THROWS\n- Detection completeness: a fake number injected into a DEEP location (summary, or a buried non-leading item in a late group) still THROWS and names the invented token (proves all items scanned)\n- clean output PASSES; unknown entryId THROWS; summary number grounded in baseSummary PASSES",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator): check/build green; test/validate.test.ts 11/11. Whole-token semantics ('30k'≠'30','50%'≠'50','1,000'=='1000','1' not in '2021'); per-entry scoping enforced; deep-buried fake number throws naming the token; unknown entryId throws; baseSummary-grounded summary passes. Mechanical only, no LLM call."
    },
    {
      "id": "T016",
      "title": "Shared eval-core: hash(jd,entries), flip-predicate, canonical JDs, tag-shuffle",
      "status": "done",
      "depends_on": [
        "T002",
        "T003",
        "T008"
      ],
      "files": [
        "src/server/tailor/evalcore.ts"
      ],
      "origin": "red-team Finding A (fixture provenance) + Finding B (tag-scoring); spec §22",
      "context": "SINGLE SOURCE that closes the two structural holes. Author src/server/tailor/evalcore.ts exporting: (1) hashKey(jd, entries) — the ONE fixture key used by BOTH FixtureEngine (T010) and record-fixtures (T013), so a fixture can't be keyed one way and looked up another; (2) CONTRAST_JDS — the 3 §22 JDs, authored so NONE contains any entry id or a fact substring ≥ ~12 chars from its target (no keyword leakage), each mapped to its expected target (platform-sdk/rules-engine/frontend-rewrite); (3) flipPredicate(resume: TailoredResume, targetId) → { leads, rationaleNamesSignal } — target is its group's UNIQUE lowest-rank leading item AND its group.leadRationale contains a token from resume.signals; used identically by eval, record-fixtures, and the keyless suite; (4) tagShuffle(entries) — returns entries with tags permuted/stripped across entries (for the invariance control). This module is imported by T010, T013, T014, T015 — the provenance chain's spine.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- hashKey is deterministic and order-insensitive over entries; hashKey(CONTRAST_JDS[i], SEED_ENTRIES) is stable\n- unit test: none of CONTRAST_JDS contains an entry id or a ≥12-char fact substring from its target (guards keyword leakage)\n- flipPredicate returns leads=true only when target is the UNIQUE minimum-rank leading item of its group; false on a rank tie or when target is in cut\n- tagShuffle preserves entry ids/facts/sortKeys and only permutes tags",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): check/build green; test/evalcore.test.ts adds 14 (68 total). hashKey deterministic + order-insensitive (sha256 over jd+entries-sorted-by-id); ANTI-LEAKAGE verified — each CONTRAST_JDS jd contains neither its target id nor any ≥12-char substring of that target's facts (builder's drafts leaked 'integration path'/'the codebase'/'consistent patterns' and were reworded until clean). JDs read as realistic employer descriptions of the NEED, not keyword lists. flipPredicate: leads = target is its group's items[0] (leading) & not in cut; rationaleNamesSignal = leadRationale references a resume.signals token (true/false/filler/empty cases tested). tagShuffle permutes tags across entries, preserves id/facts/sortKey, no mutation. No new deps. Scope: only evalcore.ts + test."
    },
    {
      "id": "T010",
      "title": "TailorEngine (Fixture + Provider) + tailor() orchestration",
      "status": "done",
      "depends_on": [
        "T002",
        "T004",
        "T005",
        "T007",
        "T008",
        "T009",
        "T016"
      ],
      "files": [
        "src/server/tailor/engine.ts",
        "test/engine.test.ts"
      ],
      "origin": "spec §6.1",
      "context": "Author src/server/tailor/engine.ts per §6.1: interface TailorEngine { decide(jd, entries): Promise<TailorDecision> }. ProviderEngine (real): resolveModel(cfg) then generateObject({ model, schema: TailorDecisionZ, system: SYSTEM_PROMPT + renderLibrary(entries), prompt: 'Tailor for this job description:\\n\\n'+jd, providerOptions: providerOptionsFor(provider) }); retry EXACTLY once on failure then throw. FixtureEngine (keyless): loadFixtureDecision by hashKey(jd, entries) FROM T016 (shared) out of test/fixtures/decisions; a MISS throws a no_fixture-tagged error (→422), NEVER a silent fallback / first-file. tailor(engine, jd, entries, layout, baseSummary?): decision = engine.decide → assemble → validateNoFabrication → return (engine-agnostic). Engine factory by env LEDE_TAILOR_ENGINE (live|fixture; fixture default under NODE_ENV=test). The engine split is what makes everything except the live call keyless.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Validate-wired contrast: tailor() with a stub engine returning a decision containing a FABRICATED number THROWS FabricationError; same stub with a clean decision returns a resume (proves validate runs, not just assemble)\n- Hash keying with ≥2 fixtures: decide(jdA)→fixtureA, decide(jdB)→fixtureB (distinct); decide(jdC unrecorded) THROWS no_fixture WHILE fixtures exist (closes first-file/silent-fallback)\n- Retry count: a mock model throwing once then succeeding → decide resolves after EXACTLY 2 calls; throwing twice → rejects after 2\n- Engine factory returns FixtureEngine under NODE_ENV=test and ProviderEngine under live (assert by type); ProviderEngine calls generateObject with schema===TailorDecisionZ and a system string containing renderLibrary(entries) output",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): check/build green; test/engine.test.ts 10 (78 total). tailor() = decide→assemble→validateNoFabrication (validate WIRED, contrast test: fabricated '9999%' throws FabricationError, clean returns resume). ProviderEngine retry EXACTLY once (try/catch/retry, generateObject ≤2 calls, asserted). FixtureEngine keys by evalcore.hashKey; MISS throws NoFixtureError code 'no_fixture' listing scenarios (no silent fallback / first-file). makeEngine: fixture under NODE_ENV=test/LEDE_TAILOR_ENGINE=fixture, ProviderEngine (google/gemini-2.5-flash) under live. Wiring test asserts generateObject called with schema===TailorDecisionZ + system containing renderLibrary output. Scope: only engine.ts+test."
    },
    {
      "id": "T011",
      "title": "Fastify /api/tailor + /api/health + config (Phase 0, no DB/auth)",
      "status": "done",
      "depends_on": [
        "T005",
        "T006",
        "T010"
      ],
      "files": [
        "src/server/index.ts",
        "src/server/config.ts",
        "test/server.test.ts"
      ],
      "origin": "spec §9, §24 Phase 0",
      "context": "Extend src/server/index.ts (from T001) + add src/server/config.ts. config.ts: fail-fast env loader (§17) — PORT (8787), LEDE_TAILOR_ENGINE (live|fixture). Phase 0 does NOT require LEDE_MASTER_KEY/SESSION_SECRET yet (arrive with auth/BYOK in Phase 2). Route POST /api/tailor: zod-validate { jobDescription } (1-20000, §17); build engine per config (live=ProviderEngine using the Google bootstrap key from env; fixture=FixtureEngine); call tailor(engine, jd, SEED_ENTRIES, defaultLayout) — the route MUST go through tailor() (assemble+validate), never return a raw decision or canned resume. Error mapping (§9): provider auth→'key invalid', rate-limit→429, other provider fail→502, zod-fail on LLM output→502, fixture miss→422 no_fixture. GET /api/health → {ok:true}.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Response is a zod-valid TailoredResume whose EVERY item entryId ∈ SEED_ENTRIES ids; a fixture carrying a number absent from facts makes the route return 502 (proves validateNoFabrication runs IN-REQUEST)\n- Distinct statuses each asserted: empty jd → 400; jd > 20000 → 400; unrecorded fixture → 422 {error:'no_fixture'}\n- GET /api/health served by the Fastify app instance → {ok:true}",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): check/build green; 84 tests (6 new server tests). Real HTTP boot: GET /api/health→{ok:true}, POST /api/tailor empty jd→400. Route goes through tailor(engine, jd, SEED_ENTRIES, defaultLayout) — no raw decision/canned resume. Error mapping distinct: NoFixtureError→422 {error:'no_fixture'}, FabricationError→502, provider auth→401, rate-limit→429, else→502. buildApp() exported (testable via inject; .listen guarded to entrypoint). config.ts fail-fast PORT+LEDE_TAILOR_ENGINE (no MASTER_KEY/SESSION_SECRET — deferred to Phase 2). Scope: index.ts+config.ts+test/server.test.ts."
    },
    {
      "id": "T012",
      "title": "Client Phase 0: JD paste → tailor → deterministic resume render",
      "status": "done",
      "depends_on": [
        "T002",
        "T011"
      ],
      "files": [
        "src/client/App.tsx",
        "src/client/api.ts",
        "src/client/components/TailorView.tsx",
        "src/client/components/JDInput.tsx",
        "src/client/components/ResumePage.tsx",
        "src/client/styles/print.css",
        "src/client/styles/app.css",
        "test/resumepage.test.tsx"
      ],
      "origin": "spec §10, §13 (minimal)",
      "context": "Minimal Phase 0 client (no shadcn/Tailwind yet — lands in E1; plain CSS here). api.ts: typed fetch to /api/tailor throwing ApiError{status,code,message}. TailorView: JDInput (textarea) + tailor button → /api/tailor → ResumePage. ResumePage: SINGLE-COLUMN deterministic render of TailoredResume (§10) — ProfileHeader stub, summary, then each TailoredSection (registry label + groups) in LAYOUT order (data-driven, not hardcoded). print.css (@media print): US-Letter, #000 on #fff, real text, no images/tables/multi-column. leadRationale/cut[] MUST NOT render on the resume page (§10) — reasoning UI is Phase 3. Wire into App.tsx.",
      "acceptance": "- `bun run check` + `bun run build` pass\n- Leak check: feed a TailoredResume fixture whose leadRationale and every cut[].reason contain UNIQUE SENTINEL strings; assert those exact strings are ABSENT from the rendered resume DOM textContent\n- Data-driven order: use a NON-DEFAULT (permuted) layout; assert rendered section order equals layout order (not hardcoded); items within a group follow given order\n- print.css @media print: no column-count/columns>1, no display:table/table layout, #000 on #fff (grep-checkable)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): check/build green; 88 tests (4 new resumepage). §10 LEAK invariant: ResumePage renders NO leadRationale/cut (grep shows only a forbidding comment; sentinel test: renderToStaticMarkup HTML .not.toContain the 4 sentinels). Data-driven order: sections render in resume order ([project,experience] non-alpha) via SECTIONS labels, items in array order (not hardcoded). print.css @media print single-column, no column-count/columns2+/display:table. api.ts throws typed ApiError; JDInput disabled while pending. Scope: exactly the 8 declared files."
    },
    {
      "id": "T013",
      "title": "record-fixtures script + recorded decision fixtures (KEY-GATED)",
      "status": "done",
      "depends_on": [
        "T006",
        "T010",
        "T016"
      ],
      "files": [
        "scripts/record-fixtures.ts",
        "test/fixtures/decisions/platform-sdk.json",
        "test/fixtures/decisions/rules-engine.json",
        "test/fixtures/decisions/frontend-rewrite.json",
        "test/fixtures/decisions/manifest.json"
      ],
      "origin": "spec §18, §22; red-team Finding A",
      "context": "Author scripts/record-fixtures.ts: runs the LIVE ProviderEngine (Google bootstrap key, GOOGLE_GENERATIVE_AI_API_KEY) over T016's CONTRAST_JDS against SEED_ENTRIES and writes each TailorDecision to test/fixtures/decisions/<name>.json keyed by T016 hashKey(jd, SEED_ENTRIES). Fails LOUD (non-zero, no key). BEFORE writing each fixture it runs the decision through tailor()→assemble→validateNoFabrication AND T016 flipPredicate — a non-flipping or fabricating decision CANNOT be written. Writes manifest.json (model id, prompt hash, timestamp, token usage) — provenance. These committed fixtures are what makes the keyless suite (T015) replay the REAL scenarios; a hand-authored fixture would not survive the live-reproduces-fixtures phase gate (oracle Phase 0).",
      "acceptance": "- `bun run scripts/record-fixtures.ts` (with GOOGLE key) writes 3 fixtures + manifest.json; each recorded decision is TailorDecisionZ-valid, passes validateNoFabrication, and passes T016 flipPredicate for its target\n- fixture key = T016 hashKey(jd, SEED_ENTRIES); a subsequent FixtureEngine.decide(jd, SEED_ENTRIES) returns exactly that fixture (shared-hash proof)\n- across the 3 fixtures the rank-1 (leading) entry does NOT track sortKey order (proves a real rank flip, not recency)\n- each fixture embeds/matches the canonical JD text and the SEED_ENTRIES hash (guards library/JD swap)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): baseline green (84 tests). Live recorder wrote 3 fixtures + manifest.json (model gemini-2.5-flash). Fixtures are REAL TailorDecisions (2-3 items, cut lists, 342-485-char summaries, ranks 1..N — not stubs). KEYLESS replay CONFIRMED (GOOGLE key blanked): FixtureEngine+tailor() over the 3 CONTRAST_JDS reproduces all flips PASS (platform-sdk/rules-engine/frontend-rewrite lead, rationale names signal). Keys === hashKey(jd,SEED_ENTRIES). rules-engine leads despite OLDEST sortKey (rank≠recency). Provenance gate satisfied — matches my live ground truth [v2-013]. Scope: scripts/record-fixtures.ts + 3 fixtures + manifest only."
    },
    {
      "id": "T014",
      "title": "Phase 0 acceptance oracle — scripts/eval.ts (KEY-GATED, THE gate)",
      "status": "done",
      "depends_on": [
        "T006",
        "T007",
        "T010",
        "T016"
      ],
      "files": [
        "scripts/eval.ts"
      ],
      "origin": "spec §22, §25 Tier 0; red-team Findings A+B",
      "context": "Author scripts/eval.ts — THE Phase 0 behavioral oracle (§22, §25). Constructs ProviderEngine EXPLICITLY (Google bootstrap key, model gemini-2.5-flash per ledger [v2-005] — pro is quota-blocked) and EXITS NON-ZERO if no key (never skip-as-pass, never fall back to FixtureEngine). Runs the live model over T016 CONTRAST_JDS against EXACTLY SEED_ENTRIES via tailor(), using T016 flipPredicate. Exit 0 iff all 3 flip correctly, non-zero (naming the miss) otherwise. Locked: if it doesn't flip sharply, the PROMPT (§6.2, T007) is wrong — not the code.",
      "acceptance": "- `bun run scripts/eval.ts` (with GOOGLE_GENERATIVE_AI_API_KEY) exits 0; asserts engine instanceof ProviderEngine and ≥1 live response with token usage\n- over exactly SEED_ENTRIES (length===3, ids match): for each JD the expected target is present in items (NOT cut) and is the STRICT UNIQUE minimum rank\n- the three leading entryIds are MUTUALLY DISTINCT and equal the SET {cloudcase-platform-sdk, cloudcase-rules-engine, cloudcase-frontend-rewrite} (set assertion, not 3 isolated checks)\n- each target's leadRationale contains a token from that JD's decision.signals; the three rationales are pairwise different\n- TAG-SHUFFLE CONTROL (Finding B): re-run over tagShuffle(SEED_ENTRIES); the flip is UNCHANGED (if shuffling tags changes the lede → tag-scoring → FAIL)\n- exits non-zero naming the failing JD when a lede does not flip",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator RAN THE GATE LIVE): `bun run scripts/eval.ts` → exit 0. BASE: all 3 JDs flip to correct target (platform-sdk/rules-engine/frontend-rewrite), leads+rationaleNamesSignal, mutually-distinct set. TAG-SHUFFLE: same 3 flips hold + same target per JD (fact-driven, not tag-driven). No-key run → exit 1 (refuses, no FixtureEngine fallback). Gaming read: uses ProviderEngine explicitly, EXPECTED_TARGETS as a Set, no fallback. THIS IS THE PHASE-0 §22/§25-Tier0 ORACLE PASSING. Scope: only scripts/eval.ts."
    },
    {
      "id": "T015",
      "title": "Keyless pipeline e2e tests (FixtureEngine replay)",
      "status": "done",
      "depends_on": [
        "T011",
        "T013",
        "T016"
      ],
      "files": [
        "test/tailor.e2e.test.ts"
      ],
      "origin": "spec §18, §25 keyless guarantee; red-team Findings A+B",
      "context": "Author test/tailor.e2e.test.ts: drive tailor() AND POST /api/tailor with FixtureEngine over the recorded fixtures (T013) for the 3 CONTRAST_JDS — NO API KEY. Asserts the assembled resume's structure/order/coercion/validation AND that the correct target leads each JD (replaying the flip keylessly via T016 flipPredicate). The standing regression net (§25 keyless guarantee). Honest limit (record in ledger): this replays FROZEN decisions, so it proves the MACHINERY, not that the live model still flips — that rests on T014 being genuinely run. It also cannot catch a fabricated fixture; the anti-recency check below is the one recency-vs-rank cheat it CAN kill alone.",
      "acceptance": "- `bun run test` (GOOGLE_GENERATIVE_AI_API_KEY UNSET) passes; assert the engine used is FixtureEngine\n- for each of the 3 JDs the SPECIFIC expected target is its group's UNIQUE leading item, and the three leads are MUTUALLY DISTINCT\n- Anti-recency: rules-engine JD → cloudcase-rules-engine leads (it has the OLDEST sortKey 202101, impossible under recency), and ≥2 of 3 leads are NOT the max-sortKey entry\n- a specifically-UNRECORDED jd yields the tagged no_fixture error via tailor() and 422 {error:'no_fixture'} via HTTP\n- structure-ignores-tags: replay with tagShuffle(SEED_ENTRIES) → identical assembled order/selection (proves assemble/render never let tags influence structure)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): KEYLESS full suite passes with GOOGLE key UNSET — 10 files/93 tests. e2e replays the 3 §22 flips via FixtureEngine (each target is its group's items[0]; leads mutually distinct = the 3 seed ids); ANTI-RECENCY (rules-engine leads despite OLDEST sortKey 202101, verified via Math.min; ≥2 leads not max-sortKey); HTTP via buildApp().inject() recorded→200/unrecorded→422 no_fixture; structure-ignores-tags (assemble over SEED_ENTRIES vs tagShuffle deep-equal). Honest-limit comment present. No ProviderEngine (keyless). Scope: only test/tailor.e2e.test.ts."
    },
    {
      "id": "E1",
      "title": "Phase 1 — Drizzle + SQLite persistence; entry library editor; profile; layout; shadcn/Tailwind foundation",
      "status": "decomposed",
      "depends_on": [
        "T014",
        "T015"
      ],
      "files": [],
      "origin": "spec §24 Phase 1 (§4, §9, §12, §13, §17)",
      "context": "COARSE EPIC — decompose when Phase 0 closes into: (a) shadcn/ui + Tailwind + §12 token theming foundation; (b) Drizzle schema src/server/db/schema.ts (§4.2: entries/profile/settings/secrets, JSON columns, singletons id=1+CHECK) + db/index.ts (better-sqlite3, WAL, DATA_DIR) + migrations run on boot + singleton INSERT OR IGNORE seeding (settings.layout defaults §4.2); (c) REPLACE Phase 0 plain-zod entry schema with drizzle-zod createInsertSchema (§4.4) — keep TailorDecisionZ hand-written and the §17 bounds (facts≤12,tags≤8,…) that derivation can't infer; (d) /api/entries CRUD + import/export, /api/profile, /api/settings (layout) — zod-validated (§9); (e) /api/tailor reads entries from DB; (f) client: TanStack Query + react-router, LibraryView + LibraryToolbar + SectionAccordion + EntryCard + EntryEditor (section-aware via registry, one editor for all sections) + SectionMetaFields + RepeatableList + TagInput + ProfileEditor + LayoutEditor. Sections first: experience/project/education/skill (§2). RED-TEAM — child tickets MUST include: (1) export = ENTRIES ONLY (assert payload has no ciphertext/plaintext key/auth hash/non-entry table); (2) real restart: process A writes → exit → process B reads same DATA_DIR (real .sqlite, not :memory:); (3) migrations from empty DATA_DIR create all 4 tables AND the CHECK constraints (query sqlite_master; INSERT id=2 into a singleton FAILS); (4) layout contrast: a NON-DEFAULT layout (reordered + one disabled) changes render order and drops the disabled section.",
      "acceptance": "See oracle.md Phase 1 (merged-tree): migrations on boot; entries CRUD persists across restart; import/export round-trip (secrets excluded); LibraryView create/edit/delete end-to-end; profile round-trip; layout ordering respected.",
      "attempts": [],
      "evidence": "Decomposed into E1-A..E1-F3 (8 child tickets). See ledger [v2-016]."
    },
    {
      "id": "E1-A",
      "title": "UI foundation: Tailwind + shadcn primitives + §12 tokens + self-hosted IBM Plex",
      "status": "done",
      "depends_on": [],
      "files": [
        "tailwind.config.ts",
        "postcss.config.js",
        "src/client/styles/tokens.css",
        "src/client/styles/app.css",
        "src/client/lib/utils.ts",
        "src/client/components/ui/button.tsx",
        "src/client/components/ui/input.tsx",
        "src/client/components/ui/textarea.tsx",
        "src/client/components/ui/label.tsx",
        "src/client/components/ui/form.tsx",
        "src/client/components/ui/dialog.tsx",
        "src/client/components/ui/dropdown-menu.tsx",
        "src/client/components/ui/tooltip.tsx",
        "src/client/components/ui/tabs.tsx",
        "src/client/components/ui/badge.tsx",
        "src/client/components/ui/card.tsx",
        "src/client/components/ui/sonner.tsx",
        "src/client/components/ui/alert.tsx",
        "src/client/components/ui/select.tsx",
        "src/client/components/ui/skeleton.tsx",
        "test/ui-foundation.test.tsx"
      ],
      "origin": "decomposed from E1 (spec §12, §13)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nSCAFFOLD/CONFIG ticket (behavior-test-exempt except the contrast below). Establish the design-system foundation per spec §12 + §13. Set up Tailwind (v3, PostCSS) so vite processes `src/client/styles/*.css`. Create `tokens.css` defining the §12 CSS variables VERBATIM: --ink:#1a1a1a --ink-soft:#52525b --ink-faint:#a1a1aa --bg:#fff --bg-subtle:#fafafa --border:#e4e4e7 --border-strong:#d4d4d8 --accent:#2f5fdd --accent-weak:#eef2fe --success:#15803d --warn:#b45309 --danger:#b91c1c; radius 4px (6px cards/dialogs); spacing scale 4/8/12/16/24/32/48. Map shadcn's CSS variables (--primary, --background, --foreground, --border, --radius, etc.) to these tokens; --accent is primary. Copy the shadcn/ui primitives listed in §13 into src/client/components/ui/ (owned, re-themed) — button input textarea label form dialog dropdown-menu tooltip tabs badge card sonner alert select skeleton — pulling their Radix deps + class-variance-authority/clsx/tailwind-merge/lucide-react into package.json. lib/utils.ts holds `cn()`. Self-host IBM Plex via @fontsource/ibm-plex-sans, @fontsource/ibm-plex-mono, @fontsource/ibm-plex-serif imported from app.css (Plex Sans=UI/body, Plex Mono=IDs/numbers, Plex Serif=wordmark+callouts only) — NO Google Fonts CDN. Do NOT build any domain components (Library/Editor/etc.) — those are later tickets; this is foundation only. If copying 15 primitives is more than one session, return tooBig splitting (tokens+config) from (primitive copy).",
      "acceptance": "- baseline passes (check+build+test)\n- tokens.css contains the §12 color variables incl. --accent:#2f5fdd; shadcn primary maps to --accent (grep tokens.css + the theme mapping)\n- CONTRAST (fonts self-hosted, gameable-resistant): after `bun run build`, grep the dist/ output → NO `fonts.googleapis.com` / `fonts.gstatic.com` / any remote font URL present; @fontsource/ibm-plex-{sans,mono,serif} are in package.json deps and imported from app.css\n- smoke test (test/ui-foundation.test.tsx): <Button> from components/ui/button renders and mounts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree re-verify (2026-07-02): `bun run check` exit 0; `bun run build` exit 0 (dist has self-hosted IBM Plex woff/woff2, grep dist for fonts.googleapis/gstatic -> none); `bun run test` with ALL provider keys unset -> 12 files / 101 tests PASS (was 93; +4 test/db.test.ts, +4 test/ui-foundation.test.tsx). Workflow Verify stage passed both tickets independently (scope clean, no gaming). Merge clean, no conflicts. E1-A: tokens.css has the §12 vars incl --accent:#2f5fdd, shadcn --primary maps to var(--accent); 15 ui/ primitives copied + re-themed; @fontsource IBM Plex imported from app.css."
    },
    {
      "id": "E1-B",
      "title": "Drizzle schema + db init + migrations-on-boot + singleton seeding",
      "status": "done",
      "depends_on": [],
      "files": [
        "drizzle.config.ts",
        "src/server/db/schema.ts",
        "src/server/db/index.ts",
        "src/server/config.ts",
        "src/server/index.ts",
        "drizzle/",
        "test/db.test.ts"
      ],
      "origin": "decomposed from E1 (spec §4.2)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nImplement the §4.2 Drizzle schema + db layer. `src/server/db/schema.ts`: the four tables VERBATIM from §4.2 — entries (id pk, section, meta/facts/tags/framings JSON with $type, sortKey, createdAt, updatedAt), profile (singleton id=1 default, name/headline/email/phone/location/links JSON/baseSummary/updatedAt, CHECK id=1), settings (singleton, provider default 'anthropic', model default 'claude-opus-4-8', baseUrl, layout JSON, updatedAt, CHECK id=1), secrets (singleton, apiKeyEnc JSON nullable, apiKeyValidatedAt, auth JSON nullable, updatedAt, CHECK id=1). CRITICAL BOUNDARY: schema.ts must import ONLY from 'drizzle-orm/sqlite-core' + 'drizzle-orm' (pure, no driver) so the client can import table types without pulling better-sqlite3 — the native driver lives ONLY in db/index.ts. `src/server/db/index.ts`: open better-sqlite3 at `${DATA_DIR}/lede.sqlite` with WAL, expose a drizzle db; a function to run migrations (drizzle-orm/better-sqlite3/migrator, migrationsFolder 'drizzle') and INSERT OR IGNORE the three singleton rows (settings.layout seeded to the FULL §4.2 order all enabled: [summary, experience, project, skill, education, award, certification, publication, language, interest, reference]). Generate migrations with `bunx drizzle-kit generate` into `drizzle/` and COMMIT them (the CHECK constraints must appear in the generated SQL). config.ts: add DATA_DIR (default './data'). index.ts boot: init db + run migrations + seed singletons BEFORE app.listen. Keep Phase-0 /api/tailor working (it still reads SEED_ENTRIES for now — E1-E rewires it; do not change tailor here beyond what's needed to boot).",
      "acceptance": "- baseline passes\n- CONTRAST (fresh migrate): against an EMPTY tmp DATA_DIR (real .sqlite file, NOT :memory:), running migrations creates exactly tables {entries, profile, settings, secrets} (query sqlite_master) AND the singleton CHECK holds — `INSERT` with id=2 into profile / settings / secrets each THROWS\n- CONTRAST (restart persistence): open db at a tmp DATA_DIR, insert an entry row, close the handle, reopen a fresh db at the SAME DATA_DIR → the entry reads back (proves real file, not in-memory)\n- singleton seeding: after migrate+seed, settings id=1 exists with layout = the full §4.2 order all enabled; profile id=1 and secrets id=1 exist\n- new tests in test/db.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree re-verify (2026-07-02): `bun run check` exit 0; `bun run build` exit 0 (dist has self-hosted IBM Plex woff/woff2, grep dist for fonts.googleapis/gstatic -> none); `bun run test` with ALL provider keys unset -> 12 files / 101 tests PASS (was 93; +4 test/db.test.ts, +4 test/ui-foundation.test.tsx). Workflow Verify stage passed both tickets independently (scope clean, no gaming). Merge clean, no conflicts. E1-B: 4 tables verbatim §4.2; schema.ts driver-free; migrations drizzle/0000_sloppy_domino.sql has 3 CHECK(id=1); db.test proves fresh-migrate table set + CHECK-reject id=2 + real-file restart persistence + singleton seed with full layout. NOTE: bun-runtime boot fails (better-sqlite3 dlopen) -> repair T016."
    },
    {
      "id": "T017",
      "title": "Server runs under Node (tsx), not Bun — better-sqlite3 native ABI; add real-boot smoke gate",
      "status": "done",
      "depends_on": [
        "E1-B"
      ],
      "files": [
        "package.json",
        "test/boot.smoke.test.ts"
      ],
      "origin": "repair: bun runtime cannot dlopen better-sqlite3 native addon (escaped E1-B); ledger [v2-017]",
      "context": "Baseline (oracle.md): bun run check + bun run build + bun run test pass, keyless. Touch ONLY declared files (+ package.json manifest). Add tests for new behavior.\n\nESCAPED ISSUE (verified by coordinator 2026-07-02): running `bun src/server/index.ts` dies with ERR_DLOPEN_FAILED — better-sqlite3 ships a Node-ABI prebuilt (NODE_MODULE_VERSION 127) that Bun 1.3.14's embedded V8 (137) cannot load; better-sqlite3 uses direct V8/NAN bindings, not N-API, so this is NOT rebuild-away (oven-sh/bun#4290). Under Node v22 via tsx the server boots, runs migrations, and GET /api/health returns {ok:true} (coordinator confirmed).\nRESOLUTION (mechanical, ledger [v2-017]): the server PROCESS runs under Node — reverting the runner to spec §3.1's own 'Node >= 20'. Bun stays the package-manager + script runner (bun install / bun run check|build|test unchanged; they already use vitest/node).\nCHANGES: in package.json scripts, change `start` from `bun src/server/index.ts` to `tsx src/server/index.ts`; change `dev:api` from `bun --watch src/server/index.ts` to `tsx watch src/server/index.ts`. Add `tsx` to devDependencies. Do NOT touch check/build/test scripts. Do NOT introduce bun:sqlite (it breaks vitest).\n\nSTRENGTHENED CHECK (escaped-bug rule): add test/boot.smoke.test.ts that spawns the REAL entrypoint under the actual runner (node via tsx — e.g. child_process spawn of `tsx src/server/index.ts`, or `node --import tsx`) against a fresh tmp DATA_DIR on a free port; poll GET /api/health until {ok:true} (fail on ~15s timeout); assert the .sqlite file was created under DATA_DIR; tear the child down. This is the boot gate the baseline lacked — it catches a runner that can't load the native driver.",
      "acceptance": "- baseline passes (check+build+test, keyless)\n- CONTRAST (real boot, gameable-resistant): test/boot.smoke.test.ts spawns the entrypoint under the real runner, GET /api/health -> {ok:true}, and the DATA_DIR .sqlite file exists after boot; the test FAILS if the server can't boot (proves the runner loads better-sqlite3)\n- package.json `start` and `dev:api` no longer invoke `bun` on src/server/index.ts (grep: no `bun` invoking src/server/index.ts in scripts); `tsx` present in devDependencies\n- new test in test/boot.smoke.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree re-verify (2026-07-02, master @ dfa0484): bun run check exit 0; bun run build exit 0; bun run test keyless (all provider keys unset) -> 13 files / 113 tests PASS (incl. new boot.smoke.test.ts). Workflow Gate agent also passed independently; Verify passed both (scope clean, no gaming); merge clean. T017: package.json start/dev:api now `tsx ...` (no bun on entrypoint), tsx@^4.19.2 devDep; boot.smoke.test.ts spawns real entrypoint under tsx -> /api/health {ok:true} + DATA_DIR/lede.sqlite created (fails if runner cannot dlopen better-sqlite3)."
    },
    {
      "id": "E1-C",
      "title": "drizzle-zod entry validators + profile/settings validators (replace Phase-0 hand-rolled entryInput)",
      "status": "done",
      "depends_on": [
        "E1-B"
      ],
      "files": [
        "src/shared/schema.ts",
        "test/schema.test.ts"
      ],
      "origin": "decomposed from E1 (spec §4.4, §17)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nReplace the Phase-0 hand-rolled `entryInput` in src/shared/schema.ts with `createInsertSchema(entries, {...})` from drizzle-zod (§4.4), importing the `entries` table from src/server/db/schema.ts (safe — that file is driver-free per E1-B). PRESERVE exactly: the discriminatedUnion `entryMetaZ` (hand-written, unchanged), the §17 bounds that drizzle-zod can't infer (facts ≤12 × 1–300; label sections exactly 1 fact; cert/reference allow 0; tags ≤8 × 1–40; framings ≤6 × 1–200; meta string fields 1–120; id 1–80 optional; jobDescription bound stays where it is), `.omit({createdAt, updatedAt})`, and the superRefine (meta.section === section; per-section facts arity). Keep `entryImport = z.array(entryInput).max(200)`. Keep `TailorDecisionZ` HAND-WRITTEN (never derived). ADD `profileInput` and `settingsInput` (provider/model/baseUrl/layout) zod objects per §4.2/§9. All existing schema tests must stay green (regression).",
      "acceptance": "- baseline passes; ALL existing test/schema.test.ts cases still green (per-section meta, arity rules, foreign-field rejection)\n- entryInput is derived via createInsertSchema AND still enforces the §17 bounds: rejects facts.length>12, a tag >40 chars, a meta string >120, a foreign meta field\n- CONTRAST (boundary intact): `bun run build` (vite client build) succeeds and the dist bundle does NOT contain 'better-sqlite3' or 'node:' native driver refs — the shared→db import stays driver-free\n- profileInput / settingsInput accept a valid payload and reject a malformed one\n- tests updated in test/schema.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree re-verify (2026-07-02, master @ dfa0484): bun run check exit 0; bun run build exit 0; bun run test keyless (all provider keys unset) -> 13 files / 113 tests PASS (incl. new boot.smoke.test.ts). Workflow Gate agent also passed independently; Verify passed both (scope clean, no gaming); merge clean. E1-C: entryInput derived via createInsertSchema(entries) (drizzle-zod ^0.8.3); §17 bounds + superRefine + entryMetaz + entryImport preserved; TailorDecisionZ still hand-written; +profileInput/settingsInput; schema.test 24 tests green; client bundle grep better-sqlite3 -> 0 (boundary intact)."
    },
    {
      "id": "E1-D",
      "title": "HTTP routes: /api/entries CRUD + import, /api/profile, /api/settings",
      "status": "done",
      "depends_on": [
        "E1-B",
        "E1-C"
      ],
      "files": [
        "src/server/routes/entries.ts",
        "src/server/routes/profile.ts",
        "src/server/routes/settings.ts",
        "src/server/slug.ts",
        "src/server/index.ts",
        "test/api.entries.test.ts",
        "test/api.profile-settings.test.ts"
      ],
      "origin": "decomposed from E1 (spec §9, §17)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nImplement the §9 JSON routes over the drizzle db (E1-B), validated with the E1-C zod validators. Register them in index.ts as Fastify plugins receiving the db. Routes: GET /api/entries (optional ?section= filter) → Entry[]; POST /api/entries (no id → server slug per §17: kebab of section + a distinguishing meta field + a few fact words, ≤80 chars, dedupe -2) → Entry; PUT /api/entries/:id → Entry; DELETE /api/entries/:id → {ok}; POST /api/entries/import (Entry[], cap 200) → {imported:n}. GET /api/profile + PUT /api/profile (singleton round-trip). GET /api/settings → {keySet, provider, model, baseUrl, layout} where keySet = (secrets.apiKeyEnc != null) and the KEY ITSELF IS NEVER RETURNED; PUT /api/settings (provider?/model?/baseUrl?/layout?). Bad body → 400. NOTE: there is no separate export endpoint — export = GET /api/entries (entries table only, secrets are a different table). Auth guard is Phase 2 — do NOT add auth here. slug.ts holds the §17 slug generator (unit-test it). Put createdAt/updatedAt stamping in the route/db layer (Date.now()).",
      "acceptance": "- baseline passes\n- entries CRUD round-trips AND persists across restart: write entries via one buildApp() instance over a tmp DATA_DIR, rebuild the app on the SAME DATA_DIR, GET /api/entries returns them (keyless integration, real .sqlite)\n- POST with no id → server-generated slug (§17); POST/PUT with a bad body → 400\n- import: POST /api/entries/import with 2 entries → {imported:2}; GET returns them\n- CONTRAST (no secret leak, gameable-resistant): GET /api/settings body has `keySet` boolean but contains NO apiKey/ciphertext/iv/tag and NO auth hash; the GET /api/entries export payload contains ONLY Entry fields (id/section/meta/facts/tags/framings/sortKey[/createdAt/updatedAt]) — assert no ciphertext, no auth hash, no settings/secrets content anywhere in it\n- profile GET/PUT round-trip; settings GET/PUT (incl. layout) round-trip\n- new tests in test/api.entries.test.ts + test/api.profile-settings.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR + workflow-Gate both green (merged tree, master @ 6187f69->post-merge). bun run check exit 0; build exit 0; test keyless -> 15 files / 126 tests PASS (+8 api.entries, +5 api.profile-settings; server.test + boot.smoke still green). Verify passed (scope clean, no gaming). LEAK CONTRAST held: GET /api/settings returns only {keySet,provider,model,baseUrl,layout} (keySet derived from secrets.apiKeyEnc!=null in routes/settings.ts, key never read/returned); GET /api/entries payload = Entry fields only, no ciphertext/hash/salt. Restart persistence via real .sqlite proven. Slug §17 in slug.ts (unit-tested). buildApp(db?) refactor kept server.test.ts green."
    },
    {
      "id": "E1-E",
      "title": "/api/tailor reads entries + settings.layout from DB; first-boot seeds SEED_ENTRIES",
      "status": "done",
      "depends_on": [
        "E1-B",
        "E1-D"
      ],
      "files": [
        "src/server/index.ts",
        "src/server/seed.ts",
        "test/api.tailor-db.test.ts"
      ],
      "origin": "decomposed from E1 (spec §22, §4.3)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nPhase 0's /api/tailor uses the hardcoded `SEED_ENTRIES` const + a `defaultLayout` const. Rewire it to read ALL entries and `settings.layout` from the DB (E1-B). Keep the FixtureEngine keyless path intact (engine selection unchanged). On FIRST boot (entries table empty), seed SEED_ENTRIES into the db (§22 'loaded if entries empty on first run') — refactor seed.ts to keep the SEED_ENTRIES export (tests/eval depend on it) AND add a seedIfEmpty(db) used at boot. tailor() already accepts (entries, layout) params — pass the DB-sourced values. Do NOT change assemble()/engine/prompt. Do NOT touch the routes files (E1-D owns them).",
      "acceptance": "- baseline passes; full suite green with provider key UNSET (keyless guarantee holds)\n- CONTRAST (reads DB, not the const): seed the db with a DIFFERENT entry set than SEED_ENTRIES (e.g. a single distinct entry), POST /api/tailor (fixture engine) → the assembled resume reflects the DB entries, NOT the 3 hardcoded SEED_ENTRIES\n- CONTRAST (layout respected by render): with a NON-DEFAULT settings.layout (two sections reordered + one section disabled), the assembled TailoredResume.sections order reflects the reorder AND omits the disabled section; with the default layout, the standard §4.2 order\n- first-boot seeding: empty entries table → after boot, SEED_ENTRIES are present (GET /api/entries returns them)\n- new tests in test/api.tailor-db.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ d079e3f; bun install; keyless): check 0, build 0, test 18 files / 138 PASS. Workflow Gate also green; Verify passed both (scope clean, no gaming); merge landed on master (template patch worked). E1-E: /api/tailor now reads all entries + settings.layout from the db (rowToEntry mapper preserves SEED key order so hashKey fixtures still match — builder self-caught). CONTRAST reads-db-not-const: custom-seeded entry → 200 with its id, SEED ids absent (would be 422 no_fixture if still const). CONTRAST layout: reorder+disable → sections [certification,skill], award omitted. seedIfEmpty(db) idempotent first-boot seed; injected test dbs left untouched."
    },
    {
      "id": "E1-F1",
      "title": "Client infra: TanStack Query + react-router + AppShell/NavTabs + LibraryView browse/delete",
      "status": "done",
      "depends_on": [
        "E1-A",
        "E1-D"
      ],
      "files": [
        "src/client/main.tsx",
        "src/client/App.tsx",
        "src/client/api.ts",
        "src/client/components/AppShell.tsx",
        "src/client/components/NavTabs.tsx",
        "src/client/components/LibraryView.tsx",
        "src/client/components/LibraryToolbar.tsx",
        "src/client/components/SectionAccordion.tsx",
        "src/client/components/EntryCard.tsx",
        "src/client/hooks/queries.ts",
        "test/library.test.tsx"
      ],
      "origin": "decomposed from E1 (spec §13, §14)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nClient state + shell + library browse, per §13/§14. main.tsx: wrap in QueryClientProvider + a react-router RouterProvider (routes /tailor /library /settings under AppShell). App.tsx: AppShell + NavTabs (Tailor | Library | Settings). api.ts: EXTEND (keep the existing tailor()) with typed fetch for entries/profile/settings CRUD, throwing ApiError{status,code,message}; expose the codes so 401→login and 400 no_api_key→settings can be handled later. hooks/queries.ts: TanStack Query hooks — useEntries (key ['entries'], optional section), useDeleteEntry (invalidates ['entries']), plus query stubs for profile/settings (E1-F2/F3 extend). LibraryView: SectionAccordion (one block per Section, using the registry label from @shared/sections) containing EntryCard (facts preview + tag chips as badges + edit/delete buttons). LibraryToolbar (Add · Import · Export buttons — wiring the editor open is E1-F2; here Add can open a not-yet-implemented editor slot or a disabled button, keep the open/close plumbing minimal). Use E1-A's shadcn primitives (button/card/badge/tabs/etc.); NEVER re-roll a primitive. The existing TailorView stays as the /tailor route. Do NOT build EntryEditor/ProfileEditor/LayoutEditor (later tickets).",
      "acceptance": "- baseline passes\n- LibraryView renders entries grouped by section with registry labels; EntryCard shows facts preview + tag chips\n- CONTRAST (delete e2e, gameable-resistant): component test with fetch/api mocked — render LibraryView (wrapped in QueryClientProvider + router) seeded with 2 entries; click delete on entry A → DELETE /api/entries/A is fired, ['entries'] invalidated, A's card removed while B's card remains\n- NavTabs renders links to /tailor /library /settings and routing mounts the right view\n- new tests in test/library.test.tsx",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ merge 46ffdce; then bun install for new deps): bun run check exit 0; bun run build exit 0; bun run test keyless -> 16 files / 129 tests PASS incl. new test/library.test.tsx (3). NOTE: workflow Gate initially RED — builder left work UNCOMMITTED in its worktree (branch==master, nothing to merge) AND new deps not installed in main tree. Coordinator recovered: committed the scope-clean branch (only declared files+manifest), merged, ran bun install, re-gated green. delete-e2e test verified NON-gamed (stateful fetch mock: asserts DELETE URL + post-invalidation refetch + only target card removed). No client tag-scoring."
    },
    {
      "id": "E1-F2",
      "title": "Section-aware EntryEditor + create/edit (SectionMetaFields, RepeatableList, TagInput)",
      "status": "done",
      "depends_on": [
        "E1-F1"
      ],
      "files": [
        "src/client/components/EntryEditor.tsx",
        "src/client/components/SectionMetaFields.tsx",
        "src/client/components/RepeatableList.tsx",
        "src/client/components/TagInput.tsx",
        "src/client/components/LibraryView.tsx",
        "src/client/hooks/queries.ts",
        "test/entry-editor.test.tsx"
      ],
      "origin": "decomposed from E1 (spec §13)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nBuild ONE section-aware EntryEditor (Dialog) for ALL sections, driven by the section registry (@shared/sections) — NOT one editor per section (§13 rule). SectionMetaFields renders the correct meta inputs per section from the §4.1 meta shapes (experience→company/role/period/location; project→name/role/period/url; education→school/degree/field/period/location; skill→category/level; etc.). Label sections (skill/interest/language) show a compact single-fact form. RepeatableList handles facts + framings (add/remove/reorder rows); TagInput handles tags (chip add/remove, ≤8). Wire create (POST /api/entries) and edit (PUT /api/entries/:id) via TanStack mutations that invalidate ['entries'] (add useCreateEntry/useUpdateEntry to hooks/queries.ts). Validate §17 bounds client-side before submit. Wire the editor open from LibraryView's Add button and EntryCard's edit button. Use E1-A primitives (dialog/input/label/button/form). Depends on E1-F1 (LibraryView + queries.ts already exist — EXTEND them, don't clobber F1's exports).",
      "acceptance": "- baseline passes\n- CONTRAST (ONE registry-driven editor, gameable-resistant): render EntryEditor with section=experience → company/role/period fields present; switch section to skill → the meta fields CHANGE to the skill compact single-fact form and the experience fields are GONE (proves registry-driven, not a fixed/duplicated form)\n- CONTRAST (create e2e): open editor, fill an experience entry, save → POST /api/entries fired with the entered meta/facts/tags; on success the new EntryCard appears in LibraryView\n- edit: open an existing entry → fields populated → change a fact → PUT /api/entries/:id fired\n- new tests in test/entry-editor.test.tsx",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ d079e3f; bun install; keyless): check 0, build 0, test 18 files / 138 PASS. Workflow Gate also green; Verify passed both (scope clean, no gaming); merge landed on master (template patch worked). E1-F2: ONE registry-driven EntryEditor (Dialog) — CONTRAST: section=experience shows company/role/period; switch to skill → those gone, compact skill form shown. create e2e (POST fired, card appears) + edit (PUT fired) pass. SectionMetaFields/RepeatableList/TagInput built. DRIFT (minor): EntryCard/LibraryToolbar were not in F2 declared files, so F2 added its own Add trigger + edit-picker in LibraryView rather than wiring F1s disabled stub buttons — functionally correct + scope-honest; unify in a later polish ticket. Client §17 validation is a local validate() (not importing @shared zod) to keep the client bundle driver-free; server remains authoritative (E1-D zod)."
    },
    {
      "id": "E1-F3",
      "title": "ProfileEditor + LayoutEditor (reorder/toggle resume sections)",
      "status": "done",
      "depends_on": [
        "E1-F1"
      ],
      "files": [
        "src/client/components/ProfileEditor.tsx",
        "src/client/components/LayoutEditor.tsx",
        "src/client/components/LibraryView.tsx",
        "src/client/hooks/queries.ts",
        "test/profile-layout.test.tsx"
      ],
      "origin": "decomposed from E1 (spec §13, §16)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nProfileEditor (Dialog, §16): edit name/headline/email/phone/location/links[]/baseSummary; GET /api/profile populates, PUT /api/profile saves (round-trip); add useProfile/useUpdateProfile to hooks/queries.ts (invalidate ['profile']). LayoutEditor (§13): reorder + enable/disable the resume sections in settings.layout; GET /api/settings populates, PUT /api/settings persists; add useSettings/useUpdateSettings (invalidate ['settings']). Wire ProfileEditor open from LibraryView (or AppShell) and LayoutEditor into the Library/Settings area. Use E1-A primitives. Depends on E1-F1 — EXTEND LibraryView + queries.ts, do not clobber F1/F2 exports. (The render-order EFFECT of layout is verified server-side in E1-E; here the acceptance is the editor persisting the layout correctly.)",
      "acceptance": "- baseline passes\n- CONTRAST (profile round-trip): ProfileEditor mounts with GET /api/profile data populated; edit name + add a link + set baseSummary, save → PUT /api/profile fired with the new values; reopening shows them\n- CONTRAST (layout persist): LayoutEditor reorders two sections + disables one, save → PUT /api/settings fired with the reordered layout in which the disabled section has enabled:false\n- new tests in test/profile-layout.test.tsx",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 6a2a79c; keyless): check 0 / build 0 / test 19 files / 140 PASS incl. new test/profile-layout.test.tsx. Workflow Gate + Verify both green; merge landed on master; worktree pruned. CONTRAST profile round-trip (PUT /api/profile with edited name+link+baseSummary, reopen shows) + layout persist (reorder + disable → PUT /api/settings with enabled:false) pass. queries.ts extended (useUpdateProfile/useUpdateSettings) without clobbering F1/F2 exports."
    },
    {
      "id": "E2",
      "title": "Phase 2 — auth gate + BYOK (encrypted key storage, validate-on-save, Settings)",
      "status": "decomposed",
      "depends_on": [
        "E1-E",
        "E1-F2",
        "E1-F3"
      ],
      "files": [],
      "origin": "spec §24 Phase 2 (§7, §8, §23)",
      "context": "COARSE EPIC — decompose after E1 into: (a) src/server/crypto.ts (AES-256-GCM encrypt/decrypt using LEDE_MASTER_KEY; scrypt hash/verify) + config.ts fail-fast REQUIRING LEDE_MASTER_KEY + LEDE_SESSION_SECRET at boot; (b) secrets-table access; (c) auth.ts + /api/auth/{setup,login,logout} (@fastify/secure-session) + session guard on ALL /api/* except /api/auth/* and /api/health; first-run set-password; LEDE_AUTH_DISABLED escape hatch (default false); (d) BYOK: PUT /api/settings/key (validate via one cheap live generateText BEFORE storing, then store ciphertext), DELETE, GET /api/settings returns {keySet,provider,model,baseUrl,layout} NEVER the key; (e) /api/tailor switches to ProviderEngine with the DECRYPTED BYOK key (in-memory only), no_api_key→400; (f) client: LoginGate, SettingsView + ApiKeyForm (write-only) + ProviderPicker + ModelPicker. RED-TEAM — child tickets MUST include: (1) SENTINEL scan — store key sk-SENTINEL-<uuid>, hit EVERY endpoint incl. tailor error paths, grep every response body + all stdout/stderr/log files + the .sqlite/-wal/-shm + entire DATA_DIR; sentinel appears nowhere plaintext, only in secrets.apiKeyEnc ciphertext; (2) master key load-bearing — decryption THROWS/garbles when LEDE_MASTER_KEY changed; two encryptions of same plaintext have DIFFERENT iv (catches base64/ECB fakes); (3) PROCESS-LEVEL boot refusal — spawn real server with LEDE_MASTER_KEY unset AND with a malformed key → non-zero exit, no listening port, does NOT auto-generate a master key; (4) master-key material never in DATA_DIR (the backup unit, §23); (5) validate-BEFORE-store — bad key → 4xx AND secrets.apiKeyEnc unchanged; (6) guard total with auth ENABLED — every route except auth/health returns 401 sans session (esp. /api/tailor).",
      "acceptance": "See oracle.md Phase 2 (merged-tree): boot refuses without LEDE_MASTER_KEY (process-level); encrypt/decrypt round-trip w/ distinct IVs; login→session, guarded routes 401; key stored as ciphertext, sentinel scan clean across responses/logs/DATA_DIR; validate-on-save; /api/tailor no-key→400 no_api_key.",
      "attempts": [],
      "evidence": "Decomposed into E2-A..E2-E (5 child tickets). See ledger [v2-023]."
    },
    {
      "id": "E2-A",
      "title": "crypto.ts (AES-256-GCM + scrypt) + config fail-fast (LEDE_MASTER_KEY/SESSION_SECRET) + test-env setup + process-level boot refusal",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/server/crypto.ts",
        "src/server/config.ts",
        "test/setup.ts",
        "vite.config.ts",
        "test/boot.smoke.test.ts",
        "test/crypto.test.ts",
        "test/config.test.ts"
      ],
      "origin": "decomposed from E2 (spec §8, §17, §19, §23)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nFOUNDATION for Phase 2 (everything else depends on this).\n(1) src/server/crypto.ts (node:crypto only): `encrypt(plaintext, masterKey)` -> {iv, tag, ciphertext} using AES-256-GCM with a FRESH random iv each call; `decrypt({iv,tag,ciphertext}, masterKey)` -> plaintext (throws on tampered tag/wrong key); `hashPassword(pw)` -> {hash, salt} using scrypt (random salt); `verifyPassword(pw, {hash,salt})` -> bool (timing-safe compare). masterKey is the decoded 32 bytes.\n(2) src/server/config.ts: EXTEND loadConfig() to parse+VALIDATE `masterKey` (from LEDE_MASTER_KEY, base64 decoding to EXACTLY 32 bytes — else throw), `sessionSecret` (from LEDE_SESSION_SECRET, non-empty — @fastify/secure-session needs >=32 chars, validate), `authDisabled` (LEDE_AUTH_DISABLED === 'true', default false). loadConfig() FAIL-FAST throws a clear error if master key or session secret is missing/malformed. Do NOT auto-generate a master key. Keep existing port/tailorEngine/dataDir.\n(3) test/setup.ts (NEW vitest global setup) + wire it in vite.config.ts `test.setupFiles`: set process.env.LEDE_MASTER_KEY (a valid 32-byte base64), LEDE_SESSION_SECRET (a >=32-char test value), and LEDE_AUTH_DISABLED='true' BEFORE tests run, so the whole existing keyless suite (buildApp etc.) still boots. (The auth guard is proven ENABLED separately in E2-B.)\n(4) test/boot.smoke.test.ts (EXTEND, owned-contract now changes): the spawned real entrypoint must now receive LEDE_MASTER_KEY + LEDE_SESSION_SECRET in its env (add them to the existing happy-path spawn). ADD the process-level boot-refusal cases: spawn the real entrypoint with LEDE_MASTER_KEY UNSET, and separately with a MALFORMED master key (not 32 bytes) -> the process exits NON-ZERO, never starts listening, and does NOT write/auto-generate a master key anywhere.\nRED-TEAM acceptance encodes: master-key load-bearing (distinct IVs; wrong-key decrypt throws) + process boot refusal.",
      "acceptance": "- baseline passes; ALL existing suites stay green (test/setup.ts provides operator secrets + LEDE_AUTH_DISABLED=true)\n- CONTRAST (crypto load-bearing, gameable-resistant): encrypt(x) then decrypt -> x; TWO encrypts of the SAME plaintext yield DIFFERENT iv AND different ciphertext (catches ECB/base64 fakes); decrypt with a DIFFERENT masterKey THROWS (not silent garbage); scrypt verifyPassword true for right pw, false for wrong\n- CONTRAST (config fail-fast): loadConfig() THROWS when LEDE_MASTER_KEY is unset, and when it is malformed (base64 not decoding to 32 bytes); throws when LEDE_SESSION_SECRET missing\n- CONTRAST (process-level boot refusal): spawning the real entrypoint with master key unset / malformed -> non-zero exit, no listening port, no auto-generated key file in DATA_DIR\n- new tests in test/crypto.test.ts + test/config.test.ts; boot.smoke extended",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 121655a, keyless): check 0, build 0, test 21 files / 157 PASS on 4/4 repeated runs. crypto.test (8): round-trip, DISTINCT iv+ciphertext per encrypt, wrong-key decrypt THROWS, tampered tag/ciphertext throw, scrypt verify. config.test (7): fail-fast throws (master unset/malformed/session missing/too-short), no auto-gen. boot.smoke extended (3): real boot with secrets + 2 process-level boot-refusal cases (master unset/malformed -> non-zero exit, never listens, no key file). NOTE: workflow Gate flagged a FLAKY cross-suite race (NOT E2-A code): server.test.ts + api.tailor-db.test.ts write temp fixtures into the SHARED test/fixtures/decisions/ dir while FixtureEngine.loadFixtures parses ALL .json -> mid-write partial file -> 502. Pre-existing (Phase0 server.test x E1-E api.tailor-db); E2-A merely changed timing. E2-A contrasts are deterministic. -> repair T018."
    },
    {
      "id": "T018",
      "title": "Fix flaky test: FixtureEngine.loadFixtures must tolerate concurrent/partial fixture files (shared decisions dir race)",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/server/tailor/engine.ts",
        "test/engine.test.ts"
      ],
      "origin": "repair: flaky suite — shared test/fixtures/decisions dir + parallel vitest workers + loadFixtures parses ALL .json; exposed by E2-A gate; ledger [v2-024]",
      "context": "Baseline (oracle.md): bun run check + build + test pass KEYLESS. Touch ONLY declared files. Add a test for the new behavior. RUNTIME v2-017: server under Node/tsx.\n\nFLAKY-TEST DEFECT (measured, stack-traced): test/server.test.ts and test/api.tailor-db.test.ts each writeFileSync/rmSync temp .json fixtures into the SHARED dir test/fixtures/decisions/ from SEPARATE parallel vitest workers. FixtureEngine.loadFixtures (src/server/tailor/engine.ts ~lines 82-90) does readdirSync(dir).filter(.json).map(f => JSON.parse(readFileSync(...))) — it reads+parses EVERY .json with NO per-file guard, so when a worker catches another worker's file mid-write (partial/empty) or mid-delete (ENOENT), JSON.parse/readFileSync throws, the whole map() throws, decide() throws, and /api/tailor returns 502 instead of 200. Intermittent (~1 in 2-5 runs) -> it will randomly red-gate future tickets.\n\nFIX (product-robustness, minimal): in loadFixtures, wrap EACH file's readFileSync+JSON.parse in its own try/catch; on failure SKIP that file (do not abort the whole scan) and emit a console.warn naming the file (so a genuinely corrupt RECORDED fixture is still visible in logs, not silently swallowed). Return the successfully-parsed fixtures. A mid-write/mid-delete concurrent file becomes a no-op for other workers, while a test's own fully-written fixture is still found by key. Do NOT change decide()/hashKey/matching semantics; do NOT touch the tests that write fixtures (the fix is in the engine). Keep DEFAULT_FIXTURES_DIR behavior.\n\nSTRENGTHENED CHECK (escaped-bug rule): add a test in test/engine.test.ts that points a FixtureEngine at a temp dir containing (a) one VALID fixture .json and (b) one MALFORMED/partial .json (truncated, and/or an empty file), then asserts decide() for the valid fixture's key STILL RESOLVES (the malformed file is skipped, not fatal) and loadFixtures does not throw — the guard that would have caught the race.",
      "acceptance": "- baseline passes; the full suite is GREEN across repeated runs (a concurrent partial/deleted fixture file no longer aborts a scan)\n- CONTRAST (tolerate bad fixture files, gameable-resistant): a FixtureEngine over a temp dir holding one valid + one MALFORMED/empty .json still resolves the valid key via decide() (the bad file is skipped); loadFixtures does not throw\n- unchanged: a valid recorded fixture still matches by hashKey; no change to decide()/matching semantics\n- new/updated test in test/engine.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 2e1777d, keyless): check 0, build 0, test 21 files / 158 PASS — DETERMINISTIC across 5 total runs (workflow 3 + coordinator 2). Fix: loadFixtures per-file try/catch (skip+console.warn on partial/ENOENT), no map()-abort. New test/engine.test.ts case: valid fixture resolves alongside a truncated + empty .json (bad files skipped). No change to decide()/hashKey/matching. Verify passed; merged; worktree pruned."
    },
    {
      "id": "T019",
      "title": "Stabilize vitest harness: raise testTimeout + cap worker parallelism so the full suite is deterministic",
      "status": "done",
      "depends_on": [],
      "files": [
        "vite.config.ts"
      ],
      "origin": "repair: full suite non-deterministic — parallel-worker timeouts under contention (full-server integration suites blow 5000ms); exposed by E2-B/E2-C gate; ledger [v2-026]",
      "context": "Baseline (oracle.md): bun run check + build + test pass KEYLESS. Touch ONLY declared files. RUNTIME v2-017: server under Node/tsx; Bun runs the scripts.\n\nHARNESS DETERMINISM DEFECT (measured by coordinator over repeated runs): the full `bun run test` suite is now NON-DETERMINISTIC — repeated runs fail a DISJOINT, rotating set of tests, and EVERY failure is a bare per-test TIMEOUT ('Test timed out in 5000ms'), never an assertion/logic error. Root cause: vitest runs the default parallel worker pool with the default 5000ms testTimeout, but the suite now has ~10 integration suites that each boot a full Fastify server (pino logger) and boot.smoke spawns real tsx subprocesses; on a shared/contended multi-core box, workers starve and individual server-boot injects blow the 5000ms deadline. In isolation / single-worker every test passes in 0.6-2.4s. Wall time balloons (89s-160s+).\n\nFIX (vite.config.ts test config ONLY — this is a harness fix, NOT a product change; do NOT touch any product or test-logic file): give the integration suites headroom and reduce worker contention so `bun run test` is DETERMINISTICALLY green. Concretely: set test.testTimeout and test.hookTimeout to a generous value (e.g. 30000ms), and stabilize the pool so full-server suites don't starve — cap worker parallelism to a modest number (e.g. pool:'forks' with poolOptions.forks so maxForks is a small cap like 4, or set maxWorkers/minWorkers, or fileParallelism tuned) — pick the vitest knobs that make repeated full-suite runs reliably pass without making wall time absurd. KEEP the existing test.setupFiles (test/setup.ts) and test.exclude '**/.claude/**' and the @shared alias. Do NOT change any test's logic or any product code.\n\nThis is the harness-stability gate the loop needs so every subsequent ticket gates on a deterministic suite (companion to T018 which fixed the fixture-dir race).",
      "acceptance": "- baseline passes: bun run check exit 0, bun run build exit 0\n- CONTRAST (determinism, the whole point): run `bun run test` (keyless, provider key UNSET) 3 TIMES back-to-back — ALL THREE are fully green (every file/every test passes, exit 0), no per-test timeouts. Report the three runs' summary lines verbatim.\n- only vite.config.ts changed (no product/test-logic file touched); test.setupFiles + test.exclude preserved",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 1f281b1, keyless): check 0, build 0, test 23 files / 172 PASS — DETERMINISTIC across 5 runs (workflow 3 + coordinator 2, ~30s each, zero timeouts). T019: vite.config.ts test now testTimeout/hookTimeout 30000 + pool:forks singleFork:true — removed the parallel-worker CPU-contention timeout flake (full-server + tsx-spawn suites no longer starve). Only vite.config.ts changed."
    },
    {
      "id": "E2-B",
      "title": "Auth: scrypt password gate + @fastify/secure-session + session guard on all /api/* except auth/health + /api/auth/{setup,login,logout}",
      "status": "done",
      "depends_on": [
        "E2-A"
      ],
      "files": [
        "src/server/auth.ts",
        "src/server/routes/auth.ts",
        "src/server/index.ts",
        "test/api.auth.test.ts"
      ],
      "origin": "decomposed from E2 (spec §7, §9)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nSingle-user password gate (§7) — NOT accounts. Uses crypto.ts (E2-A: hashPassword/verifyPassword) + config (E2-A: sessionSecret, authDisabled).\n(1) Register @fastify/secure-session in buildApp() with the sessionSecret (add dep). (2) src/server/routes/auth.ts: POST /api/auth/setup (FIRST-RUN only: if secrets.auth is null, store scrypt hash+salt; if already set -> 409/403), POST /api/auth/login (verifyPassword against secrets.auth -> set session), POST /api/auth/logout (clear session). Bodies zod-validated {password}. (3) src/server/auth.ts: the guard preHandler — every /api/* EXCEPT /api/auth/* and /api/health requires a valid session; no session -> 401. When config.authDisabled is true, the guard is a no-op (escape hatch).\n(4) src/server/index.ts (buildApp): EXTEND signature to buildApp(db?, configOverride?) so a test can force auth ENABLED (configOverride.authDisabled=false) even though the global test env sets it true; register secure-session + auth routes + the guard hook. Keep existing route registration (entries/profile/settings/tailor) and the injected-db path intact. The guard must cover /api/tailor, /api/entries, /api/profile, /api/settings, /api/settings/key.\nRED-TEAM acceptance: guard TOTAL with auth ENABLED — every guarded route 401 without a session; first-run setup then login issues a working session.",
      "acceptance": "- baseline passes (existing suites stay green — they run with LEDE_AUTH_DISABLED=true from test/setup.ts)\n- CONTRAST (guard total, auth ENABLED, gameable-resistant): build an app with auth ENABLED (configOverride authDisabled=false); WITHOUT a session, GET/POST to /api/entries, /api/profile, /api/settings, /api/settings/key, and POST /api/tailor ALL return 401; /api/health and /api/auth/* are reachable without a session\n- first-run setup: POST /api/auth/setup sets the password (scrypt hash stored in secrets.auth); a second setup attempt is rejected (already configured)\n- login: POST /api/auth/login with the right password issues a session that then passes the guard; wrong password -> 401; logout clears it\n- new tests in test/api.auth.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 1f281b1, keyless): check 0, build 0, test 23 files / 172 PASS — DETERMINISTIC across 5 runs (workflow 3 + coordinator 2, ~30s each, zero timeouts). E2-B: @fastify/secure-session + scrypt gate. api.auth.test (7): guard ENABLED -> 401 on /api/entries|profile|settings|settings/key|tailor without session; /api/health + /api/auth/* public; setup 200 then 409; login right->session passes guard, wrong->401; logout clears. Coordinator read auth.ts: PUBLIC_PREFIXES={/api/auth/,/api/health}, authDisabled->no-op, else session.authed!==true->401. Guard covers all /api/*."
    },
    {
      "id": "E2-C",
      "title": "BYOK key storage: PUT/DELETE /api/settings/key, validate-before-store, ciphertext-only, sentinel leak scan",
      "status": "done",
      "depends_on": [
        "E2-A"
      ],
      "files": [
        "src/server/routes/settings.ts",
        "src/server/keyvalidation.ts",
        "test/api.byok.test.ts"
      ],
      "origin": "decomposed from E2 (spec §8, §9, §23)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nBYOK key handling (§8). Uses crypto.ts (E2-A encrypt/decrypt + the master key from config). EXTEND src/server/routes/settings.ts (E1-D created it; keep GET/PUT /api/settings intact — GET already returns {keySet,provider,model,baseUrl,layout}, key NEVER returned).\n(1) src/server/keyvalidation.ts: `validateProviderKey({provider,model,apiKey,baseUrl})` -> does ONE cheap live `generateText` (Vercel AI SDK) and returns ok / throws on failure. INJECTABLE: settingsRoutes must accept an optional validator so tests pass a fake (keyless) — default is the real one. This is the seam that keeps validate-before-store keyless-testable.\n(2) PUT /api/settings/key {apiKey}: VALIDATE via validateProviderKey FIRST (using current settings.provider/model/baseUrl); on failure -> 4xx and secrets.apiKeyEnc UNCHANGED; on success -> encrypt(apiKey, masterKey) and store {iv,tag,ciphertext} in secrets.apiKeyEnc, set apiKeyValidatedAt, return {keySet:true}. (3) DELETE /api/settings/key -> purge (secrets.apiKeyEnc=null), return {keySet:false}. (4) NEVER log the key; NEVER return it.\nRED-TEAM acceptance (the security core): SENTINEL scan + validate-before-store + ciphertext-only + master-key-not-in-DATA_DIR.",
      "acceptance": "- baseline passes\n- CONTRAST (validate-before-store): with an injected validator that REJECTS, PUT /api/settings/key -> 4xx AND secrets.apiKeyEnc is unchanged (still null / still the prior value); with a validator that ACCEPTS, the key is stored as {iv,tag,ciphertext} (NOT plaintext) and keySet becomes true\n- CONTRAST (SENTINEL leak scan, gameable-resistant): store apiKey `sk-SENTINEL-<uuid>` (validator accepts); then hit every endpoint incl. GET /api/settings and the tailor error paths; assert the sentinel string appears in NONE of: any response body, the server logs (capture stdout/stderr), and NO plaintext occurrence anywhere under DATA_DIR (the .sqlite + -wal + -shm). It may appear ONLY inside secrets.apiKeyEnc as ciphertext (i.e. the raw sentinel substring is absent from the DB file bytes)\n- CONTRAST (master key not in DATA_DIR): the LEDE_MASTER_KEY value never appears in any file under DATA_DIR (§23 — DB leak without the master key keeps the key safe)\n- DELETE purges: after DELETE /api/settings/key, GET /api/settings keySet=false\n- new tests in test/api.byok.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 1f281b1, keyless): check 0, build 0, test 23 files / 172 PASS — DETERMINISTIC across 5 runs (workflow 3 + coordinator 2, ~30s each, zero timeouts). E2-C: PUT/DELETE /api/settings/key. api.byok.test (7): validate-before-store (reject->400 key_invalid + apiKeyEnc UNCHANGED; accept->stored as {iv,tag,ciphertext}, not plaintext); SENTINEL scan (sk-SENTINEL-<uuid> absent from all response bodies, captured stdout/stderr logs, and DATA_DIR .sqlite/-wal/-shm bytes — present ONLY as ciphertext in secrets.apiKeyEnc); master key absent from DATA_DIR bytes; DELETE purges -> keySet false. Coordinator read routes/settings.ts: validate() FIRST then encrypt(apiKey,masterKey); GET returns keySet boolean only, key never returned/logged."
    },
    {
      "id": "E2-D",
      "title": "/api/tailor uses the decrypted BYOK key via ProviderEngine (in-memory only); no stored key -> 400 no_api_key",
      "status": "done",
      "depends_on": [
        "E2-B",
        "E2-C"
      ],
      "files": [
        "src/server/index.ts",
        "test/api.tailor-byok.test.ts"
      ],
      "origin": "decomposed from E2 (spec §8, §9, §6.1)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nWire /api/tailor (buildApp() in src/server/index.ts) to the user's BYOK key in LIVE mode, keeping the keyless FixtureEngine path for tests/demo. Uses crypto.decrypt (E2-A) + the stored secrets.apiKeyEnc (E2-C). Depends on E2-B (index.ts guard/session — avoid conflicts) and E2-C (key storage).\nLOGIC: keep the engine-mode split. In FIXTURE mode (LEDE_TAILOR_ENGINE=fixture, i.e. tests/CI/demo) -> FixtureEngine, no key needed (current behavior, MUST stay keyless-green). In LIVE mode -> read secrets.apiKeyEnc; if NULL -> respond 400 {error:'no_api_key'} BEFORE any provider call; else decrypt the key in memory (do NOT persist/log the plaintext, discard after the call) and build a per-request ProviderEngine({provider,model,baseUrl,apiKey}) from settings.provider/model/baseUrl + the decrypted key, then run tailor(). Keep the existing mapTailorError distinct statuses (§9). Do NOT change assemble/validate/prompt. Do NOT touch routes/* or auth.\nNOTE: the no_api_key path is keyless-testable — set LEDE_TAILOR_ENGINE=live for that one test with NO stored key -> expect 400 no_api_key (it short-circuits before any live call). Fixture-mode tailor tests remain keyless and green.",
      "acceptance": "- baseline passes; all existing keyless tailor tests (fixture mode) stay green\n- CONTRAST (no_api_key, gameable-resistant): with engine mode LIVE and NO stored key, POST /api/tailor -> 400 {error:'no_api_key'} (short-circuits, no provider call); this differs from fixture-mode 422 no_fixture\n- decrypt-in-memory: with a stored (test-encrypted) key and an injected/stubbed provider path, the tailor route decrypts and builds ProviderEngine with settings.provider/model + the decrypted key; the plaintext key is never logged (assert absence in captured logs) nor persisted beyond secrets.apiKeyEnc\n- new tests in test/api.tailor-byok.test.ts",
      "attempts": [
        {
          "n": 1,
          "failed": "acceptance CONTRAST (fixture-mode 422 no_fixture) fails DETERMINISTICALLY in isolation: expected 422, got 502 provider_error. buildApp decides fixture mode from config.tailorEngine, then calls makeEngine() which IGNORES that decision and re-derives mode from process.env (LEDE_TAILOR_ENGINE ?? NODE_ENV===test?fixture:live). Under vitest NODE_ENV=development -> makeEngine returns a LIVE ProviderEngine even when config.tailorEngine===fixture. Full-suite green was SPURIOUS (relied on NODE_ENV=test leaking from other test files in the shared singleFork process).",
          "hypothesis": "Double source of truth for engine mode: buildApp holds config.tailorEngine but delegates construction to makeEngine(), which reads env independently. The two disagree under test. Also the CONTRAST test is order-dependent (leans on NODE_ENV leakage) rather than proving the app honors its own {tailorEngine:fixture} override.",
          "fixNote": "In src/server/index.ts buildApp: when config.tailorEngine===\"fixture\", construct the fixture engine WITHOUT re-reading env. Cleanest in-scope fix: import { FixtureEngine } from ./tailor/engine and use `config.tailorEngine === \"fixture\" ? new FixtureEngine() : undefined` instead of makeEngine(). (Do NOT change engine.ts/makeEngine — other callers rely on its env behavior; keep the fix inside index.ts, the declared file.) THEN make test/api.tailor-byok.test.ts robust to execution order: the fixture-mode half of the CONTRAST must get 422 no_fixture regardless of process.env.NODE_ENV — e.g. explicitly build the app with {tailorEngine:\"fixture\"} (now honored) and do NOT depend on any leaked NODE_ENV. Keep the LIVE no_api_key (400) and decrypt-in-memory + no-key-leak assertions as-is (those passed reliably). Re-run the new file IN ISOLATION (vitest run test/api.tailor-byok.test.ts) and confirm all pass with NODE_ENV unset."
        }
      ],
      "evidence": "INDEPENDENT RE-VERIFY (branch dcf2130, merged as 9686714): scope=2 declared files only (src/server/index.ts + test/api.tailor-byok.test.ts); gaming read clean (fix real: new FixtureEngine() directly, no makeEngine env indirection; test uses explicit {tailorEngine} overrides, order-independent, real 400-vs-422 contrast + key-leak asserts). bun run check exit 0; bun run build exit 0; ISOLATION `env -u NODE_ENV bunx vitest run test/api.tailor-byok.test.ts` -> 4/4 PASS (the exact prior-failure condition: fixture-mode now 422 no_fixture, was 502); full suite 185/185 (one exit-1 confirmed environmental flake by clean re-run)."
    },
    {
      "id": "E2-E",
      "title": "Client: LoginGate + SettingsView + ApiKeyForm (write-only) + ProviderPicker + ModelPicker",
      "status": "done",
      "depends_on": [
        "E2-B",
        "E2-C"
      ],
      "files": [
        "src/client/components/LoginGate.tsx",
        "src/client/components/SettingsView.tsx",
        "src/client/components/ApiKeyForm.tsx",
        "src/client/components/ProviderPicker.tsx",
        "src/client/components/ModelPicker.tsx",
        "src/client/api.ts",
        "src/client/App.tsx",
        "src/client/hooks/queries.ts",
        "test/settings-auth.test.tsx",
        "test/library.test.tsx"
      ],
      "origin": "decomposed from E2 (spec §7, §8, §13)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nClient auth + settings UI (§13). EXTEND src/client/api.ts (auth: setup/login/logout; key: PUT/DELETE /api/settings/key) throwing ApiError{status,code}. EXTEND src/client/hooks/queries.ts WITHOUT clobbering existing hooks (useEntries/useDeleteEntry/useCreateEntry/useUpdateEntry/useProfile/useSettings/useUpdateProfile/useUpdateSettings) — add auth + key mutations.\n(1) LoginGate: wraps the app; if a query returns 401 (ApiError.status===401) show the login/setup form (first-run = setup password, else login); on success re-render the app. (2) SettingsView (replace the /settings stub route added in E1-F1 — wire it in App.tsx): ApiKeyForm (WRITE-ONLY — shows keySet status from GET /api/settings, an input to set a NEW key, a Delete button; NEVER renders the stored key because the API never returns it), ProviderPicker (from @shared/providers ProviderId list) + ModelPicker (provider+model saved via PUT /api/settings). Use E1-A primitives (select/input/button/dialog). Depends on E2-B (auth endpoints) + E2-C (key endpoints).\nRED-TEAM acceptance: write-only key (the form never displays a key value) + 401 routes to LoginGate.",
      "acceptance": "- baseline passes; all existing client suites (library, entry-editor, profile-layout) stay green\n- CONTRAST (write-only key, gameable-resistant): render ApiKeyForm with a mocked GET /api/settings returning {keySet:true, ...} (and NO key field, because the API never sends one) -> the form shows a 'key is set' status and an input to REPLACE it, but renders NO actual key value anywhere (there is none to render); saving fires PUT /api/settings/key; Delete fires DELETE /api/settings/key\n- CONTRAST (401 -> LoginGate): with a mocked query returning 401, LoginGate shows the login form instead of the app; after a mocked successful login the app content renders\n- ProviderPicker/ModelPicker: changing provider+model fires PUT /api/settings with the new values\n- new tests in test/settings-auth.test.tsx",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY (branch f71462a, merged as d992aff): bun run check exit 0; bun run build exit 0; bun run test 24 files / 181 PASS. Acceptance settings-auth.test.tsx 9/9: write-only key (ApiKeyForm has no key prop; shows keySet status; Save->PUT /api/settings/key, Delete->DELETE; typed key never re-rendered), 401->LoginGate (login form shown, app hidden; post-login app renders), Provider/ModelPicker->PUT /api/settings. No gaming. Scope: only overflow was test/library.test.tsx (forced cascading fix: App now wraps LoginGate/useQuery, NavTabs test needed QueryClientProvider+fetch stub matching sibling tests; assertions preserved). Coordinator judgment [v2-028]: legitimate cascade -> files amended to include it; accepted."
    },
    {
      "id": "E3",
      "title": "Phase 3 — reasoning UI (timebox) + remaining sections",
      "status": "decomposed",
      "depends_on": [
        "E2-D",
        "E2-E"
      ],
      "files": [],
      "origin": "spec §24 Phase 3 (§10, §11, §23)",
      "context": "COARSE EPIC — decompose after E2 into: (a) ReasoningPanel split view (ResultView = ResumePage | ReasoningPanel): SignalsBar→WeightBar, per-group SectionRationale→serif Callout (leadRationale on each job's leading bullet), CutList from cut[]; (b) GUARANTEE leadRationale + cut[] NEVER in the printed resume DOM (§10); (c) remaining sections award/certification/publication/interest/language/reference as data + rendering. RED-TEAM — child tickets MUST include: (1) DOM-ABSENCE not display:none — feed a fixture with NON-EMPTY leadRationale + cut reasons, assert those exact strings are absent from the ACTUAL print-target subtree's textContent (a display:none-only impl FAILS; render the real print root, not ResumePage in isolation); (2) TIMEBOX ceiling — text callouts only, GREP-FORBID EventSource/SSE (non-MVP §15), animation libs (framer-motion), any new tailors/history table; cap reasoning-UI ticket count; (3) remaining sections — feed one entry of EACH new section and assert each renders with its registry label/order, and empty-facts entries (cert/reference) render from meta with text:''.",
      "acceptance": "See oracle.md Phase 3 (merged-tree): ReasoningPanel renders signals + per-group leadRationale + cut; leadRationale/cut ABSENT from the printed DOM tree (not merely hidden); remaining sections render from data.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E3-A",
      "title": "Reasoning UI: ResultView split (ResumePage | ReasoningPanel) — signals + per-group leadRationale + cut; reasoning NEVER in the printed DOM",
      "status": "done",
      "depends_on": [
        "E2-E"
      ],
      "files": [
        "src/client/components/ResultView.tsx",
        "src/client/components/ReasoningPanel.tsx",
        "src/client/components/WeightBar.tsx",
        "src/client/components/Callout.tsx",
        "src/client/components/CutList.tsx",
        "src/client/components/TailorView.tsx",
        "src/client/styles/app.css",
        "src/client/styles/print.css",
        "test/reasoning-ui.test.tsx"
      ],
      "origin": "decomposed from E3 (spec §10, §11, §13)",
      "context": "Build the Phase-3 reasoning UI — spec §11 (the differentiator) + §10 (rendering). Locked (oracle.md, cite; never re-litigate): React18+Vite SPA, TanStack Query, shadcn/ui primitives in src/client/components/ui/ (button/card/badge/tabs/tooltip/etc. — compose, never re-roll). Path alias @shared/*. Client tests: @testing-library/react + jsdom docblock (/** @vitest-environment jsdom */) under /test, run by `bun run test` (vitest). Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Server runs Node/tsx; Bun runs check/build/test. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM. Types (src/shared/types.ts): TailoredResume={signals:JDSignals, summary:string, sections:TailoredSection[], cut:{entryId,reason}[]}; JDSignals={roleLevel:string, weights:string[], hardRequirements:string[]}; TailoredSection={section:Section, groups:TailoredGroup[]}; TailoredGroup={heading?:string, leadRationale?:string, items:{entryId:string,text:string}[]}. Registry SECTIONS (src/shared/sections.ts) gives per-section {label,rephrase,order,groupBy}.\n\nCURRENT STATE: src/client/components/TailorView.tsx renders ONLY `<ResumePage resume={resume} />` after a tailor. src/client/components/ResumePage.tsx already renders header/summary/sections/groups/items generically from the registry label and CORRECTLY omits leadRationale + cut (it must STAY that way — do NOT add them to ResumePage). The reasoning data already rides on the result: TailoredGroup.leadRationale (on each group's leading item) and TailoredResume.cut[] and TailoredResume.signals — but nothing renders them yet.\n\nBUILD (§11 'simplest legible version, text callouts, timebox'):\n(1) ResultView.tsx — a SPLIT view rendering ResumePage (the print target) and ReasoningPanel as SIBLINGS (never nest the panel inside ResumePage). Wire ResultView into TailorView (replace the direct <ResumePage> usage; ResultView takes the TailoredResume).\n(2) ReasoningPanel.tsx (never printed) composes: WeightBar.tsx (renders signals — roleLevel, the weights[] in order, and hardRequirements[]); a serif Callout.tsx used for each group's leadRationale (one editorial serif voice, §12 — a plain serif font-family class is fine; do NOT pull design tokens, that is Phase 4); CutList.tsx = a 'What got buried' list, one row per cut[] {entryId, reason}.\n(3) PRINT: reasoning must not appear on paper. In src/client/styles/print.css (@media print) hide the reasoning subtree; AND structurally keep it a sibling of ResumePage so the print-target subtree (.resume-page) never CONTAINS reasoning strings. Minimal split-layout styles go in app.css.\n\nTIMEBOX CEILING (hard): text callouts ONLY. Do NOT add EventSource/SSE/streaming (non-MVP §15), animation libs (framer-motion), or any saved-tailors/history table/persistence. No new deps.",
      "acceptance": "- baseline passes (check/build/test keyless); existing client suites (library, entry-editor, profile-layout, settings-auth) stay green\n- ReasoningPanel renders signals via WeightBar (assert roleLevel, each weights[] entry in order, and hardRequirements[] appear), each group's leadRationale via a Callout, and each cut[] reason via CutList\n- ResultView shows BOTH ResumePage and ReasoningPanel (split)\n- CONTRAST (DOM-ABSENCE, not display:none — gameable-resistant): render the FULL ResultView with a fixture whose leadRationale and cut reasons are NON-EMPTY, DISTINCTIVE strings; grab the print-target node (the .resume-page subtree) and assert those exact strings are ABSENT from ITS textContent, while PRESENT in the reasoning subtree. (A panel nested inside ResumePage, or a display:none-only approach that leaves the strings in the .resume-page subtree, MUST fail this.)\n- TIMEBOX (grep the diff): NO EventSource/SSE, NO framer-motion/animation lib, NO new tailors/history/persistence table; no new dependency\n- new tests in test/reasoning-ui.test.tsx",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY + merged-tree gate (branch 49eff8b, merged b571baf; master 41249cc). Scope: exactly 9 declared files. check 0/build 0; test 26 files 191 PASS incl. reasoning-ui.test.tsx 6/6. DOM-ABSENCE contrast: ResultView rendered with SENTINEL_RATIONALE_*/SENTINEL_CUT_* — resumePage.textContent excludes them, reasoningPanel.textContent includes them (panel is a sibling, not nested; print.css hides .reasoning-panel). WeightBar renders roleLevel+weights-in-order+hardRequirements; Callout per group leadRationale; CutList per cut reason. TIMEBOX grep clean (no SSE/framer-motion/history); no new deps. Gaming read: clean."
    },
    {
      "id": "E3-B",
      "title": "Remaining sections render from meta: rephrase:'none' label sections with empty facts show meta-derived text (cert/publication/reference/interest/language)",
      "status": "done",
      "depends_on": [
        "E2-E"
      ],
      "files": [
        "src/shared/sections.ts",
        "src/server/tailor/assemble.ts",
        "test/remaining-sections.test.tsx",
        "test/assemble.test.ts"
      ],
      "origin": "decomposed from E3 (spec §10, §4.3, §24)",
      "context": "Close the Phase-3 'remaining sections as data + rendering' gap — spec §10 + §4.3. Locked (oracle.md, cite; never re-litigate): React18+Vite SPA, TanStack Query, shadcn/ui primitives in src/client/components/ui/ (button/card/badge/tabs/tooltip/etc. — compose, never re-roll). Path alias @shared/*. Client tests: @testing-library/react + jsdom docblock (/** @vitest-environment jsdom */) under /test, run by `bun run test` (vitest). Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Server runs Node/tsx; Bun runs check/build/test. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM. Types (src/shared/types.ts): TailoredResume={signals:JDSignals, summary:string, sections:TailoredSection[], cut:{entryId,reason}[]}; JDSignals={roleLevel:string, weights:string[], hardRequirements:string[]}; TailoredSection={section:Section, groups:TailoredGroup[]}; TailoredGroup={heading?:string, leadRationale?:string, items:{entryId:string,text:string}[]}. Registry SECTIONS (src/shared/sections.ts) gives per-section {label,rephrase,order,groupBy}.\n\nWHAT ALREADY WORKS (do NOT rebuild): the section registry (src/shared/sections.ts SECTIONS) covers all 10 sections; the zod schema (src/shared/schema.ts entryMetaZ) is a discriminated union over all sections; SectionMetaFields edits award/cert/publication/reference/interest/language; assemble() and ResumePage are fully registry-GENERIC and already render whatever sections are present under SECTIONS[section].label. Leave ResumePage.tsx and SectionMetaFields.tsx ALONE.\n\nTHE REAL GAP (in src/server/tailor/assemble.ts coerceText): for rephrase:'none' sections it returns `r.entry.facts.join(' ')`. Label sections (certification, publication, reference, interest, language — all rephrase:'none') often have EMPTY facts, so this yields '' and the item renders BLANK. TailoredItem is {entryId,text} and carries NO meta, so the client cannot recover it — the existing code comment even promises 'rendered from meta alone downstream' but nothing does. FIX: compose human-readable display text from the entry's META for rephrase:'none' sections when facts are empty (fall back to facts when present). Put the meta->label-text formatter in the REGISTRY single source (src/shared/sections.ts — §4.3/§17: the registry is THE source of per-section behavior), and have assemble.coerceText consume it. Do NOT scatter meta-formatting into the client, and do NOT add meta to TailoredItem/the shared output types. Read src/shared/types.ts EntryMeta + src/shared/schema.ts for the EXACT per-section meta field names (e.g. certification issuer/name/year, publication title/venue/year, reference name/relationship, language name/level, interest name) — use the real fields, do not invent.\n\nScope: registry formatter + assemble wiring + tests only.",
      "acceptance": "- baseline passes (check/build/test keyless); all existing suites stay green (assemble.test.ts must remain green — extend it or add the new file, do not weaken it)\n- COVERAGE: assemble() over a set with one entry of EACH remaining section (award, certification, publication, interest, language, reference) + a layout enabling them, then render <ResumePage> of the result: each section appears under its registry label SECTIONS[section].label\n- CONTRAST (empty-facts from meta — gameable-resistant): a certification AND a reference entry with facts:[] must render NON-BLANK, meta-derived text (assert the specific meta values, e.g. the cert name/issuer and the reference name, appear in the rendered output). A build that leaves them blank (the current behavior) MUST fail this.\n- rephrase:'none' with NON-empty facts still renders facts (no regression); rephrase!='none' sections (experience/project/education/skill light|full) unchanged\n- new tests in test/remaining-sections.test.tsx",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY + merged-tree gate (branch c9a0797, merged; master 41249cc). coerceText: facts.length? join : rule.metaText?.(meta) ?? '' — registry-owned metaText formatter in sections.ts for certification/publication/reference/language (interest has no meta beyond section). Contrast: empty-facts cert renders 'AWS Certified Cloud Practitioner'/'Amazon Web Services'; empty-facts reference renders 'Jane Smith'; non-empty facts still render facts; rephrase:'full' unchanged; model 'FABRICATED' text still ignored for rephrase:'none' (anti-fabrication holds). remaining-sections.test.tsx 5/5. SCOPE JUDGMENT [v2-032]: touched undeclared test/assemble.test.ts (verify stage missed it) — coordinator judged a FORCED cascade: the one changed assertion previously asserted text==='' for an empty-facts cert, literally encoding the bug this ticket fixes; changed to assert 'Some Cert' (meta-derived) — STRENGTHENS the check toward §10/§4.3 intent, not a weakening. Amended B.files += test/assemble.test.ts; accepted."
    },
    {
      "id": "E4",
      "title": "Phase 4 — render polish + Docker/compose/README (self-host)",
      "status": "decomposed",
      "depends_on": [
        "E3-A",
        "E3-B"
      ],
      "files": [],
      "origin": "spec §24 Phase 4 (§10, §12, §19)",
      "context": "COARSE EPIC — decompose after E3 into: (a) print.css polish to ATS-safe (§10: single-column, #000/#fff, no tables/images/multi-column/borders, system serif/sans NOT Plex); (b) §12 design finish — tokens.css, shadcn theme mapped to tokens, self-hosted IBM Plex via @fontsource, low radius, --accent primary; (c) Dockerfile multi-stage (build SPA+server → slim BUN runtime image; deviation from §19 node-slim per toolchain decision) + docker-compose.yml (one service, volume at DATA_DIR) + .env.example with the two required secrets; migrations on boot; (d) README self-host first-run. RED-TEAM — child tickets MUST include: (1) DEEP boot check — after compose up, migrations ran (tables in the mounted volume) and GET / returns the built index.html (not a stub); (2) carry boot-refusal into the image — compose up with the two secrets ABSENT → container exits non-zero; NO real secret baked in image/compose (grep layers+compose; .env.example placeholders only); (3) VOLUME persistence — write data, compose down && up, data survives; (4) fonts self-hosted — grep build output for fonts.googleapis/CDN font URLs → ABSENT; @fontsource present; (5) print.css grep-checkable (no <table>/<img> in ResumePage, no multi-column in print target, #000/#fff) but TRUE ATS text-extraction/reading-order needs headless print — flag as only partially machine-checkable, requires manual/browser verification (state honestly).",
      "acceptance": "See oracle.md Phase 4 (merged-tree): print.css ATS-safe (grep + honest manual caveat); §12 tokens+shadcn+self-hosted Plex; `docker compose up` deep-boots with the two secrets (migrations ran, real SPA served), refuses without them, volume persists; README first-run documented.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E4-A",
      "title": "Render/design polish (§10, §12): §12 tokens + shadcn theming applied, Plex fonts self-hosted, print.css ATS-safe — with a proving test",
      "status": "done",
      "depends_on": [
        "E3-A",
        "E3-B"
      ],
      "files": [
        "src/client/styles/tokens.css",
        "src/client/styles/app.css",
        "src/client/styles/print.css",
        "tailwind.config.ts",
        "test/render-polish.test.ts"
      ],
      "origin": "decomposed from E4 (spec §10, §12)",
      "context": "Locked (oracle.md; never re-litigate): React18+Vite SPA + Fastify 4 (server) · better-sqlite3+Drizzle · TanStack Query · shadcn/ui+Tailwind. Path alias @shared/*. Toolchain: Bun runs check/build/test (`bun run check|build|test`); the SERVER PROCESS runs under NODE via tsx (better-sqlite3's Node-ABI prebuilt is incompatible with Bun's V8 — locked [v2-017]); `start`=`tsx src/server/index.ts`, `build`=`vite build`. Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM, key never leaks, boot refuses w/o LEDE_MASTER_KEY/LEDE_SESSION_SECRET.\n\nWHAT ALREADY EXISTS (verify, do NOT rebuild): src/client/styles/tokens.css already has the full §12 palette verbatim (--ink #1a1a1a, --bg #fff, --accent #2f5fdd, etc.), --radius 4px, Plex font-family vars, and the shadcn CSS-variable mapping (--primary=var(--accent) etc.). src/client/styles/app.css already @imports @fontsource/ibm-plex-sans|mono|serif (self-hosted, no CDN) then tokens.css, and body uses var(--font-sans)/var(--foreground). src/client/styles/print.css already has @media print: letter page, #000 on #fff, single-column (display:block), Georgia serif (NOT Plex — §10, to avoid PDF embedding surprises), hides .jd-input/.tailor-view__pending/.tailor-view__error/.reasoning-panel, list-style bullets. @fontsource deps + tailwind.config.ts + postcss.config.js all present.\n\nTHE GAP: there is NO machine-checkable test proving Phase-4 render polish, and the phase oracle requires it. ADD test/render-polish.test.ts that asserts the §10/§12 properties by reading the CSS files and/or rendering: (1) tokens.css declares the §12 color tokens with their EXACT spec values + --radius:4px + the three Plex font-family vars; (2) app.css self-hosts Plex via @fontsource @import and does NOT reference fonts.googleapis/CDN; (3) print.css @media print block sets color:#000 + background:#fff, is single-column (no display:flex/grid/columns/float multi-column, no table/img rules) and hides .reasoning-panel. Fix any real gap the test surfaces (e.g. a stray CDN font ref, a non-token hardcoded color in app chrome, a multi-column rule in print). Do NOT touch the resume DOM structure or reasoning UI (Phase 3, done). Keep changes minimal — most of this is already in place.",
      "acceptance": "- baseline passes (check/build/test keyless); all existing suites stay green\n- §12 tokens present in tokens.css with the EXACT spec values (--ink #1a1a1a, --bg #fff, --accent #2f5fdd, --border #e4e4e7, --radius 4px, Plex font vars) and mapped to shadcn CSS variables\n- Plex fonts self-hosted via @fontsource (@import in app.css); NO Google-Fonts/CDN reference anywhere in styles\n- print.css @media print: #000 on #fff, single-column, no table/image/multi-column rules, reasoning panel hidden — asserted mechanically (read the CSS)\n- new test in test/render-polish.test.ts covering the above (fails if a token is wrong/missing, a CDN font is introduced, or print gains a multi-column/table rule)",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY + merged-tree gate (branch 18ea8b4; master 74a0420). Scope: only test/render-polish.test.ts (existing tokens.css/app.css/print.css/tailwind.config.ts already matched §12/§10 verbatim — verified unchanged). check 0/build 0 (dist bundles self-hosted ibm-plex *.woff2, no CDN). render-polish.test.ts 11/11: §12 token values exact + shadcn mapping; @fontsource imports + no googleapis/gstatic; print @media #000-on-#fff, single-column, no table/img/multi-column, .reasoning-panel hidden. Full suite 207 (branch) / 215 (merged)."
    },
    {
      "id": "E4-B",
      "title": "Serve the built SPA from the server (§19): GET / returns index.html with SPA fallback; /api/* not shadowed — implements the index.ts:139 TODO",
      "status": "done",
      "depends_on": [
        "E2-D",
        "E2-E"
      ],
      "files": [
        "src/server/index.ts",
        "package.json",
        "test/spa-serving.test.ts"
      ],
      "origin": "decomposed from E4 (spec §19; index.ts:139 TODO)",
      "context": "Locked (oracle.md; never re-litigate): React18+Vite SPA + Fastify 4 (server) · better-sqlite3+Drizzle · TanStack Query · shadcn/ui+Tailwind. Path alias @shared/*. Toolchain: Bun runs check/build/test (`bun run check|build|test`); the SERVER PROCESS runs under NODE via tsx (better-sqlite3's Node-ABI prebuilt is incompatible with Bun's V8 — locked [v2-017]); `start`=`tsx src/server/index.ts`, `build`=`vite build`. Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM, key never leaks, boot refuses w/o LEDE_MASTER_KEY/LEDE_SESSION_SECRET.\n\nImplement the deferred TODO at src/server/index.ts:139 ('in production, serve the built SPA from dist/ as static files'). Add @fastify/static (a server dep listed in §20 but NOT yet in package.json — add it; manifest allowlisted). In buildApp(), register static serving of the Vite build output (repo-root dist/, containing index.html + assets/) with a SPA history fallback: a GET request that is NOT under /api/* and does not match a static asset returns dist/index.html (so client-side react-router routes like /library, /settings work on refresh). /api/* routes and the auth guard MUST be unaffected and take precedence — /api 404s must still return JSON, never index.html.\n\nCRITICAL — keyless tests run WITHOUT a built dist/: static serving must be TOLERANT of a missing dist directory (register the static plugin/fallback only if the dist dir exists, or otherwise ensure boot + the full keyless suite + boot.smoke still pass when dist/ is absent). Do NOT break existing api.*/boot.smoke/tailor tests. The dist path should be resolvable relative to the server file (e.g. <repo>/dist), overridable if a config field already exists — do not invent new config unless needed.",
      "acceptance": "- baseline passes (check/build/test keyless) WITHOUT a built dist present — existing api.*, boot.smoke, tailor suites stay green (static serving must be a no-op / tolerant when dist/ is absent)\n- NEW test/spa-serving.test.ts: with a temp dist/ containing a marker index.html, GET / returns that index.html; a non-/api client route (e.g. GET /library) also returns index.html (SPA fallback)\n- CONTRAST (/api not shadowed — gameable-resistant): with the same dist present, GET /api/health still returns {ok:true} (JSON, not the SPA html); an unknown GET /api/nope returns a JSON 404, NOT index.html\n- @fastify/static added to package.json; no other deps",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY + merged-tree gate (branch 9e2275d; master 74a0420). Scope: src/server/index.ts + package.json + test/spa-serving.test.ts (declared) + bun.lock (allowlist). Added @fastify/static 7.0.4 only. spa-serving.test.ts 8/8: dist-present GET / + client routes -> index.html, real assets served, CONTRAST /api/health stays JSON {ok:true} + unknown /api/* -> JSON 404 (not shadowed); dist-ABSENT GET / -> plain 404 (no crash), /api/health unaffected, full keyless suite green. Merged-tree gate: full suite 215/215, boot.smoke 3/3 clean, LIVE boot (bun start, real dist) GET / -> real SPA (<title>Lede</title>) + /api/health {ok:true}. check 0."
    },
    {
      "id": "E4-C",
      "title": "Docker + compose + README self-host (§19): Node multi-stage image (build SPA + run server under tsx), docker-compose.yml, first-run README",
      "status": "done",
      "depends_on": [
        "E4-B"
      ],
      "files": [
        "Dockerfile",
        "docker-compose.yml",
        "README.md",
        ".dockerignore"
      ],
      "origin": "decomposed from E4 (spec §19, §24)",
      "context": "Locked (oracle.md; never re-litigate): React18+Vite SPA + Fastify 4 (server) · better-sqlite3+Drizzle · TanStack Query · shadcn/ui+Tailwind. Path alias @shared/*. Toolchain: Bun runs check/build/test (`bun run check|build|test`); the SERVER PROCESS runs under NODE via tsx (better-sqlite3's Node-ABI prebuilt is incompatible with Bun's V8 — locked [v2-017]); `start`=`tsx src/server/index.ts`, `build`=`vite build`. Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM, key never leaks, boot refuses w/o LEDE_MASTER_KEY/LEDE_SESSION_SECRET.\n\nProduction self-host packaging (§19). The CURRENT Dockerfile is WRONG and must be rewritten: it uses `FROM oven/bun` and `CMD [\"bun\",\"run\",\"src/main.ts\"]` — but (a) the server MUST run under Node/tsx not Bun (locked [v2-017]: better-sqlite3 Node-ABI prebuilt is incompatible with Bun's V8 — a Bun runtime dies ERR_DLOPEN_FAILED at boot), (b) src/main.ts does not exist (server entry is src/server/index.ts; `start`=`tsx src/server/index.ts`), (c) it is single-stage and never runs `vite build`, so no SPA is served, (d) EXPOSE 3000 is wrong (PORT default 8787).\n\nBuild a NODE multi-stage image: a build stage that installs deps and runs `bun run build` (or `vite build`) to produce dist/, and a slim Node runtime stage that has the deps + src/ + dist/ and runs the server via tsx (e.g. `tsx src/server/index.ts`, matching `start`). EXPOSE 8787. Data lives at DATA_DIR (default /app/data) — declare a VOLUME. Add docker-compose.yml: one service, a named volume mounted at DATA_DIR, env LEDE_MASTER_KEY + LEDE_SESSION_SECRET (required) + PORT + DATA_DIR (+ optional LEDE_AUTH_DISABLED), port mapping. Update README.md with the first-run flow: `docker compose up --build` → set the two secrets (openssl rand -base64 32) → open the app → set password → add provider key in Settings → tailor. Update .dockerignore (exclude node_modules/.git/dist/data as appropriate for the build context). This depends on E4-B so the running image actually serves the SPA.",
      "acceptance": "- baseline passes (check/build/test keyless — these run on the host, unaffected)\n- Dockerfile is NODE-based multi-stage (grep: NOT oven/bun; runs the server via tsx/node; runs vite build; EXPOSE 8787) and declares a VOLUME at DATA_DIR\n- docker-compose.yml: one service, volume at DATA_DIR, LEDE_MASTER_KEY+LEDE_SESSION_SECRET env, port mapping\n- LIVE (heavy, Docker is available in this env): `docker compose up --build -d` with the two secrets set builds and boots; GET /api/health -> {ok:true} AND GET / returns the SPA index.html (a UI-less API must NOT pass); then bring it down. Capture the real curl output.\n- README documents the first-run self-host flow",
      "attempts": [],
      "evidence": "INDEPENDENT RE-VERIFY (branch f049350, merged). Scope: 4 declared files (Dockerfile, docker-compose.yml, README.md, .dockerignore). Dockerfile = multi-stage, RUNTIME node:22-bookworm-slim (NOT Bun, per [v2-017]); npm --omit=dev + tsx; builds+copies dist/; EXPOSE 8787; VOLUME /app/data. Coordinator ran `docker compose up --build` (host 8899): container booted (Server listening 0.0.0.0:8787); GET /api/health -> 200 {ok:true}; GET / -> 200 <title>Lede</title> + /assets/index-DdnsMgKH.js (real SPA, not UI-less); GET /api/nope -> 401 JSON (API not shadowed by SPA fallback). Teardown clean. README documents first-run. No gaming."
    },
    {
      "id": "E5-A",
      "title": "Playwright scaffold + Phase 1 LibraryView CRUD browser spec",
      "status": "done",
      "depends_on": [],
      "files": [
        "package.json",
        "bun.lock",
        "playwright.config.ts",
        ".gitignore",
        "vitest.config.ts",
        "test/e2e/library-crud.spec.ts",
        "test/e2e/helpers/tmpdata.ts"
      ],
      "origin": "spec §9, §21 + oracle [v2-036] browser acceptance (Phase 1)",
      "context": "Locked (oracle.md; never re-litigate): server runs under Node/tsx not Bun [v2-017]; PORT default 8787; keyless baseline unless doing the KEY-GATED T014 eval. Install `@playwright/test` as a dev dep. Create `playwright.config.ts`: chromium project only, `webServer` boots `bun run start` (Node/tsx per [v2-017]) on a per-run DATA_DIR (tmp dir; the helper `test/e2e/helpers/tmpdata.ts` creates and passes it via env), baseURL http://localhost:${PORT}, `reuseExistingServer: !process.env.CI`, `testDir: 'test/e2e'`, timeout 60s. Update `vitest.config.ts` to exclude `test/e2e/**` (vitest must not try to run playwright specs). Update `package.json`: `test` becomes composite `vitest run && playwright test` (baseline per [v2-036]); add `test:e2e` (`playwright test`) for iteration; do NOT add a `postinstall` — chromium install is a one-time human step documented in README (later ticket may extend). Author `test/e2e/library-crud.spec.ts` covering experience + project sections (skill/education intentionally deferred to a follow-up if time permits — call out in evidence): Create (open form → fill required fields → save → assert row appears in list), Edit (open row → change a field → save → assert list reflects change), Delete (delete → assert row gone), Reload persistence (create → page.reload() → assert row still present, from the browser side, complementing the existing keyless api restart test). If auth is enabled by default, skip via `LEDE_AUTH_DISABLED=1` in the webServer env — Phase 2 spec (E5-B) is what proves the auth flow. LOCKED: no CI-only fixtures; the spec must run locally against the same server the human runs.",
      "acceptance": "- `bun install` succeeds; `bun run check` exit 0; `bun run build` exit 0\n- `bun run test` (composite) runs BOTH vitest and playwright; both green; the composite exit code is 0 iff both are\n- library-crud.spec.ts passes chromium against a fresh DATA_DIR (webServer wired to a tmp dir via helpers/tmpdata.ts)\n- DELIBERATE-BREAK sanity (do it, then revert; evidence must record both runs): temporarily change the POST /api/entries handler to no-op (return 200 but do not persist) → the spec FAILS at the Create assertion (proves it drives real UI+API, not a mock)\n- vitest.config.ts excludes test/e2e/** (evidence: `bun run vitest run test/e2e/library-crud.spec.ts` — an intentional call — reports 'No test files found', not a syntax error)\n- scope: only files listed touched; bun.lock allowlisted",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify on worktree branch (now merged @07848f0, byte-identical, no-ff no intervening commits): bun run check exit 0; bun run build exit 0; composite `bun run test` (PORT=18899 — 8787 occupied by unrelated dev server on this shared box) -> vitest 215/215 passed THEN playwright 8/8 passed, composite exit 0. Scope: git diff --name-only master = .gitignore, bun.lock, package.json + new playwright.config.ts, vitest.config.ts, test/e2e/{helpers/tmpdata.ts, library-crud.spec.ts} — exactly the declared set; src/ diff empty (deliberate-break reverted). Gaming read CLEAN: spec drives real chromium against a real Node/tsx server boot (webServer), role-based selectors read from actual LibraryView/EntryEditor; CONTRAST assertions (edit: new text visible AND old toHaveCount(0); delete: toHaveCount(0); reload persistence: page.reload() then re-assert visible). Builder deliberate-break corroborated: 8 failed at the Create assertion when POST /api/entries was no-op'd; 8 passed after exact revert. vitest excludes test/e2e via mergeConfig array-concat (configDefaults.exclude preserved); `bunx vitest run test/e2e/...` -> 'No test files found'."
    },
    {
      "id": "E5-B",
      "title": "Phase 2 auth: first-run set-password → login → protected route browser spec",
      "status": "done",
      "depends_on": [
        "E5-A"
      ],
      "files": [
        "test/e2e/auth.spec.ts",
        "test/e2e/helpers/session.ts",
        "playwright.config.ts",
        "src/server/index.ts"
      ],
      "origin": "spec §7, §8, §24 Phase 2 + oracle [v2-036] browser acceptance (Phase 2) + repair: browser gate exposed pre-existing session-cookie path bug (scope expanded to src/server/index.ts — see ledger [v2-038])",
      "context": "Locked: LEDE_MASTER_KEY + LEDE_SESSION_SECRET required to boot; session cookie must be HttpOnly (§8, §17); /api/tailor with no stored key → 400 no_api_key (keyless test, unchanged — do NOT set a provider key in this spec). Extend the playwright config: a second project 'auth' (or a per-test webServer env override) that boots WITHOUT `LEDE_AUTH_DISABLED` (default) against a fresh DATA_DIR — the auth gate must actually be active. Author `test/e2e/auth.spec.ts` driving: (1) visit / on fresh boot → assert set-password screen; submit a password; (2) redirected to logged-in view; (3) log out (UI action if exists) or clear session cookie via context.clearCookies(); (4) direct-nav to a protected route → refused, redirected to login; (5) log in with the previously-set password → protected route now reachable. Assert the session cookie has `HttpOnly` true via context.cookies(). Extract a helper `test/e2e/helpers/session.ts` exporting `ensureFirstRunPassword(page, password)` and `login(page, password)` so E5-C reuses without duplication. LOCKED: this spec does NOT store an api key.\n\n[COORDINATOR SCOPE EXPANSION — ledger v2-038] The auth spec you wrote CAUGHT A REAL PRE-EXISTING BUG (verified by the coordinator): @fastify/secure-session in src/server/index.ts (~L109) registers with NO `cookie` option, so the session cookie's Path defaults (RFC 6265) to the login endpoint's dir '/api/auth' — after login the cookie is never sent to /api/settings etc., so LoginGate's ping stays 401 and the app is unreachable in a real browser. Inject-based tests (.inject ignores cookie Path) structurally cannot catch this — the browser spec is the only possible proof, so the repair is folded into THIS ticket. FIX (verified fail->fix->pass by coordinator): add `cookie: { path: \"/\" }` to the secureSession options. Declared files now INCLUDE src/server/index.ts for exactly this one-line fix. Escaped-bug rule: your auth.spec.ts IS the strengthening check that locks it in.",
      "acceptance": "- `bun run test` composite still green; the auth spec passes chromium\n- HttpOnly assertion is real: DELIBERATE-BREAK — temporarily remove `httpOnly: true` from the session cookie config on the server → the spec FAILS the HttpOnly assertion (evidence records both runs)\n- guard assertion is real: DELIBERATE-BREAK — temporarily remove the auth preHandler from one protected route → the spec FAILS step (4) (evidence records both runs)\n- helpers/session.ts is imported and used by the spec (not dead code) — grep-verifiable\n- scope: only files listed touched",
      "attempts": [
        {
          "n": 1,
          "failed": "blocked: composite baseline could not pass — auth.spec.ts (correctly) fails post-login against unfixed src",
          "hypothesis": "session cookie Path defaults to /api/auth (no cookie option on secureSession); cookie not sent to /api/settings ping -> app unreachable post-login in a real browser. Inject tests blind to Path.",
          "fixNote": "Scope expanded to include src/server/index.ts. Apply the one-line fix `cookie: { path: '/' }` to secureSession options; re-run composite to confirm auth spec + library-crud all green."
        }
      ],
      "evidence": "INDEPENDENT re-verify by coordinator on worktree branch (merged @a10a0ec, byte-identical: clean ort merge of 4 disjoint files, no intervening commits). The spec CAUGHT + FIXED a real bug (see ledger v2-038): session cookie Path defaulted to /api/auth -> app unreachable post-login in a real browser; fix = `cookie: { path: '/' }` (src diff is ONLY that one line). check exit 0; build exit 0; final composite `bun run test` (PORT=18899) = vitest 215/215 THEN playwright 9/9 (8 library-crud + 1 auth), exit 0. Deliberate-breaks BOTH confirmed load-bearing: (1) httpOnly:false -> auth FAILS at line 45 httpOnly assertion -> reverted; (2) /api/settings added to PUBLIC_PREFIXES (guard defeated) -> auth FAILS at step 1 set-password assertion -> reverted. Final src diff = only the cookie line. Flake hardening (coordinator, test-timing only — not a semantic weakening): logout step now waits deterministically for POST /api/auth/logout via waitForResponse, and post-transition assertions in auth.spec.ts + session.ts get 15s headroom (matches vite.config.ts's existing 30s-vitest contention convention). Verified: full playwright suite 5/5 green under back-to-back contention (incl. a 30.6s contended run that previously flaked). Scope: git diff --name-only master = playwright.config.ts, src/server/index.ts + new test/e2e/auth.spec.ts, test/e2e/helpers/session.ts — exactly the declared set (src/server/index.ts added by the v2-038 scope expansion). library-crud.spec.ts untouched. Design: two-server/two-project playwright config — 'chromium' project (auth-disabled server, library-crud) + 'auth' project (auth-enabled server on PORT+1, fresh DATA_DIR). session.ts helpers (ensureFirstRunPassword, login) general + reused-ready for E5-C."
    },
    {
      "id": "E5-C",
      "title": "Phase 4 docker + browser: SPA mounts and /api/* round-trips inside the container",
      "status": "done",
      "depends_on": [
        "E5-A",
        "E5-B"
      ],
      "files": [
        "test/e2e/docker-spa.spec.ts",
        "test/e2e/helpers/docker.ts",
        "playwright.config.ts",
        "package.json"
      ],
      "origin": "spec §19 + oracle [v2-036] browser acceptance (Phase 4, sharpens [v2-033])",
      "context": "Locked: image runs under Node/tsx not Bun [v2-017]; container needs LEDE_MASTER_KEY + LEDE_SESSION_SECRET; §19 first-run flow. Add a Playwright project 'docker' in `playwright.config.ts` — no `webServer` for this project; instead a globalSetup (`test/e2e/helpers/docker.ts`) runs `docker compose -p lede-e2e up --build -d` with the two secrets set to test values (generate with openssl rand or hardcode test-only values — clearly marked), waits until GET /api/health returns 200 on the published host port, then Playwright uses that port as baseURL for this project only. globalTeardown runs `docker compose -p lede-e2e down -v` and asserts no orphaned container remains for the project. The 'docker' project is EXCLUDED from `playwright test` default (it's slow + requires docker). The composite baseline must invoke it explicitly — extend package.json `test` script to: `vitest run && playwright test && playwright test --project=docker`. Author `test/e2e/docker-spa.spec.ts`: (1) open baseURL — assert response is HTML with a script tag pointing to /assets/*.js; (2) wait for React root to mount — root selector's innerHTML length > threshold AND expected app-shell text present (not just a title tag — a static HTML file could pass a title assertion); (3) collect page.on('pageerror') + page.on('console', err) listeners from load start; assert none fired by domcontentloaded + a settle wait; (4) use the E5-B session helper to reach a protected page → observe a /api/settings XHR resolve 200 through the SPA (proves the app-inside-the-container talks to the api-inside-the-container end-to-end).",
      "acceptance": "- docker present locally (baseline requirement; ticket blocks with clear reason otherwise)\n- `bun run test` composite runs vitest + playwright default + `playwright test --project=docker`; all green\n- DELIBERATE-BREAK (mount check is real): temporarily rename dist/assets/*.js in the image (e.g. rebuild with a broken vite config OR shell into container and mv the asset) → docker spec FAILS step (2) mount OR step (3) console-error listener (evidence records the failing run + the revert run)\n- DELIBERATE-BREAK (console-error listener is real): temporarily introduce a runtime `console.error` in App bootstrap → docker spec FAILS step (3) (evidence records both runs)\n- globalTeardown runs `docker compose down -v`; a `docker ps -a --filter name=lede-e2e` after the run shows no orphan containers\n- scope: only files listed touched",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify by coordinator + FINAL merged-tree run on master @9d552be (after `bun install`): composite `bun run test` (PORT=39001) = vitest 215/215 THEN playwright default 9/9 (chromium+auth) THEN LEDE_E2E_DOCKER=1 playwright --project=docker 1/1 (real `docker compose -p lede-e2e up --build` of the shipped image, health-poll, run, `down -v`), exit 0. Container Created->Started->Stopped->Removed; `docker ps -a --filter name=lede-e2e` empty afterward (teardown throws on orphans). check exit 0; build exit 0. Gaming read CLEAN: mount check asserts real app-shell (#root innerHTML>200 + LoginForm 'Password' label + 'First time here?' text — not a static shell/title); console-error listener allowlists ONLY the pre-login GET /api/settings 401 (LoginGate's own probe) and does NOT allowlist pageerror; step-4 round-trip waits for GET /api/settings 200 through the authed session in-container (the exact path the E5-B cookie bug broke). Nested-env networking (docker-outside-of-docker: /.dockerenv detected -> join compose net, address http://lede:8787) falls back to http://localhost:8899 on a real host — same production container either way, not a CI-only fixture. Fixed TEST-ONLY master key documented (avoids Compose recreating the container between runs). Deliberate-breaks BOTH confirmed by builder (assertions read as load-bearing): (1) truncate served dist/assets/*.js -> mount assertion fails; (2) console.error in main.tsx bootstrap (distinct text) -> console-error assertion fails; each reverted -> passes. src diff empty after. Scope: git diff --name-only master = package.json, playwright.config.ts + new test/e2e/docker-spa.spec.ts, test/e2e/helpers/docker.ts — exactly declared; bun.lock untouched; other e2e specs/helpers + src/ untouched. docker project gated on LEDE_E2E_DOCKER=1 (project only defined + globalSetup no-ops otherwise), so a bare `playwright test` never touches Docker."
    },
    {
      "id": "E6-A1",
      "title": "Applications table + Application type + zod + migration 0001",
      "depends_on": [],
      "files": [
        "src/server/db/schema.ts",
        "src/shared/types.ts",
        "src/shared/schema.ts",
        "drizzle/0001_applications.sql",
        "drizzle/meta/_journal.json",
        "drizzle/meta/0001_snapshot.json",
        "test/schema.test.ts",
        "test/db.test.ts"
      ],
      "origin": "spec §27 (Application domain) + §4.2 conventions — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nAdd the persistent Applications entity. NET-NEW (nothing exists today; current schema has only entries/profile/settings/secrets in src/server/db/schema.ts).\n1) src/server/db/schema.ts — add `applications` sqliteTable per §27: id text PK; company text?; role text?; jobDescription text NOT NULL; context text?; current text json $type<TailoredResume>? ; locked text json $type<TailoredResume>? ; genState text NOT NULL default 'untailored'; currentMeta text json $type<{at:number;provider:ProviderId;model:string}>? ; createdAt integer NOT NULL; updatedAt integer NOT NULL. Follow the existing json-column + naming idiom in this file (snake_case db names).\n2) src/shared/types.ts — add `Application` TS type mirroring §27 exactly (genState union: 'untailored'|'tailoring'|'tailored'|'failed'). Import ProviderId/TailoredResume already there.\n3) src/shared/schema.ts — add zod: `applicationCreate` ({ jobDescription required 1..20000, company?, role?, context? }) and `applicationUpdate` (all optional). Match the existing zod idiom in this file.\n4) Generate the migration: run `bunx drizzle-kit generate` (drizzle.config.ts points at drizzle/). It emits drizzle/0001_*.sql + updates drizzle/meta. If the generated basename differs from 0001_applications.sql, that filename is allowlisted-equivalent — keep whatever drizzle-kit emits and update the declared files note in your report. Migrations run on boot via migrateDb() in src/server/db/index.ts (no code change needed there).\n5) Extend test/schema.test.ts + test/db.test.ts: assert a fresh-boot DB has the applications table with all §27 columns and that an insert→read round-trips (incl json current/locked/currentMeta). No behavior beyond schema — these tests ARE this ticket's tests.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- Fresh DATA_DIR boot runs migration 0001 and creates `applications` with every §27 column (assert by insert+read of a full row incl json fields).\n- `Application` TS type compiles and matches §27; applicationCreate zod rejects a missing jobDescription and accepts optional company/role/context.\n- No hiring-status field exists (only genState) — not-a-tracker guard.\nRED-TEAM SHARPENED [v3-001]: (#7) insert a row with NON-NULL `current` (a real TailoredResume) + `currentMeta` → read back DEEP-EQUAL (proves json $type round-trips, not mere column presence); insert OMITTING `genState` → read returns 'untailored' (the §27 default actually applies).",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @1f51f5e (coordinator ran, not builder): scope CLEAN (8 files ⊆ declared; migration emitted 0001_marvelous_steel_serpent.sql). check exit 0; lint (biome) clean; vitest 227/227 incl schema.test (applicationCreate rejects missing/empty/>20000 jobDescription; applicationUpdate all-optional) + db.test (§27 table has all columns; NON-NULL current+currentMeta deep-equal round-trip [red-team #7]; genState defaults untailored on omit [#7]; no hiring-status column [not-a-tracker]); boot.smoke 3/3 (migration 0001 boots + /api/health, boot-refusal invariants hold). Gaming read: schema matches §27 exactly (snake_case, current/locked json, gen_state default), real CREATE TABLE, tests use real on-disk migrate — no gaming.",
      "status": "done"
    },
    {
      "id": "E6-A2",
      "title": "Applications CRUD routes + client API + query hook",
      "depends_on": [
        "E6-A1"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/server/index.ts",
        "src/client/api.ts",
        "src/client/queries/useApplications.ts",
        "test/api.applications.test.ts"
      ],
      "origin": "spec §9 (applications CRUD) — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nNET-NEW routes. Model on existing src/server/routes/entries.ts (Fastify plugin, zod-validated, session-guarded — the auth guard is registered globally in index.ts so /api/* is already protected).\n1) src/server/routes/applications.ts (new plugin `applicationsRoutes`): GET /api/applications → Application[] but LIST payload is metadata + genState + currentMeta ONLY — OMIT the heavy `current`/`locked` snapshots (perf + §9 'no heavy snapshots'). POST /api/applications (body applicationCreate) → new Application (slug/uuid id; genState 'untailored'). GET /api/applications/:id → full Application incl current+locked. PUT /api/applications/:id (applicationUpdate). DELETE /api/applications/:id → { ok }.\n2) Register applicationsRoutes in src/server/index.ts buildApp() alongside entriesRoutes/profileRoutes (~L120). Do NOT touch the inline /api/tailor route here (that is E6-A3).\n3) src/client/api.ts — typed helpers: listApplications, createApplication, getApplication, updateApplication, deleteApplication (throw ApiError like the existing helpers).\n4) src/client/queries/useApplications.ts (new) — TanStack Query hooks; key ['applications'] for list, ['applications',id] for detail; mutations invalidate.\n5) test/api.applications.test.ts (new) — keyless via buildApp().inject().",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- inject round-trip: create → list → get → update → delete all succeed with correct shapes.\n- LIST payload asserts `current`/`locked` are ABSENT; GET :id includes them (null initially).\n- Persistence: after create, reopen the DB / build a fresh app instance against the same DATA_DIR → the row is still returned (survives restart).\n- Unauthenticated request → 401 (guard applies).\nRED-TEAM SHARPENED [v3-001]: (#4) do NOT test list-omits-snapshots while current is null — directly DB-insert an application with a NON-NULL `current`, then assert the list payload has NO `current`/`locked` key for it while GET :id returns the full `current`. Persistence must use an on-disk DATA_DIR + a genuinely NEW db connection (not :memory:/shared singleton).",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @2a44417: scope CLEAN (5 files ⊆ declared). check 0; lint clean; vitest 233/233 incl api.applications.test (7: CRUD round-trip w/ shapes, bad-body 400, unknown-id 404, fresh-connection persistence [real restart sim], 401 unauth via authDisabled:false, RED-TEAM #4 non-null current inserted → LIST not.toHaveProperty current/locked + GET:id toEqual(resume)). Gaming read CLEAN: GET list uses explicit LIST_COLUMNS omitting current/locked (real omission), uuid id, genState untailored, mirrors entries.ts, real DB. NOTE: hooks landed at src/client/queries/useApplications.ts per declared files, but repo convention is src/client/hooks/queries.ts — coordinator ticket-authoring path guess (spec §21); functional, B1 imports from actual path; consolidation optional.",
      "status": "done"
    },
    {
      "id": "E6-A3",
      "title": "App-scoped tailor (replaces stateless /api/tailor) + context plumbing",
      "depends_on": [
        "E6-A2"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/server/tailor/engine.ts",
        "src/client/api.ts",
        "src/client/queries/useApplications.ts",
        "test/engine.test.ts",
        "test/validate.test.ts",
        "test/api.applications-tailor.test.ts"
      ],
      "origin": "spec §9 (:id/tailor persists current) + §6.2 (context boundary) — v3; REPLACES stateless POST /api/tailor",
      "context": "Locked: provider-agnostic AI SDK; Drizzle/better-sqlite3; Fastify; Node/tsx; Bun test runner. Applications NOT a tracker (only genState). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nSCOPE (coordinator-refined [v3-006]): PURELY ADDITIVE + BACKWARD-COMPATIBLE. Do NOT remove the stateless POST /api/tailor route, the api.ts tailor() helper, or TailorView — those are torn down ATOMICALLY in E6-B2 (the UI cutover), because TailorView still imports tailor() and removing it here would red the build. Everything you add must leave the existing stateless path building and passing.\nCURRENT STATE: stateless POST /api/tailor is inline in src/server/index.ts; client tailor() is in src/client/api.ts (~L68) and used inline by TailorView.tsx. There is NO useTailor hook. applicationsRoutes + the applications query hook (src/client/queries/useApplications.ts) already exist (E6-A2).\n1) src/server/tailor/engine.ts: extend TailorEngine.decide(jd, entries, context?) and tailor(engine, jd, entries, layout, baseSummary?, context?) — context OPTIONAL so the existing stateless caller still compiles unchanged. Extract a pure helper buildUserPrompt(jd, context?): with NO context it MUST return the exact current user message (byte-identical to engine.ts line ~48: \"Tailor for this job description:\" then a blank line then the jd) so the T014 flip-path is unchanged; with context, append a clearly-labelled context block. ProviderEngine.attempt calls buildUserPrompt. FixtureEngine.decide keeps keying on hashKey(jd, entries) — do NOT add context to the hash. Do NOT edit prompt.ts (SYSTEM_PROMPT is frozen). validateNoFabrication is called with entries ONLY.\n2) src/server/routes/applications.ts: add POST /api/applications/:id/tailor — load the app (404 if missing); run tailor() over the CURRENT library + app.jobDescription + app.context; on success persist the result as current, set genState \"tailored\" + currentMeta {at,provider,model}; on FAILURE leave current UNTOUCHED and set genState \"failed\". Live mode with no stored key → 400 {error:\"no_api_key\"}. Map provider errors per §9. Reuse the engine-selection + key-decrypt logic the stateless route already uses (import/share it, do not silently fork a second copy).\n3) src/client/api.ts: ADD tailorApplication(id) (KEEP tailor()). src/client/queries/useApplications.ts: ADD a useTailorApplication mutation that invalidates [\"applications\", id].\n4) Tests: test/engine.test.ts (buildUserPrompt: null===baseline string; with-context contains ctx); test/validate.test.ts (red-team #6); test/api.applications-tailor.test.ts NEW (:id/tailor behavior + red-team #1 & #11, keyless via FixtureEngine — create the app with a JD that is a RECORDED fixture so decide replays).",
      "acceptance": "Narrow gate (all green): bun run format; bun run lint; bun run check exit 0; bunx vitest run --exclude test/boot.smoke.test.ts; bun run test:e2e (playwright chromium). New tests ship. Touch ONLY declared files.\n- buildUserPrompt(jd, null/undefined) === the exact current user message (byte-identical baseline — guards the flip); buildUserPrompt(jd, ctx) contains ctx.\n- Existing 3 fixtures still replay (hashKey unchanged); the existing stateless tailor tests (tailor-db / tailor.e2e / api.tailor-byok) STILL PASS UNCHANGED (you did not touch them; the stateless path is intact).\n- POST :id/tailor persists current + currentMeta + genState \"tailored\"; survives a fresh app instance/restart; re-tailor overwrites current.\n- RED-TEAM #1 (HIGH — real path, not just the helper): with a spy/mock engine, tailoring an app whose stored context is non-empty → engine.decide RECEIVES the context AND the composed user message contains it; empty context → user message byte-identical to baseline (same spy). Exercise route→engine, not only the pure fn.\n- RED-TEAM #6: validateNoFabrication unit — a number present only in a context string (absent from every entry) still throws FabricationError; validateNoFabrication is called with entries only (context not in its args).\n- RED-TEAM #11: failed-tailor with a PRIOR current — tailor once (current=S), then force a failing re-tailor → current still deep-equals S (not null) AND genState===\"failed\".\n- Live mode, no stored key → 400 {error:\"no_api_key\"}.\n- DEFERRED (do NOT attempt, no key available): the live context-shifts-emphasis eval.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @2cf5f16: scope CLEAN (7 files ⊆ declared). check 0; lint clean; vitest 253/253 (clean isolated run; a 1-test flake under concurrent-process contention did NOT reproduce — env, cf [v2-035]); playwright chromium 8/8. Gaming read CLEAN: engine.ts buildUserPrompt(jd,null)===baseline byte-identical, context appended to USER MESSAGE only, SYSTEM_PROMPT/prompt.ts untouched, FixtureEngine keys hashKey(jd,entries) (context excluded), tailor() calls validateNoFabrication(entries only). Route :id/tailor persists current+currentMeta+genState tailored, failure→genState failed + current untouched, no-key→400; resolveEngine/rowToEntry/mapTailorError kept in lockstep w/ index.ts (transient dup, dies w/ stateless route in B2). Red-team GENUINE: #1 spy proves context reaches decide()+composed msg (empty→byte-identical), #6 validateNoFabrication.length===3 + context-only number throws, #11 failed re-tailor keeps prior current. ADDITIVE — stateless path intact (its 3 tests untouched & green).",
      "status": "done"
    },
    {
      "id": "E6-A4",
      "title": "Lock / unlock + snapshot integrity (self-contained snapshots)",
      "depends_on": [
        "E6-A3"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/client/api.ts",
        "test/api.applications-lock.test.ts"
      ],
      "origin": "spec §27 (lock final + integrity invariant) — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nAdd the immutable 'final' snapshot and prove snapshot integrity.\n1) src/server/routes/applications.ts — POST /api/applications/:id/lock: copy the current `current` snapshot into `locked` (full content copy — snapshots are self-contained, NEVER entry-ID refs). Lock with no `current` → 400. DELETE /api/applications/:id/lock: clear `locked` → { ok } / updated Application.\n2) src/client/api.ts — lockApplication(id) / unlockApplication(id) helpers.\n3) test/api.applications-lock.test.ts (new) — the §27 integrity chain.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- lock freezes current→locked; re-tailor afterwards overwrites `current` but `locked` is UNCHANGED (immutable).\n- INTEGRITY: after tailor+lock, DELETE (and separately, edit) a Library entry that fed the snapshot → GET :id shows `current` and `locked` byte-identical (full content preserved, no broken refs).\n- DELETE :id/lock clears `locked`; lock with no current → 400.\nRED-TEAM SHARPENED [v3-001]: (#5) the integrity target entry MUST be present as an item in `current` before mutating; the EDIT must change a RENDERED fact/text field (so a ref-resolving impl would visibly diverge); compare `current`/`locked` via a FRESH GET :id after mutation and after re-tailor (re-read from DB, not the pre-lock in-memory object) — proves `locked` is a deep copy, not a shared reference.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @7983d88: scope CLEAN (3 files ⊆ declared). check 0; lint clean; vitest 258/258 (isolated, no flake); playwright chromium 8/8. Gaming read CLEAN: POST :id/lock deep-copies via structuredClone(current)→locked (self-contained, no refs), 404 missing / 400 no_current; DELETE :id/lock clears. Red-team #5 GENUINE: uses a rephrase:none skill entry (rendered text===facts, maximally exposes ref bugs); lock→re-tailor overwrites current but locked stays toEqual(originalCurrent) via fresh GET; edit/delete Library entry → locked/current unchanged. Builder followed no-e2e protocol cleanly (no stall).",
      "status": "done"
    },
    {
      "id": "E6-A5",
      "title": "Full-instance export/import (library + profile + applications)",
      "depends_on": [
        "E6-A2"
      ],
      "files": [
        "src/server/routes/backup.ts",
        "src/server/index.ts",
        "src/client/api.ts",
        "src/client/components/LibraryToolbar.tsx",
        "test/api.export-import.test.ts",
        "test/library-toolbar.test.tsx"
      ],
      "origin": "spec §9 + §27 backup (export/import span all three) — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nCURRENT STATE: no /api/export exists; import is entries-only (POST /api/entries/import). LibraryToolbar Import/Export buttons are disabled stubs.\n1) src/server/routes/backup.ts (new plugin `backupRoutes`): GET /api/export → { entries, profile, applications } (full backup incl current/locked snapshots). POST /api/import → { entries?, profile?, applications? } restores all three (zod-validated), returns { imported }. Reuse existing entry import semantics (upsert by id) and extend to profile (singleton upsert) + applications.\n2) Register backupRoutes in src/server/index.ts.\n3) src/client/api.ts — exportAll() / importAll(payload) helpers; wire the disabled Import/Export buttons in src/client/components/LibraryToolbar.tsx to them.\n4) test/api.export-import.test.ts (new).",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- GET /api/export returns { entries, profile, applications } (all three, snapshots included).\n- POST /api/import restores all three (zod-validated; rejects malformed payload).\n- Round-trip: export → wipe DB → import reproduces entries + profile + applications with `current`/`locked` intact.\n- LibraryToolbar Import/Export buttons are functional (no longer disabled stubs).\nRED-TEAM SHARPENED [v3-001]: (#3) the exported application MUST carry non-null `current` AND `locked` (real TailoredResumes); after a genuine wipe assert GET /api/export returns empty entries+applications (+default profile) BEFORE import, then import, then DEEP-EQUAL current/locked to the original; malformed import (application missing jobDescription) → 4xx not 500. (#12) add a jsdom test (test/library-toolbar.test.tsx) asserting the Import/Export buttons are NOT disabled and their onClick invokes importAll/exportAll.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @2cf5f16: scope CLEAN (6 files ⊆ declared). check 0; lint clean; vitest 253/253; playwright chromium 8/8. Gaming read CLEAN: GET /api/export returns {entries,profile,applications} incl snapshots; POST /api/import zod is REAL (applicationImport=applicationCreate.extend w/ tailoredResumeZ, not z.any()) so jobDescription required. Red-team GENUINE: #3 round-trip non-vacuous (non-null current+locked, fresh-dir wipe asserts export empty BEFORE import via toEqual([]), deep-equal restore, malformed missing-jobDescription→≥400 not 500), #12 LibraryToolbar buttons not disabled + onClick→export/importAll. NOTE: entry-upsert logic duplicated from entries.ts + tailoredResumeZ hand-written (entries.ts/shared schema out of scope) — minor DRY cost, acceptable.",
      "status": "done"
    },
    {
      "id": "E6-B1",
      "title": "ApplicationsView + ApplicationDetail (list/create/detail/tailor/lock/staleness/loop)",
      "depends_on": [
        "E6-A4"
      ],
      "files": [
        "src/client/components/ApplicationsView.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/components/ApplicationCard.tsx",
        "src/client/components/NewApplication.tsx",
        "src/client/components/JobPanel.tsx",
        "src/client/components/ResultView.tsx",
        "test/applications-ui.test.tsx",
        "src/client/queries/useApplications.ts"
      ],
      "origin": "spec §13 component tree (ApplicationsView/ApplicationDetail) + §26/§27 — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nNET-NEW UI (no ApplicationsView exists). Reuse existing ResumePage.tsx + ReasoningPanel.tsx (built) for the result render. Compose shadcn primitives — never re-roll button/dialog (§13 rule). Use the useApplications hooks (E6-A2) + tailor/lock helpers.\nComponent tree (§13):\n- ApplicationsView (route body): ApplicationsList of ApplicationCard (company/role, JD preview, genState badge, updatedAt) + NewApplication (create act — JD required, optional company/role/context).\n- ApplicationDetail: JobPanel (edit JD/context/company/role; Tailor / Re-tailor / Lock final buttons) · TailorProgress (§15 honest progress) · ResultView(split) showing the app's `current` (or `locked`): ResumePage | ReasoningPanel. Empty state: 'add missing facts in Library →' linking to /library (the Applications↔Library loop, §26).\n- Staleness: surface currentMeta ('tailored from your Library as of <date> — re-tailor to fold in newer entries', §27) — render when currentMeta present.\nNOTE: routing/nav wiring is E6-B2; here just build the components (importable). test/applications-ui.test.tsx: jsdom component tests.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- ApplicationsList renders cards from data incl genState + updatedAt; NewApplication create calls createApplication.\n- ApplicationDetail renders the `current` snapshot via ResumePage + ReasoningPanel; Lock button calls lockApplication.\n- Staleness note renders when currentMeta present; empty state links to /library (the loop).\n- No hiring-status UI (only genState) — not-a-tracker guard.\nRED-TEAM SHARPENED [v3-001]: (#10) data-driven contrast — feed ≥2 apps with distinct company/genState → each rendered distinctly; feed an app whose `current` contains a SENTINEL string → assert it appears in ApplicationDetail (not an empty ResumePage); staleness note ABSENT when currentMeta is null (contrast the present case).",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @bf491a7: scope CLEAN (7 files touched ⊆ 8 declared; ResultView reused unchanged). check 0; lint clean; build ok; vitest 264/264 incl applications-ui.test (6). Gaming read CLEAN: ApplicationCard uses GEN_STATE_LABEL/VARIANT Record maps (data-driven, not hardcoded); no hiring-status UI (a test guard asserts HTML lacks Applied/Interviewing/Rejected). Red-team #10 GENUINE: 2 apps distinct company+genState via within(cardA/cardB) bidirectional distinctness; SENTINEL string in current.summary asserted rendered; staleness note present-when-currentMeta / absent-when-null. Empty state Links to /library (the loop). Builder followed no-e2e protocol cleanly.",
      "status": "done"
    },
    {
      "id": "E6-B2",
      "title": "Top-bar nav + routes + IA gate; remove stateless TailorView",
      "depends_on": [
        "E6-B1"
      ],
      "files": [
        "src/client/main.tsx",
        "src/client/components/NavTabs.tsx",
        "src/client/App.tsx",
        "src/client/components/TailorView.tsx",
        "src/server/index.ts",
        "src/client/api.ts",
        "test/ia.test.tsx",
        "test/tailor.e2e.test.ts",
        "test/api.tailor-db.test.ts",
        "test/api.tailor-byok.test.ts"
      ],
      "origin": "spec §26 Information Architecture — v3; intake decision: top-bar tabs, Applications default",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nIntake decision [v3-001]: top-bar tabs; Profile/Layout stay in Library (already so — do NOT move them). Realize §26.\nCURRENT STATE: main.tsx routes / → /tailor, /tailor→TailorView, /library, /settings; NavTabs TABS = [/tailor,/library,/settings].\n1) src/client/main.tsx — routes: / → redirect to /applications; /applications → ApplicationsView; /applications/:id → ApplicationDetail; /library; /settings; a catch-all (unknown non-/api path) that resolves to a KNOWN destination (redirect to /applications), NEVER a blank/404 shell. Remove the /tailor route.\n2) src/client/components/NavTabs.tsx — TABS = Applications | Library | Settings (in that order), top-bar; active reflects the current route.\n3) src/client/App.tsx — keep LoginGate wrapping all destinations; adjust the shell/Outlet for the new routes (the current file special-cases /settings — keep behavior consistent).\n4) DELETE src/client/components/TailorView.tsx (stateless flow replaced by Applications). If JDInput.tsx becomes unused, note it (leave removal to avoid undeclared-file churn unless it's in your declared set — it is not, so leave it and note).\n5) test/ia.test.tsx (new) — the §26 structural invariants.\n[v3-006 UPDATE] This ticket ALSO performs the ATOMIC stateless-tailor teardown (moved here from E6-A3 so no intermediate build breaks): remove the inline stateless POST /api/tailor route from src/server/index.ts; remove the tailor() helper from src/client/api.ts; delete TailorView.tsx and its /tailor route; and delete the 3 stateless tailor tests (test/tailor.e2e.test.ts, test/api.tailor-db.test.ts, test/api.tailor-byok.test.ts) whose coverage now lives in E6-A3 test/api.applications-tailor.test.ts on the :id/tailor route. After teardown grep-confirm no reference to \"/api/tailor\" or the tailor() helper remains.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- NavTabs lists Applications | Library | Settings in order; clicking each navigates + active state reflects route; every destination reachable from nav.\n- / redirects to /applications; an unknown non-/api path resolves to a known destination (app-shell renders — not blank/404).\n- No /tailor route; TailorView.tsx removed; no orphan routes.\nRED-TEAM SHARPENED [v3-001]: (#2 HIGH) an unknown path (/tailor, /nonsense) must REDIRECT to /applications (assert location.pathname==='/applications') and render the SAME content as visiting /applications directly (a known-destination sentinel from ApplicationsView) — NOT a bespoke fallback/404 component. Assert exactly ONE NavTabs item is active and it matches the resolved route (contrast: on /library, Library active AND Applications not active).\n[v3-006] Stateless teardown complete: no /api/tailor route, no api.ts tailor() helper, no TailorView / /tailor route, no dangling refs (grep-clean); the 3 old stateless tailor tests removed (coverage in api.applications-tailor.test.ts).",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @d02e61f: check 0; lint clean; build ok; vitest 250/250; playwright chromium 8/8 (routing cutover did NOT regress auth/library-crud e2e). Grep-clean CONFIRMED: no stateless /api/tailor route, no api.ts tailor() helper, no TailorView. Routing: index→Navigate /applications, catch-all *→Navigate /applications (redirect, not blank/404); NavTabs Applications|Library|Settings. Red-team #2 GENUINE (ia.test): unknown path resolves pathname===/applications + renders real ApplicationsView sentinel (not fallback) + exactly one active tab matching route (contrast /library). SCOPE: declared + 2 JUSTIFIED cascading out-of-scope edits — deleted test/server.test.ts (its POST /api/tailor block + redundant health, still covered by boot.smoke/api.auth/spa-serving) and trimmed a stale /tailor-link block from test/library.test.tsx; both necessary to keep vitest green after the route removal, no concurrent ticket owns them (serial). ACCEPTED.",
      "status": "done"
    },
    {
      "id": "E6-B3",
      "title": "Applications browser e2e — full lifecycle (keyless fixture JD)",
      "depends_on": [
        "E6-B2"
      ],
      "files": [
        "test/e2e/applications.spec.ts"
      ],
      "origin": "oracle Phase 5 browser gate (§26/§27, extends [v2-036]) — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nNET-NEW Playwright spec. Follow the existing test/e2e/library-crud.spec.ts + helpers/{session,tmpdata}.ts patterns; webServer boots `bun run start` under Node/tsx with FixtureEngine (keyless). CRITICAL: create the Application with a JD that is a RECORDED fixture (one of the 3 CONTRAST_JDS whose decisions live in test/fixtures/decisions/) so FixtureEngine.decide (keys on hashKey(jd,entries)) replays — an arbitrary JD would throw no_fixture. Read the fixture JD text from the existing keyless tailor tests / fixtures.\nDrive: first-run set-password → login → create Application (fixture JD) → Tailor → `current` renders (assert ResumePage root mounts non-empty AND ReasoningPanel present) → reload page → `current` still shown (persistence) → Re-tailor → Lock final → `locked` renders. Assert NO uncaught console/page errors (narrow allowlist only for known-expected, documented).",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps). (this spec runs inside the playwright half of the composite)\n- The lifecycle spec passes: create→tailor→render→reload-persist→re-tailor→lock→locked-render, no uncaught console errors.\n- Uses a recorded fixture JD (keyless); does not require any API key.\nRED-TEAM SHARPENED [v3-001]: (#8) after tailor assert a SPECIFIC expected token from the recorded fixture's assembled resume (an item text / summary fragment) is in the DOM — not just non-empty; persistence uses a full page.reload() then re-asserts that token WITHOUT any re-tailor (genState still 'tailored'); the console-error allowlist entries are EXACT-MATCH strings, documented, and never include pageerror.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @a956245: scope was applications.spec.ts + playwright.config.ts (justified additive project, same as E5 pattern; projects isolated by testMatch, no double-run). Authoritative FULL composite `bun run test` GREEN end-to-end: vitest 253/253; playwright non-docker 10/10 (chromium 8 + auth 1 + applications 1); docker 1/1. Gaming read CLEAN: lifecycle asserts specific RESUME_TOKEN, tailorRequests count for persistence (reload=1, re-tailor=2), lock→Unlock; console allowlist exact-match, !loggedIn-scoped, pageerror never swallowed (red-team #8). NOTE: closing this ticket required a coordinator REPAIR (commit a956245) — see [v3-008].",
      "status": "done"
    },
    {
      "id": "E6-C1",
      "title": "Progressive LibraryFilter (section · tag · free-text)",
      "depends_on": [],
      "files": [
        "src/client/components/LibraryFilter.tsx",
        "src/client/components/LibraryView.tsx",
        "test/library-filter.test.tsx"
      ],
      "origin": "spec §26 Library-as-bank / findability — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nCURRENT STATE: LibraryView groups entries by section only (groupBySection) — no filter/search UI. File-disjoint from the backend + nav tickets, so it can build in parallel.\nAdd src/client/components/LibraryFilter.tsx: filter by section, by tag, and by free-text over facts/meta. PROGRESSIVE (§26): the filter UI stays minimal/near-invisible on a small or empty library and earns its space as the corpus grows (e.g. only show the tag/text controls past a small threshold). Tags are used for GROUPING/FILTERING ONLY — NEVER selection scoring (core anti-pattern §1: no code intersects/sorts entry.tags against anything). Wire it into LibraryView (client-side filtering of the already-loaded entries; no new API).\ntest/library-filter.test.tsx: jsdom component tests.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- Filter by section narrows the list; by tag narrows; free-text narrows (matches facts/meta).\n- Small/empty library → filter UI minimal (progressive) — asserted.\n- Grep guard: no tag-scoring (no entry.tags intersect/sort/score against JD/signals) in the client.\nRED-TEAM SHARPENED [v3-001]: (#9) define the threshold N — library size < N → tag/text controls ABSENT from DOM; ≥ N → PRESENT (assert both sides). Each filter is BIDIRECTIONAL: a non-matching entry disappears AND a matching entry remains, for section, tag, and free-text; free-text must match a term present only in a FACT (content), confirming it searches facts/meta, not tags.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @1f51f5e: scope CLEAN (3 files ⊆ declared). check 0; lint clean; vitest library-filter 4/4; playwright chromium 8/8 (library-crud regression intact). Gaming read: filterEntries does section exact-match, tag exact includes() (sanctioned §1 filtering, NOT scoring), free-text over facts+string-meta and deliberately NOT tags; SEARCH_THRESHOLD=8 real progressive gate. Tests genuine: threshold both-sides asserted; section/tag/free-text bidirectional; fact-vs-tag proof (kubernetes in a FACT matches, in a TAG does not) [red-team #9]. No tag-scoring anywhere in client.",
      "status": "done"
    }
  ]
}
