{
  "$doc": [
    "Loop driver for Lede v2 (provider-agnostic/BYOK/auth/Drizzle spec).",
    "Phase 0 detailed (it IS the whole risk, §24); Phases 1-4 coarse epics,",
    "decomposed as Phase 0 teaches the shape. Ready/batches/breaches come from",
    "schedule.mjs, never by eye. files is a CONTRACT (verifier fails undeclared",
    "touches; package.json + bun.lock allowlisted). Since nothing type-checks",
    "without T001's tsconfig, every ticket transitively depends on T001.",
    "Acceptance hardened by the intake red-team (ledger [v2-004]) — contrast",
    "checks, not existence checks. Two structural holes closed: (A) fixture",
    "provenance via the shared eval-core T016 + the Phase-0 live-reproduces-",
    "fixtures gate; (B) tag-scoring via the tag-shuffle invariance control."
  ],
  "project": "lede",
  "caps": {
    "maxAttempts": 3,
    "thrash": 2,
    "chunk": 30
  },
  "tickets": [
    {
      "id": "T001",
      "title": "Replace Bun scaffold with Fastify + Vite/React skeleton (bun toolchain)",
      "status": "done",
      "depends_on": [],
      "files": [
        "tsconfig.json",
        "tsconfig.server.json",
        "vite.config.ts",
        "index.html",
        ".env.example",
        "src/server/index.ts",
        "src/client/main.tsx",
        "src/client/App.tsx",
        "src/client/styles/app.css",
        "src/client/styles/print.css",
        "src/main.ts",
        "src/routes/health.ts"
      ],
      "origin": "spec §3, §21, §24 Phase 0",
      "context": "Establish the §21 layout on the LOCKED stack (§3.1) but with the BUN toolchain (oracle.md 'Toolchain'): Fastify 4 API + Vite/React 18 SPA, one process. DELETE the existing Bun.serve scaffold (src/main.ts, src/routes/health.ts) — replaced, do not keep Bun.serve. Set up: tsconfig (client, ESM, react-jsx, path alias @shared/* → src/shared/*, include src/**), tsconfig.server.json, vite.config.ts (react plugin, @shared alias, dev proxy /api → http://localhost:8787), index.html mounting src/client/main.tsx (bare App shell), src/client/styles/{app.css,print.css} stubs. src/server/index.ts: minimal Fastify server booting on PORT (default 8787, §17) serving GET /api/health → {ok:true} (in prod also serves dist/ static — TODO stub fine). package.json scripts (bun): dev (concurrently dev:api+dev:web), dev:api (bun --watch src/server/index.ts), dev:web (vite), build (vite build), check (tsc --noEmit both configs), start (bun src/server/index.ts), test (vitest run). Deps per §20 (fastify, react, react-dom, vite, @vitejs/plugin-react, vitest, concurrently, typescript). Locked: PORT 8787, Fastify not Bun.serve, ESM, @shared.",
      "acceptance": "- `bun install` succeeds; `bun run check` passes; `bun run build` emits dist/index.html\n- server boots (`bun run start`); `curl localhost:8787/api/health` served by the running Fastify instance → `{\"ok\":true}`\n- src/main.ts & src/routes/health.ts DO NOT exist; `grep -r 'Bun.serve' src` → no hits; src/server/index.ts imports fastify\n- meta-check: tsconfig include covers src/**, so a deliberate type error in src FAILS `bun run check`\n- an `@shared/*` import resolves under BOTH check and build; vite proxies /api → :8787\n- pure scaffold: no behavior tests required (stated)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-02): `bun run check`→exit 0; `bun run build`→dist/index.html + assets, exit 0; `bun run test`→'No test files found, exiting with code 0' (passWithNoTests, no dummy test); server boot `curl localhost:8787/api/health`→{\"ok\":true}. Scope: 14 touched paths all ⊆ declared ∪ {package.json,bun.lock}; no drift. Gaming read: test script = 'vitest run --passWithNoTests' (real green-on-empty), tsconfig include=['src/**/*'] (meta-check holds), src/server/index.ts imports fastify, no Bun.serve. Needed one re-dispatch to make the test runner green-on-empty."
    },
    {
      "id": "T002",
      "title": "Shared domain types (Entry, EntryMeta, TailorDecision, TailoredResume)",
      "status": "done",
      "depends_on": [
        "T001"
      ],
      "files": [
        "src/shared/types.ts"
      ],
      "origin": "spec §4.1, §5",
      "context": "Author src/shared/types.ts EXACTLY per §4.1 and §5: Section (10-member union), EntryMeta (discriminated union on `section`), Entry (id, section, meta, facts=FACT-LOCK, tags, framings?, sortKey), Profile (§4.2 shape), ProviderId ('anthropic'|'openai'|'google'|'openai-compatible'), JDSignals, TailorDecision (the MODEL's flat output — signals, summary, items[{entryId,text,rank,leadRationale?}], cut[]), TailoredItem/Group/Section/Resume (SERVER-assembled), Layout = { section: Section|'summary'; enabled: boolean }[]. Imported by both server and client via @shared. Locked (oracle): model returns judgment only; facts are the fact-lock; tags never for scoring.",
      "acceptance": "- `bun run check` passes\n- types match §4.1/§5 field-for-field (EntryMeta discriminated on section; TailorDecision flat with per-item rank; TailoredResume is the assembled shape)\n- types-only ticket: no runtime tests required (stated)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-02): `bun run check`→exit 0; `bun run test`→exit 0 (green-on-empty, no test file added). Scope: only src/shared/types.ts touched. Correctness read: file matches spec §4.1/§5 field-for-field — EntryMeta is a discriminated union on `section` (10 variants), TailorDecision is FLAT (signals/summary/items[]{entryId,text,rank,leadRationale?}/cut[]), TailoredResume is the nested assembled shape (sections→groups→items); Profile/Layout/ProviderId/JDSignals all exported. No runtime code, no gaming."
    },
    {
      "id": "T003",
      "title": "Section registry (SECTIONS)",
      "status": "done",
      "depends_on": [
        "T002"
      ],
      "files": [
        "src/shared/sections.ts"
      ],
      "origin": "spec §4.3",
      "context": "Author src/shared/sections.ts: the SECTIONS: Record<Section, {label; rephrase:'full'|'light'|'none'; order:'recency'|'relevance'|'manual'; groupBy?}> registry VERBATIM per §4.3 (experience/project full+recency+groupBy; education light+recency+groupBy; award/cert/publication recency; reference manual; skill none+relevance+groupBy category; interest relevance; language manual). Export SECTION_VALUES (the 10 keys). SINGLE SOURCE (§17) for rephrase/order/group — consumed by prompt, assemble, render — must be exact.",
      "acceptance": "- `bun run check` passes; `bun run test` green (config-only, test-exempt per baseline — nothing behavioral to test; consumed by T008 assemble tests later)\n- correctness (coordinator diff-read vs §4.3): SECTIONS has ALL 10 sections, each with the exact rephrase/order/groupBy triple, and SECTION_VALUES exports the 10 keys\n- do NOT add a test file (not in declared files)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (fan-out workflow Verify+Gate, re-confirmed by coordinator on merged tree): check/build/test green. src/shared/sections.ts has all 10 sections with exact §4.3 rephrase/order/groupBy triples; SECTION_VALUES exports 10 keys. Diff-read confirms verbatim §4.3. Config-only, test-exempt."
    },
    {
      "id": "T004",
      "title": "Providers registry (PROVIDERS, resolveModel, providerOptionsFor)",
      "status": "done",
      "depends_on": [
        "T002"
      ],
      "files": [
        "src/shared/providers.ts",
        "test/providers.test.ts"
      ],
      "origin": "spec §6.1, §8",
      "context": "Author src/shared/providers.ts per §6.1: PROVIDERS registry mapping each ProviderId → { label, models[], default }. resolveModel({provider,model,apiKey,baseURL}) builds an AI SDK LanguageModel via createAnthropic/createOpenAI/createGoogleGenerativeAI (openai-compatible = createOpenAI+baseURL). providerOptionsFor(provider) returns provider-specific extras (Anthropic: thinking/effort + prompt-cache control; sane defaults elsewhere). Deps: ai, @ai-sdk/anthropic, @ai-sdk/openai, @ai-sdk/google. Default Anthropic claude-opus-4-8; include a Google model. PHASE-0 EVAL MODEL = gemini-2.5-flash (ledger [v2-005]: key tested live — flash works incl. structured output; gemini-2.5-pro is 429 quota-blocked/no-billing). The Google factory ALSO reads GOOGLE_GENERATIVE_AI_API_KEY from env — Phase 0 relies on that.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- parametrized over ALL 4 ProviderIds: each returned model's provider/modelId matches the REQUESTED provider (dispatch correctness — not just non-throw)\n- the passed apiKey is forwarded to the factory (not dropped); google resolves via GOOGLE_GENERATIVE_AI_API_KEY env alone\n- providerOptionsFor('anthropic') includes cache-control/thinking keys; other providers return defined sane defaults",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator): check/build green; test/providers.test.ts 14/14. resolveModel dispatch correct for all 4 providers (modelId exact; provider strings distinct incl. openai vs openai-compatible); apiKey forwarded (verified via AI SDK header closure, no network); google resolves via GOOGLE_GENERATIVE_AI_API_KEY env; providerOptionsFor('anthropic') has cacheControl+thinking. Deps ai@7/@ai-sdk/* added. No tag-scoring."
    },
    {
      "id": "T005",
      "title": "zod schemas: TailorDecisionZ + entry/meta validation",
      "status": "done",
      "depends_on": [
        "T002",
        "T003"
      ],
      "files": [
        "src/shared/schema.ts",
        "test/schema.test.ts"
      ],
      "origin": "spec §4.4, §5, §17",
      "context": "Author src/shared/schema.ts. Critical: TailorDecisionZ — hand-written zod for the model's flat TailorDecision (§5), passed straight to generateObject. Also entryMetaZ (STRICT discriminatedUnion on 'section', one object per §4.1 — reject foreign fields) and entryInput (plain zod for Phase 0: id?, section enum, meta=entryMetaZ, facts 1-300 x≤12, tags ≤8 x1-40, framings? ≤6 x1-200, sortKey int; superRefine meta.section===section + per-section facts arity per §17). drizzle-zod derivation deferred to Phase 1 (no Drizzle yet). Locked: TailorDecisionZ hand-written, never table-derived.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Rejection table (each a distinct assert): TailorDecisionZ REJECTS empty entryId, non-numeric rank, missing signals.hardRequirements, a cut entry missing reason; ACCEPTS one fully-populated valid decision\n- Strict meta: canonical experience meta PASSES; same object + foreign field (school) REJECTED; missing role REJECTED\n- Facts arity: skill with 2 facts REJECTED (label sections exactly 1); experience with 0 facts REJECTED (narrative ≥1); certification with [] facts PASSES; meta.section !== section REJECTED",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+Gate, re-confirmed on merged tree): check/build green; test/schema.test.ts 13/13. TailorDecisionZ rejects empty entryId / non-numeric rank / missing signals.hardRequirements / cut missing reason, accepts a valid decision; entryMetaZ strict (foreign 'school' field rejected, missing role rejected); facts arity (skill!=2, experience>=1, cert [] ok, meta.section mismatch rejected). zod dep added. Hand-written, not table-derived."
    },
    {
      "id": "T006",
      "title": "Phase 0 seed entries (SEED_ENTRIES)",
      "status": "done",
      "depends_on": [
        "T002"
      ],
      "files": [
        "src/server/seed.ts"
      ],
      "origin": "spec §22",
      "context": "Author src/server/seed.ts exporting SEED_ENTRIES: Entry[] VERBATIM from §22 — the three Cloudcase experience entries (cloudcase-rules-engine sortKey 202101, cloudcase-frontend-rewrite 202501, cloudcase-platform-sdk 202503). All tagged platform-arch ON PURPOSE (forces discrimination on FACTS not tags) and all ONE job (same company/role/period → one group, so every flip is the group's leading item). Copy facts/tags/framings/sortKey EXACTLY — do NOT edit facts to inject target keywords (that would make the flip trivial keyword-lookup and void the Tier-0 premise). NOTE the secondary-tag hole (sdk/frontend/delivery map 1:1 to targets) is neutralized downstream by the tag-shuffle control (T014/T015), not by the seed.",
      "acceptance": "- `bun run check` passes; `bun run test` green (data-only, test-exempt per baseline — verified by diff-read; consumed by T013/T014 later)\n- correctness (coordinator diff-read vs §22): SEED_ENTRIES deep-equals the §22 literal — the 3 ids (cloudcase-rules-engine/-frontend-rewrite/-platform-sdk), meta, facts element-for-element, tags, framings, sortKey\n- all three share company/role/period (one group); every entry's tags include platform-arch; sortKeys exactly 202101/202501/202503 (the OLDEST, rules-engine, can still lead — proving rank not recency); facts NOT edited to inject target keywords\n- do NOT add a test file (not in declared files)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator diff-read): check/build/test green. src/server/seed.ts SEED_ENTRIES = §22 literal element-for-element (3 ids, sortKeys 202101/202501/202503, all one Cloudcase group, all tags incl platform-arch, facts NOT keyword-injected, framings only on rules-engine). Data-only, test-exempt."
    },
    {
      "id": "T007",
      "title": "System prompt + renderLibrary (the product IP)",
      "status": "done",
      "depends_on": [
        "T002",
        "T003"
      ],
      "files": [
        "src/server/tailor/prompt.ts",
        "test/prompt.test.ts"
      ],
      "origin": "spec §6.1, §6.2",
      "context": "Author src/server/tailor/prompt.ts — THE product IP (§6.2). SYSTEM_PROMPT (frozen; never interpolate JD/timestamp) encodes: judge from FACTS not tags; the fact-lock (never invent, never strengthen — 'contributed to' ≠ 'led'); the rephrase policy full/light/none GENERATED from SECTIONS (so prompt/code never drift); rank-within-group = the lede; a leadRationale on each group's leading item tied to a NAMED JD signal; return ONLY the flat TailorDecision with every number grounded in facts. renderLibrary(entries) serializes deterministically (by section, then sortKey desc, then id; stable key order) and FOREGROUNDS facts (tags not presented as a scorable field). This prompt is what makes the §22 lede flip — if the eval (T014) doesn't flip sharply, THIS file is the bug, not the code.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Derivation not literal: the policy block is produced by a pure fn(SECTIONS); a test passing a SECTIONS copy with one section's rephrase FLIPPED asserts the rendered block reflects the flipped value (a hardcoded string fails this)\n- Order-independence: SCRAMBLED input entries → output equals the canonical section→sortKey-desc→id ordering, and two different input orderings yield BYTE-IDENTICAL output\n- Facts foregrounded: renderLibrary(e) output contains every string in e.facts; tags are not framed as 'match these'\n- SYSTEM_PROMPT contains no JD/timestamp interpolation",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator): check/build green; test/prompt.test.ts 9/9. SYSTEM_PROMPT frozen (no JD/timestamp interpolation), encodes facts-not-tags + fact-lock ('contributed to'≠'led') + rank=lede + leadRationale-names-signal. Rephrase policy DERIVED from SECTIONS via pure fn (flip test: changing a section's rephrase in a SECTIONS copy changes the rendered block — not a hardcoded literal). renderLibrary deterministic (section→sortKey desc→id, byte-identical across input orderings), facts foregrounded, tags labeled 'never a relevance signal'. NOTE: real proof of the flip is the live eval T014."
    },
    {
      "id": "T008",
      "title": "assemble() — deterministic structure from the flat decision",
      "status": "done",
      "depends_on": [
        "T002",
        "T003"
      ],
      "files": [
        "src/server/tailor/assemble.ts",
        "test/assemble.test.ts"
      ],
      "origin": "spec §5, §4.3",
      "context": "Author src/server/tailor/assemble.ts: assemble(decision, entries, layout, sections): TailoredResume. Server owns ALL structure (§5): group by registry groupBy; group/flat order by registry `order` (recency = MAX member sortKey desc, server-computed; relevance = ascending min member rank; manual = sortKey); items-WITHIN-group ALWAYS by rank (lowest = lede); coerce text for rephrase:'none' to facts verbatim (empty-facts → text:''); filter to sections enabled in layout. The model NEVER controls structure. Keyless-testable — the regression net's core.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Max-member contrast: group A sortKeys [202101,202503] supplied in that order, group B [202501] → A ordered before B (A's key = max = 202503, distinguishing max from min/first)\n- Coercion is an OVERRIDE not pass-through: a rephrase:'none' entry fed decision text:\"FABRICATED\" → output text is the verbatim fact, not \"FABRICATED\"; empty-facts entry → text:\"\"\n- Section order follows LAYOUT: layout orders B before A while entries inserted A before B → output sections [B,A]; an enabled:false section present in entries is absent\n- items within a group order by rank (lowest first); flipping rank flips the leading item",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator): check/build green; test/assemble.test.ts 7/7. Server owns structure: recency groups by MAX member sortKey desc (contrast test distinguishes max from min/first); rephrase:'none' text COERCED to facts verbatim (decision text 'FABRICATED' overridden; empty-facts→''); section order follows LAYOUT (permuted) + disabled sections dropped; items within group by rank (flip test). Deterministic core of the keyless regression net."
    },
    {
      "id": "T009",
      "title": "validateNoFabrication + extractNumbers/hasNumberToken",
      "status": "done",
      "depends_on": [
        "T002"
      ],
      "files": [
        "src/server/tailor/validate.ts",
        "test/validate.test.ts"
      ],
      "origin": "spec §6.3, §23",
      "context": "Author src/server/tailor/validate.ts per §6.3: validateNoFabrication(resume, entries, baseSummary?) throws FabricationError on (a) an item entryId not a real entry, (b) a number in item.text not present in THAT item's OWN entry facts (per-entry scoped, not pooled), (c) a summary number not traceable to a kept entry's facts OR baseSummary. extractNumbers/hasNumberToken compare WHOLE number tokens INCLUDING attached %/k/x/units (so 30k≠30, 50%≠50), separators normalized (1,000==1000), NOT substrings ('1' ≠ inside '2021'). Mechanical only (§23) — NO LLM-checks-LLM pass, ever.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Token semantics (one assert per pair): text \"30k\" PASSES only if facts contain \"30k\", THROWS if facts contain only \"30\"; \"50%\" does NOT match a fact of \"50\"; \"1,000\" matches a fact of \"1000\"\n- Per-entry scoping: an item whose number is present in a DIFFERENT entry's facts but absent from its OWN entry THROWS\n- Detection completeness: a fake number injected into a DEEP location (summary, or a buried non-leading item in a late group) still THROWS and names the invented token (proves all items scanned)\n- clean output PASSES; unknown entryId THROWS; summary number grounded in baseSummary PASSES",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (workflow Verify+coordinator): check/build green; test/validate.test.ts 11/11. Whole-token semantics ('30k'≠'30','50%'≠'50','1,000'=='1000','1' not in '2021'); per-entry scoping enforced; deep-buried fake number throws naming the token; unknown entryId throws; baseSummary-grounded summary passes. Mechanical only, no LLM call."
    },
    {
      "id": "T016",
      "title": "Shared eval-core: hash(jd,entries), flip-predicate, canonical JDs, tag-shuffle",
      "status": "done",
      "depends_on": [
        "T002",
        "T003",
        "T008"
      ],
      "files": [
        "src/server/tailor/evalcore.ts"
      ],
      "origin": "red-team Finding A (fixture provenance) + Finding B (tag-scoring); spec §22",
      "context": "SINGLE SOURCE that closes the two structural holes. Author src/server/tailor/evalcore.ts exporting: (1) hashKey(jd, entries) — the ONE fixture key used by BOTH FixtureEngine (T010) and record-fixtures (T013), so a fixture can't be keyed one way and looked up another; (2) CONTRAST_JDS — the 3 §22 JDs, authored so NONE contains any entry id or a fact substring ≥ ~12 chars from its target (no keyword leakage), each mapped to its expected target (platform-sdk/rules-engine/frontend-rewrite); (3) flipPredicate(resume: TailoredResume, targetId) → { leads, rationaleNamesSignal } — target is its group's UNIQUE lowest-rank leading item AND its group.leadRationale contains a token from resume.signals; used identically by eval, record-fixtures, and the keyless suite; (4) tagShuffle(entries) — returns entries with tags permuted/stripped across entries (for the invariance control). This module is imported by T010, T013, T014, T015 — the provenance chain's spine.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- hashKey is deterministic and order-insensitive over entries; hashKey(CONTRAST_JDS[i], SEED_ENTRIES) is stable\n- unit test: none of CONTRAST_JDS contains an entry id or a ≥12-char fact substring from its target (guards keyword leakage)\n- flipPredicate returns leads=true only when target is the UNIQUE minimum-rank leading item of its group; false on a rank tie or when target is in cut\n- tagShuffle preserves entry ids/facts/sortKeys and only permutes tags",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): check/build green; test/evalcore.test.ts adds 14 (68 total). hashKey deterministic + order-insensitive (sha256 over jd+entries-sorted-by-id); ANTI-LEAKAGE verified — each CONTRAST_JDS jd contains neither its target id nor any ≥12-char substring of that target's facts (builder's drafts leaked 'integration path'/'the codebase'/'consistent patterns' and were reworded until clean). JDs read as realistic employer descriptions of the NEED, not keyword lists. flipPredicate: leads = target is its group's items[0] (leading) & not in cut; rationaleNamesSignal = leadRationale references a resume.signals token (true/false/filler/empty cases tested). tagShuffle permutes tags across entries, preserves id/facts/sortKey, no mutation. No new deps. Scope: only evalcore.ts + test."
    },
    {
      "id": "T010",
      "title": "TailorEngine (Fixture + Provider) + tailor() orchestration",
      "status": "done",
      "depends_on": [
        "T002",
        "T004",
        "T005",
        "T007",
        "T008",
        "T009",
        "T016"
      ],
      "files": [
        "src/server/tailor/engine.ts",
        "test/engine.test.ts"
      ],
      "origin": "spec §6.1",
      "context": "Author src/server/tailor/engine.ts per §6.1: interface TailorEngine { decide(jd, entries): Promise<TailorDecision> }. ProviderEngine (real): resolveModel(cfg) then generateObject({ model, schema: TailorDecisionZ, system: SYSTEM_PROMPT + renderLibrary(entries), prompt: 'Tailor for this job description:\\n\\n'+jd, providerOptions: providerOptionsFor(provider) }); retry EXACTLY once on failure then throw. FixtureEngine (keyless): loadFixtureDecision by hashKey(jd, entries) FROM T016 (shared) out of test/fixtures/decisions; a MISS throws a no_fixture-tagged error (→422), NEVER a silent fallback / first-file. tailor(engine, jd, entries, layout, baseSummary?): decision = engine.decide → assemble → validateNoFabrication → return (engine-agnostic). Engine factory by env LEDE_TAILOR_ENGINE (live|fixture; fixture default under NODE_ENV=test). The engine split is what makes everything except the live call keyless.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Validate-wired contrast: tailor() with a stub engine returning a decision containing a FABRICATED number THROWS FabricationError; same stub with a clean decision returns a resume (proves validate runs, not just assemble)\n- Hash keying with ≥2 fixtures: decide(jdA)→fixtureA, decide(jdB)→fixtureB (distinct); decide(jdC unrecorded) THROWS no_fixture WHILE fixtures exist (closes first-file/silent-fallback)\n- Retry count: a mock model throwing once then succeeding → decide resolves after EXACTLY 2 calls; throwing twice → rejects after 2\n- Engine factory returns FixtureEngine under NODE_ENV=test and ProviderEngine under live (assert by type); ProviderEngine calls generateObject with schema===TailorDecisionZ and a system string containing renderLibrary(entries) output",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): check/build green; test/engine.test.ts 10 (78 total). tailor() = decide→assemble→validateNoFabrication (validate WIRED, contrast test: fabricated '9999%' throws FabricationError, clean returns resume). ProviderEngine retry EXACTLY once (try/catch/retry, generateObject ≤2 calls, asserted). FixtureEngine keys by evalcore.hashKey; MISS throws NoFixtureError code 'no_fixture' listing scenarios (no silent fallback / first-file). makeEngine: fixture under NODE_ENV=test/LEDE_TAILOR_ENGINE=fixture, ProviderEngine (google/gemini-2.5-flash) under live. Wiring test asserts generateObject called with schema===TailorDecisionZ + system containing renderLibrary output. Scope: only engine.ts+test."
    },
    {
      "id": "T011",
      "title": "Fastify /api/tailor + /api/health + config (Phase 0, no DB/auth)",
      "status": "done",
      "depends_on": [
        "T005",
        "T006",
        "T010"
      ],
      "files": [
        "src/server/index.ts",
        "src/server/config.ts",
        "test/server.test.ts"
      ],
      "origin": "spec §9, §24 Phase 0",
      "context": "Extend src/server/index.ts (from T001) + add src/server/config.ts. config.ts: fail-fast env loader (§17) — PORT (8787), LEDE_TAILOR_ENGINE (live|fixture). Phase 0 does NOT require LEDE_MASTER_KEY/SESSION_SECRET yet (arrive with auth/BYOK in Phase 2). Route POST /api/tailor: zod-validate { jobDescription } (1-20000, §17); build engine per config (live=ProviderEngine using the Google bootstrap key from env; fixture=FixtureEngine); call tailor(engine, jd, SEED_ENTRIES, defaultLayout) — the route MUST go through tailor() (assemble+validate), never return a raw decision or canned resume. Error mapping (§9): provider auth→'key invalid', rate-limit→429, other provider fail→502, zod-fail on LLM output→502, fixture miss→422 no_fixture. GET /api/health → {ok:true}.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- Response is a zod-valid TailoredResume whose EVERY item entryId ∈ SEED_ENTRIES ids; a fixture carrying a number absent from facts makes the route return 502 (proves validateNoFabrication runs IN-REQUEST)\n- Distinct statuses each asserted: empty jd → 400; jd > 20000 → 400; unrecorded fixture → 422 {error:'no_fixture'}\n- GET /api/health served by the Fastify app instance → {ok:true}",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): check/build green; 84 tests (6 new server tests). Real HTTP boot: GET /api/health→{ok:true}, POST /api/tailor empty jd→400. Route goes through tailor(engine, jd, SEED_ENTRIES, defaultLayout) — no raw decision/canned resume. Error mapping distinct: NoFixtureError→422 {error:'no_fixture'}, FabricationError→502, provider auth→401, rate-limit→429, else→502. buildApp() exported (testable via inject; .listen guarded to entrypoint). config.ts fail-fast PORT+LEDE_TAILOR_ENGINE (no MASTER_KEY/SESSION_SECRET — deferred to Phase 2). Scope: index.ts+config.ts+test/server.test.ts."
    },
    {
      "id": "T012",
      "title": "Client Phase 0: JD paste → tailor → deterministic resume render",
      "status": "done",
      "depends_on": [
        "T002",
        "T011"
      ],
      "files": [
        "src/client/App.tsx",
        "src/client/api.ts",
        "src/client/components/TailorView.tsx",
        "src/client/components/JDInput.tsx",
        "src/client/components/ResumePage.tsx",
        "src/client/styles/print.css",
        "src/client/styles/app.css",
        "test/resumepage.test.tsx"
      ],
      "origin": "spec §10, §13 (minimal)",
      "context": "Minimal Phase 0 client (no shadcn/Tailwind yet — lands in E1; plain CSS here). api.ts: typed fetch to /api/tailor throwing ApiError{status,code,message}. TailorView: JDInput (textarea) + tailor button → /api/tailor → ResumePage. ResumePage: SINGLE-COLUMN deterministic render of TailoredResume (§10) — ProfileHeader stub, summary, then each TailoredSection (registry label + groups) in LAYOUT order (data-driven, not hardcoded). print.css (@media print): US-Letter, #000 on #fff, real text, no images/tables/multi-column. leadRationale/cut[] MUST NOT render on the resume page (§10) — reasoning UI is Phase 3. Wire into App.tsx.",
      "acceptance": "- `bun run check` + `bun run build` pass\n- Leak check: feed a TailoredResume fixture whose leadRationale and every cut[].reason contain UNIQUE SENTINEL strings; assert those exact strings are ABSENT from the rendered resume DOM textContent\n- Data-driven order: use a NON-DEFAULT (permuted) layout; assert rendered section order equals layout order (not hardcoded); items within a group follow given order\n- print.css @media print: no column-count/columns>1, no display:table/table layout, #000 on #fff (grep-checkable)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): check/build green; 88 tests (4 new resumepage). §10 LEAK invariant: ResumePage renders NO leadRationale/cut (grep shows only a forbidding comment; sentinel test: renderToStaticMarkup HTML .not.toContain the 4 sentinels). Data-driven order: sections render in resume order ([project,experience] non-alpha) via SECTIONS labels, items in array order (not hardcoded). print.css @media print single-column, no column-count/columns2+/display:table. api.ts throws typed ApiError; JDInput disabled while pending. Scope: exactly the 8 declared files."
    },
    {
      "id": "T013",
      "title": "record-fixtures script + recorded decision fixtures (KEY-GATED)",
      "status": "done",
      "depends_on": [
        "T006",
        "T010",
        "T016"
      ],
      "files": [
        "scripts/record-fixtures.ts",
        "test/fixtures/decisions/platform-sdk.json",
        "test/fixtures/decisions/rules-engine.json",
        "test/fixtures/decisions/frontend-rewrite.json",
        "test/fixtures/decisions/manifest.json"
      ],
      "origin": "spec §18, §22; red-team Finding A",
      "context": "Author scripts/record-fixtures.ts: runs the LIVE ProviderEngine (Google bootstrap key, GOOGLE_GENERATIVE_AI_API_KEY) over T016's CONTRAST_JDS against SEED_ENTRIES and writes each TailorDecision to test/fixtures/decisions/<name>.json keyed by T016 hashKey(jd, SEED_ENTRIES). Fails LOUD (non-zero, no key). BEFORE writing each fixture it runs the decision through tailor()→assemble→validateNoFabrication AND T016 flipPredicate — a non-flipping or fabricating decision CANNOT be written. Writes manifest.json (model id, prompt hash, timestamp, token usage) — provenance. These committed fixtures are what makes the keyless suite (T015) replay the REAL scenarios; a hand-authored fixture would not survive the live-reproduces-fixtures phase gate (oracle Phase 0).",
      "acceptance": "- `bun run scripts/record-fixtures.ts` (with GOOGLE key) writes 3 fixtures + manifest.json; each recorded decision is TailorDecisionZ-valid, passes validateNoFabrication, and passes T016 flipPredicate for its target\n- fixture key = T016 hashKey(jd, SEED_ENTRIES); a subsequent FixtureEngine.decide(jd, SEED_ENTRIES) returns exactly that fixture (shared-hash proof)\n- across the 3 fixtures the rank-1 (leading) entry does NOT track sortKey order (proves a real rank flip, not recency)\n- each fixture embeds/matches the canonical JD text and the SEED_ENTRIES hash (guards library/JD swap)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): baseline green (84 tests). Live recorder wrote 3 fixtures + manifest.json (model gemini-2.5-flash). Fixtures are REAL TailorDecisions (2-3 items, cut lists, 342-485-char summaries, ranks 1..N — not stubs). KEYLESS replay CONFIRMED (GOOGLE key blanked): FixtureEngine+tailor() over the 3 CONTRAST_JDS reproduces all flips PASS (platform-sdk/rules-engine/frontend-rewrite lead, rationale names signal). Keys === hashKey(jd,SEED_ENTRIES). rules-engine leads despite OLDEST sortKey (rank≠recency). Provenance gate satisfied — matches my live ground truth [v2-013]. Scope: scripts/record-fixtures.ts + 3 fixtures + manifest only."
    },
    {
      "id": "T014",
      "title": "Phase 0 acceptance oracle — scripts/eval.ts (KEY-GATED, THE gate)",
      "status": "done",
      "depends_on": [
        "T006",
        "T007",
        "T010",
        "T016"
      ],
      "files": [
        "scripts/eval.ts"
      ],
      "origin": "spec §22, §25 Tier 0; red-team Findings A+B",
      "context": "Author scripts/eval.ts — THE Phase 0 behavioral oracle (§22, §25). Constructs ProviderEngine EXPLICITLY (Google bootstrap key, model gemini-2.5-flash per ledger [v2-005] — pro is quota-blocked) and EXITS NON-ZERO if no key (never skip-as-pass, never fall back to FixtureEngine). Runs the live model over T016 CONTRAST_JDS against EXACTLY SEED_ENTRIES via tailor(), using T016 flipPredicate. Exit 0 iff all 3 flip correctly, non-zero (naming the miss) otherwise. Locked: if it doesn't flip sharply, the PROMPT (§6.2, T007) is wrong — not the code.",
      "acceptance": "- `bun run scripts/eval.ts` (with GOOGLE_GENERATIVE_AI_API_KEY) exits 0; asserts engine instanceof ProviderEngine and ≥1 live response with token usage\n- over exactly SEED_ENTRIES (length===3, ids match): for each JD the expected target is present in items (NOT cut) and is the STRICT UNIQUE minimum rank\n- the three leading entryIds are MUTUALLY DISTINCT and equal the SET {cloudcase-platform-sdk, cloudcase-rules-engine, cloudcase-frontend-rewrite} (set assertion, not 3 isolated checks)\n- each target's leadRationale contains a token from that JD's decision.signals; the three rationales are pairwise different\n- TAG-SHUFFLE CONTROL (Finding B): re-run over tagShuffle(SEED_ENTRIES); the flip is UNCHANGED (if shuffling tags changes the lede → tag-scoring → FAIL)\n- exits non-zero naming the failing JD when a lede does not flip",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator RAN THE GATE LIVE): `bun run scripts/eval.ts` → exit 0. BASE: all 3 JDs flip to correct target (platform-sdk/rules-engine/frontend-rewrite), leads+rationaleNamesSignal, mutually-distinct set. TAG-SHUFFLE: same 3 flips hold + same target per JD (fact-driven, not tag-driven). No-key run → exit 1 (refuses, no FixtureEngine fallback). Gaming read: uses ProviderEngine explicitly, EXPECTED_TARGETS as a Set, no fallback. THIS IS THE PHASE-0 §22/§25-Tier0 ORACLE PASSING. Scope: only scripts/eval.ts."
    },
    {
      "id": "T015",
      "title": "Keyless pipeline e2e tests (FixtureEngine replay)",
      "status": "done",
      "depends_on": [
        "T011",
        "T013",
        "T016"
      ],
      "files": [
        "test/tailor.e2e.test.ts"
      ],
      "origin": "spec §18, §25 keyless guarantee; red-team Findings A+B",
      "context": "Author test/tailor.e2e.test.ts: drive tailor() AND POST /api/tailor with FixtureEngine over the recorded fixtures (T013) for the 3 CONTRAST_JDS — NO API KEY. Asserts the assembled resume's structure/order/coercion/validation AND that the correct target leads each JD (replaying the flip keylessly via T016 flipPredicate). The standing regression net (§25 keyless guarantee). Honest limit (record in ledger): this replays FROZEN decisions, so it proves the MACHINERY, not that the live model still flips — that rests on T014 being genuinely run. It also cannot catch a fabricated fixture; the anti-recency check below is the one recency-vs-rank cheat it CAN kill alone.",
      "acceptance": "- `bun run test` (GOOGLE_GENERATIVE_AI_API_KEY UNSET) passes; assert the engine used is FixtureEngine\n- for each of the 3 JDs the SPECIFIC expected target is its group's UNIQUE leading item, and the three leads are MUTUALLY DISTINCT\n- Anti-recency: rules-engine JD → cloudcase-rules-engine leads (it has the OLDEST sortKey 202101, impossible under recency), and ≥2 of 3 leads are NOT the max-sortKey entry\n- a specifically-UNRECORDED jd yields the tagged no_fixture error via tailor() and 422 {error:'no_fixture'} via HTTP\n- structure-ignores-tags: replay with tagShuffle(SEED_ENTRIES) → identical assembled order/selection (proves assemble/render never let tags influence structure)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator): KEYLESS full suite passes with GOOGLE key UNSET — 10 files/93 tests. e2e replays the 3 §22 flips via FixtureEngine (each target is its group's items[0]; leads mutually distinct = the 3 seed ids); ANTI-RECENCY (rules-engine leads despite OLDEST sortKey 202101, verified via Math.min; ≥2 leads not max-sortKey); HTTP via buildApp().inject() recorded→200/unrecorded→422 no_fixture; structure-ignores-tags (assemble over SEED_ENTRIES vs tagShuffle deep-equal). Honest-limit comment present. No ProviderEngine (keyless). Scope: only test/tailor.e2e.test.ts."
    },
    {
      "id": "E1",
      "title": "Phase 1 — Drizzle + SQLite persistence; entry library editor; profile; layout; shadcn/Tailwind foundation",
      "status": "decomposed",
      "depends_on": [
        "T014",
        "T015"
      ],
      "files": [],
      "origin": "spec §24 Phase 1 (§4, §9, §12, §13, §17)",
      "context": "COARSE EPIC — decompose when Phase 0 closes into: (a) shadcn/ui + Tailwind + §12 token theming foundation; (b) Drizzle schema src/server/db/schema.ts (§4.2: entries/profile/settings/secrets, JSON columns, singletons id=1+CHECK) + db/index.ts (better-sqlite3, WAL, DATA_DIR) + migrations run on boot + singleton INSERT OR IGNORE seeding (settings.layout defaults §4.2); (c) REPLACE Phase 0 plain-zod entry schema with drizzle-zod createInsertSchema (§4.4) — keep TailorDecisionZ hand-written and the §17 bounds (facts≤12,tags≤8,…) that derivation can't infer; (d) /api/entries CRUD + import/export, /api/profile, /api/settings (layout) — zod-validated (§9); (e) /api/tailor reads entries from DB; (f) client: TanStack Query + react-router, LibraryView + LibraryToolbar + SectionAccordion + EntryCard + EntryEditor (section-aware via registry, one editor for all sections) + SectionMetaFields + RepeatableList + TagInput + ProfileEditor + LayoutEditor. Sections first: experience/project/education/skill (§2). RED-TEAM — child tickets MUST include: (1) export = ENTRIES ONLY (assert payload has no ciphertext/plaintext key/auth hash/non-entry table); (2) real restart: process A writes → exit → process B reads same DATA_DIR (real .sqlite, not :memory:); (3) migrations from empty DATA_DIR create all 4 tables AND the CHECK constraints (query sqlite_master; INSERT id=2 into a singleton FAILS); (4) layout contrast: a NON-DEFAULT layout (reordered + one disabled) changes render order and drops the disabled section.",
      "acceptance": "See oracle.md Phase 1 (merged-tree): migrations on boot; entries CRUD persists across restart; import/export round-trip (secrets excluded); LibraryView create/edit/delete end-to-end; profile round-trip; layout ordering respected.",
      "attempts": [],
      "evidence": "Decomposed into E1-A..E1-F3 (8 child tickets). See ledger [v2-016]."
    },
    {
      "id": "E1-A",
      "title": "UI foundation: Tailwind + shadcn primitives + §12 tokens + self-hosted IBM Plex",
      "status": "done",
      "depends_on": [],
      "files": [
        "tailwind.config.ts",
        "postcss.config.js",
        "src/client/styles/tokens.css",
        "src/client/styles/app.css",
        "src/client/lib/utils.ts",
        "src/client/components/ui/button.tsx",
        "src/client/components/ui/input.tsx",
        "src/client/components/ui/textarea.tsx",
        "src/client/components/ui/label.tsx",
        "src/client/components/ui/form.tsx",
        "src/client/components/ui/dialog.tsx",
        "src/client/components/ui/dropdown-menu.tsx",
        "src/client/components/ui/tooltip.tsx",
        "src/client/components/ui/tabs.tsx",
        "src/client/components/ui/badge.tsx",
        "src/client/components/ui/card.tsx",
        "src/client/components/ui/sonner.tsx",
        "src/client/components/ui/alert.tsx",
        "src/client/components/ui/select.tsx",
        "src/client/components/ui/skeleton.tsx",
        "test/ui-foundation.test.tsx"
      ],
      "origin": "decomposed from E1 (spec §12, §13)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nSCAFFOLD/CONFIG ticket (behavior-test-exempt except the contrast below). Establish the design-system foundation per spec §12 + §13. Set up Tailwind (v3, PostCSS) so vite processes `src/client/styles/*.css`. Create `tokens.css` defining the §12 CSS variables VERBATIM: --ink:#1a1a1a --ink-soft:#52525b --ink-faint:#a1a1aa --bg:#fff --bg-subtle:#fafafa --border:#e4e4e7 --border-strong:#d4d4d8 --accent:#2f5fdd --accent-weak:#eef2fe --success:#15803d --warn:#b45309 --danger:#b91c1c; radius 4px (6px cards/dialogs); spacing scale 4/8/12/16/24/32/48. Map shadcn's CSS variables (--primary, --background, --foreground, --border, --radius, etc.) to these tokens; --accent is primary. Copy the shadcn/ui primitives listed in §13 into src/client/components/ui/ (owned, re-themed) — button input textarea label form dialog dropdown-menu tooltip tabs badge card sonner alert select skeleton — pulling their Radix deps + class-variance-authority/clsx/tailwind-merge/lucide-react into package.json. lib/utils.ts holds `cn()`. Self-host IBM Plex via @fontsource/ibm-plex-sans, @fontsource/ibm-plex-mono, @fontsource/ibm-plex-serif imported from app.css (Plex Sans=UI/body, Plex Mono=IDs/numbers, Plex Serif=wordmark+callouts only) — NO Google Fonts CDN. Do NOT build any domain components (Library/Editor/etc.) — those are later tickets; this is foundation only. If copying 15 primitives is more than one session, return tooBig splitting (tokens+config) from (primitive copy).",
      "acceptance": "- baseline passes (check+build+test)\n- tokens.css contains the §12 color variables incl. --accent:#2f5fdd; shadcn primary maps to --accent (grep tokens.css + the theme mapping)\n- CONTRAST (fonts self-hosted, gameable-resistant): after `bun run build`, grep the dist/ output → NO `fonts.googleapis.com` / `fonts.gstatic.com` / any remote font URL present; @fontsource/ibm-plex-{sans,mono,serif} are in package.json deps and imported from app.css\n- smoke test (test/ui-foundation.test.tsx): <Button> from components/ui/button renders and mounts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree re-verify (2026-07-02): `bun run check` exit 0; `bun run build` exit 0 (dist has self-hosted IBM Plex woff/woff2, grep dist for fonts.googleapis/gstatic -> none); `bun run test` with ALL provider keys unset -> 12 files / 101 tests PASS (was 93; +4 test/db.test.ts, +4 test/ui-foundation.test.tsx). Workflow Verify stage passed both tickets independently (scope clean, no gaming). Merge clean, no conflicts. E1-A: tokens.css has the §12 vars incl --accent:#2f5fdd, shadcn --primary maps to var(--accent); 15 ui/ primitives copied + re-themed; @fontsource IBM Plex imported from app.css."
    },
    {
      "id": "E1-B",
      "title": "Drizzle schema + db init + migrations-on-boot + singleton seeding",
      "status": "done",
      "depends_on": [],
      "files": [
        "drizzle.config.ts",
        "src/server/db/schema.ts",
        "src/server/db/index.ts",
        "src/server/config.ts",
        "src/server/index.ts",
        "drizzle/",
        "test/db.test.ts"
      ],
      "origin": "decomposed from E1 (spec §4.2)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nImplement the §4.2 Drizzle schema + db layer. `src/server/db/schema.ts`: the four tables VERBATIM from §4.2 — entries (id pk, section, meta/facts/tags/framings JSON with $type, sortKey, createdAt, updatedAt), profile (singleton id=1 default, name/headline/email/phone/location/links JSON/baseSummary/updatedAt, CHECK id=1), settings (singleton, provider default 'anthropic', model default 'claude-opus-4-8', baseUrl, layout JSON, updatedAt, CHECK id=1), secrets (singleton, apiKeyEnc JSON nullable, apiKeyValidatedAt, auth JSON nullable, updatedAt, CHECK id=1). CRITICAL BOUNDARY: schema.ts must import ONLY from 'drizzle-orm/sqlite-core' + 'drizzle-orm' (pure, no driver) so the client can import table types without pulling better-sqlite3 — the native driver lives ONLY in db/index.ts. `src/server/db/index.ts`: open better-sqlite3 at `${DATA_DIR}/lede.sqlite` with WAL, expose a drizzle db; a function to run migrations (drizzle-orm/better-sqlite3/migrator, migrationsFolder 'drizzle') and INSERT OR IGNORE the three singleton rows (settings.layout seeded to the FULL §4.2 order all enabled: [summary, experience, project, skill, education, award, certification, publication, language, interest, reference]). Generate migrations with `bunx drizzle-kit generate` into `drizzle/` and COMMIT them (the CHECK constraints must appear in the generated SQL). config.ts: add DATA_DIR (default './data'). index.ts boot: init db + run migrations + seed singletons BEFORE app.listen. Keep Phase-0 /api/tailor working (it still reads SEED_ENTRIES for now — E1-E rewires it; do not change tailor here beyond what's needed to boot).",
      "acceptance": "- baseline passes\n- CONTRAST (fresh migrate): against an EMPTY tmp DATA_DIR (real .sqlite file, NOT :memory:), running migrations creates exactly tables {entries, profile, settings, secrets} (query sqlite_master) AND the singleton CHECK holds — `INSERT` with id=2 into profile / settings / secrets each THROWS\n- CONTRAST (restart persistence): open db at a tmp DATA_DIR, insert an entry row, close the handle, reopen a fresh db at the SAME DATA_DIR → the entry reads back (proves real file, not in-memory)\n- singleton seeding: after migrate+seed, settings id=1 exists with layout = the full §4.2 order all enabled; profile id=1 and secrets id=1 exist\n- new tests in test/db.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree re-verify (2026-07-02): `bun run check` exit 0; `bun run build` exit 0 (dist has self-hosted IBM Plex woff/woff2, grep dist for fonts.googleapis/gstatic -> none); `bun run test` with ALL provider keys unset -> 12 files / 101 tests PASS (was 93; +4 test/db.test.ts, +4 test/ui-foundation.test.tsx). Workflow Verify stage passed both tickets independently (scope clean, no gaming). Merge clean, no conflicts. E1-B: 4 tables verbatim §4.2; schema.ts driver-free; migrations drizzle/0000_sloppy_domino.sql has 3 CHECK(id=1); db.test proves fresh-migrate table set + CHECK-reject id=2 + real-file restart persistence + singleton seed with full layout. NOTE: bun-runtime boot fails (better-sqlite3 dlopen) -> repair T016."
    },
    {
      "id": "T017",
      "title": "Server runs under Node (tsx), not Bun — better-sqlite3 native ABI; add real-boot smoke gate",
      "status": "done",
      "depends_on": [
        "E1-B"
      ],
      "files": [
        "package.json",
        "test/boot.smoke.test.ts"
      ],
      "origin": "repair: bun runtime cannot dlopen better-sqlite3 native addon (escaped E1-B); ledger [v2-017]",
      "context": "Baseline (oracle.md): bun run check + bun run build + bun run test pass, keyless. Touch ONLY declared files (+ package.json manifest). Add tests for new behavior.\n\nESCAPED ISSUE (verified by coordinator 2026-07-02): running `bun src/server/index.ts` dies with ERR_DLOPEN_FAILED — better-sqlite3 ships a Node-ABI prebuilt (NODE_MODULE_VERSION 127) that Bun 1.3.14's embedded V8 (137) cannot load; better-sqlite3 uses direct V8/NAN bindings, not N-API, so this is NOT rebuild-away (oven-sh/bun#4290). Under Node v22 via tsx the server boots, runs migrations, and GET /api/health returns {ok:true} (coordinator confirmed).\nRESOLUTION (mechanical, ledger [v2-017]): the server PROCESS runs under Node — reverting the runner to spec §3.1's own 'Node >= 20'. Bun stays the package-manager + script runner (bun install / bun run check|build|test unchanged; they already use vitest/node).\nCHANGES: in package.json scripts, change `start` from `bun src/server/index.ts` to `tsx src/server/index.ts`; change `dev:api` from `bun --watch src/server/index.ts` to `tsx watch src/server/index.ts`. Add `tsx` to devDependencies. Do NOT touch check/build/test scripts. Do NOT introduce bun:sqlite (it breaks vitest).\n\nSTRENGTHENED CHECK (escaped-bug rule): add test/boot.smoke.test.ts that spawns the REAL entrypoint under the actual runner (node via tsx — e.g. child_process spawn of `tsx src/server/index.ts`, or `node --import tsx`) against a fresh tmp DATA_DIR on a free port; poll GET /api/health until {ok:true} (fail on ~15s timeout); assert the .sqlite file was created under DATA_DIR; tear the child down. This is the boot gate the baseline lacked — it catches a runner that can't load the native driver.",
      "acceptance": "- baseline passes (check+build+test, keyless)\n- CONTRAST (real boot, gameable-resistant): test/boot.smoke.test.ts spawns the entrypoint under the real runner, GET /api/health -> {ok:true}, and the DATA_DIR .sqlite file exists after boot; the test FAILS if the server can't boot (proves the runner loads better-sqlite3)\n- package.json `start` and `dev:api` no longer invoke `bun` on src/server/index.ts (grep: no `bun` invoking src/server/index.ts in scripts); `tsx` present in devDependencies\n- new test in test/boot.smoke.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree re-verify (2026-07-02, master @ dfa0484): bun run check exit 0; bun run build exit 0; bun run test keyless (all provider keys unset) -> 13 files / 113 tests PASS (incl. new boot.smoke.test.ts). Workflow Gate agent also passed independently; Verify passed both (scope clean, no gaming); merge clean. T017: package.json start/dev:api now `tsx ...` (no bun on entrypoint), tsx@^4.19.2 devDep; boot.smoke.test.ts spawns real entrypoint under tsx -> /api/health {ok:true} + DATA_DIR/lede.sqlite created (fails if runner cannot dlopen better-sqlite3)."
    },
    {
      "id": "E1-C",
      "title": "drizzle-zod entry validators + profile/settings validators (replace Phase-0 hand-rolled entryInput)",
      "status": "done",
      "depends_on": [
        "E1-B"
      ],
      "files": [
        "src/shared/schema.ts",
        "test/schema.test.ts"
      ],
      "origin": "decomposed from E1 (spec §4.4, §17)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nReplace the Phase-0 hand-rolled `entryInput` in src/shared/schema.ts with `createInsertSchema(entries, {...})` from drizzle-zod (§4.4), importing the `entries` table from src/server/db/schema.ts (safe — that file is driver-free per E1-B). PRESERVE exactly: the discriminatedUnion `entryMetaZ` (hand-written, unchanged), the §17 bounds that drizzle-zod can't infer (facts ≤12 × 1–300; label sections exactly 1 fact; cert/reference allow 0; tags ≤8 × 1–40; framings ≤6 × 1–200; meta string fields 1–120; id 1–80 optional; jobDescription bound stays where it is), `.omit({createdAt, updatedAt})`, and the superRefine (meta.section === section; per-section facts arity). Keep `entryImport = z.array(entryInput).max(200)`. Keep `TailorDecisionZ` HAND-WRITTEN (never derived). ADD `profileInput` and `settingsInput` (provider/model/baseUrl/layout) zod objects per §4.2/§9. All existing schema tests must stay green (regression).",
      "acceptance": "- baseline passes; ALL existing test/schema.test.ts cases still green (per-section meta, arity rules, foreign-field rejection)\n- entryInput is derived via createInsertSchema AND still enforces the §17 bounds: rejects facts.length>12, a tag >40 chars, a meta string >120, a foreign meta field\n- CONTRAST (boundary intact): `bun run build` (vite client build) succeeds and the dist bundle does NOT contain 'better-sqlite3' or 'node:' native driver refs — the shared→db import stays driver-free\n- profileInput / settingsInput accept a valid payload and reject a malformed one\n- tests updated in test/schema.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree re-verify (2026-07-02, master @ dfa0484): bun run check exit 0; bun run build exit 0; bun run test keyless (all provider keys unset) -> 13 files / 113 tests PASS (incl. new boot.smoke.test.ts). Workflow Gate agent also passed independently; Verify passed both (scope clean, no gaming); merge clean. E1-C: entryInput derived via createInsertSchema(entries) (drizzle-zod ^0.8.3); §17 bounds + superRefine + entryMetaz + entryImport preserved; TailorDecisionZ still hand-written; +profileInput/settingsInput; schema.test 24 tests green; client bundle grep better-sqlite3 -> 0 (boundary intact)."
    },
    {
      "id": "E1-D",
      "title": "HTTP routes: /api/entries CRUD + import, /api/profile, /api/settings",
      "status": "done",
      "depends_on": [
        "E1-B",
        "E1-C"
      ],
      "files": [
        "src/server/routes/entries.ts",
        "src/server/routes/profile.ts",
        "src/server/routes/settings.ts",
        "src/server/slug.ts",
        "src/server/index.ts",
        "test/api.entries.test.ts",
        "test/api.profile-settings.test.ts"
      ],
      "origin": "decomposed from E1 (spec §9, §17)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nImplement the §9 JSON routes over the drizzle db (E1-B), validated with the E1-C zod validators. Register them in index.ts as Fastify plugins receiving the db. Routes: GET /api/entries (optional ?section= filter) → Entry[]; POST /api/entries (no id → server slug per §17: kebab of section + a distinguishing meta field + a few fact words, ≤80 chars, dedupe -2) → Entry; PUT /api/entries/:id → Entry; DELETE /api/entries/:id → {ok}; POST /api/entries/import (Entry[], cap 200) → {imported:n}. GET /api/profile + PUT /api/profile (singleton round-trip). GET /api/settings → {keySet, provider, model, baseUrl, layout} where keySet = (secrets.apiKeyEnc != null) and the KEY ITSELF IS NEVER RETURNED; PUT /api/settings (provider?/model?/baseUrl?/layout?). Bad body → 400. NOTE: there is no separate export endpoint — export = GET /api/entries (entries table only, secrets are a different table). Auth guard is Phase 2 — do NOT add auth here. slug.ts holds the §17 slug generator (unit-test it). Put createdAt/updatedAt stamping in the route/db layer (Date.now()).",
      "acceptance": "- baseline passes\n- entries CRUD round-trips AND persists across restart: write entries via one buildApp() instance over a tmp DATA_DIR, rebuild the app on the SAME DATA_DIR, GET /api/entries returns them (keyless integration, real .sqlite)\n- POST with no id → server-generated slug (§17); POST/PUT with a bad body → 400\n- import: POST /api/entries/import with 2 entries → {imported:2}; GET returns them\n- CONTRAST (no secret leak, gameable-resistant): GET /api/settings body has `keySet` boolean but contains NO apiKey/ciphertext/iv/tag and NO auth hash; the GET /api/entries export payload contains ONLY Entry fields (id/section/meta/facts/tags/framings/sortKey[/createdAt/updatedAt]) — assert no ciphertext, no auth hash, no settings/secrets content anywhere in it\n- profile GET/PUT round-trip; settings GET/PUT (incl. layout) round-trip\n- new tests in test/api.entries.test.ts + test/api.profile-settings.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR + workflow-Gate both green (merged tree, master @ 6187f69->post-merge). bun run check exit 0; build exit 0; test keyless -> 15 files / 126 tests PASS (+8 api.entries, +5 api.profile-settings; server.test + boot.smoke still green). Verify passed (scope clean, no gaming). LEAK CONTRAST held: GET /api/settings returns only {keySet,provider,model,baseUrl,layout} (keySet derived from secrets.apiKeyEnc!=null in routes/settings.ts, key never read/returned); GET /api/entries payload = Entry fields only, no ciphertext/hash/salt. Restart persistence via real .sqlite proven. Slug §17 in slug.ts (unit-tested). buildApp(db?) refactor kept server.test.ts green."
    },
    {
      "id": "E1-E",
      "title": "/api/tailor reads entries + settings.layout from DB; first-boot seeds SEED_ENTRIES",
      "status": "done",
      "depends_on": [
        "E1-B",
        "E1-D"
      ],
      "files": [
        "src/server/index.ts",
        "src/server/seed.ts",
        "test/api.tailor-db.test.ts"
      ],
      "origin": "decomposed from E1 (spec §22, §4.3)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nPhase 0's /api/tailor uses the hardcoded `SEED_ENTRIES` const + a `defaultLayout` const. Rewire it to read ALL entries and `settings.layout` from the DB (E1-B). Keep the FixtureEngine keyless path intact (engine selection unchanged). On FIRST boot (entries table empty), seed SEED_ENTRIES into the db (§22 'loaded if entries empty on first run') — refactor seed.ts to keep the SEED_ENTRIES export (tests/eval depend on it) AND add a seedIfEmpty(db) used at boot. tailor() already accepts (entries, layout) params — pass the DB-sourced values. Do NOT change assemble()/engine/prompt. Do NOT touch the routes files (E1-D owns them).",
      "acceptance": "- baseline passes; full suite green with provider key UNSET (keyless guarantee holds)\n- CONTRAST (reads DB, not the const): seed the db with a DIFFERENT entry set than SEED_ENTRIES (e.g. a single distinct entry), POST /api/tailor (fixture engine) → the assembled resume reflects the DB entries, NOT the 3 hardcoded SEED_ENTRIES\n- CONTRAST (layout respected by render): with a NON-DEFAULT settings.layout (two sections reordered + one section disabled), the assembled TailoredResume.sections order reflects the reorder AND omits the disabled section; with the default layout, the standard §4.2 order\n- first-boot seeding: empty entries table → after boot, SEED_ENTRIES are present (GET /api/entries returns them)\n- new tests in test/api.tailor-db.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ d079e3f; bun install; keyless): check 0, build 0, test 18 files / 138 PASS. Workflow Gate also green; Verify passed both (scope clean, no gaming); merge landed on master (template patch worked). E1-E: /api/tailor now reads all entries + settings.layout from the db (rowToEntry mapper preserves SEED key order so hashKey fixtures still match — builder self-caught). CONTRAST reads-db-not-const: custom-seeded entry → 200 with its id, SEED ids absent (would be 422 no_fixture if still const). CONTRAST layout: reorder+disable → sections [certification,skill], award omitted. seedIfEmpty(db) idempotent first-boot seed; injected test dbs left untouched."
    },
    {
      "id": "E1-F1",
      "title": "Client infra: TanStack Query + react-router + AppShell/NavTabs + LibraryView browse/delete",
      "status": "done",
      "depends_on": [
        "E1-A",
        "E1-D"
      ],
      "files": [
        "src/client/main.tsx",
        "src/client/App.tsx",
        "src/client/api.ts",
        "src/client/components/AppShell.tsx",
        "src/client/components/NavTabs.tsx",
        "src/client/components/LibraryView.tsx",
        "src/client/components/LibraryToolbar.tsx",
        "src/client/components/SectionAccordion.tsx",
        "src/client/components/EntryCard.tsx",
        "src/client/hooks/queries.ts",
        "test/library.test.tsx"
      ],
      "origin": "decomposed from E1 (spec §13, §14)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nClient state + shell + library browse, per §13/§14. main.tsx: wrap in QueryClientProvider + a react-router RouterProvider (routes /tailor /library /settings under AppShell). App.tsx: AppShell + NavTabs (Tailor | Library | Settings). api.ts: EXTEND (keep the existing tailor()) with typed fetch for entries/profile/settings CRUD, throwing ApiError{status,code,message}; expose the codes so 401→login and 400 no_api_key→settings can be handled later. hooks/queries.ts: TanStack Query hooks — useEntries (key ['entries'], optional section), useDeleteEntry (invalidates ['entries']), plus query stubs for profile/settings (E1-F2/F3 extend). LibraryView: SectionAccordion (one block per Section, using the registry label from @shared/sections) containing EntryCard (facts preview + tag chips as badges + edit/delete buttons). LibraryToolbar (Add · Import · Export buttons — wiring the editor open is E1-F2; here Add can open a not-yet-implemented editor slot or a disabled button, keep the open/close plumbing minimal). Use E1-A's shadcn primitives (button/card/badge/tabs/etc.); NEVER re-roll a primitive. The existing TailorView stays as the /tailor route. Do NOT build EntryEditor/ProfileEditor/LayoutEditor (later tickets).",
      "acceptance": "- baseline passes\n- LibraryView renders entries grouped by section with registry labels; EntryCard shows facts preview + tag chips\n- CONTRAST (delete e2e, gameable-resistant): component test with fetch/api mocked — render LibraryView (wrapped in QueryClientProvider + router) seeded with 2 entries; click delete on entry A → DELETE /api/entries/A is fired, ['entries'] invalidated, A's card removed while B's card remains\n- NavTabs renders links to /tailor /library /settings and routing mounts the right view\n- new tests in test/library.test.tsx",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ merge 46ffdce; then bun install for new deps): bun run check exit 0; bun run build exit 0; bun run test keyless -> 16 files / 129 tests PASS incl. new test/library.test.tsx (3). NOTE: workflow Gate initially RED — builder left work UNCOMMITTED in its worktree (branch==master, nothing to merge) AND new deps not installed in main tree. Coordinator recovered: committed the scope-clean branch (only declared files+manifest), merged, ran bun install, re-gated green. delete-e2e test verified NON-gamed (stateful fetch mock: asserts DELETE URL + post-invalidation refetch + only target card removed). No client tag-scoring."
    },
    {
      "id": "E1-F2",
      "title": "Section-aware EntryEditor + create/edit (SectionMetaFields, RepeatableList, TagInput)",
      "status": "done",
      "depends_on": [
        "E1-F1"
      ],
      "files": [
        "src/client/components/EntryEditor.tsx",
        "src/client/components/SectionMetaFields.tsx",
        "src/client/components/RepeatableList.tsx",
        "src/client/components/TagInput.tsx",
        "src/client/components/LibraryView.tsx",
        "src/client/hooks/queries.ts",
        "test/entry-editor.test.tsx"
      ],
      "origin": "decomposed from E1 (spec §13)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nBuild ONE section-aware EntryEditor (Dialog) for ALL sections, driven by the section registry (@shared/sections) — NOT one editor per section (§13 rule). SectionMetaFields renders the correct meta inputs per section from the §4.1 meta shapes (experience→company/role/period/location; project→name/role/period/url; education→school/degree/field/period/location; skill→category/level; etc.). Label sections (skill/interest/language) show a compact single-fact form. RepeatableList handles facts + framings (add/remove/reorder rows); TagInput handles tags (chip add/remove, ≤8). Wire create (POST /api/entries) and edit (PUT /api/entries/:id) via TanStack mutations that invalidate ['entries'] (add useCreateEntry/useUpdateEntry to hooks/queries.ts). Validate §17 bounds client-side before submit. Wire the editor open from LibraryView's Add button and EntryCard's edit button. Use E1-A primitives (dialog/input/label/button/form). Depends on E1-F1 (LibraryView + queries.ts already exist — EXTEND them, don't clobber F1's exports).",
      "acceptance": "- baseline passes\n- CONTRAST (ONE registry-driven editor, gameable-resistant): render EntryEditor with section=experience → company/role/period fields present; switch section to skill → the meta fields CHANGE to the skill compact single-fact form and the experience fields are GONE (proves registry-driven, not a fixed/duplicated form)\n- CONTRAST (create e2e): open editor, fill an experience entry, save → POST /api/entries fired with the entered meta/facts/tags; on success the new EntryCard appears in LibraryView\n- edit: open an existing entry → fields populated → change a fact → PUT /api/entries/:id fired\n- new tests in test/entry-editor.test.tsx",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ d079e3f; bun install; keyless): check 0, build 0, test 18 files / 138 PASS. Workflow Gate also green; Verify passed both (scope clean, no gaming); merge landed on master (template patch worked). E1-F2: ONE registry-driven EntryEditor (Dialog) — CONTRAST: section=experience shows company/role/period; switch to skill → those gone, compact skill form shown. create e2e (POST fired, card appears) + edit (PUT fired) pass. SectionMetaFields/RepeatableList/TagInput built. DRIFT (minor): EntryCard/LibraryToolbar were not in F2 declared files, so F2 added its own Add trigger + edit-picker in LibraryView rather than wiring F1s disabled stub buttons — functionally correct + scope-honest; unify in a later polish ticket. Client §17 validation is a local validate() (not importing @shared zod) to keep the client bundle driver-free; server remains authoritative (E1-D zod)."
    },
    {
      "id": "E1-F3",
      "title": "ProfileEditor + LayoutEditor (reorder/toggle resume sections)",
      "status": "done",
      "depends_on": [
        "E1-F1"
      ],
      "files": [
        "src/client/components/ProfileEditor.tsx",
        "src/client/components/LayoutEditor.tsx",
        "src/client/components/LibraryView.tsx",
        "src/client/hooks/queries.ts",
        "test/profile-layout.test.tsx"
      ],
      "origin": "decomposed from E1 (spec §13, §16)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, keyless (no provider key set). Locked stack (oracle.md, cite; never re-litigate): Bun runtime + Fastify server, Vite + React SPA, drizzle-orm over better-sqlite3 (single-file SQLite, WAL), zod validation, TanStack Query + react-router client, shadcn/ui primitives + Tailwind, self-hosted IBM Plex via @fontsource. Path alias `@shared/*`→`src/shared/*` only (no @server/@client). Tests live in /test, run under vitest (node); server runs under bun — better-sqlite3 works under BOTH (do not use bun:sqlite, it breaks vitest). Touch ONLY your declared `files` (+ package.json/lockfile for deps). Add tests for new behavior.\n\nProfileEditor (Dialog, §16): edit name/headline/email/phone/location/links[]/baseSummary; GET /api/profile populates, PUT /api/profile saves (round-trip); add useProfile/useUpdateProfile to hooks/queries.ts (invalidate ['profile']). LayoutEditor (§13): reorder + enable/disable the resume sections in settings.layout; GET /api/settings populates, PUT /api/settings persists; add useSettings/useUpdateSettings (invalidate ['settings']). Wire ProfileEditor open from LibraryView (or AppShell) and LayoutEditor into the Library/Settings area. Use E1-A primitives. Depends on E1-F1 — EXTEND LibraryView + queries.ts, do not clobber F1/F2 exports. (The render-order EFFECT of layout is verified server-side in E1-E; here the acceptance is the editor persisting the layout correctly.)",
      "acceptance": "- baseline passes\n- CONTRAST (profile round-trip): ProfileEditor mounts with GET /api/profile data populated; edit name + add a link + set baseSummary, save → PUT /api/profile fired with the new values; reopening shows them\n- CONTRAST (layout persist): LayoutEditor reorders two sections + disables one, save → PUT /api/settings fired with the reordered layout in which the disabled section has enabled:false\n- new tests in test/profile-layout.test.tsx",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 6a2a79c; keyless): check 0 / build 0 / test 19 files / 140 PASS incl. new test/profile-layout.test.tsx. Workflow Gate + Verify both green; merge landed on master; worktree pruned. CONTRAST profile round-trip (PUT /api/profile with edited name+link+baseSummary, reopen shows) + layout persist (reorder + disable → PUT /api/settings with enabled:false) pass. queries.ts extended (useUpdateProfile/useUpdateSettings) without clobbering F1/F2 exports."
    },
    {
      "id": "E2",
      "title": "Phase 2 — auth gate + BYOK (encrypted key storage, validate-on-save, Settings)",
      "status": "decomposed",
      "depends_on": [
        "E1-E",
        "E1-F2",
        "E1-F3"
      ],
      "files": [],
      "origin": "spec §24 Phase 2 (§7, §8, §23)",
      "context": "COARSE EPIC — decompose after E1 into: (a) src/server/crypto.ts (AES-256-GCM encrypt/decrypt using LEDE_MASTER_KEY; scrypt hash/verify) + config.ts fail-fast REQUIRING LEDE_MASTER_KEY + LEDE_SESSION_SECRET at boot; (b) secrets-table access; (c) auth.ts + /api/auth/{setup,login,logout} (@fastify/secure-session) + session guard on ALL /api/* except /api/auth/* and /api/health; first-run set-password; LEDE_AUTH_DISABLED escape hatch (default false); (d) BYOK: PUT /api/settings/key (validate via one cheap live generateText BEFORE storing, then store ciphertext), DELETE, GET /api/settings returns {keySet,provider,model,baseUrl,layout} NEVER the key; (e) /api/tailor switches to ProviderEngine with the DECRYPTED BYOK key (in-memory only), no_api_key→400; (f) client: LoginGate, SettingsView + ApiKeyForm (write-only) + ProviderPicker + ModelPicker. RED-TEAM — child tickets MUST include: (1) SENTINEL scan — store key sk-SENTINEL-<uuid>, hit EVERY endpoint incl. tailor error paths, grep every response body + all stdout/stderr/log files + the .sqlite/-wal/-shm + entire DATA_DIR; sentinel appears nowhere plaintext, only in secrets.apiKeyEnc ciphertext; (2) master key load-bearing — decryption THROWS/garbles when LEDE_MASTER_KEY changed; two encryptions of same plaintext have DIFFERENT iv (catches base64/ECB fakes); (3) PROCESS-LEVEL boot refusal — spawn real server with LEDE_MASTER_KEY unset AND with a malformed key → non-zero exit, no listening port, does NOT auto-generate a master key; (4) master-key material never in DATA_DIR (the backup unit, §23); (5) validate-BEFORE-store — bad key → 4xx AND secrets.apiKeyEnc unchanged; (6) guard total with auth ENABLED — every route except auth/health returns 401 sans session (esp. /api/tailor).",
      "acceptance": "See oracle.md Phase 2 (merged-tree): boot refuses without LEDE_MASTER_KEY (process-level); encrypt/decrypt round-trip w/ distinct IVs; login→session, guarded routes 401; key stored as ciphertext, sentinel scan clean across responses/logs/DATA_DIR; validate-on-save; /api/tailor no-key→400 no_api_key.",
      "attempts": [],
      "evidence": "Decomposed into E2-A..E2-E (5 child tickets). See ledger [v2-023]."
    },
    {
      "id": "E2-A",
      "title": "crypto.ts (AES-256-GCM + scrypt) + config fail-fast (LEDE_MASTER_KEY/SESSION_SECRET) + test-env setup + process-level boot refusal",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/server/crypto.ts",
        "src/server/config.ts",
        "test/setup.ts",
        "vite.config.ts",
        "test/boot.smoke.test.ts",
        "test/crypto.test.ts",
        "test/config.test.ts"
      ],
      "origin": "decomposed from E2 (spec §8, §17, §19, §23)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nFOUNDATION for Phase 2 (everything else depends on this).\n(1) src/server/crypto.ts (node:crypto only): `encrypt(plaintext, masterKey)` -> {iv, tag, ciphertext} using AES-256-GCM with a FRESH random iv each call; `decrypt({iv,tag,ciphertext}, masterKey)` -> plaintext (throws on tampered tag/wrong key); `hashPassword(pw)` -> {hash, salt} using scrypt (random salt); `verifyPassword(pw, {hash,salt})` -> bool (timing-safe compare). masterKey is the decoded 32 bytes.\n(2) src/server/config.ts: EXTEND loadConfig() to parse+VALIDATE `masterKey` (from LEDE_MASTER_KEY, base64 decoding to EXACTLY 32 bytes — else throw), `sessionSecret` (from LEDE_SESSION_SECRET, non-empty — @fastify/secure-session needs >=32 chars, validate), `authDisabled` (LEDE_AUTH_DISABLED === 'true', default false). loadConfig() FAIL-FAST throws a clear error if master key or session secret is missing/malformed. Do NOT auto-generate a master key. Keep existing port/tailorEngine/dataDir.\n(3) test/setup.ts (NEW vitest global setup) + wire it in vite.config.ts `test.setupFiles`: set process.env.LEDE_MASTER_KEY (a valid 32-byte base64), LEDE_SESSION_SECRET (a >=32-char test value), and LEDE_AUTH_DISABLED='true' BEFORE tests run, so the whole existing keyless suite (buildApp etc.) still boots. (The auth guard is proven ENABLED separately in E2-B.)\n(4) test/boot.smoke.test.ts (EXTEND, owned-contract now changes): the spawned real entrypoint must now receive LEDE_MASTER_KEY + LEDE_SESSION_SECRET in its env (add them to the existing happy-path spawn). ADD the process-level boot-refusal cases: spawn the real entrypoint with LEDE_MASTER_KEY UNSET, and separately with a MALFORMED master key (not 32 bytes) -> the process exits NON-ZERO, never starts listening, and does NOT write/auto-generate a master key anywhere.\nRED-TEAM acceptance encodes: master-key load-bearing (distinct IVs; wrong-key decrypt throws) + process boot refusal.",
      "acceptance": "- baseline passes; ALL existing suites stay green (test/setup.ts provides operator secrets + LEDE_AUTH_DISABLED=true)\n- CONTRAST (crypto load-bearing, gameable-resistant): encrypt(x) then decrypt -> x; TWO encrypts of the SAME plaintext yield DIFFERENT iv AND different ciphertext (catches ECB/base64 fakes); decrypt with a DIFFERENT masterKey THROWS (not silent garbage); scrypt verifyPassword true for right pw, false for wrong\n- CONTRAST (config fail-fast): loadConfig() THROWS when LEDE_MASTER_KEY is unset, and when it is malformed (base64 not decoding to 32 bytes); throws when LEDE_SESSION_SECRET missing\n- CONTRAST (process-level boot refusal): spawning the real entrypoint with master key unset / malformed -> non-zero exit, no listening port, no auto-generated key file in DATA_DIR\n- new tests in test/crypto.test.ts + test/config.test.ts; boot.smoke extended",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 121655a, keyless): check 0, build 0, test 21 files / 157 PASS on 4/4 repeated runs. crypto.test (8): round-trip, DISTINCT iv+ciphertext per encrypt, wrong-key decrypt THROWS, tampered tag/ciphertext throw, scrypt verify. config.test (7): fail-fast throws (master unset/malformed/session missing/too-short), no auto-gen. boot.smoke extended (3): real boot with secrets + 2 process-level boot-refusal cases (master unset/malformed -> non-zero exit, never listens, no key file). NOTE: workflow Gate flagged a FLAKY cross-suite race (NOT E2-A code): server.test.ts + api.tailor-db.test.ts write temp fixtures into the SHARED test/fixtures/decisions/ dir while FixtureEngine.loadFixtures parses ALL .json -> mid-write partial file -> 502. Pre-existing (Phase0 server.test x E1-E api.tailor-db); E2-A merely changed timing. E2-A contrasts are deterministic. -> repair T018."
    },
    {
      "id": "T018",
      "title": "Fix flaky test: FixtureEngine.loadFixtures must tolerate concurrent/partial fixture files (shared decisions dir race)",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/server/tailor/engine.ts",
        "test/engine.test.ts"
      ],
      "origin": "repair: flaky suite — shared test/fixtures/decisions dir + parallel vitest workers + loadFixtures parses ALL .json; exposed by E2-A gate; ledger [v2-024]",
      "context": "Baseline (oracle.md): bun run check + build + test pass KEYLESS. Touch ONLY declared files. Add a test for the new behavior. RUNTIME v2-017: server under Node/tsx.\n\nFLAKY-TEST DEFECT (measured, stack-traced): test/server.test.ts and test/api.tailor-db.test.ts each writeFileSync/rmSync temp .json fixtures into the SHARED dir test/fixtures/decisions/ from SEPARATE parallel vitest workers. FixtureEngine.loadFixtures (src/server/tailor/engine.ts ~lines 82-90) does readdirSync(dir).filter(.json).map(f => JSON.parse(readFileSync(...))) — it reads+parses EVERY .json with NO per-file guard, so when a worker catches another worker's file mid-write (partial/empty) or mid-delete (ENOENT), JSON.parse/readFileSync throws, the whole map() throws, decide() throws, and /api/tailor returns 502 instead of 200. Intermittent (~1 in 2-5 runs) -> it will randomly red-gate future tickets.\n\nFIX (product-robustness, minimal): in loadFixtures, wrap EACH file's readFileSync+JSON.parse in its own try/catch; on failure SKIP that file (do not abort the whole scan) and emit a console.warn naming the file (so a genuinely corrupt RECORDED fixture is still visible in logs, not silently swallowed). Return the successfully-parsed fixtures. A mid-write/mid-delete concurrent file becomes a no-op for other workers, while a test's own fully-written fixture is still found by key. Do NOT change decide()/hashKey/matching semantics; do NOT touch the tests that write fixtures (the fix is in the engine). Keep DEFAULT_FIXTURES_DIR behavior.\n\nSTRENGTHENED CHECK (escaped-bug rule): add a test in test/engine.test.ts that points a FixtureEngine at a temp dir containing (a) one VALID fixture .json and (b) one MALFORMED/partial .json (truncated, and/or an empty file), then asserts decide() for the valid fixture's key STILL RESOLVES (the malformed file is skipped, not fatal) and loadFixtures does not throw — the guard that would have caught the race.",
      "acceptance": "- baseline passes; the full suite is GREEN across repeated runs (a concurrent partial/deleted fixture file no longer aborts a scan)\n- CONTRAST (tolerate bad fixture files, gameable-resistant): a FixtureEngine over a temp dir holding one valid + one MALFORMED/empty .json still resolves the valid key via decide() (the bad file is skipped); loadFixtures does not throw\n- unchanged: a valid recorded fixture still matches by hashKey; no change to decide()/matching semantics\n- new/updated test in test/engine.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 2e1777d, keyless): check 0, build 0, test 21 files / 158 PASS — DETERMINISTIC across 5 total runs (workflow 3 + coordinator 2). Fix: loadFixtures per-file try/catch (skip+console.warn on partial/ENOENT), no map()-abort. New test/engine.test.ts case: valid fixture resolves alongside a truncated + empty .json (bad files skipped). No change to decide()/hashKey/matching. Verify passed; merged; worktree pruned."
    },
    {
      "id": "T019",
      "title": "Stabilize vitest harness: raise testTimeout + cap worker parallelism so the full suite is deterministic",
      "status": "done",
      "depends_on": [],
      "files": [
        "vite.config.ts"
      ],
      "origin": "repair: full suite non-deterministic — parallel-worker timeouts under contention (full-server integration suites blow 5000ms); exposed by E2-B/E2-C gate; ledger [v2-026]",
      "context": "Baseline (oracle.md): bun run check + build + test pass KEYLESS. Touch ONLY declared files. RUNTIME v2-017: server under Node/tsx; Bun runs the scripts.\n\nHARNESS DETERMINISM DEFECT (measured by coordinator over repeated runs): the full `bun run test` suite is now NON-DETERMINISTIC — repeated runs fail a DISJOINT, rotating set of tests, and EVERY failure is a bare per-test TIMEOUT ('Test timed out in 5000ms'), never an assertion/logic error. Root cause: vitest runs the default parallel worker pool with the default 5000ms testTimeout, but the suite now has ~10 integration suites that each boot a full Fastify server (pino logger) and boot.smoke spawns real tsx subprocesses; on a shared/contended multi-core box, workers starve and individual server-boot injects blow the 5000ms deadline. In isolation / single-worker every test passes in 0.6-2.4s. Wall time balloons (89s-160s+).\n\nFIX (vite.config.ts test config ONLY — this is a harness fix, NOT a product change; do NOT touch any product or test-logic file): give the integration suites headroom and reduce worker contention so `bun run test` is DETERMINISTICALLY green. Concretely: set test.testTimeout and test.hookTimeout to a generous value (e.g. 30000ms), and stabilize the pool so full-server suites don't starve — cap worker parallelism to a modest number (e.g. pool:'forks' with poolOptions.forks so maxForks is a small cap like 4, or set maxWorkers/minWorkers, or fileParallelism tuned) — pick the vitest knobs that make repeated full-suite runs reliably pass without making wall time absurd. KEEP the existing test.setupFiles (test/setup.ts) and test.exclude '**/.claude/**' and the @shared alias. Do NOT change any test's logic or any product code.\n\nThis is the harness-stability gate the loop needs so every subsequent ticket gates on a deterministic suite (companion to T018 which fixed the fixture-dir race).",
      "acceptance": "- baseline passes: bun run check exit 0, bun run build exit 0\n- CONTRAST (determinism, the whole point): run `bun run test` (keyless, provider key UNSET) 3 TIMES back-to-back — ALL THREE are fully green (every file/every test passes, exit 0), no per-test timeouts. Report the three runs' summary lines verbatim.\n- only vite.config.ts changed (no product/test-logic file touched); test.setupFiles + test.exclude preserved",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 1f281b1, keyless): check 0, build 0, test 23 files / 172 PASS — DETERMINISTIC across 5 runs (workflow 3 + coordinator 2, ~30s each, zero timeouts). T019: vite.config.ts test now testTimeout/hookTimeout 30000 + pool:forks singleFork:true — removed the parallel-worker CPU-contention timeout flake (full-server + tsx-spawn suites no longer starve). Only vite.config.ts changed."
    },
    {
      "id": "E2-B",
      "title": "Auth: scrypt password gate + @fastify/secure-session + session guard on all /api/* except auth/health + /api/auth/{setup,login,logout}",
      "status": "done",
      "depends_on": [
        "E2-A"
      ],
      "files": [
        "src/server/auth.ts",
        "src/server/routes/auth.ts",
        "src/server/index.ts",
        "test/api.auth.test.ts"
      ],
      "origin": "decomposed from E2 (spec §7, §9)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nSingle-user password gate (§7) — NOT accounts. Uses crypto.ts (E2-A: hashPassword/verifyPassword) + config (E2-A: sessionSecret, authDisabled).\n(1) Register @fastify/secure-session in buildApp() with the sessionSecret (add dep). (2) src/server/routes/auth.ts: POST /api/auth/setup (FIRST-RUN only: if secrets.auth is null, store scrypt hash+salt; if already set -> 409/403), POST /api/auth/login (verifyPassword against secrets.auth -> set session), POST /api/auth/logout (clear session). Bodies zod-validated {password}. (3) src/server/auth.ts: the guard preHandler — every /api/* EXCEPT /api/auth/* and /api/health requires a valid session; no session -> 401. When config.authDisabled is true, the guard is a no-op (escape hatch).\n(4) src/server/index.ts (buildApp): EXTEND signature to buildApp(db?, configOverride?) so a test can force auth ENABLED (configOverride.authDisabled=false) even though the global test env sets it true; register secure-session + auth routes + the guard hook. Keep existing route registration (entries/profile/settings/tailor) and the injected-db path intact. The guard must cover /api/tailor, /api/entries, /api/profile, /api/settings, /api/settings/key.\nRED-TEAM acceptance: guard TOTAL with auth ENABLED — every guarded route 401 without a session; first-run setup then login issues a working session.",
      "acceptance": "- baseline passes (existing suites stay green — they run with LEDE_AUTH_DISABLED=true from test/setup.ts)\n- CONTRAST (guard total, auth ENABLED, gameable-resistant): build an app with auth ENABLED (configOverride authDisabled=false); WITHOUT a session, GET/POST to /api/entries, /api/profile, /api/settings, /api/settings/key, and POST /api/tailor ALL return 401; /api/health and /api/auth/* are reachable without a session\n- first-run setup: POST /api/auth/setup sets the password (scrypt hash stored in secrets.auth); a second setup attempt is rejected (already configured)\n- login: POST /api/auth/login with the right password issues a session that then passes the guard; wrong password -> 401; logout clears it\n- new tests in test/api.auth.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 1f281b1, keyless): check 0, build 0, test 23 files / 172 PASS — DETERMINISTIC across 5 runs (workflow 3 + coordinator 2, ~30s each, zero timeouts). E2-B: @fastify/secure-session + scrypt gate. api.auth.test (7): guard ENABLED -> 401 on /api/entries|profile|settings|settings/key|tailor without session; /api/health + /api/auth/* public; setup 200 then 409; login right->session passes guard, wrong->401; logout clears. Coordinator read auth.ts: PUBLIC_PREFIXES={/api/auth/,/api/health}, authDisabled->no-op, else session.authed!==true->401. Guard covers all /api/*."
    },
    {
      "id": "E2-C",
      "title": "BYOK key storage: PUT/DELETE /api/settings/key, validate-before-store, ciphertext-only, sentinel leak scan",
      "status": "done",
      "depends_on": [
        "E2-A"
      ],
      "files": [
        "src/server/routes/settings.ts",
        "src/server/keyvalidation.ts",
        "test/api.byok.test.ts"
      ],
      "origin": "decomposed from E2 (spec §8, §9, §23)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nBYOK key handling (§8). Uses crypto.ts (E2-A encrypt/decrypt + the master key from config). EXTEND src/server/routes/settings.ts (E1-D created it; keep GET/PUT /api/settings intact — GET already returns {keySet,provider,model,baseUrl,layout}, key NEVER returned).\n(1) src/server/keyvalidation.ts: `validateProviderKey({provider,model,apiKey,baseUrl})` -> does ONE cheap live `generateText` (Vercel AI SDK) and returns ok / throws on failure. INJECTABLE: settingsRoutes must accept an optional validator so tests pass a fake (keyless) — default is the real one. This is the seam that keeps validate-before-store keyless-testable.\n(2) PUT /api/settings/key {apiKey}: VALIDATE via validateProviderKey FIRST (using current settings.provider/model/baseUrl); on failure -> 4xx and secrets.apiKeyEnc UNCHANGED; on success -> encrypt(apiKey, masterKey) and store {iv,tag,ciphertext} in secrets.apiKeyEnc, set apiKeyValidatedAt, return {keySet:true}. (3) DELETE /api/settings/key -> purge (secrets.apiKeyEnc=null), return {keySet:false}. (4) NEVER log the key; NEVER return it.\nRED-TEAM acceptance (the security core): SENTINEL scan + validate-before-store + ciphertext-only + master-key-not-in-DATA_DIR.",
      "acceptance": "- baseline passes\n- CONTRAST (validate-before-store): with an injected validator that REJECTS, PUT /api/settings/key -> 4xx AND secrets.apiKeyEnc is unchanged (still null / still the prior value); with a validator that ACCEPTS, the key is stored as {iv,tag,ciphertext} (NOT plaintext) and keySet becomes true\n- CONTRAST (SENTINEL leak scan, gameable-resistant): store apiKey `sk-SENTINEL-<uuid>` (validator accepts); then hit every endpoint incl. GET /api/settings and the tailor error paths; assert the sentinel string appears in NONE of: any response body, the server logs (capture stdout/stderr), and NO plaintext occurrence anywhere under DATA_DIR (the .sqlite + -wal + -shm). It may appear ONLY inside secrets.apiKeyEnc as ciphertext (i.e. the raw sentinel substring is absent from the DB file bytes)\n- CONTRAST (master key not in DATA_DIR): the LEDE_MASTER_KEY value never appears in any file under DATA_DIR (§23 — DB leak without the master key keeps the key safe)\n- DELETE purges: after DELETE /api/settings/key, GET /api/settings keySet=false\n- new tests in test/api.byok.test.ts",
      "attempts": [],
      "evidence": "COORDINATOR merged-tree gate (master @ 1f281b1, keyless): check 0, build 0, test 23 files / 172 PASS — DETERMINISTIC across 5 runs (workflow 3 + coordinator 2, ~30s each, zero timeouts). E2-C: PUT/DELETE /api/settings/key. api.byok.test (7): validate-before-store (reject->400 key_invalid + apiKeyEnc UNCHANGED; accept->stored as {iv,tag,ciphertext}, not plaintext); SENTINEL scan (sk-SENTINEL-<uuid> absent from all response bodies, captured stdout/stderr logs, and DATA_DIR .sqlite/-wal/-shm bytes — present ONLY as ciphertext in secrets.apiKeyEnc); master key absent from DATA_DIR bytes; DELETE purges -> keySet false. Coordinator read routes/settings.ts: validate() FIRST then encrypt(apiKey,masterKey); GET returns keySet boolean only, key never returned/logged."
    },
    {
      "id": "E2-D",
      "title": "/api/tailor uses the decrypted BYOK key via ProviderEngine (in-memory only); no stored key -> 400 no_api_key",
      "status": "done",
      "depends_on": [
        "E2-B",
        "E2-C"
      ],
      "files": [
        "src/server/index.ts",
        "test/api.tailor-byok.test.ts"
      ],
      "origin": "decomposed from E2 (spec §8, §9, §6.1)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nWire /api/tailor (buildApp() in src/server/index.ts) to the user's BYOK key in LIVE mode, keeping the keyless FixtureEngine path for tests/demo. Uses crypto.decrypt (E2-A) + the stored secrets.apiKeyEnc (E2-C). Depends on E2-B (index.ts guard/session — avoid conflicts) and E2-C (key storage).\nLOGIC: keep the engine-mode split. In FIXTURE mode (LEDE_TAILOR_ENGINE=fixture, i.e. tests/CI/demo) -> FixtureEngine, no key needed (current behavior, MUST stay keyless-green). In LIVE mode -> read secrets.apiKeyEnc; if NULL -> respond 400 {error:'no_api_key'} BEFORE any provider call; else decrypt the key in memory (do NOT persist/log the plaintext, discard after the call) and build a per-request ProviderEngine({provider,model,baseUrl,apiKey}) from settings.provider/model/baseUrl + the decrypted key, then run tailor(). Keep the existing mapTailorError distinct statuses (§9). Do NOT change assemble/validate/prompt. Do NOT touch routes/* or auth.\nNOTE: the no_api_key path is keyless-testable — set LEDE_TAILOR_ENGINE=live for that one test with NO stored key -> expect 400 no_api_key (it short-circuits before any live call). Fixture-mode tailor tests remain keyless and green.",
      "acceptance": "- baseline passes; all existing keyless tailor tests (fixture mode) stay green\n- CONTRAST (no_api_key, gameable-resistant): with engine mode LIVE and NO stored key, POST /api/tailor -> 400 {error:'no_api_key'} (short-circuits, no provider call); this differs from fixture-mode 422 no_fixture\n- decrypt-in-memory: with a stored (test-encrypted) key and an injected/stubbed provider path, the tailor route decrypts and builds ProviderEngine with settings.provider/model + the decrypted key; the plaintext key is never logged (assert absence in captured logs) nor persisted beyond secrets.apiKeyEnc\n- new tests in test/api.tailor-byok.test.ts",
      "attempts": [
        {
          "n": 1,
          "failed": "acceptance CONTRAST (fixture-mode 422 no_fixture) fails DETERMINISTICALLY in isolation: expected 422, got 502 provider_error. buildApp decides fixture mode from config.tailorEngine, then calls makeEngine() which IGNORES that decision and re-derives mode from process.env (LEDE_TAILOR_ENGINE ?? NODE_ENV===test?fixture:live). Under vitest NODE_ENV=development -> makeEngine returns a LIVE ProviderEngine even when config.tailorEngine===fixture. Full-suite green was SPURIOUS (relied on NODE_ENV=test leaking from other test files in the shared singleFork process).",
          "hypothesis": "Double source of truth for engine mode: buildApp holds config.tailorEngine but delegates construction to makeEngine(), which reads env independently. The two disagree under test. Also the CONTRAST test is order-dependent (leans on NODE_ENV leakage) rather than proving the app honors its own {tailorEngine:fixture} override.",
          "fixNote": "In src/server/index.ts buildApp: when config.tailorEngine===\"fixture\", construct the fixture engine WITHOUT re-reading env. Cleanest in-scope fix: import { FixtureEngine } from ./tailor/engine and use `config.tailorEngine === \"fixture\" ? new FixtureEngine() : undefined` instead of makeEngine(). (Do NOT change engine.ts/makeEngine — other callers rely on its env behavior; keep the fix inside index.ts, the declared file.) THEN make test/api.tailor-byok.test.ts robust to execution order: the fixture-mode half of the CONTRAST must get 422 no_fixture regardless of process.env.NODE_ENV — e.g. explicitly build the app with {tailorEngine:\"fixture\"} (now honored) and do NOT depend on any leaked NODE_ENV. Keep the LIVE no_api_key (400) and decrypt-in-memory + no-key-leak assertions as-is (those passed reliably). Re-run the new file IN ISOLATION (vitest run test/api.tailor-byok.test.ts) and confirm all pass with NODE_ENV unset."
        }
      ],
      "evidence": "INDEPENDENT RE-VERIFY (branch dcf2130, merged as 9686714): scope=2 declared files only (src/server/index.ts + test/api.tailor-byok.test.ts); gaming read clean (fix real: new FixtureEngine() directly, no makeEngine env indirection; test uses explicit {tailorEngine} overrides, order-independent, real 400-vs-422 contrast + key-leak asserts). bun run check exit 0; bun run build exit 0; ISOLATION `env -u NODE_ENV bunx vitest run test/api.tailor-byok.test.ts` -> 4/4 PASS (the exact prior-failure condition: fixture-mode now 422 no_fixture, was 502); full suite 185/185 (one exit-1 confirmed environmental flake by clean re-run)."
    },
    {
      "id": "E2-E",
      "title": "Client: LoginGate + SettingsView + ApiKeyForm (write-only) + ProviderPicker + ModelPicker",
      "status": "done",
      "depends_on": [
        "E2-B",
        "E2-C"
      ],
      "files": [
        "src/client/components/LoginGate.tsx",
        "src/client/components/SettingsView.tsx",
        "src/client/components/ApiKeyForm.tsx",
        "src/client/components/ProviderPicker.tsx",
        "src/client/components/ModelPicker.tsx",
        "src/client/api.ts",
        "src/client/App.tsx",
        "src/client/hooks/queries.ts",
        "test/settings-auth.test.tsx",
        "test/library.test.tsx"
      ],
      "origin": "decomposed from E2 (spec §7, §8, §13)",
      "context": "Baseline (oracle.md, every ticket): `bun run check` + `bun run build` + `bun run test` all pass, KEYLESS (no provider key). Locked stack: Fastify + Vite/React SPA, drizzle-orm over better-sqlite3, zod+drizzle-zod, TanStack Query + react-router, shadcn/ui + Tailwind, node crypto (AES-256-GCM + scrypt) + @fastify/secure-session. Path alias `@shared/*` only. Tests in /test under vitest(node); client tests use @testing-library/react + jsdom docblock. RUNTIME (v2-017): server runs under Node/tsx; Bun runs check/build/test. Secrets are OPERATOR secrets (LEDE_MASTER_KEY 32-byte base64 + LEDE_SESSION_SECRET) required to BOOT — distinct from a provider BYOK key (encrypted in the secrets table). Touch ONLY declared files (+ package.json/lockfile). Add tests for new behavior. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM.\n\nClient auth + settings UI (§13). EXTEND src/client/api.ts (auth: setup/login/logout; key: PUT/DELETE /api/settings/key) throwing ApiError{status,code}. EXTEND src/client/hooks/queries.ts WITHOUT clobbering existing hooks (useEntries/useDeleteEntry/useCreateEntry/useUpdateEntry/useProfile/useSettings/useUpdateProfile/useUpdateSettings) — add auth + key mutations.\n(1) LoginGate: wraps the app; if a query returns 401 (ApiError.status===401) show the login/setup form (first-run = setup password, else login); on success re-render the app. (2) SettingsView (replace the /settings stub route added in E1-F1 — wire it in App.tsx): ApiKeyForm (WRITE-ONLY — shows keySet status from GET /api/settings, an input to set a NEW key, a Delete button; NEVER renders the stored key because the API never returns it), ProviderPicker (from @shared/providers ProviderId list) + ModelPicker (provider+model saved via PUT /api/settings). Use E1-A primitives (select/input/button/dialog). Depends on E2-B (auth endpoints) + E2-C (key endpoints).\nRED-TEAM acceptance: write-only key (the form never displays a key value) + 401 routes to LoginGate.",
      "acceptance": "- baseline passes; all existing client suites (library, entry-editor, profile-layout) stay green\n- CONTRAST (write-only key, gameable-resistant): render ApiKeyForm with a mocked GET /api/settings returning {keySet:true, ...} (and NO key field, because the API never sends one) -> the form shows a 'key is set' status and an input to REPLACE it, but renders NO actual key value anywhere (there is none to render); saving fires PUT /api/settings/key; Delete fires DELETE /api/settings/key\n- CONTRAST (401 -> LoginGate): with a mocked query returning 401, LoginGate shows the login form instead of the app; after a mocked successful login the app content renders\n- ProviderPicker/ModelPicker: changing provider+model fires PUT /api/settings with the new values\n- new tests in test/settings-auth.test.tsx",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY (branch f71462a, merged as d992aff): bun run check exit 0; bun run build exit 0; bun run test 24 files / 181 PASS. Acceptance settings-auth.test.tsx 9/9: write-only key (ApiKeyForm has no key prop; shows keySet status; Save->PUT /api/settings/key, Delete->DELETE; typed key never re-rendered), 401->LoginGate (login form shown, app hidden; post-login app renders), Provider/ModelPicker->PUT /api/settings. No gaming. Scope: only overflow was test/library.test.tsx (forced cascading fix: App now wraps LoginGate/useQuery, NavTabs test needed QueryClientProvider+fetch stub matching sibling tests; assertions preserved). Coordinator judgment [v2-028]: legitimate cascade -> files amended to include it; accepted."
    },
    {
      "id": "E3",
      "title": "Phase 3 — reasoning UI (timebox) + remaining sections",
      "status": "decomposed",
      "depends_on": [
        "E2-D",
        "E2-E"
      ],
      "files": [],
      "origin": "spec §24 Phase 3 (§10, §11, §23)",
      "context": "COARSE EPIC — decompose after E2 into: (a) ReasoningPanel split view (ResultView = ResumePage | ReasoningPanel): SignalsBar→WeightBar, per-group SectionRationale→serif Callout (leadRationale on each job's leading bullet), CutList from cut[]; (b) GUARANTEE leadRationale + cut[] NEVER in the printed resume DOM (§10); (c) remaining sections award/certification/publication/interest/language/reference as data + rendering. RED-TEAM — child tickets MUST include: (1) DOM-ABSENCE not display:none — feed a fixture with NON-EMPTY leadRationale + cut reasons, assert those exact strings are absent from the ACTUAL print-target subtree's textContent (a display:none-only impl FAILS; render the real print root, not ResumePage in isolation); (2) TIMEBOX ceiling — text callouts only, GREP-FORBID EventSource/SSE (non-MVP §15), animation libs (framer-motion), any new tailors/history table; cap reasoning-UI ticket count; (3) remaining sections — feed one entry of EACH new section and assert each renders with its registry label/order, and empty-facts entries (cert/reference) render from meta with text:''.",
      "acceptance": "See oracle.md Phase 3 (merged-tree): ReasoningPanel renders signals + per-group leadRationale + cut; leadRationale/cut ABSENT from the printed DOM tree (not merely hidden); remaining sections render from data.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E3-A",
      "title": "Reasoning UI: ResultView split (ResumePage | ReasoningPanel) — signals + per-group leadRationale + cut; reasoning NEVER in the printed DOM",
      "status": "done",
      "depends_on": [
        "E2-E"
      ],
      "files": [
        "src/client/components/ResultView.tsx",
        "src/client/components/ReasoningPanel.tsx",
        "src/client/components/WeightBar.tsx",
        "src/client/components/Callout.tsx",
        "src/client/components/CutList.tsx",
        "src/client/components/TailorView.tsx",
        "src/client/styles/app.css",
        "src/client/styles/print.css",
        "test/reasoning-ui.test.tsx"
      ],
      "origin": "decomposed from E3 (spec §10, §11, §13)",
      "context": "Build the Phase-3 reasoning UI — spec §11 (the differentiator) + §10 (rendering). Locked (oracle.md, cite; never re-litigate): React18+Vite SPA, TanStack Query, shadcn/ui primitives in src/client/components/ui/ (button/card/badge/tabs/tooltip/etc. — compose, never re-roll). Path alias @shared/*. Client tests: @testing-library/react + jsdom docblock (/** @vitest-environment jsdom */) under /test, run by `bun run test` (vitest). Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Server runs Node/tsx; Bun runs check/build/test. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM. Types (src/shared/types.ts): TailoredResume={signals:JDSignals, summary:string, sections:TailoredSection[], cut:{entryId,reason}[]}; JDSignals={roleLevel:string, weights:string[], hardRequirements:string[]}; TailoredSection={section:Section, groups:TailoredGroup[]}; TailoredGroup={heading?:string, leadRationale?:string, items:{entryId:string,text:string}[]}. Registry SECTIONS (src/shared/sections.ts) gives per-section {label,rephrase,order,groupBy}.\n\nCURRENT STATE: src/client/components/TailorView.tsx renders ONLY `<ResumePage resume={resume} />` after a tailor. src/client/components/ResumePage.tsx already renders header/summary/sections/groups/items generically from the registry label and CORRECTLY omits leadRationale + cut (it must STAY that way — do NOT add them to ResumePage). The reasoning data already rides on the result: TailoredGroup.leadRationale (on each group's leading item) and TailoredResume.cut[] and TailoredResume.signals — but nothing renders them yet.\n\nBUILD (§11 'simplest legible version, text callouts, timebox'):\n(1) ResultView.tsx — a SPLIT view rendering ResumePage (the print target) and ReasoningPanel as SIBLINGS (never nest the panel inside ResumePage). Wire ResultView into TailorView (replace the direct <ResumePage> usage; ResultView takes the TailoredResume).\n(2) ReasoningPanel.tsx (never printed) composes: WeightBar.tsx (renders signals — roleLevel, the weights[] in order, and hardRequirements[]); a serif Callout.tsx used for each group's leadRationale (one editorial serif voice, §12 — a plain serif font-family class is fine; do NOT pull design tokens, that is Phase 4); CutList.tsx = a 'What got buried' list, one row per cut[] {entryId, reason}.\n(3) PRINT: reasoning must not appear on paper. In src/client/styles/print.css (@media print) hide the reasoning subtree; AND structurally keep it a sibling of ResumePage so the print-target subtree (.resume-page) never CONTAINS reasoning strings. Minimal split-layout styles go in app.css.\n\nTIMEBOX CEILING (hard): text callouts ONLY. Do NOT add EventSource/SSE/streaming (non-MVP §15), animation libs (framer-motion), or any saved-tailors/history table/persistence. No new deps.",
      "acceptance": "- baseline passes (check/build/test keyless); existing client suites (library, entry-editor, profile-layout, settings-auth) stay green\n- ReasoningPanel renders signals via WeightBar (assert roleLevel, each weights[] entry in order, and hardRequirements[] appear), each group's leadRationale via a Callout, and each cut[] reason via CutList\n- ResultView shows BOTH ResumePage and ReasoningPanel (split)\n- CONTRAST (DOM-ABSENCE, not display:none — gameable-resistant): render the FULL ResultView with a fixture whose leadRationale and cut reasons are NON-EMPTY, DISTINCTIVE strings; grab the print-target node (the .resume-page subtree) and assert those exact strings are ABSENT from ITS textContent, while PRESENT in the reasoning subtree. (A panel nested inside ResumePage, or a display:none-only approach that leaves the strings in the .resume-page subtree, MUST fail this.)\n- TIMEBOX (grep the diff): NO EventSource/SSE, NO framer-motion/animation lib, NO new tailors/history/persistence table; no new dependency\n- new tests in test/reasoning-ui.test.tsx",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY + merged-tree gate (branch 49eff8b, merged b571baf; master 41249cc). Scope: exactly 9 declared files. check 0/build 0; test 26 files 191 PASS incl. reasoning-ui.test.tsx 6/6. DOM-ABSENCE contrast: ResultView rendered with SENTINEL_RATIONALE_*/SENTINEL_CUT_* — resumePage.textContent excludes them, reasoningPanel.textContent includes them (panel is a sibling, not nested; print.css hides .reasoning-panel). WeightBar renders roleLevel+weights-in-order+hardRequirements; Callout per group leadRationale; CutList per cut reason. TIMEBOX grep clean (no SSE/framer-motion/history); no new deps. Gaming read: clean."
    },
    {
      "id": "E3-B",
      "title": "Remaining sections render from meta: rephrase:'none' label sections with empty facts show meta-derived text (cert/publication/reference/interest/language)",
      "status": "done",
      "depends_on": [
        "E2-E"
      ],
      "files": [
        "src/shared/sections.ts",
        "src/server/tailor/assemble.ts",
        "test/remaining-sections.test.tsx",
        "test/assemble.test.ts"
      ],
      "origin": "decomposed from E3 (spec §10, §4.3, §24)",
      "context": "Close the Phase-3 'remaining sections as data + rendering' gap — spec §10 + §4.3. Locked (oracle.md, cite; never re-litigate): React18+Vite SPA, TanStack Query, shadcn/ui primitives in src/client/components/ui/ (button/card/badge/tabs/tooltip/etc. — compose, never re-roll). Path alias @shared/*. Client tests: @testing-library/react + jsdom docblock (/** @vitest-environment jsdom */) under /test, run by `bun run test` (vitest). Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Server runs Node/tsx; Bun runs check/build/test. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM. Types (src/shared/types.ts): TailoredResume={signals:JDSignals, summary:string, sections:TailoredSection[], cut:{entryId,reason}[]}; JDSignals={roleLevel:string, weights:string[], hardRequirements:string[]}; TailoredSection={section:Section, groups:TailoredGroup[]}; TailoredGroup={heading?:string, leadRationale?:string, items:{entryId:string,text:string}[]}. Registry SECTIONS (src/shared/sections.ts) gives per-section {label,rephrase,order,groupBy}.\n\nWHAT ALREADY WORKS (do NOT rebuild): the section registry (src/shared/sections.ts SECTIONS) covers all 10 sections; the zod schema (src/shared/schema.ts entryMetaZ) is a discriminated union over all sections; SectionMetaFields edits award/cert/publication/reference/interest/language; assemble() and ResumePage are fully registry-GENERIC and already render whatever sections are present under SECTIONS[section].label. Leave ResumePage.tsx and SectionMetaFields.tsx ALONE.\n\nTHE REAL GAP (in src/server/tailor/assemble.ts coerceText): for rephrase:'none' sections it returns `r.entry.facts.join(' ')`. Label sections (certification, publication, reference, interest, language — all rephrase:'none') often have EMPTY facts, so this yields '' and the item renders BLANK. TailoredItem is {entryId,text} and carries NO meta, so the client cannot recover it — the existing code comment even promises 'rendered from meta alone downstream' but nothing does. FIX: compose human-readable display text from the entry's META for rephrase:'none' sections when facts are empty (fall back to facts when present). Put the meta->label-text formatter in the REGISTRY single source (src/shared/sections.ts — §4.3/§17: the registry is THE source of per-section behavior), and have assemble.coerceText consume it. Do NOT scatter meta-formatting into the client, and do NOT add meta to TailoredItem/the shared output types. Read src/shared/types.ts EntryMeta + src/shared/schema.ts for the EXACT per-section meta field names (e.g. certification issuer/name/year, publication title/venue/year, reference name/relationship, language name/level, interest name) — use the real fields, do not invent.\n\nScope: registry formatter + assemble wiring + tests only.",
      "acceptance": "- baseline passes (check/build/test keyless); all existing suites stay green (assemble.test.ts must remain green — extend it or add the new file, do not weaken it)\n- COVERAGE: assemble() over a set with one entry of EACH remaining section (award, certification, publication, interest, language, reference) + a layout enabling them, then render <ResumePage> of the result: each section appears under its registry label SECTIONS[section].label\n- CONTRAST (empty-facts from meta — gameable-resistant): a certification AND a reference entry with facts:[] must render NON-BLANK, meta-derived text (assert the specific meta values, e.g. the cert name/issuer and the reference name, appear in the rendered output). A build that leaves them blank (the current behavior) MUST fail this.\n- rephrase:'none' with NON-empty facts still renders facts (no regression); rephrase!='none' sections (experience/project/education/skill light|full) unchanged\n- new tests in test/remaining-sections.test.tsx",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY + merged-tree gate (branch c9a0797, merged; master 41249cc). coerceText: facts.length? join : rule.metaText?.(meta) ?? '' — registry-owned metaText formatter in sections.ts for certification/publication/reference/language (interest has no meta beyond section). Contrast: empty-facts cert renders 'AWS Certified Cloud Practitioner'/'Amazon Web Services'; empty-facts reference renders 'Jane Smith'; non-empty facts still render facts; rephrase:'full' unchanged; model 'FABRICATED' text still ignored for rephrase:'none' (anti-fabrication holds). remaining-sections.test.tsx 5/5. SCOPE JUDGMENT [v2-032]: touched undeclared test/assemble.test.ts (verify stage missed it) — coordinator judged a FORCED cascade: the one changed assertion previously asserted text==='' for an empty-facts cert, literally encoding the bug this ticket fixes; changed to assert 'Some Cert' (meta-derived) — STRENGTHENS the check toward §10/§4.3 intent, not a weakening. Amended B.files += test/assemble.test.ts; accepted."
    },
    {
      "id": "E4",
      "title": "Phase 4 — render polish + Docker/compose/README (self-host)",
      "status": "decomposed",
      "depends_on": [
        "E3-A",
        "E3-B"
      ],
      "files": [],
      "origin": "spec §24 Phase 4 (§10, §12, §19)",
      "context": "COARSE EPIC — decompose after E3 into: (a) print.css polish to ATS-safe (§10: single-column, #000/#fff, no tables/images/multi-column/borders, system serif/sans NOT Plex); (b) §12 design finish — tokens.css, shadcn theme mapped to tokens, self-hosted IBM Plex via @fontsource, low radius, --accent primary; (c) Dockerfile multi-stage (build SPA+server → slim BUN runtime image; deviation from §19 node-slim per toolchain decision) + docker-compose.yml (one service, volume at DATA_DIR) + .env.example with the two required secrets; migrations on boot; (d) README self-host first-run. RED-TEAM — child tickets MUST include: (1) DEEP boot check — after compose up, migrations ran (tables in the mounted volume) and GET / returns the built index.html (not a stub); (2) carry boot-refusal into the image — compose up with the two secrets ABSENT → container exits non-zero; NO real secret baked in image/compose (grep layers+compose; .env.example placeholders only); (3) VOLUME persistence — write data, compose down && up, data survives; (4) fonts self-hosted — grep build output for fonts.googleapis/CDN font URLs → ABSENT; @fontsource present; (5) print.css grep-checkable (no <table>/<img> in ResumePage, no multi-column in print target, #000/#fff) but TRUE ATS text-extraction/reading-order needs headless print — flag as only partially machine-checkable, requires manual/browser verification (state honestly).",
      "acceptance": "See oracle.md Phase 4 (merged-tree): print.css ATS-safe (grep + honest manual caveat); §12 tokens+shadcn+self-hosted Plex; `docker compose up` deep-boots with the two secrets (migrations ran, real SPA served), refuses without them, volume persists; README first-run documented.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E4-A",
      "title": "Render/design polish (§10, §12): §12 tokens + shadcn theming applied, Plex fonts self-hosted, print.css ATS-safe — with a proving test",
      "status": "done",
      "depends_on": [
        "E3-A",
        "E3-B"
      ],
      "files": [
        "src/client/styles/tokens.css",
        "src/client/styles/app.css",
        "src/client/styles/print.css",
        "tailwind.config.ts",
        "test/render-polish.test.ts"
      ],
      "origin": "decomposed from E4 (spec §10, §12)",
      "context": "Locked (oracle.md; never re-litigate): React18+Vite SPA + Fastify 4 (server) · better-sqlite3+Drizzle · TanStack Query · shadcn/ui+Tailwind. Path alias @shared/*. Toolchain: Bun runs check/build/test (`bun run check|build|test`); the SERVER PROCESS runs under NODE via tsx (better-sqlite3's Node-ABI prebuilt is incompatible with Bun's V8 — locked [v2-017]); `start`=`tsx src/server/index.ts`, `build`=`vite build`. Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM, key never leaks, boot refuses w/o LEDE_MASTER_KEY/LEDE_SESSION_SECRET.\n\nWHAT ALREADY EXISTS (verify, do NOT rebuild): src/client/styles/tokens.css already has the full §12 palette verbatim (--ink #1a1a1a, --bg #fff, --accent #2f5fdd, etc.), --radius 4px, Plex font-family vars, and the shadcn CSS-variable mapping (--primary=var(--accent) etc.). src/client/styles/app.css already @imports @fontsource/ibm-plex-sans|mono|serif (self-hosted, no CDN) then tokens.css, and body uses var(--font-sans)/var(--foreground). src/client/styles/print.css already has @media print: letter page, #000 on #fff, single-column (display:block), Georgia serif (NOT Plex — §10, to avoid PDF embedding surprises), hides .jd-input/.tailor-view__pending/.tailor-view__error/.reasoning-panel, list-style bullets. @fontsource deps + tailwind.config.ts + postcss.config.js all present.\n\nTHE GAP: there is NO machine-checkable test proving Phase-4 render polish, and the phase oracle requires it. ADD test/render-polish.test.ts that asserts the §10/§12 properties by reading the CSS files and/or rendering: (1) tokens.css declares the §12 color tokens with their EXACT spec values + --radius:4px + the three Plex font-family vars; (2) app.css self-hosts Plex via @fontsource @import and does NOT reference fonts.googleapis/CDN; (3) print.css @media print block sets color:#000 + background:#fff, is single-column (no display:flex/grid/columns/float multi-column, no table/img rules) and hides .reasoning-panel. Fix any real gap the test surfaces (e.g. a stray CDN font ref, a non-token hardcoded color in app chrome, a multi-column rule in print). Do NOT touch the resume DOM structure or reasoning UI (Phase 3, done). Keep changes minimal — most of this is already in place.",
      "acceptance": "- baseline passes (check/build/test keyless); all existing suites stay green\n- §12 tokens present in tokens.css with the EXACT spec values (--ink #1a1a1a, --bg #fff, --accent #2f5fdd, --border #e4e4e7, --radius 4px, Plex font vars) and mapped to shadcn CSS variables\n- Plex fonts self-hosted via @fontsource (@import in app.css); NO Google-Fonts/CDN reference anywhere in styles\n- print.css @media print: #000 on #fff, single-column, no table/image/multi-column rules, reasoning panel hidden — asserted mechanically (read the CSS)\n- new test in test/render-polish.test.ts covering the above (fails if a token is wrong/missing, a CDN font is introduced, or print gains a multi-column/table rule)",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY + merged-tree gate (branch 18ea8b4; master 74a0420). Scope: only test/render-polish.test.ts (existing tokens.css/app.css/print.css/tailwind.config.ts already matched §12/§10 verbatim — verified unchanged). check 0/build 0 (dist bundles self-hosted ibm-plex *.woff2, no CDN). render-polish.test.ts 11/11: §12 token values exact + shadcn mapping; @fontsource imports + no googleapis/gstatic; print @media #000-on-#fff, single-column, no table/img/multi-column, .reasoning-panel hidden. Full suite 207 (branch) / 215 (merged)."
    },
    {
      "id": "E4-B",
      "title": "Serve the built SPA from the server (§19): GET / returns index.html with SPA fallback; /api/* not shadowed — implements the index.ts:139 TODO",
      "status": "done",
      "depends_on": [
        "E2-D",
        "E2-E"
      ],
      "files": [
        "src/server/index.ts",
        "package.json",
        "test/spa-serving.test.ts"
      ],
      "origin": "decomposed from E4 (spec §19; index.ts:139 TODO)",
      "context": "Locked (oracle.md; never re-litigate): React18+Vite SPA + Fastify 4 (server) · better-sqlite3+Drizzle · TanStack Query · shadcn/ui+Tailwind. Path alias @shared/*. Toolchain: Bun runs check/build/test (`bun run check|build|test`); the SERVER PROCESS runs under NODE via tsx (better-sqlite3's Node-ABI prebuilt is incompatible with Bun's V8 — locked [v2-017]); `start`=`tsx src/server/index.ts`, `build`=`vite build`. Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM, key never leaks, boot refuses w/o LEDE_MASTER_KEY/LEDE_SESSION_SECRET.\n\nImplement the deferred TODO at src/server/index.ts:139 ('in production, serve the built SPA from dist/ as static files'). Add @fastify/static (a server dep listed in §20 but NOT yet in package.json — add it; manifest allowlisted). In buildApp(), register static serving of the Vite build output (repo-root dist/, containing index.html + assets/) with a SPA history fallback: a GET request that is NOT under /api/* and does not match a static asset returns dist/index.html (so client-side react-router routes like /library, /settings work on refresh). /api/* routes and the auth guard MUST be unaffected and take precedence — /api 404s must still return JSON, never index.html.\n\nCRITICAL — keyless tests run WITHOUT a built dist/: static serving must be TOLERANT of a missing dist directory (register the static plugin/fallback only if the dist dir exists, or otherwise ensure boot + the full keyless suite + boot.smoke still pass when dist/ is absent). Do NOT break existing api.*/boot.smoke/tailor tests. The dist path should be resolvable relative to the server file (e.g. <repo>/dist), overridable if a config field already exists — do not invent new config unless needed.",
      "acceptance": "- baseline passes (check/build/test keyless) WITHOUT a built dist present — existing api.*, boot.smoke, tailor suites stay green (static serving must be a no-op / tolerant when dist/ is absent)\n- NEW test/spa-serving.test.ts: with a temp dist/ containing a marker index.html, GET / returns that index.html; a non-/api client route (e.g. GET /library) also returns index.html (SPA fallback)\n- CONTRAST (/api not shadowed — gameable-resistant): with the same dist present, GET /api/health still returns {ok:true} (JSON, not the SPA html); an unknown GET /api/nope returns a JSON 404, NOT index.html\n- @fastify/static added to package.json; no other deps",
      "attempts": [],
      "evidence": "INDEPENDENT VERIFY + merged-tree gate (branch 9e2275d; master 74a0420). Scope: src/server/index.ts + package.json + test/spa-serving.test.ts (declared) + bun.lock (allowlist). Added @fastify/static 7.0.4 only. spa-serving.test.ts 8/8: dist-present GET / + client routes -> index.html, real assets served, CONTRAST /api/health stays JSON {ok:true} + unknown /api/* -> JSON 404 (not shadowed); dist-ABSENT GET / -> plain 404 (no crash), /api/health unaffected, full keyless suite green. Merged-tree gate: full suite 215/215, boot.smoke 3/3 clean, LIVE boot (bun start, real dist) GET / -> real SPA (<title>Lede</title>) + /api/health {ok:true}. check 0."
    },
    {
      "id": "E4-C",
      "title": "Docker + compose + README self-host (§19): Node multi-stage image (build SPA + run server under tsx), docker-compose.yml, first-run README",
      "status": "done",
      "depends_on": [
        "E4-B"
      ],
      "files": [
        "Dockerfile",
        "docker-compose.yml",
        "README.md",
        ".dockerignore"
      ],
      "origin": "decomposed from E4 (spec §19, §24)",
      "context": "Locked (oracle.md; never re-litigate): React18+Vite SPA + Fastify 4 (server) · better-sqlite3+Drizzle · TanStack Query · shadcn/ui+Tailwind. Path alias @shared/*. Toolchain: Bun runs check/build/test (`bun run check|build|test`); the SERVER PROCESS runs under NODE via tsx (better-sqlite3's Node-ABI prebuilt is incompatible with Bun's V8 — locked [v2-017]); `start`=`tsx src/server/index.ts`, `build`=`vite build`. Baseline every ticket: `bun run check`+`bun run build`+`bun run test` all pass KEYLESS. Cross-cutting invariants stay green: NO tag-scoring, NO LLM-checks-LLM, key never leaks, boot refuses w/o LEDE_MASTER_KEY/LEDE_SESSION_SECRET.\n\nProduction self-host packaging (§19). The CURRENT Dockerfile is WRONG and must be rewritten: it uses `FROM oven/bun` and `CMD [\"bun\",\"run\",\"src/main.ts\"]` — but (a) the server MUST run under Node/tsx not Bun (locked [v2-017]: better-sqlite3 Node-ABI prebuilt is incompatible with Bun's V8 — a Bun runtime dies ERR_DLOPEN_FAILED at boot), (b) src/main.ts does not exist (server entry is src/server/index.ts; `start`=`tsx src/server/index.ts`), (c) it is single-stage and never runs `vite build`, so no SPA is served, (d) EXPOSE 3000 is wrong (PORT default 8787).\n\nBuild a NODE multi-stage image: a build stage that installs deps and runs `bun run build` (or `vite build`) to produce dist/, and a slim Node runtime stage that has the deps + src/ + dist/ and runs the server via tsx (e.g. `tsx src/server/index.ts`, matching `start`). EXPOSE 8787. Data lives at DATA_DIR (default /app/data) — declare a VOLUME. Add docker-compose.yml: one service, a named volume mounted at DATA_DIR, env LEDE_MASTER_KEY + LEDE_SESSION_SECRET (required) + PORT + DATA_DIR (+ optional LEDE_AUTH_DISABLED), port mapping. Update README.md with the first-run flow: `docker compose up --build` → set the two secrets (openssl rand -base64 32) → open the app → set password → add provider key in Settings → tailor. Update .dockerignore (exclude node_modules/.git/dist/data as appropriate for the build context). This depends on E4-B so the running image actually serves the SPA.",
      "acceptance": "- baseline passes (check/build/test keyless — these run on the host, unaffected)\n- Dockerfile is NODE-based multi-stage (grep: NOT oven/bun; runs the server via tsx/node; runs vite build; EXPOSE 8787) and declares a VOLUME at DATA_DIR\n- docker-compose.yml: one service, volume at DATA_DIR, LEDE_MASTER_KEY+LEDE_SESSION_SECRET env, port mapping\n- LIVE (heavy, Docker is available in this env): `docker compose up --build -d` with the two secrets set builds and boots; GET /api/health -> {ok:true} AND GET / returns the SPA index.html (a UI-less API must NOT pass); then bring it down. Capture the real curl output.\n- README documents the first-run self-host flow",
      "attempts": [],
      "evidence": "INDEPENDENT RE-VERIFY (branch f049350, merged). Scope: 4 declared files (Dockerfile, docker-compose.yml, README.md, .dockerignore). Dockerfile = multi-stage, RUNTIME node:22-bookworm-slim (NOT Bun, per [v2-017]); npm --omit=dev + tsx; builds+copies dist/; EXPOSE 8787; VOLUME /app/data. Coordinator ran `docker compose up --build` (host 8899): container booted (Server listening 0.0.0.0:8787); GET /api/health -> 200 {ok:true}; GET / -> 200 <title>Lede</title> + /assets/index-DdnsMgKH.js (real SPA, not UI-less); GET /api/nope -> 401 JSON (API not shadowed by SPA fallback). Teardown clean. README documents first-run. No gaming."
    },
    {
      "id": "E5-A",
      "title": "Playwright scaffold + Phase 1 LibraryView CRUD browser spec",
      "status": "done",
      "depends_on": [],
      "files": [
        "package.json",
        "bun.lock",
        "playwright.config.ts",
        ".gitignore",
        "vitest.config.ts",
        "test/e2e/library-crud.spec.ts",
        "test/e2e/helpers/tmpdata.ts"
      ],
      "origin": "spec §9, §21 + oracle [v2-036] browser acceptance (Phase 1)",
      "context": "Locked (oracle.md; never re-litigate): server runs under Node/tsx not Bun [v2-017]; PORT default 8787; keyless baseline unless doing the KEY-GATED T014 eval. Install `@playwright/test` as a dev dep. Create `playwright.config.ts`: chromium project only, `webServer` boots `bun run start` (Node/tsx per [v2-017]) on a per-run DATA_DIR (tmp dir; the helper `test/e2e/helpers/tmpdata.ts` creates and passes it via env), baseURL http://localhost:${PORT}, `reuseExistingServer: !process.env.CI`, `testDir: 'test/e2e'`, timeout 60s. Update `vitest.config.ts` to exclude `test/e2e/**` (vitest must not try to run playwright specs). Update `package.json`: `test` becomes composite `vitest run && playwright test` (baseline per [v2-036]); add `test:e2e` (`playwright test`) for iteration; do NOT add a `postinstall` — chromium install is a one-time human step documented in README (later ticket may extend). Author `test/e2e/library-crud.spec.ts` covering experience + project sections (skill/education intentionally deferred to a follow-up if time permits — call out in evidence): Create (open form → fill required fields → save → assert row appears in list), Edit (open row → change a field → save → assert list reflects change), Delete (delete → assert row gone), Reload persistence (create → page.reload() → assert row still present, from the browser side, complementing the existing keyless api restart test). If auth is enabled by default, skip via `LEDE_AUTH_DISABLED=1` in the webServer env — Phase 2 spec (E5-B) is what proves the auth flow. LOCKED: no CI-only fixtures; the spec must run locally against the same server the human runs.",
      "acceptance": "- `bun install` succeeds; `bun run check` exit 0; `bun run build` exit 0\n- `bun run test` (composite) runs BOTH vitest and playwright; both green; the composite exit code is 0 iff both are\n- library-crud.spec.ts passes chromium against a fresh DATA_DIR (webServer wired to a tmp dir via helpers/tmpdata.ts)\n- DELIBERATE-BREAK sanity (do it, then revert; evidence must record both runs): temporarily change the POST /api/entries handler to no-op (return 200 but do not persist) → the spec FAILS at the Create assertion (proves it drives real UI+API, not a mock)\n- vitest.config.ts excludes test/e2e/** (evidence: `bun run vitest run test/e2e/library-crud.spec.ts` — an intentional call — reports 'No test files found', not a syntax error)\n- scope: only files listed touched; bun.lock allowlisted",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify on worktree branch (now merged @07848f0, byte-identical, no-ff no intervening commits): bun run check exit 0; bun run build exit 0; composite `bun run test` (PORT=18899 — 8787 occupied by unrelated dev server on this shared box) -> vitest 215/215 passed THEN playwright 8/8 passed, composite exit 0. Scope: git diff --name-only master = .gitignore, bun.lock, package.json + new playwright.config.ts, vitest.config.ts, test/e2e/{helpers/tmpdata.ts, library-crud.spec.ts} — exactly the declared set; src/ diff empty (deliberate-break reverted). Gaming read CLEAN: spec drives real chromium against a real Node/tsx server boot (webServer), role-based selectors read from actual LibraryView/EntryEditor; CONTRAST assertions (edit: new text visible AND old toHaveCount(0); delete: toHaveCount(0); reload persistence: page.reload() then re-assert visible). Builder deliberate-break corroborated: 8 failed at the Create assertion when POST /api/entries was no-op'd; 8 passed after exact revert. vitest excludes test/e2e via mergeConfig array-concat (configDefaults.exclude preserved); `bunx vitest run test/e2e/...` -> 'No test files found'."
    },
    {
      "id": "E5-B",
      "title": "Phase 2 auth: first-run set-password → login → protected route browser spec",
      "status": "done",
      "depends_on": [
        "E5-A"
      ],
      "files": [
        "test/e2e/auth.spec.ts",
        "test/e2e/helpers/session.ts",
        "playwright.config.ts",
        "src/server/index.ts"
      ],
      "origin": "spec §7, §8, §24 Phase 2 + oracle [v2-036] browser acceptance (Phase 2) + repair: browser gate exposed pre-existing session-cookie path bug (scope expanded to src/server/index.ts — see ledger [v2-038])",
      "context": "Locked: LEDE_MASTER_KEY + LEDE_SESSION_SECRET required to boot; session cookie must be HttpOnly (§8, §17); /api/tailor with no stored key → 400 no_api_key (keyless test, unchanged — do NOT set a provider key in this spec). Extend the playwright config: a second project 'auth' (or a per-test webServer env override) that boots WITHOUT `LEDE_AUTH_DISABLED` (default) against a fresh DATA_DIR — the auth gate must actually be active. Author `test/e2e/auth.spec.ts` driving: (1) visit / on fresh boot → assert set-password screen; submit a password; (2) redirected to logged-in view; (3) log out (UI action if exists) or clear session cookie via context.clearCookies(); (4) direct-nav to a protected route → refused, redirected to login; (5) log in with the previously-set password → protected route now reachable. Assert the session cookie has `HttpOnly` true via context.cookies(). Extract a helper `test/e2e/helpers/session.ts` exporting `ensureFirstRunPassword(page, password)` and `login(page, password)` so E5-C reuses without duplication. LOCKED: this spec does NOT store an api key.\n\n[COORDINATOR SCOPE EXPANSION — ledger v2-038] The auth spec you wrote CAUGHT A REAL PRE-EXISTING BUG (verified by the coordinator): @fastify/secure-session in src/server/index.ts (~L109) registers with NO `cookie` option, so the session cookie's Path defaults (RFC 6265) to the login endpoint's dir '/api/auth' — after login the cookie is never sent to /api/settings etc., so LoginGate's ping stays 401 and the app is unreachable in a real browser. Inject-based tests (.inject ignores cookie Path) structurally cannot catch this — the browser spec is the only possible proof, so the repair is folded into THIS ticket. FIX (verified fail->fix->pass by coordinator): add `cookie: { path: \"/\" }` to the secureSession options. Declared files now INCLUDE src/server/index.ts for exactly this one-line fix. Escaped-bug rule: your auth.spec.ts IS the strengthening check that locks it in.",
      "acceptance": "- `bun run test` composite still green; the auth spec passes chromium\n- HttpOnly assertion is real: DELIBERATE-BREAK — temporarily remove `httpOnly: true` from the session cookie config on the server → the spec FAILS the HttpOnly assertion (evidence records both runs)\n- guard assertion is real: DELIBERATE-BREAK — temporarily remove the auth preHandler from one protected route → the spec FAILS step (4) (evidence records both runs)\n- helpers/session.ts is imported and used by the spec (not dead code) — grep-verifiable\n- scope: only files listed touched",
      "attempts": [
        {
          "n": 1,
          "failed": "blocked: composite baseline could not pass — auth.spec.ts (correctly) fails post-login against unfixed src",
          "hypothesis": "session cookie Path defaults to /api/auth (no cookie option on secureSession); cookie not sent to /api/settings ping -> app unreachable post-login in a real browser. Inject tests blind to Path.",
          "fixNote": "Scope expanded to include src/server/index.ts. Apply the one-line fix `cookie: { path: '/' }` to secureSession options; re-run composite to confirm auth spec + library-crud all green."
        }
      ],
      "evidence": "INDEPENDENT re-verify by coordinator on worktree branch (merged @a10a0ec, byte-identical: clean ort merge of 4 disjoint files, no intervening commits). The spec CAUGHT + FIXED a real bug (see ledger v2-038): session cookie Path defaulted to /api/auth -> app unreachable post-login in a real browser; fix = `cookie: { path: '/' }` (src diff is ONLY that one line). check exit 0; build exit 0; final composite `bun run test` (PORT=18899) = vitest 215/215 THEN playwright 9/9 (8 library-crud + 1 auth), exit 0. Deliberate-breaks BOTH confirmed load-bearing: (1) httpOnly:false -> auth FAILS at line 45 httpOnly assertion -> reverted; (2) /api/settings added to PUBLIC_PREFIXES (guard defeated) -> auth FAILS at step 1 set-password assertion -> reverted. Final src diff = only the cookie line. Flake hardening (coordinator, test-timing only — not a semantic weakening): logout step now waits deterministically for POST /api/auth/logout via waitForResponse, and post-transition assertions in auth.spec.ts + session.ts get 15s headroom (matches vite.config.ts's existing 30s-vitest contention convention). Verified: full playwright suite 5/5 green under back-to-back contention (incl. a 30.6s contended run that previously flaked). Scope: git diff --name-only master = playwright.config.ts, src/server/index.ts + new test/e2e/auth.spec.ts, test/e2e/helpers/session.ts — exactly the declared set (src/server/index.ts added by the v2-038 scope expansion). library-crud.spec.ts untouched. Design: two-server/two-project playwright config — 'chromium' project (auth-disabled server, library-crud) + 'auth' project (auth-enabled server on PORT+1, fresh DATA_DIR). session.ts helpers (ensureFirstRunPassword, login) general + reused-ready for E5-C."
    },
    {
      "id": "E5-C",
      "title": "Phase 4 docker + browser: SPA mounts and /api/* round-trips inside the container",
      "status": "done",
      "depends_on": [
        "E5-A",
        "E5-B"
      ],
      "files": [
        "test/e2e/docker-spa.spec.ts",
        "test/e2e/helpers/docker.ts",
        "playwright.config.ts",
        "package.json"
      ],
      "origin": "spec §19 + oracle [v2-036] browser acceptance (Phase 4, sharpens [v2-033])",
      "context": "Locked: image runs under Node/tsx not Bun [v2-017]; container needs LEDE_MASTER_KEY + LEDE_SESSION_SECRET; §19 first-run flow. Add a Playwright project 'docker' in `playwright.config.ts` — no `webServer` for this project; instead a globalSetup (`test/e2e/helpers/docker.ts`) runs `docker compose -p lede-e2e up --build -d` with the two secrets set to test values (generate with openssl rand or hardcode test-only values — clearly marked), waits until GET /api/health returns 200 on the published host port, then Playwright uses that port as baseURL for this project only. globalTeardown runs `docker compose -p lede-e2e down -v` and asserts no orphaned container remains for the project. The 'docker' project is EXCLUDED from `playwright test` default (it's slow + requires docker). The composite baseline must invoke it explicitly — extend package.json `test` script to: `vitest run && playwright test && playwright test --project=docker`. Author `test/e2e/docker-spa.spec.ts`: (1) open baseURL — assert response is HTML with a script tag pointing to /assets/*.js; (2) wait for React root to mount — root selector's innerHTML length > threshold AND expected app-shell text present (not just a title tag — a static HTML file could pass a title assertion); (3) collect page.on('pageerror') + page.on('console', err) listeners from load start; assert none fired by domcontentloaded + a settle wait; (4) use the E5-B session helper to reach a protected page → observe a /api/settings XHR resolve 200 through the SPA (proves the app-inside-the-container talks to the api-inside-the-container end-to-end).",
      "acceptance": "- docker present locally (baseline requirement; ticket blocks with clear reason otherwise)\n- `bun run test` composite runs vitest + playwright default + `playwright test --project=docker`; all green\n- DELIBERATE-BREAK (mount check is real): temporarily rename dist/assets/*.js in the image (e.g. rebuild with a broken vite config OR shell into container and mv the asset) → docker spec FAILS step (2) mount OR step (3) console-error listener (evidence records the failing run + the revert run)\n- DELIBERATE-BREAK (console-error listener is real): temporarily introduce a runtime `console.error` in App bootstrap → docker spec FAILS step (3) (evidence records both runs)\n- globalTeardown runs `docker compose down -v`; a `docker ps -a --filter name=lede-e2e` after the run shows no orphan containers\n- scope: only files listed touched",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify by coordinator + FINAL merged-tree run on master @9d552be (after `bun install`): composite `bun run test` (PORT=39001) = vitest 215/215 THEN playwright default 9/9 (chromium+auth) THEN LEDE_E2E_DOCKER=1 playwright --project=docker 1/1 (real `docker compose -p lede-e2e up --build` of the shipped image, health-poll, run, `down -v`), exit 0. Container Created->Started->Stopped->Removed; `docker ps -a --filter name=lede-e2e` empty afterward (teardown throws on orphans). check exit 0; build exit 0. Gaming read CLEAN: mount check asserts real app-shell (#root innerHTML>200 + LoginForm 'Password' label + 'First time here?' text — not a static shell/title); console-error listener allowlists ONLY the pre-login GET /api/settings 401 (LoginGate's own probe) and does NOT allowlist pageerror; step-4 round-trip waits for GET /api/settings 200 through the authed session in-container (the exact path the E5-B cookie bug broke). Nested-env networking (docker-outside-of-docker: /.dockerenv detected -> join compose net, address http://lede:8787) falls back to http://localhost:8899 on a real host — same production container either way, not a CI-only fixture. Fixed TEST-ONLY master key documented (avoids Compose recreating the container between runs). Deliberate-breaks BOTH confirmed by builder (assertions read as load-bearing): (1) truncate served dist/assets/*.js -> mount assertion fails; (2) console.error in main.tsx bootstrap (distinct text) -> console-error assertion fails; each reverted -> passes. src diff empty after. Scope: git diff --name-only master = package.json, playwright.config.ts + new test/e2e/docker-spa.spec.ts, test/e2e/helpers/docker.ts — exactly declared; bun.lock untouched; other e2e specs/helpers + src/ untouched. docker project gated on LEDE_E2E_DOCKER=1 (project only defined + globalSetup no-ops otherwise), so a bare `playwright test` never touches Docker."
    },
    {
      "id": "E6-A1",
      "title": "Applications table + Application type + zod + migration 0001",
      "depends_on": [],
      "files": [
        "src/server/db/schema.ts",
        "src/shared/types.ts",
        "src/shared/schema.ts",
        "drizzle/0001_applications.sql",
        "drizzle/meta/_journal.json",
        "drizzle/meta/0001_snapshot.json",
        "test/schema.test.ts",
        "test/db.test.ts"
      ],
      "origin": "spec §27 (Application domain) + §4.2 conventions — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nAdd the persistent Applications entity. NET-NEW (nothing exists today; current schema has only entries/profile/settings/secrets in src/server/db/schema.ts).\n1) src/server/db/schema.ts — add `applications` sqliteTable per §27: id text PK; company text?; role text?; jobDescription text NOT NULL; context text?; current text json $type<TailoredResume>? ; locked text json $type<TailoredResume>? ; genState text NOT NULL default 'untailored'; currentMeta text json $type<{at:number;provider:ProviderId;model:string}>? ; createdAt integer NOT NULL; updatedAt integer NOT NULL. Follow the existing json-column + naming idiom in this file (snake_case db names).\n2) src/shared/types.ts — add `Application` TS type mirroring §27 exactly (genState union: 'untailored'|'tailoring'|'tailored'|'failed'). Import ProviderId/TailoredResume already there.\n3) src/shared/schema.ts — add zod: `applicationCreate` ({ jobDescription required 1..20000, company?, role?, context? }) and `applicationUpdate` (all optional). Match the existing zod idiom in this file.\n4) Generate the migration: run `bunx drizzle-kit generate` (drizzle.config.ts points at drizzle/). It emits drizzle/0001_*.sql + updates drizzle/meta. If the generated basename differs from 0001_applications.sql, that filename is allowlisted-equivalent — keep whatever drizzle-kit emits and update the declared files note in your report. Migrations run on boot via migrateDb() in src/server/db/index.ts (no code change needed there).\n5) Extend test/schema.test.ts + test/db.test.ts: assert a fresh-boot DB has the applications table with all §27 columns and that an insert→read round-trips (incl json current/locked/currentMeta). No behavior beyond schema — these tests ARE this ticket's tests.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- Fresh DATA_DIR boot runs migration 0001 and creates `applications` with every §27 column (assert by insert+read of a full row incl json fields).\n- `Application` TS type compiles and matches §27; applicationCreate zod rejects a missing jobDescription and accepts optional company/role/context.\n- No hiring-status field exists (only genState) — not-a-tracker guard.\nRED-TEAM SHARPENED [v3-001]: (#7) insert a row with NON-NULL `current` (a real TailoredResume) + `currentMeta` → read back DEEP-EQUAL (proves json $type round-trips, not mere column presence); insert OMITTING `genState` → read returns 'untailored' (the §27 default actually applies).",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @1f51f5e (coordinator ran, not builder): scope CLEAN (8 files ⊆ declared; migration emitted 0001_marvelous_steel_serpent.sql). check exit 0; lint (biome) clean; vitest 227/227 incl schema.test (applicationCreate rejects missing/empty/>20000 jobDescription; applicationUpdate all-optional) + db.test (§27 table has all columns; NON-NULL current+currentMeta deep-equal round-trip [red-team #7]; genState defaults untailored on omit [#7]; no hiring-status column [not-a-tracker]); boot.smoke 3/3 (migration 0001 boots + /api/health, boot-refusal invariants hold). Gaming read: schema matches §27 exactly (snake_case, current/locked json, gen_state default), real CREATE TABLE, tests use real on-disk migrate — no gaming.",
      "status": "done"
    },
    {
      "id": "E6-A2",
      "title": "Applications CRUD routes + client API + query hook",
      "depends_on": [
        "E6-A1"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/server/index.ts",
        "src/client/api.ts",
        "src/client/queries/useApplications.ts",
        "test/api.applications.test.ts"
      ],
      "origin": "spec §9 (applications CRUD) — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nNET-NEW routes. Model on existing src/server/routes/entries.ts (Fastify plugin, zod-validated, session-guarded — the auth guard is registered globally in index.ts so /api/* is already protected).\n1) src/server/routes/applications.ts (new plugin `applicationsRoutes`): GET /api/applications → Application[] but LIST payload is metadata + genState + currentMeta ONLY — OMIT the heavy `current`/`locked` snapshots (perf + §9 'no heavy snapshots'). POST /api/applications (body applicationCreate) → new Application (slug/uuid id; genState 'untailored'). GET /api/applications/:id → full Application incl current+locked. PUT /api/applications/:id (applicationUpdate). DELETE /api/applications/:id → { ok }.\n2) Register applicationsRoutes in src/server/index.ts buildApp() alongside entriesRoutes/profileRoutes (~L120). Do NOT touch the inline /api/tailor route here (that is E6-A3).\n3) src/client/api.ts — typed helpers: listApplications, createApplication, getApplication, updateApplication, deleteApplication (throw ApiError like the existing helpers).\n4) src/client/queries/useApplications.ts (new) — TanStack Query hooks; key ['applications'] for list, ['applications',id] for detail; mutations invalidate.\n5) test/api.applications.test.ts (new) — keyless via buildApp().inject().",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- inject round-trip: create → list → get → update → delete all succeed with correct shapes.\n- LIST payload asserts `current`/`locked` are ABSENT; GET :id includes them (null initially).\n- Persistence: after create, reopen the DB / build a fresh app instance against the same DATA_DIR → the row is still returned (survives restart).\n- Unauthenticated request → 401 (guard applies).\nRED-TEAM SHARPENED [v3-001]: (#4) do NOT test list-omits-snapshots while current is null — directly DB-insert an application with a NON-NULL `current`, then assert the list payload has NO `current`/`locked` key for it while GET :id returns the full `current`. Persistence must use an on-disk DATA_DIR + a genuinely NEW db connection (not :memory:/shared singleton).",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @2a44417: scope CLEAN (5 files ⊆ declared). check 0; lint clean; vitest 233/233 incl api.applications.test (7: CRUD round-trip w/ shapes, bad-body 400, unknown-id 404, fresh-connection persistence [real restart sim], 401 unauth via authDisabled:false, RED-TEAM #4 non-null current inserted → LIST not.toHaveProperty current/locked + GET:id toEqual(resume)). Gaming read CLEAN: GET list uses explicit LIST_COLUMNS omitting current/locked (real omission), uuid id, genState untailored, mirrors entries.ts, real DB. NOTE: hooks landed at src/client/queries/useApplications.ts per declared files, but repo convention is src/client/hooks/queries.ts — coordinator ticket-authoring path guess (spec §21); functional, B1 imports from actual path; consolidation optional.",
      "status": "done"
    },
    {
      "id": "E6-A3",
      "title": "App-scoped tailor (replaces stateless /api/tailor) + context plumbing",
      "depends_on": [
        "E6-A2"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/server/tailor/engine.ts",
        "src/client/api.ts",
        "src/client/queries/useApplications.ts",
        "test/engine.test.ts",
        "test/validate.test.ts",
        "test/api.applications-tailor.test.ts"
      ],
      "origin": "spec §9 (:id/tailor persists current) + §6.2 (context boundary) — v3; REPLACES stateless POST /api/tailor",
      "context": "Locked: provider-agnostic AI SDK; Drizzle/better-sqlite3; Fastify; Node/tsx; Bun test runner. Applications NOT a tracker (only genState). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nSCOPE (coordinator-refined [v3-006]): PURELY ADDITIVE + BACKWARD-COMPATIBLE. Do NOT remove the stateless POST /api/tailor route, the api.ts tailor() helper, or TailorView — those are torn down ATOMICALLY in E6-B2 (the UI cutover), because TailorView still imports tailor() and removing it here would red the build. Everything you add must leave the existing stateless path building and passing.\nCURRENT STATE: stateless POST /api/tailor is inline in src/server/index.ts; client tailor() is in src/client/api.ts (~L68) and used inline by TailorView.tsx. There is NO useTailor hook. applicationsRoutes + the applications query hook (src/client/queries/useApplications.ts) already exist (E6-A2).\n1) src/server/tailor/engine.ts: extend TailorEngine.decide(jd, entries, context?) and tailor(engine, jd, entries, layout, baseSummary?, context?) — context OPTIONAL so the existing stateless caller still compiles unchanged. Extract a pure helper buildUserPrompt(jd, context?): with NO context it MUST return the exact current user message (byte-identical to engine.ts line ~48: \"Tailor for this job description:\" then a blank line then the jd) so the T014 flip-path is unchanged; with context, append a clearly-labelled context block. ProviderEngine.attempt calls buildUserPrompt. FixtureEngine.decide keeps keying on hashKey(jd, entries) — do NOT add context to the hash. Do NOT edit prompt.ts (SYSTEM_PROMPT is frozen). validateNoFabrication is called with entries ONLY.\n2) src/server/routes/applications.ts: add POST /api/applications/:id/tailor — load the app (404 if missing); run tailor() over the CURRENT library + app.jobDescription + app.context; on success persist the result as current, set genState \"tailored\" + currentMeta {at,provider,model}; on FAILURE leave current UNTOUCHED and set genState \"failed\". Live mode with no stored key → 400 {error:\"no_api_key\"}. Map provider errors per §9. Reuse the engine-selection + key-decrypt logic the stateless route already uses (import/share it, do not silently fork a second copy).\n3) src/client/api.ts: ADD tailorApplication(id) (KEEP tailor()). src/client/queries/useApplications.ts: ADD a useTailorApplication mutation that invalidates [\"applications\", id].\n4) Tests: test/engine.test.ts (buildUserPrompt: null===baseline string; with-context contains ctx); test/validate.test.ts (red-team #6); test/api.applications-tailor.test.ts NEW (:id/tailor behavior + red-team #1 & #11, keyless via FixtureEngine — create the app with a JD that is a RECORDED fixture so decide replays).",
      "acceptance": "Narrow gate (all green): bun run format; bun run lint; bun run check exit 0; bunx vitest run --exclude test/boot.smoke.test.ts; bun run test:e2e (playwright chromium). New tests ship. Touch ONLY declared files.\n- buildUserPrompt(jd, null/undefined) === the exact current user message (byte-identical baseline — guards the flip); buildUserPrompt(jd, ctx) contains ctx.\n- Existing 3 fixtures still replay (hashKey unchanged); the existing stateless tailor tests (tailor-db / tailor.e2e / api.tailor-byok) STILL PASS UNCHANGED (you did not touch them; the stateless path is intact).\n- POST :id/tailor persists current + currentMeta + genState \"tailored\"; survives a fresh app instance/restart; re-tailor overwrites current.\n- RED-TEAM #1 (HIGH — real path, not just the helper): with a spy/mock engine, tailoring an app whose stored context is non-empty → engine.decide RECEIVES the context AND the composed user message contains it; empty context → user message byte-identical to baseline (same spy). Exercise route→engine, not only the pure fn.\n- RED-TEAM #6: validateNoFabrication unit — a number present only in a context string (absent from every entry) still throws FabricationError; validateNoFabrication is called with entries only (context not in its args).\n- RED-TEAM #11: failed-tailor with a PRIOR current — tailor once (current=S), then force a failing re-tailor → current still deep-equals S (not null) AND genState===\"failed\".\n- Live mode, no stored key → 400 {error:\"no_api_key\"}.\n- DEFERRED (do NOT attempt, no key available): the live context-shifts-emphasis eval.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @2cf5f16: scope CLEAN (7 files ⊆ declared). check 0; lint clean; vitest 253/253 (clean isolated run; a 1-test flake under concurrent-process contention did NOT reproduce — env, cf [v2-035]); playwright chromium 8/8. Gaming read CLEAN: engine.ts buildUserPrompt(jd,null)===baseline byte-identical, context appended to USER MESSAGE only, SYSTEM_PROMPT/prompt.ts untouched, FixtureEngine keys hashKey(jd,entries) (context excluded), tailor() calls validateNoFabrication(entries only). Route :id/tailor persists current+currentMeta+genState tailored, failure→genState failed + current untouched, no-key→400; resolveEngine/rowToEntry/mapTailorError kept in lockstep w/ index.ts (transient dup, dies w/ stateless route in B2). Red-team GENUINE: #1 spy proves context reaches decide()+composed msg (empty→byte-identical), #6 validateNoFabrication.length===3 + context-only number throws, #11 failed re-tailor keeps prior current. ADDITIVE — stateless path intact (its 3 tests untouched & green).",
      "status": "done"
    },
    {
      "id": "E6-A4",
      "title": "Lock / unlock + snapshot integrity (self-contained snapshots)",
      "depends_on": [
        "E6-A3"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/client/api.ts",
        "test/api.applications-lock.test.ts"
      ],
      "origin": "spec §27 (lock final + integrity invariant) — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nAdd the immutable 'final' snapshot and prove snapshot integrity.\n1) src/server/routes/applications.ts — POST /api/applications/:id/lock: copy the current `current` snapshot into `locked` (full content copy — snapshots are self-contained, NEVER entry-ID refs). Lock with no `current` → 400. DELETE /api/applications/:id/lock: clear `locked` → { ok } / updated Application.\n2) src/client/api.ts — lockApplication(id) / unlockApplication(id) helpers.\n3) test/api.applications-lock.test.ts (new) — the §27 integrity chain.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- lock freezes current→locked; re-tailor afterwards overwrites `current` but `locked` is UNCHANGED (immutable).\n- INTEGRITY: after tailor+lock, DELETE (and separately, edit) a Library entry that fed the snapshot → GET :id shows `current` and `locked` byte-identical (full content preserved, no broken refs).\n- DELETE :id/lock clears `locked`; lock with no current → 400.\nRED-TEAM SHARPENED [v3-001]: (#5) the integrity target entry MUST be present as an item in `current` before mutating; the EDIT must change a RENDERED fact/text field (so a ref-resolving impl would visibly diverge); compare `current`/`locked` via a FRESH GET :id after mutation and after re-tailor (re-read from DB, not the pre-lock in-memory object) — proves `locked` is a deep copy, not a shared reference.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @7983d88: scope CLEAN (3 files ⊆ declared). check 0; lint clean; vitest 258/258 (isolated, no flake); playwright chromium 8/8. Gaming read CLEAN: POST :id/lock deep-copies via structuredClone(current)→locked (self-contained, no refs), 404 missing / 400 no_current; DELETE :id/lock clears. Red-team #5 GENUINE: uses a rephrase:none skill entry (rendered text===facts, maximally exposes ref bugs); lock→re-tailor overwrites current but locked stays toEqual(originalCurrent) via fresh GET; edit/delete Library entry → locked/current unchanged. Builder followed no-e2e protocol cleanly (no stall).",
      "status": "done"
    },
    {
      "id": "E6-A5",
      "title": "Full-instance export/import (library + profile + applications)",
      "depends_on": [
        "E6-A2"
      ],
      "files": [
        "src/server/routes/backup.ts",
        "src/server/index.ts",
        "src/client/api.ts",
        "src/client/components/LibraryToolbar.tsx",
        "test/api.export-import.test.ts",
        "test/library-toolbar.test.tsx"
      ],
      "origin": "spec §9 + §27 backup (export/import span all three) — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nCURRENT STATE: no /api/export exists; import is entries-only (POST /api/entries/import). LibraryToolbar Import/Export buttons are disabled stubs.\n1) src/server/routes/backup.ts (new plugin `backupRoutes`): GET /api/export → { entries, profile, applications } (full backup incl current/locked snapshots). POST /api/import → { entries?, profile?, applications? } restores all three (zod-validated), returns { imported }. Reuse existing entry import semantics (upsert by id) and extend to profile (singleton upsert) + applications.\n2) Register backupRoutes in src/server/index.ts.\n3) src/client/api.ts — exportAll() / importAll(payload) helpers; wire the disabled Import/Export buttons in src/client/components/LibraryToolbar.tsx to them.\n4) test/api.export-import.test.ts (new).",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- GET /api/export returns { entries, profile, applications } (all three, snapshots included).\n- POST /api/import restores all three (zod-validated; rejects malformed payload).\n- Round-trip: export → wipe DB → import reproduces entries + profile + applications with `current`/`locked` intact.\n- LibraryToolbar Import/Export buttons are functional (no longer disabled stubs).\nRED-TEAM SHARPENED [v3-001]: (#3) the exported application MUST carry non-null `current` AND `locked` (real TailoredResumes); after a genuine wipe assert GET /api/export returns empty entries+applications (+default profile) BEFORE import, then import, then DEEP-EQUAL current/locked to the original; malformed import (application missing jobDescription) → 4xx not 500. (#12) add a jsdom test (test/library-toolbar.test.tsx) asserting the Import/Export buttons are NOT disabled and their onClick invokes importAll/exportAll.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @2cf5f16: scope CLEAN (6 files ⊆ declared). check 0; lint clean; vitest 253/253; playwright chromium 8/8. Gaming read CLEAN: GET /api/export returns {entries,profile,applications} incl snapshots; POST /api/import zod is REAL (applicationImport=applicationCreate.extend w/ tailoredResumeZ, not z.any()) so jobDescription required. Red-team GENUINE: #3 round-trip non-vacuous (non-null current+locked, fresh-dir wipe asserts export empty BEFORE import via toEqual([]), deep-equal restore, malformed missing-jobDescription→≥400 not 500), #12 LibraryToolbar buttons not disabled + onClick→export/importAll. NOTE: entry-upsert logic duplicated from entries.ts + tailoredResumeZ hand-written (entries.ts/shared schema out of scope) — minor DRY cost, acceptable.",
      "status": "done"
    },
    {
      "id": "E6-B1",
      "title": "ApplicationsView + ApplicationDetail (list/create/detail/tailor/lock/staleness/loop)",
      "depends_on": [
        "E6-A4"
      ],
      "files": [
        "src/client/components/ApplicationsView.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/components/ApplicationCard.tsx",
        "src/client/components/NewApplication.tsx",
        "src/client/components/JobPanel.tsx",
        "src/client/components/ResultView.tsx",
        "test/applications-ui.test.tsx",
        "src/client/queries/useApplications.ts"
      ],
      "origin": "spec §13 component tree (ApplicationsView/ApplicationDetail) + §26/§27 — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nNET-NEW UI (no ApplicationsView exists). Reuse existing ResumePage.tsx + ReasoningPanel.tsx (built) for the result render. Compose shadcn primitives — never re-roll button/dialog (§13 rule). Use the useApplications hooks (E6-A2) + tailor/lock helpers.\nComponent tree (§13):\n- ApplicationsView (route body): ApplicationsList of ApplicationCard (company/role, JD preview, genState badge, updatedAt) + NewApplication (create act — JD required, optional company/role/context).\n- ApplicationDetail: JobPanel (edit JD/context/company/role; Tailor / Re-tailor / Lock final buttons) · TailorProgress (§15 honest progress) · ResultView(split) showing the app's `current` (or `locked`): ResumePage | ReasoningPanel. Empty state: 'add missing facts in Library →' linking to /library (the Applications↔Library loop, §26).\n- Staleness: surface currentMeta ('tailored from your Library as of <date> — re-tailor to fold in newer entries', §27) — render when currentMeta present.\nNOTE: routing/nav wiring is E6-B2; here just build the components (importable). test/applications-ui.test.tsx: jsdom component tests.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- ApplicationsList renders cards from data incl genState + updatedAt; NewApplication create calls createApplication.\n- ApplicationDetail renders the `current` snapshot via ResumePage + ReasoningPanel; Lock button calls lockApplication.\n- Staleness note renders when currentMeta present; empty state links to /library (the loop).\n- No hiring-status UI (only genState) — not-a-tracker guard.\nRED-TEAM SHARPENED [v3-001]: (#10) data-driven contrast — feed ≥2 apps with distinct company/genState → each rendered distinctly; feed an app whose `current` contains a SENTINEL string → assert it appears in ApplicationDetail (not an empty ResumePage); staleness note ABSENT when currentMeta is null (contrast the present case).",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @bf491a7: scope CLEAN (7 files touched ⊆ 8 declared; ResultView reused unchanged). check 0; lint clean; build ok; vitest 264/264 incl applications-ui.test (6). Gaming read CLEAN: ApplicationCard uses GEN_STATE_LABEL/VARIANT Record maps (data-driven, not hardcoded); no hiring-status UI (a test guard asserts HTML lacks Applied/Interviewing/Rejected). Red-team #10 GENUINE: 2 apps distinct company+genState via within(cardA/cardB) bidirectional distinctness; SENTINEL string in current.summary asserted rendered; staleness note present-when-currentMeta / absent-when-null. Empty state Links to /library (the loop). Builder followed no-e2e protocol cleanly.",
      "status": "done"
    },
    {
      "id": "E6-B2",
      "title": "Top-bar nav + routes + IA gate; remove stateless TailorView",
      "depends_on": [
        "E6-B1"
      ],
      "files": [
        "src/client/main.tsx",
        "src/client/components/NavTabs.tsx",
        "src/client/App.tsx",
        "src/client/components/TailorView.tsx",
        "src/server/index.ts",
        "src/client/api.ts",
        "test/ia.test.tsx",
        "test/tailor.e2e.test.ts",
        "test/api.tailor-db.test.ts",
        "test/api.tailor-byok.test.ts"
      ],
      "origin": "spec §26 Information Architecture — v3; intake decision: top-bar tabs, Applications default",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nIntake decision [v3-001]: top-bar tabs; Profile/Layout stay in Library (already so — do NOT move them). Realize §26.\nCURRENT STATE: main.tsx routes / → /tailor, /tailor→TailorView, /library, /settings; NavTabs TABS = [/tailor,/library,/settings].\n1) src/client/main.tsx — routes: / → redirect to /applications; /applications → ApplicationsView; /applications/:id → ApplicationDetail; /library; /settings; a catch-all (unknown non-/api path) that resolves to a KNOWN destination (redirect to /applications), NEVER a blank/404 shell. Remove the /tailor route.\n2) src/client/components/NavTabs.tsx — TABS = Applications | Library | Settings (in that order), top-bar; active reflects the current route.\n3) src/client/App.tsx — keep LoginGate wrapping all destinations; adjust the shell/Outlet for the new routes (the current file special-cases /settings — keep behavior consistent).\n4) DELETE src/client/components/TailorView.tsx (stateless flow replaced by Applications). If JDInput.tsx becomes unused, note it (leave removal to avoid undeclared-file churn unless it's in your declared set — it is not, so leave it and note).\n5) test/ia.test.tsx (new) — the §26 structural invariants.\n[v3-006 UPDATE] This ticket ALSO performs the ATOMIC stateless-tailor teardown (moved here from E6-A3 so no intermediate build breaks): remove the inline stateless POST /api/tailor route from src/server/index.ts; remove the tailor() helper from src/client/api.ts; delete TailorView.tsx and its /tailor route; and delete the 3 stateless tailor tests (test/tailor.e2e.test.ts, test/api.tailor-db.test.ts, test/api.tailor-byok.test.ts) whose coverage now lives in E6-A3 test/api.applications-tailor.test.ts on the :id/tailor route. After teardown grep-confirm no reference to \"/api/tailor\" or the tailor() helper remains.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- NavTabs lists Applications | Library | Settings in order; clicking each navigates + active state reflects route; every destination reachable from nav.\n- / redirects to /applications; an unknown non-/api path resolves to a known destination (app-shell renders — not blank/404).\n- No /tailor route; TailorView.tsx removed; no orphan routes.\nRED-TEAM SHARPENED [v3-001]: (#2 HIGH) an unknown path (/tailor, /nonsense) must REDIRECT to /applications (assert location.pathname==='/applications') and render the SAME content as visiting /applications directly (a known-destination sentinel from ApplicationsView) — NOT a bespoke fallback/404 component. Assert exactly ONE NavTabs item is active and it matches the resolved route (contrast: on /library, Library active AND Applications not active).\n[v3-006] Stateless teardown complete: no /api/tailor route, no api.ts tailor() helper, no TailorView / /tailor route, no dangling refs (grep-clean); the 3 old stateless tailor tests removed (coverage in api.applications-tailor.test.ts).",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @d02e61f: check 0; lint clean; build ok; vitest 250/250; playwright chromium 8/8 (routing cutover did NOT regress auth/library-crud e2e). Grep-clean CONFIRMED: no stateless /api/tailor route, no api.ts tailor() helper, no TailorView. Routing: index→Navigate /applications, catch-all *→Navigate /applications (redirect, not blank/404); NavTabs Applications|Library|Settings. Red-team #2 GENUINE (ia.test): unknown path resolves pathname===/applications + renders real ApplicationsView sentinel (not fallback) + exactly one active tab matching route (contrast /library). SCOPE: declared + 2 JUSTIFIED cascading out-of-scope edits — deleted test/server.test.ts (its POST /api/tailor block + redundant health, still covered by boot.smoke/api.auth/spa-serving) and trimmed a stale /tailor-link block from test/library.test.tsx; both necessary to keep vitest green after the route removal, no concurrent ticket owns them (serial). ACCEPTED.",
      "status": "done"
    },
    {
      "id": "E6-B3",
      "title": "Applications browser e2e — full lifecycle (keyless fixture JD)",
      "depends_on": [
        "E6-B2"
      ],
      "files": [
        "test/e2e/applications.spec.ts"
      ],
      "origin": "oracle Phase 5 browser gate (§26/§27, extends [v2-036]) — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nNET-NEW Playwright spec. Follow the existing test/e2e/library-crud.spec.ts + helpers/{session,tmpdata}.ts patterns; webServer boots `bun run start` under Node/tsx with FixtureEngine (keyless). CRITICAL: create the Application with a JD that is a RECORDED fixture (one of the 3 CONTRAST_JDS whose decisions live in test/fixtures/decisions/) so FixtureEngine.decide (keys on hashKey(jd,entries)) replays — an arbitrary JD would throw no_fixture. Read the fixture JD text from the existing keyless tailor tests / fixtures.\nDrive: first-run set-password → login → create Application (fixture JD) → Tailor → `current` renders (assert ResumePage root mounts non-empty AND ReasoningPanel present) → reload page → `current` still shown (persistence) → Re-tailor → Lock final → `locked` renders. Assert NO uncaught console/page errors (narrow allowlist only for known-expected, documented).",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps). (this spec runs inside the playwright half of the composite)\n- The lifecycle spec passes: create→tailor→render→reload-persist→re-tailor→lock→locked-render, no uncaught console errors.\n- Uses a recorded fixture JD (keyless); does not require any API key.\nRED-TEAM SHARPENED [v3-001]: (#8) after tailor assert a SPECIFIC expected token from the recorded fixture's assembled resume (an item text / summary fragment) is in the DOM — not just non-empty; persistence uses a full page.reload() then re-asserts that token WITHOUT any re-tailor (genState still 'tailored'); the console-error allowlist entries are EXACT-MATCH strings, documented, and never include pageerror.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @a956245: scope was applications.spec.ts + playwright.config.ts (justified additive project, same as E5 pattern; projects isolated by testMatch, no double-run). Authoritative FULL composite `bun run test` GREEN end-to-end: vitest 253/253; playwright non-docker 10/10 (chromium 8 + auth 1 + applications 1); docker 1/1. Gaming read CLEAN: lifecycle asserts specific RESUME_TOKEN, tailorRequests count for persistence (reload=1, re-tailor=2), lock→Unlock; console allowlist exact-match, !loggedIn-scoped, pageerror never swallowed (red-team #8). NOTE: closing this ticket required a coordinator REPAIR (commit a956245) — see [v3-008].",
      "status": "done"
    },
    {
      "id": "E6-C1",
      "title": "Progressive LibraryFilter (section · tag · free-text)",
      "depends_on": [],
      "files": [
        "src/client/components/LibraryFilter.tsx",
        "src/client/components/LibraryView.tsx",
        "test/library-filter.test.tsx"
      ],
      "origin": "spec §26 Library-as-bank / findability — v3",
      "context": "Locked (cite oracle.md): provider-agnostic Vercel AI SDK · Drizzle + better-sqlite3 (migrations on boot) · Fastify · React18/Vite · zod. Server runs under Node/tsx [v2-017], Bun is pkg-mgr+test runner. Applications are tailoring records NOT a job tracker — only genState, no hiring status (§2/§27). Context guides emphasis, NEVER a fact source; validateNoFabrication stays entries-only (§6.2).\nCURRENT STATE: LibraryView groups entries by section only (groupBySection) — no filter/search UI. File-disjoint from the backend + nav tickets, so it can build in parallel.\nAdd src/client/components/LibraryFilter.tsx: filter by section, by tag, and by free-text over facts/meta. PROGRESSIVE (§26): the filter UI stays minimal/near-invisible on a small or empty library and earns its space as the corpus grows (e.g. only show the tag/text controls past a small threshold). Tags are used for GROUPING/FILTERING ONLY — NEVER selection scoring (core anti-pattern §1: no code intersects/sorts entry.tags against anything). Wire it into LibraryView (client-side filtering of the already-loaded entries; no new API).\ntest/library-filter.test.tsx: jsdom component tests.",
      "acceptance": "Baseline (every ticket): `bun run check` exit 0; `bun run build` exit 0; full suite `bun run test` green (vitest excl test/e2e/** THEN playwright chromium); new behavior ships new tests. Touch ONLY declared files (+ package.json/bun.lock for deps).\n- Filter by section narrows the list; by tag narrows; free-text narrows (matches facts/meta).\n- Small/empty library → filter UI minimal (progressive) — asserted.\n- Grep guard: no tag-scoring (no entry.tags intersect/sort/score against JD/signals) in the client.\nRED-TEAM SHARPENED [v3-001]: (#9) define the threshold N — library size < N → tag/text controls ABSENT from DOM; ≥ N → PRESENT (assert both sides). Each filter is BIDIRECTIONAL: a non-matching entry disappears AND a matching entry remains, for section, tag, and free-text; free-text must match a term present only in a FACT (content), confirming it searches facts/meta, not tags.",
      "attempts": [],
      "evidence": "INDEP RE-VERIFY on merged master @1f51f5e: scope CLEAN (3 files ⊆ declared). check 0; lint clean; vitest library-filter 4/4; playwright chromium 8/8 (library-crud regression intact). Gaming read: filterEntries does section exact-match, tag exact includes() (sanctioned §1 filtering, NOT scoring), free-text over facts+string-meta and deliberately NOT tags; SEARCH_THRESHOLD=8 real progressive gate. Tests genuine: threshold both-sides asserted; section/tag/free-text bidirectional; fact-vs-tag proof (kubernetes in a FACT matches, in a TAG does not) [red-team #9]. No tag-scoring anywhere in client.",
      "status": "done"
    },
    {
      "id": "E7-A1",
      "title": "react-pdf document model + template registry + shared section renderers + 1 strict template + profile header",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/client/document/registry.ts",
        "src/client/document/sections.tsx",
        "src/client/document/templates/strict.tsx",
        "src/client/document/renderResume.ts",
        "src/client/document/index.ts",
        "test/document-render.test.ts"
      ],
      "origin": "spec §28.0/§28.2/§28.8-A",
      "context": "Build the react-pdf document foundation that replaces the DOM resume render. Create src/client/document/: (1) a TEMPLATE REGISTRY (same pattern as §4.3's section registry) — each template = {id, name, description, layout:'single'|'sidebar-left'|'sidebar-right', atsGrade:'strict'|'good', densityLadder}. (2) SHARED SECTION RENDERERS as react-pdf primitives (Document/Page/View/Text): ProfileHeader (name + contact line email/phone/location + links, from Profile §16), SummarySection, SectionBlock (registry label + groups) → GroupBlock (optional heading) → ItemRow (item.text). ALL templates render every section through these shared renderers — templates differ in COMPOSITION only, never features (rx-resume's rule). (3) ONE 'strict' single-column template composing them. (4) renderResume.ts: a pure `renderResumeDocument({resume, profile, paper='letter', templateId})` returning the react-pdf Document element, plus a `renderResumeToBuffer(...)` helper (renderToBuffer) for tests/download. CRITICAL: leadRationale and cut[] are reasoning UI (§11) — NEVER render them on the document. Render only summary + sections/groups/items in given order. Locked (cite oracle.md §28): PDFs are produced ONLY by @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not an interim or fallback (§28.0, rejected 2026-07-05). Templates are CODE (react-pdf components), never HTML/CSS or user-authored. The renderer NEVER cuts (§28.4) — item count is invariant across densities. leadRationale + cut[] are reasoning UI ONLY and must NEVER appear on the document (§11) — this is gated by pdf.js text extraction. Fabrication validation (§6.3) is untouched (server-side, upstream of rendering). Data model (src/shared/types.ts): Profile = {name, headline?, email, phone?, location?, links:{type,label,url}[], baseSummary?}. TailoredResume = {signals, summary, sections:TailoredSection[], cut[]}; TailoredSection={section,groups}; TailoredGroup={heading?,leadRationale?,items}; TailoredItem={entryId,text}. Section labels via SECTIONS registry (@shared/sections). Client fetch: getProfile()/getSettings() in src/client/api.ts (GET /api/profile, /api/settings). Extraction oracle harness (proven runnable at intake — see ledger [v3-012]): `renderToBuffer(doc)` from @react-pdf/renderer produces a real PDF headless; extract text via `import('pdfjs-dist/legacy/build/pdf.mjs')` → getDocument({data:Uint8Array}).promise → getPage(1).getTextContent() → items.map(i=>i.str). A benign `standardFontDataUrl` warning is expected and does not affect extraction. Model note: this ticket may launch with react-pdf's built-in Helvetica (the OFL font registry is §28-B's job) — do not add fonts here.",
      "acceptance": "- bun run check + bun run lint clean; vitest + playwright-chromium baseline green (no regressions)\n- NEW test/document-render.test.ts: given a fixture TailoredResume (with SENTINEL_RATIONALE_* leadRationale + a cut[] entry with a SENTINEL reason) + a fixture Profile → renderResumeToBuffer → pdf.js extraction asserts: (a) profile.name present; (b) EVERY item.text appears in EXACTLY TailoredResume content order (index-increasing, contrast check — not mere presence); (c) the SENTINEL_RATIONALE_* and cut-reason strings are ABSENT from the extracted text; (d) the summary text present.\n- Registry exports the strict template with atsGrade:'strict', layout:'single'.",
      "attempts": [],
      "evidence": "Independently verified + merged (commits 9ff015b + merge 757ce0a on master). Re-verified on the MERGED working tree by the coordinator: bun run check exit 0; bun run lint clean (139 files); test/document-render.test.ts 2/2 green — renderResumeToBuffer → pdf.js legacy extraction confirms profile.name + summary + every item.text in exact content order (index-increasing), all four SENTINEL_RATIONALE_*/SENTINEL_CUT_* strings ABSENT. Registry exports strict template (atsGrade 'strict', layout 'single'). Files: src/client/document/{registry.ts,sections.tsx,renderResume.ts,index.ts,templates/strict.tsx}, test/document-render.test.ts; deps @react-pdf/renderer + pdfjs-dist. Verify stage: no undeclared files, no gaming."
    },
    {
      "id": "E7-A2",
      "title": "Second launch template — sidebar layout (reuses shared section renderers)",
      "status": "done",
      "depends_on": [
        "E7-A1"
      ],
      "files": [
        "src/client/document/templates/sidebar.tsx",
        "src/client/document/registry.ts",
        "test/document-templates.test.ts"
      ],
      "origin": "spec §28.2/§28.8-A (≥2 launch templates: ≥1 strict + ≥1 sidebar)",
      "context": "Add a second launch template with a 'sidebar-left' layout, composing the SAME shared section renderers from E7-A1's src/client/document/sections.tsx (differ in composition — a left sidebar column for e.g. skills/contact, main column for narrative — NEVER in features). Register it in registry.ts with atsGrade:'good' (two-column; modern parsers handle it but Workday/Taleo read strictly left-to-right — that caveat belongs in the picker later, not here). Locked (cite oracle.md §28): PDFs are produced ONLY by @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not an interim or fallback (§28.0, rejected 2026-07-05). Templates are CODE (react-pdf components), never HTML/CSS or user-authored. The renderer NEVER cuts (§28.4) — item count is invariant across densities. leadRationale + cut[] are reasoning UI ONLY and must NEVER appear on the document (§11) — this is gated by pdf.js text extraction. Fabrication validation (§6.3) is untouched (server-side, upstream of rendering). Extraction oracle harness (proven runnable at intake — see ledger [v3-012]): `renderToBuffer(doc)` from @react-pdf/renderer produces a real PDF headless; extract text via `import('pdfjs-dist/legacy/build/pdf.mjs')` → getDocument({data:Uint8Array}).promise → getPage(1).getTextContent() → items.map(i=>i.str). A benign `standardFontDataUrl` warning is expected and does not affect extraction. Note: registry.ts is shared with E7-A1 but this ticket runs AFTER A1 (dependency), so no parallel conflict.",
      "acceptance": "- baseline green (check/lint/vitest/playwright-chromium)\n- NEW test/document-templates.test.ts: the sidebar template renders the E7-A1 fixture to a valid PDF containing profile.name + every item.text; registry now exposes ≥2 templates incl ≥1 strict and ≥1 sidebar-left.\n- Both templates render the SAME fixture (same content, different composition) — no feature lives in one template but not the other.",
      "attempts": [],
      "evidence": "Verified + merged (e58a32c + merge c822861). Coordinator re-verified on MERGED tree: check 0, lint clean (141 files), test/document-templates.test.ts + document-render.test.ts 5/5 — sidebar-left renders the A1 fixture to a valid PDF with profile.name + every item.text, no leadRationale/cut sentinels; registry exposes strict(single/strict) + sidebar-left(sidebar-left/good); both templates render the SAME fixture (features equal, composition differs)."
    },
    {
      "id": "E7-A3",
      "title": "Page model — settings.paper (global) + application.targetPages (per-app)",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/shared/types.ts",
        "src/shared/schema.ts",
        "src/server/db/schema.ts",
        "drizzle/",
        "src/server/routes/settings.ts",
        "src/server/routes/applications.ts",
        "src/client/api.ts",
        "test/page-model.test.ts",
        "test/db.test.ts",
        "test/api.profile-settings.test.ts"
      ],
      "origin": "spec §28.1/§28.8-A",
      "context": "Add the §28.1 page model as data, wired end to end. (1) settings.paper: 'letter'|'a4' — a GLOBAL setting on settings, default 'letter' (affects page size + budget math only). Extend settingsInput (src/shared/schema.ts), the Settings response, and persistence (src/server/routes/settings.ts + its store). (2) application.targetPages: 1|2 — PER APPLICATION (lives on the app because it depends on the role), default 1. Extend the Application type, applicationCreate/applicationUpdate schema, and persistence (src/server/routes/applications.ts + store/migration). Expose both through src/client/api.ts. INVARIANT (spec §28.1, §28.3): formatting NEVER mutates snapshots — changing paper or targetPages must NOT alter a stored application.current / locked TailoredResume by even a byte. Locked (cite oracle.md §28): PDFs are produced ONLY by @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not an interim or fallback (§28.0, rejected 2026-07-05). Templates are CODE (react-pdf components), never HTML/CSS or user-authored. The renderer NEVER cuts (§28.4) — item count is invariant across densities. leadRationale + cut[] are reasoning UI ONLY and must NEVER appear on the document (§11) — this is gated by pdf.js text extraction. Fabrication validation (§6.3) is untouched (server-side, upstream of rendering). Data model (src/shared/types.ts): Profile = {name, headline?, email, phone?, location?, links:{type,label,url}[], baseSummary?}. TailoredResume = {signals, summary, sections:TailoredSection[], cut[]}; TailoredSection={section,groups}; TailoredGroup={heading?,leadRationale?,items}; TailoredItem={entryId,text}. Section labels via SECTIONS registry (@shared/sections). Client fetch: getProfile()/getSettings() in src/client/api.ts (GET /api/profile, /api/settings).",
      "acceptance": "- baseline green (check/lint/vitest/playwright-chromium); new fields persist across the existing SQLite migration path\n- NEW test/page-model.test.ts: PUT /api/settings {paper:'a4'} persists + round-trips via GET; a created application defaults targetPages=1, and update to 2 persists; CONTRAST/invariant: capture application.current before and after changing paper AND targetPages → the TailoredResume snapshot is byte-identical (JSON.stringify equal).\n- Defaults correct: paper 'letter', targetPages 1.",
      "attempts": [
        {
          "n": 1,
          "failed": "blocked (not built) — ticket self-contradictory",
          "hypothesis": "Coordinator bug, not a builder failure: DECLARED FILES omitted the persistence layer. Acceptance requires paper/targetPages to PERSIST via the SQLite migration path, but src/server/db/schema.ts + drizzle/* were not declared. Worker correctly refused to half-build (E6-A1 precedent 915e028: a persisted field always touches db/schema.ts + drizzle/*).",
          "fixNote": "Re-cut with src/server/db/schema.ts + drizzle/ added to declared files. The generated drizzle migration (drizzle/0002_*.sql + drizzle/meta/0002_snapshot.json + drizzle/meta/_journal.json) is EXPECTED and in-scope — run `bunx drizzle-kit generate` after editing db/schema.ts. Add columns: settings.paper (text, default 'letter'), applications.targetPages (integer, default 1)."
        },
        {
          "n": 2,
          "failed": "blocked (built full change, then reverted) — two undeclared guard tests break on the new columns",
          "hypothesis": "Coordinator under-declared files AGAIN. Pre-existing exhaustive-allowlist guard tests trip on any new column: test/db.test.ts:182 toEqual([exact applications columns]) breaks on target_pages; test/api.profile-settings.test.ts:117 allowed-keys Set breaks on paper. Both real invariants (db.test.ts:195-196 not.toContain hiring_status/status; no key/ciphertext leak) are UNAFFECTED — only the literal allowlists are stale. Worker built everything correctly (drizzle 0002_amused_wrecking_crew.sql etc.) and reverted rather than touch undeclared tests.",
          "fixNote": "ATTEMPT 3 (final before cap). Added test/db.test.ts + test/api.profile-settings.test.ts to declared files. The worker must reapply the full page-model change AND make ONLY the additive one-line edit to each guard: add \"target_pages\" to the db.test.ts toEqual column array (in position — it is appended by the migration), and add \"paper\" to the profile-settings.test.ts allowed Set. Change NOTHING else in those tests — the not.toContain(status/hiring_status) denylist and the no-secret-leak assertions MUST remain."
        }
      ],
      "evidence": "Attempt 3 done + committed (a9c3798), single-agent dispatch. Coordinator INDEPENDENTLY re-verified on the tree: scope clean (12 files, all ⊆ declared ∪ drizzle/, no undeclared touch); GAMING READ — both guard edits strictly additive (+\"paper\" to the settings allowlist Set; +\"target_pages\" appended to db.test columns; db.test shows 1 insertion/0 deletions so not.toContain(hiring_status) denylist intact) → teeth preserved, no gaming; check 0; lint clean (142 files); page-model+db+profile-settings 15/15 incl the CONTRAST invariant (paper+targetPages change leaves current/locked byte-identical). Migration drizzle/0002_freezing_ink.sql. paper default letter, targetPages default 1."
    },
    {
      "id": "E7-A4",
      "title": "pdf.js preview (DocumentPreview) — replace DOM ResumePage in ResultView",
      "status": "done",
      "depends_on": [
        "E7-A1",
        "E7-A3"
      ],
      "files": [
        "src/client/components/DocumentPreview.tsx",
        "src/client/components/ResultView.tsx",
        "test/applications-ui.test.tsx",
        "test/reasoning-ui.test.tsx",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "spec §28.0/§28.8-A (the preview IS the artifact)",
      "context": "Build DocumentPreview: render the E7-A1 react-pdf document to a PDF and show THAT FILE as the preview (the preview IS the artifact — §28.0). Use react-pdf's browser rendering (BlobProvider / usePDF from @react-pdf/renderer) to produce the blob, displayed via a pdf.js canvas OR an object/iframe of the blob URL. Pull profile via getProfile() and paper via getSettings() (E7-A3). Then in ResultView.tsx, REPLACE the DOM <ResumePage> with <DocumentPreview>. CRITICAL §11 invariant (preserve it exactly): ReasoningPanel stays a SIBLING of the document preview, NEVER nested inside it — reasoning strings must never enter the rendered PDF. Do NOT delete ResumePage.tsx or print.css in this ticket (that is E7-A6). Locked (cite oracle.md §28): PDFs are produced ONLY by @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not an interim or fallback (§28.0, rejected 2026-07-05). Templates are CODE (react-pdf components), never HTML/CSS or user-authored. The renderer NEVER cuts (§28.4) — item count is invariant across densities. leadRationale + cut[] are reasoning UI ONLY and must NEVER appear on the document (§11) — this is gated by pdf.js text extraction. Fabrication validation (§6.3) is untouched (server-side, upstream of rendering). Data model (src/shared/types.ts): Profile = {name, headline?, email, phone?, location?, links:{type,label,url}[], baseSummary?}. TailoredResume = {signals, summary, sections:TailoredSection[], cut[]}; TailoredSection={section,groups}; TailoredGroup={heading?,leadRationale?,items}; TailoredItem={entryId,text}. Section labels via SECTIONS registry (@shared/sections). Client fetch: getProfile()/getSettings() in src/client/api.ts (GET /api/profile, /api/settings). Test note: pdf.js canvas rendering is hard under jsdom — assert the component mounts and renders DocumentPreview (not the old DOM .resume-page), and that ReasoningPanel remains a sibling; the real render is covered by the playwright applications e2e.",
      "acceptance": "- baseline green (check/lint/vitest/playwright-chromium — the applications e2e that renders a tailored resume must stay green)\n- test/applications-ui.test.tsx updated: ResultView renders DocumentPreview and NOT the legacy DOM .resume-page; ReasoningPanel is present as a sibling (not a descendant of the preview).\n- No leadRationale/cut strings reachable inside the preview subtree.",
      "attempts": [],
      "evidence": "Verify RED on scope only (2 undeclared cascading test files) — coordinator judged legitimate (my ticket under-declared tests asserting on ResultView DOM), amended files, merged (306716c + 8322d1e). Re-verified on MERGED tree: check 0, lint clean (145), applications-ui+reasoning-ui+download+document-render 19/19, playwright 10/10 non-docker INCLUDING applications.spec expectCanvasPainted (react-pdf->pdf.js preview paints in real chromium end to end). Sec 11 preserved: ReasoningPanel sibling of DocumentPreview, sentinels DOM-absent from preview."
    },
    {
      "id": "E7-A5",
      "title": "Download PDF button + file hygiene (slugged filename + PDF metadata)",
      "status": "done",
      "depends_on": [
        "E7-A1",
        "E7-A3"
      ],
      "files": [
        "src/client/document/download.ts",
        "src/client/components/ApplicationDetail.tsx",
        "test/download.test.ts"
      ],
      "origin": "spec §28.0/§28.8-A (Download PDF + file hygiene)",
      "context": "Add the Download PDF action. (1) src/client/document/download.ts: a pure `pdfFilename({name, company, role})` → '<Name> — <Company> — <Role>.pdf' SLUGGED (reuse the project's slug util if present — see src/server/slug.ts for the convention; keep client-side slugging consistent), plus a `downloadResumePdf(...)` that renders via E7-A1's renderResumeToBuffer with PDF Document title+author set from the profile, and triggers a browser save. (2) In ApplicationDetail.tsx header (alongside the existing Lock/Re-tailor buttons), add a 'Download PDF' Button, enabled only when application.current exists. Locked (cite oracle.md §28): PDFs are produced ONLY by @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not an interim or fallback (§28.0, rejected 2026-07-05). Templates are CODE (react-pdf components), never HTML/CSS or user-authored. The renderer NEVER cuts (§28.4) — item count is invariant across densities. leadRationale + cut[] are reasoning UI ONLY and must NEVER appear on the document (§11) — this is gated by pdf.js text extraction. Fabrication validation (§6.3) is untouched (server-side, upstream of rendering). Data model (src/shared/types.ts): Profile = {name, headline?, email, phone?, location?, links:{type,label,url}[], baseSummary?}. TailoredResume = {signals, summary, sections:TailoredSection[], cut[]}; TailoredSection={section,groups}; TailoredGroup={heading?,leadRationale?,items}; TailoredItem={entryId,text}. Section labels via SECTIONS registry (@shared/sections). Client fetch: getProfile()/getSettings() in src/client/api.ts (GET /api/profile, /api/settings). Extraction oracle harness (proven runnable at intake — see ledger [v3-012]): `renderToBuffer(doc)` from @react-pdf/renderer produces a real PDF headless; extract text via `import('pdfjs-dist/legacy/build/pdf.mjs')` → getDocument({data:Uint8Array}).promise → getPage(1).getTextContent() → items.map(i=>i.str). A benign `standardFontDataUrl` warning is expected and does not affect extraction. Note: react-pdf Document accepts title/author props → they land in PDF metadata (verify via pdfjs getMetadata()).",
      "acceptance": "- baseline green (check/lint/vitest/playwright-chromium)\n- NEW test/download.test.ts: pdfFilename unit table — e.g. {name:'Ada Lovelace',company:'Acme Corp',role:'Staff Engineer'} → a deterministic slugged '.pdf' name (assert exact string); contrast a second distinct input yields a different name. AND: render the fixture with title/author from a fixture profile → pdfjs getMetadata() returns matching Title + Author.\n- Download button disabled when application.current is null.",
      "attempts": [],
      "evidence": "Verified by workflow + coordinator re-verify: commit 9467bb4 (declared files only), download.test 5/5 (exact slug filename + distinct-input contrast + illegal-char strip + omitted segments; renderResumeToBuffer -> pdfjs getMetadata Title+Author = profile.name); Download button disabled when current null. check 0, lint clean."
    },
    {
      "id": "E7-A6",
      "title": "Retire the print pipeline — delete print.css + DOM ResumePage; no window.print path",
      "status": "done",
      "depends_on": [
        "E7-A4",
        "E7-A5"
      ],
      "files": [
        "src/client/styles/print.css",
        "src/client/components/ResumePage.tsx",
        "test/resumepage.test.tsx",
        "test/render-polish.test.ts",
        "test/remaining-sections.test.tsx",
        "src/client/styles/app.css",
        "src/client/components/DocumentPreview.tsx",
        "src/client/components/ReasoningPanel.tsx",
        "test/reasoning-ui.test.tsx"
      ],
      "origin": "spec §28.8-A (print.css retires) + user directive 2026-07-05 (browser print is NOT a PDF path)",
      "context": "Retire the legacy browser-print pipeline entirely — now that E7-A4 (DocumentPreview) + E7-A5 (Download PDF) carry the load. IMPORTANT FINDING: print.css is ORPHANED — nothing imports it (main.tsx imports only app.css; no <link> in index.html; no @import), so there is NO load-site to remove; it and the window.print references exist only as a dead file + stale comments. DO (all in one worktree, in this order): (1) MIGRATE COVERAGE FIRST — test/remaining-sections.test.tsx currently imports and renders src/client/components/ResumePage (the DOM component being deleted) to prove every section type renders. PORT it to the react-pdf document: render via renderResumeToBuffer (from src/client/document/renderResume.ts) + extract text via pdf.js (import('pdfjs-dist/legacy/build/pdf.mjs') -> getDocument({data:Uint8Array}).promise -> getPage(1).getTextContent() -> items.map(i=>i.str)), asserting the SAME invariants against the extracted text instead of the DOM: each remaining section type (award/certification/publication/interest/language/reference/etc.) renders under its registry label (SECTIONS from @shared/sections); meta-fallback renders for empty facts (§4.3); facts render verbatim when present (rephrase:'none'); rephrase:'full'/'light' render the model's text. Use assemble() exactly as today to build the TailoredResume. This coverage must NOT be lost — it moves from DOM to the real PDF. (2) DELETE: src/client/styles/print.css, src/client/components/ResumePage.tsx, test/resumepage.test.tsx, and test/render-polish.test.ts (the latter only tests print.css's @media print ATS-safety — obsolete now that browser-print is rejected; its ATS-safety intent is now carried by the pdf.js extraction-order invariant in test/document-render.test.ts, §28.6). (3) CLEAN STALE REFERENCES: src/client/styles/app.css — remove the dead `.resume-page` style rules and any print-stylesheet comments; src/client/components/DocumentPreview.tsx + src/client/components/ReasoningPanel.tsx — update the comments that still mention window.print()/print.css/ResumePage so no window.print or print.css token remains anywhere in src/client. Locked (oracle.md §28): PDFs come ONLY from @react-pdf/renderer; browser printing (window.print()/print.css) is REJECTED — not an interim, not a fallback (§28.0, 2026-07-05, user directive). leadRationale + cut[] never on the document (§11). Do NOT re-litigate.",
      "acceptance": "- `grep -rniE 'window\\.print|print\\.css' src/client` -> ZERO matches (including comments)\n- src/client/styles/print.css and src/client/components/ResumePage.tsx no longer exist; test/resumepage.test.tsx and test/render-polish.test.ts deleted; NO dangling import breaks the build\n- test/remaining-sections.test.tsx now renders via renderResumeToBuffer + pdf.js extraction (NOT ResumePage) and still asserts: every remaining section type under its registry label, meta-fallback for empty facts, verbatim facts for rephrase:'none', model text for rephrase:'full'/'light' — coverage preserved, just moved to the real PDF\n- baseline green: bun run check exit 0, bun run lint clean, bunx vitest run green (env timeouts confirmed in isolation excepted), bunx playwright test (non-docker) green; test/document-render.test.ts (A1 extraction oracle) still green — proving the react-pdf path carries the load",
      "attempts": [],
      "evidence": "Done + committed (e3fe709). Coordinator re-verified: scope 9 files = 8 declared ∪ test/reasoning-ui.test.tsx (flagged legit cascade — that file read print.css off disk in an obsolete describe block; removed ONLY that block, §11 CONTRAST test intact). Gaming read: remaining-sections migrated to renderResumeToBuffer + pdf.js extraction, ALL invariants preserved (each section-type label via SECTIONS[].label.toUpperCase() — documented react-pdf uppercase adaptation; meta-fallback empty-fact cert+reference; verbatim facts + not.toContain negative for rephrase:none; full/light model text). grep -rniE window.print|print.css src/client → ZERO. Deleted print.css (orphaned), ResumePage.tsx, resumepage.test.tsx, render-polish.test.ts (obsolete print-ATS test; intent relocated to document-render extraction §28.6). PHASE-CLOSE GATE on merged tree: check 0, lint clean (141), full vitest 35/35 files 250/250 single run, playwright 10/10 non-docker incl applications.spec canvas-paint."
    },
    {
      "id": "E7-B1",
      "title": "[COARSE — decompose at pickup] Design panel (§28.3): typography/colors/page/photo/section-columns + per-app format + lockedFormat",
      "status": "decomposed",
      "depends_on": [
        "E7-A6"
      ],
      "files": [],
      "origin": "spec §28.3/§28.8-B",
      "context": "COARSE PLACEHOLDER — decompose into cold-start-runnable child tickets at pickup (files:[] intentionally; a worker must return {tooBig,proposedTickets} or the coordinator decomposes first). Scope: the §28.3 DocumentFormat (templateId; typography body 9-12pt + heading weight; colors primary+text; page marginX/Y/sectionGap; photo hidden-by-default + size/shape; sections per-section columns) persisted PER APPLICATION as `format` with `settings.defaultFormat`; a curated ~12-face OFL self-hosted FONT REGISTRY via Font.register (Arimo/Tinos/Carlito metric stand-ins + IBM Plex); `lockedFormat` freezing resolved format+density+paper on lock; ATS badge downgrades with photo/sidebar. Locked (cite oracle.md §28): PDFs are produced ONLY by @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not an interim or fallback (§28.0, rejected 2026-07-05). Templates are CODE (react-pdf components), never HTML/CSS or user-authored. The renderer NEVER cuts (§28.4) — item count is invariant across densities. leadRationale + cut[] are reasoning UI ONLY and must NEVER appear on the document (§11) — this is gated by pdf.js text extraction. Fabrication validation (§6.3) is untouched (server-side, upstream of rendering).",
      "acceptance": "DECOMPOSE FIRST. Spec §28.8-B oracle (for the child tickets): format round-trips; a LOCKED app renders byte-stable when live defaults change; photo hidden by default; ATS badge downgrades with photo/sidebar enabled.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E7-B1a",
      "title": "DocumentFormat foundation — shared type + zod + defaults + FontId + Profile.photoUrl",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/shared/types.ts",
        "src/shared/schema.ts",
        "src/shared/format.ts",
        "test/format.test.ts"
      ],
      "origin": "decomposed from E7-B1 (spec §28.3)",
      "context": "The shared foundation for the whole design layer — pure types/zod/defaults, NO DB, NO render. In src/shared/types.ts add DocumentFormat EXACTLY per spec §28.3: { templateId: string; typography: { body: { family: FontId; size: number; lineHeight: number }; heading: { family: FontId; weight: 400|500|600|700 } }; colors: { primary: string; text: string }; page: { marginX: number; marginY: number; sectionGap: number }; photo: { hidden: boolean; size: number; shape: 'circle'|'rounded'|'square' }; sections: Partial<Record<Section, { columns?: 1|2|3 }>> }. Define FontId as a string-literal union of the curated face ids that E7-B1b will register: 'ibm-plex-sans' | 'ibm-plex-serif' | 'ibm-plex-mono' | 'arimo' | 'tinos' | 'carlito' (metric stand-ins for Arial/Times/Calibri per §28.3). Add optional photoUrl?: string to Profile (§16 gains it; display settings stay in DocumentFormat). In src/shared/format.ts export DEFAULT_FORMAT (templateId 'strict'; body ibm-plex-sans size 10 lineHeight 1.4; heading ibm-plex-sans weight 600; colors primary '#1a1a2e'-ish accent + text near-black; page marginX 40 marginY 36 sectionGap 8 — matching the current strict template; photo hidden:true size 64 shape 'circle'; sections {}). In src/shared/schema.ts add documentFormatZ (hand-written zod, same pattern as TailorDecisionZ — NOT table-derived): body.size .min(9).max(12); lineHeight bounded ~1.0-1.8; heading.weight z.union of the 4 literals; colors hex strings (regex #rrggbb); page marginX/marginY bounded (e.g. 18-72pt), sectionGap 0-24; photo.shape enum; photo.size bounded; sections columns z.union(1,2,3) optional; templateId non-empty; family = z.enum of FontId. Locked (cite oracle.md §28): PDFs come ONLY from @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not interim/fallback (§28.0). Templates are CODE (react-pdf components). The renderer NEVER cuts (§28.4). leadRationale + cut[] are reasoning UI ONLY, NEVER on the document (§11, gated by pdf.js extraction). Fabrication validation (§6.3) untouched.",
      "acceptance": "- `bun run check` + `bun run test` pass\n- documentFormatZ REJECTS (each a distinct assert): body.size 8 and 13 (bounds 9-12); heading.weight 550; colors.primary '#zzz'/'red' (non-hex); sections columns 4; empty templateId\n- documentFormatZ ACCEPTS DEFAULT_FORMAT\n- DEFAULT_FORMAT.photo.hidden === true (photo hidden by default, §28.3)\n- DocumentFormat matches §28.3 field-for-field (typography/colors/page/photo/sections shape); FontId union has ≥4 faces incl the 3 metric stand-ins; Profile has optional photoUrl",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-05): scope CLEAN — only the 4 declared files touched (types.ts, schema.ts, format.ts, test/format.test.ts). check exit 0 (tsc both configs); lint clean (biome 143 files); build ok; bunx vitest run = 36 files/263 tests GREEN single run (no flakes). Acceptance: documentFormatZ is GENUINE bounded validation (z.enum FontId, size .min(9).max(12), lineHeight 1-1.8, hex regex /^#[0-9a-fA-F]{6}$/, weight literal-union, columns 1|2|3, shape enum, sections keyed to SECTION_VALUES) — NOT a rubber stamp; 13/13 tests are distinct reject-contrasts (size 8/13, weight 550, '#zzz'/'red', columns 4, empty templateId) + accepts DEFAULT_FORMAT + photo.hidden===true. DocumentFormat matches §28.3 field-for-field; FontId union = 6 faces incl arimo/tinos/carlito stand-ins; Profile.photoUrl optional added. Gaming read: clean, no bypass."
    },
    {
      "id": "E7-B1b",
      "title": "Self-hosted font registry — Font.register curated OFL faces, no runtime CDN",
      "status": "done",
      "depends_on": [
        "E7-B1a"
      ],
      "files": [
        "src/client/document/fonts.ts",
        "test/document-fonts.test.ts"
      ],
      "origin": "decomposed from E7-B1 (spec §28.3 fonts)",
      "context": "Build the curated, self-hosted react-pdf font registry (§28.3: '~12 OFL faces via Font.register, never a 500-font list, never free-form input'). IBM Plex (sans/serif/mono) is ALREADY vendored via @fontsource (latin .woff at node_modules/@fontsource/ibm-plex-*/files/*-latin-{400,600,700}-normal.woff). Install the metric stand-ins with `bun add @fontsource/arimo @fontsource/tinos @fontsource/carlito` (npm registry reachable — coordinator verified 2026-07-05) — Arimo→Arial, Tinos→Times New Roman, Carlito→Calibri (§28.3). In src/client/document/fonts.ts: registerDocumentFonts() — idempotent, calls Font.register once per FontId family, resolving each face's file from its installed @fontsource package's LATIN .woff (register 400 + a bold weight where the package ships one). CRITICAL: react-pdf/fontkit consumes .woff and .ttf but NOT .woff2 — register the .woff files. Also export FONT_FACES: Record<FontId,{label}> for the picker UI. NO runtime CDN — resolve local package file paths (via new URL(...import.meta.url) or the @fontsource file path), never fonts.googleapis.com/gstatic. FEASIBILITY PROVEN at intake: react-pdf 4.5.1 registered an offline @fontsource IBM Plex .woff and rendered %PDF- (coordinator probe, 4172B, 2026-07-05). Locked (cite oracle.md §28): PDFs come ONLY from @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not interim/fallback (§28.0). Templates are CODE (react-pdf components). The renderer NEVER cuts (§28.4). leadRationale + cut[] are reasoning UI ONLY, NEVER on the document (§11, gated by pdf.js extraction). Fabrication validation (§6.3) untouched.",
      "acceptance": "- `bun run check` + `bun run test` pass; `bun run build` ok\n- registerDocumentFonts() is idempotent (calling twice does not throw) and registers a face for EVERY FontId in the E7-B1a union (test asserts coverage — no FontId left unregistered)\n- CONTRAST (font actually applied, not ignored): a fixture Text rendered under family 'tinos' produces a valid PDF (%PDF- header, >0 bytes) whose bytes DIFFER from the same fixture rendered under 'ibm-plex-sans' — proving distinct embedded faces, not a silent fallback to one\n- NO runtime CDN: `grep -rniE 'fonts.googleapis|gstatic|https?://' src/client/document/fonts.ts` → 0 matches\n- new behavior shipped with the above tests",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, merged tree 2b6bd7e): scope CLEAN (fonts.ts, test, package.json, bun.lock only). Merged-tree gate: check 0 / lint clean (146) / build ok / vitest 38 files 274 tests GREEN / playwright 10/10 (chromium 8 + applications 1 + auth 1). registerDocumentFonts() idempotent (guard flag), FONT_FACES covers all 6 FontIds, tinos-vs-ibm-plex-sans byte-diff contrast proves distinct embedded faces, 0 CDN refs. @fontsource/{arimo,tinos,carlito} vendored. NODE-PROVEN ONLY: src resolved via createRequire.resolve → absolute FS path (works for renderToBuffer in Node/vitest). BROWSER GAP carried to E7-B1d: Vite/browser react-pdf cannot consume a node_modules FS path — B1d must make font src resolution browser-safe AND prove a non-default font renders in the canvas e2e. Gaming read: clean."
    },
    {
      "id": "E7-B1c",
      "title": "Persist format — DB columns + API round-trip + lockedFormat freeze + defaultFormat",
      "status": "done",
      "depends_on": [
        "E7-B1a"
      ],
      "files": [
        "src/server/db/schema.ts",
        "drizzle/",
        "src/server/routes/applications.ts",
        "src/server/routes/settings.ts",
        "src/server/routes/profile.ts",
        "src/server/routes/backup.ts",
        "test/db.test.ts",
        "test/api.profile-settings.test.ts",
        "test/api.applications-format.test.ts"
      ],
      "origin": "decomposed from E7-B1 (spec §28.3 persistence + §28.8-B lockedFormat)",
      "context": "Persist the design layer. Add to the Drizzle schema (src/server/db/schema.ts): applications.format (json DocumentFormat|null, per-app override) + applications.lockedFormat (json { format: DocumentFormat; resolvedDensity: 'comfortable'|'standard'|'compact'; paper: Paper }|null); settings.defaultFormat (json DocumentFormat, default DEFAULT_FORMAT from @shared/format); profile.photoUrl (text, nullable). Generate the migration with `bunx drizzle-kit generate` → drizzle/0003_*.sql (do NOT hand-write migration SQL). API: applications route accepts format on create/update and returns it on GET; settings route GET returns defaultFormat and PUT accepts it (validated with documentFormatZ from E7-B1a); profile route round-trips photoUrl. On POST /applications/:id/lock, freeze lockedFormat = { format: (app.format ?? settings.defaultFormat), resolvedDensity: 'comfortable' (the fit ladder is E7-C — density is 'as-set'='comfortable' until then, honest placeholder), paper: settings.paper }; DELETE /:id/lock clears lockedFormat too. Full-instance backup (backup.ts export/import) must round-trip the new columns — if it SELECTs full rows this is automatic; if it hand-picks columns, add them. CRITICAL ESCAPED-BUG (ledger [v3-014]): test/db.test.ts has an EXHAUSTIVE column allowlist (toEqual on applications + settings + profile columns) and test/api.profile-settings.test.ts has a settings-response KEY allowlist. Make ONLY ADDITIVE edits: append 'format','locked_format' to the applications column list, 'default_format' to settings, 'photo_url' to profile; append 'defaultFormat' to the settings-response key allowlist. The not.toContain hiring-status denylist (applied|interviewing|rejected|kanban…) and the key-leak teeth (no ciphertext/auth keys in settings response) MUST stay intact — do not gut them. Locked (cite oracle.md §28): PDFs come ONLY from @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not interim/fallback (§28.0). Templates are CODE (react-pdf components). The renderer NEVER cuts (§28.4). leadRationale + cut[] are reasoning UI ONLY, NEVER on the document (§11, gated by pdf.js extraction). Fabrication validation (§6.3) untouched.",
      "acceptance": "- `bun run check` + `bun run test` pass; migrations run on boot (fresh DATA_DIR)\n- format round-trips: PUT an application with a non-default DocumentFormat → GET returns it byte-equal; persists across a server restart (real .sqlite)\n- settings.defaultFormat round-trips (GET/PUT)\n- lockedFormat FREEZE CONTRAST (§28.8-B): create app (no per-app format) → lock → capture lockedFormat → then CHANGE settings.defaultFormat → the locked app's lockedFormat is BYTE-IDENTICAL (freeze holds); unlock (DELETE /:id/lock) clears both locked and lockedFormat\n- profile.photoUrl round-trips\n- backup export→import round-trips applications incl format/lockedFormat and profile.photoUrl\n- guard teeth intact: db.test.ts still fails if a hiring-status column is added (denylist present); api.profile-settings.test.ts still fails on a leaked key",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, merged tree 2b6bd7e): scope CLEAN (schema.ts, drizzle/0003 + meta, 4 routes, 3 tests). Merged-tree gate green (see B1b). Schema adds applications.format + applications.lockedFormat + settings.defaultFormat(default DEFAULT_FORMAT) + profile.photoUrl; migration 0003_typical_malice additive ALTERs. Lock FREEZE: structuredClone({format: app.format ?? settings.defaultFormat, resolvedDensity:'comfortable', paper}) — deep copy, immune to later default change; freeze-contrast test confirms lockedFormat byte-identical after defaultFormat changes + not.toEqual the new default; unlock clears locked+lockedFormat. format validated via documentFormatZ.nullish() on create/update. GUARD TEETH INTACT (gaming read): db.test.ts edit strictly additive (+format,+locked_format; not.toContain status/hiring_status preserved); api.profile-settings.test.ts additive (+defaultFormat to allowlist, +photoUrl to profile payload; hash/salt/key-leak denylist preserved). new api.applications-format.test.ts 7 tests. Clean."
    },
    {
      "id": "E7-B1d",
      "title": "Renderer consumes DocumentFormat — typography/colors/page/photo/columns + ATS grade",
      "status": "done",
      "depends_on": [
        "E7-B1a",
        "E7-B1b"
      ],
      "files": [
        "src/client/document/fonts.ts",
        "src/client/document/registry.ts",
        "src/client/document/renderResume.ts",
        "src/client/document/sections.tsx",
        "src/client/document/templates/strict.tsx",
        "src/client/document/templates/sidebar.tsx",
        "src/client/components/DocumentPreview.tsx",
        "src/client/document/download.ts",
        "test/document-render.test.ts",
        "test/document-format-render.test.ts"
      ],
      "origin": "decomposed from E7-B1 (spec §28.3 render + §28.2 ATS grade)",
      "context": "Make the renderer OBEY DocumentFormat (today templates hardcode Helvetica + fixed sizes/margins + no color). Thread a resolved DocumentFormat through TemplateProps (add `format: DocumentFormat`) and renderResume args (renderResumeDocument/renderResumeToBuffer gain a format arg, defaulting to DEFAULT_FORMAT for back-compat). The SHARED section renderers (sections.tsx) apply: body typography (family via FontId→registered family name from E7-B1b, size, lineHeight); heading family/weight and primary color on section labels/rules/links; text color as body ink; page marginX/marginY/sectionGap on the Page; per-section columns (format.sections[section]?.columns → render that section's items in 2/3 columns, e.g. skills). Call registerDocumentFonts() (E7-B1b) before rendering. Photo: render profile.photoUrl per format.photo (size/shape) ONLY when format.photo.hidden===false; default hidden ⇒ NO image. Add effectiveAtsGrade(manifest, format): caps grade at 'good' when photo is shown OR manifest.layout is a sidebar layout (§28.2 — photo/columns downgrade). Update DocumentPreview + download.ts to pass the resolved format (they receive it from the caller; keep DEFAULT_FORMAT fallback so callers not yet passing format still compile — the UI wiring is E7-B1e). MUST preserve §11: leadRationale/cut never rendered — the extraction-absent invariant stays green. Templates still differ in COMPOSITION only, never features (§28.2). Locked (cite oracle.md §28): PDFs come ONLY from @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not interim/fallback (§28.0). Templates are CODE (react-pdf components). The renderer NEVER cuts (§28.4). leadRationale + cut[] are reasoning UI ONLY, NEVER on the document (§11, gated by pdf.js extraction). Fabrication validation (§6.3) untouched.\n\n[CARRIED FORWARD from E7-B1b, coordinator 2026-07-05 — browser/Node dual-environment fonts]: B1b's registerDocumentFonts() resolves each .woff via createRequire(import.meta.url).resolve → an ABSOLUTE node_modules FS path. That works for renderToBuffer in Node/vitest but the BROWSER (DocumentPreview → react-pdf usePDF, Vite bundle) CANNOT consume a node_modules FS path — it needs a bundled/fetchable asset URL. Since THIS ticket wires registerDocumentFonts() into the render that runs in BOTH environments (Node renderToBuffer in tests AND the browser preview), you MUST make font src resolution browser-safe (e.g. Vite `?url`/`new URL(pkg/files/*.woff, import.meta.url)` asset imports, or an environment-aware resolver) WITHOUT breaking the Node byte-diff test. src/client/document/fonts.ts is now in your declared files so you may edit it. PROOF REQUIRED IN BOTH ENVIRONMENTS: (a) Node — the format-applied byte-diff contrast (renderToBuffer, tinos≠default) stays green; (b) BROWSER — the keyless playwright applications e2e (test/e2e/applications.spec.ts, expectCanvasPainted) STAYS GREEN after DocumentPreview calls registerDocumentFonts() with the default ibm-plex-sans face — a blank/errored canvas there means the browser couldn't load the registered font. If you can't satisfy both without touching files beyond your declared set, return {tooBig,...} rather than half-wire it.",
      "acceptance": "- `bun run check` + `bun run test` pass; `bun run build` ok\n- the EXISTING extraction-order invariant (document-render.test) STAYS green: strict render extracts profile header + every selected item.text in TailoredResume order; leadRationale/cut sentinel strings ABSENT from extraction\n- CONTRAST (format applied): a render with format {body.family 'tinos', colors.primary changed, page.marginX larger} produces PDF bytes DIFFERENT from the DEFAULT_FORMAT render of the same fixture (proves typography+color+page are wired, not ignored)\n- PHOTO: with a profile.photoUrl set, DEFAULT_FORMAT (photo.hidden true) render contains NO image; the same with photo.hidden=false differs (image present) — assert photo appears ONLY when hidden=false\n- effectiveAtsGrade: 'strict' template + hidden photo → 'strict'; sidebar layout → 'good'; strict + photo shown → 'good'\n- per-section columns: a section given columns:2 renders its items in a 2-column composition (still all items — count invariant)\n- BROWSER FONT PROOF: `bunx playwright test --project=applications` STAYS GREEN (canvas paints) after DocumentPreview registers fonts — proves browser font loading works, not just Node renderToBuffer\n- the Node byte-diff font contrast still passes (fonts load in Node too)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-05, committed): scope CLEAN (7 declared src + 1 new test; download.ts + document-render.test untouched — DEFAULT_FORMAT default arg keeps download compiling, invariant test unchanged & green). check 0 / lint clean (147) / build ok (47 .woff assets bundled) / vitest 39 files 282 tests GREEN single run / playwright 9/9 (applications 1 + chromium 8). Format GENUINELY applied in sections.tsx (every style reads typography/colors/page/columns/photo from format — not hardcoded); templates apply page.marginX/Y on Page + call registerDocumentFonts(). Tests are REAL contrasts: byte-diff (tinos+color+margin ≠ default) + extraction preserved; photo via pdf.js OPS.paintImageXObject (present only when hidden:false, absent when no photoUrl); columns via distinct x-offsets (≥2 for columns:2, exactly 1 default) — all items present (never cuts). effectiveAtsGrade caps 'good' for sidebar/photo. DUAL-ENV FONTS SOLVED (the carried B1b limit): import.meta.env.SSR branch — SSR/Node uses createRequire fs-path, browser uses 12 literal `new URL(pkg,import.meta.url)` Vite asset URLs; namespace import dodges Rollup stub-validation, SSR branch tree-shaken from client bundle. BROWSER PROOF: applications e2e canvas paints after DocumentPreview registers fonts. §11 extraction-absent invariant green. Gaming read: clean."
    },
    {
      "id": "E7-B1e",
      "title": "Design panel UI — per-app format editor + template picker + ATS badge + defaultFormat",
      "status": "done",
      "depends_on": [
        "E7-B1c",
        "E7-B1d"
      ],
      "files": [
        "src/client/components/DesignPanel.tsx",
        "src/client/components/TemplatePicker.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/components/SettingsView.tsx",
        "src/client/components/DocumentPreview.tsx",
        "src/client/queries/useApplications.ts",
        "src/client/hooks/queries.ts",
        "test/design-panel.test.tsx",
        "test/e2e/applications.spec.ts",
        "src/shared/types.ts",
        "src/shared/schema.ts",
        "src/client/api.ts",
        "src/client/components/ResultView.tsx"
      ],
      "origin": "decomposed from E7-B1 (spec §28.3 design panel UI + §28.2 picker)",
      "context": "Build the §28.3 design panel UI on top of the persistence (E7-B1c) and format-aware renderer (E7-B1d). DesignPanel.tsx: bounded controls grouped typography→color→page→photo→structure editing the per-app DocumentFormat, persisted via a useApplications format mutation (PUT app format). Controls are CONSTRAINED (never free-form): font family = dropdown from FONT_FACES (E7-B1b); body size / margins / sectionGap = sliders/steppers within the E7-B1a zod bounds; colors = curated palette swatches + a hex input escape hatch; photo = toggle (DEFAULT hidden) that when enabled shows a regional-norms note (DACH/JP expected, US/UK discouraged, §28.3); structure = per-section columns only (order/visibility stays LayoutEditor's job — do NOT add drag-drop, deferred §28.3). TemplatePicker.tsx: gallery of TEMPLATES with the ATS badge (effectiveAtsGrade from E7-B1d) — badge shows 'strict'/'good' and surfaces the Workday/Taleo left-to-right caveat for sidebar (§28.2), downgrading when photo/sidebar selected. DocumentPreview reflects the chosen format live (pass app.format ?? settings.defaultFormat; lockedFormat.format when locked). SettingsView gains a defaultFormat editor (reuse DesignPanel bound to settings.defaultFormat). Wire the panel into ApplicationDetail. Locked (cite oracle.md §28): PDFs come ONLY from @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not interim/fallback (§28.0). Templates are CODE (react-pdf components). The renderer NEVER cuts (§28.4). leadRationale + cut[] are reasoning UI ONLY, NEVER on the document (§11, gated by pdf.js extraction). Fabrication validation (§6.3) untouched.\n\n[TYPE-PLUMBING CASCADE — coordinator 2026-07-05, absorbed from E7-B1c which persisted the columns but did NOT update the shared/client TYPE contracts]: The DB + routes already store/return format/lockedFormat/defaultFormat/photoUrl (E7-B1c), and DEFAULT_FORMAT/DocumentFormat/documentFormatZ exist (E7-B1a), but the TS contracts a typed client needs are missing. You MUST additively extend (declared files now include these): (1) src/shared/types.ts — add `format: DocumentFormat | null` and `lockedFormat: { format: DocumentFormat; resolvedDensity: 'comfortable'|'standard'|'compact'; paper: Paper } | null` to the `Application` type (define a `LockedFormat` type if useful; mirror the E7-B1c DB shape exactly); (2) src/shared/schema.ts — extend `settingsInput` += `defaultFormat: documentFormatZ.optional()`, `applicationCreate`/`applicationUpdate` += `format: documentFormatZ.nullish()`, `profileInput` += `photoUrl: z.string().optional()` (so the typed client inputs carry them — the server already accepts them; this just aligns the shared contract, keep existing fields/refinements intact); (3) src/client/api.ts — add `defaultFormat: DocumentFormat` to `SettingsResponse`. Then the UI reads `application.format`/`application.lockedFormat`/`settings.defaultFormat` typed. RESOLVED FORMAT for preview/panel = when the app is locked use `application.lockedFormat.format`, else `application.format ?? settings.defaultFormat`. (4) src/client/components/ResultView.tsx — forward the resolved format prop into `<DocumentPreview>` (add a `format` prop to DocumentPreview; ApplicationDetail computes the resolved format and passes it down through ResultView) so editing a format field repaints the preview. Persist per-app format via the existing `useUpdateApplication` hook (PUT accepts `format`); persist settings.defaultFormat via the existing `useUpdateSettings`. Keep changes additive — do not break the ~282 existing tests or the existing applications e2e steps.",
      "acceptance": "- `bun run check` + `bun run test` pass; `bun run build` ok\n- Playwright keyless e2e (test/e2e/applications.spec.ts, chromium): open a tailored application → change a format field (template AND body font) → the preview canvas repaints (expectCanvasPainted) → RELOAD → the format change PERSISTS (control reflects it) → no uncaught console errors\n- ATS badge CONTRAST: the picker shows 'good' + the Workday/Taleo caveat when a sidebar template OR photo is selected, and 'strict' for the default strict/no-photo choice (unit test on TemplatePicker via effectiveAtsGrade)\n- photo toggle defaults to hidden (unit); enabling it reveals the regional-norms note\n- settings.defaultFormat is editable in SettingsView and round-trips\n- controls are bounded (font from FONT_FACES list; no free-form family input)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-05, committed 890df69): scope CLEAN (10 declared files; DocumentPreview/hooks declared-but-untouched — DocumentPreview.format prop came from B1d). check 0 / lint clean (150) / build ok / vitest 40 files 289 tests GREEN single run / playwright 10/10 non-docker (chromium 8 + auth 1 + applications 1). BROWSER PROOF (applications.spec extended): clicks Sidebar → PUT format.templateId='sidebar-left' + canvas repaint; opens Body font → clicks Arimo → PUT format.typography.body.family='arimo' + repaint; RELOAD → Sidebar aria-pressed=true + font combobox shows Arimo (persistence from reloaded UI). GAMING READ clean: DesignPanel controls BOUNDED (family/weight/columns via Select, hex Input escape hatch, photo checkbox default-hidden + DACH/JP regional note) — NO free-form family input; TemplatePicker badge uses effectiveAtsGrade (not declared grade) + Workday/Taleo caveat when good; documentFormatZ MOVED above input schemas (all bounds byte-identical — not weakened, verified line-by-line); type/schema/input extensions strictly ADDITIVE (Application.format/lockedFormat + LockedFormat type; settingsInput.defaultFormat; applicationCreate/Update.format; profileInput.photoUrl; SettingsResponse.defaultFormat). design-panel unit 7/7."
    },
    {
      "id": "E7-C1",
      "title": "[COARSE — decompose at pickup] Fit ladder + ATS honesty (§28.4/§28.6): density ladder, overflow, extraction view / plain-text export, CI invariant",
      "status": "decomposed",
      "depends_on": [
        "E7-B1e"
      ],
      "files": [],
      "origin": "spec §28.4/§28.6/§28.8-C",
      "context": "COARSE PLACEHOLDER — decompose at pickup. Scope: the fit ladder (comfortable→standard→compact via template density multipliers, floor 9.5pt body; first fit wins; auto-only, never persisted); overflow reporting (render at compact, show true page count, offer re-tailor-tighter or allow-2-pages — NEVER cut; item count invariant across densities); the §28.6 'What the ATS sees' extraction view + plain-text export; and the extraction-order invariant promoted to a CI regression test for strict templates. Locked (cite oracle.md §28): PDFs are produced ONLY by @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not an interim or fallback (§28.0, rejected 2026-07-05). Templates are CODE (react-pdf components), never HTML/CSS or user-authored. The renderer NEVER cuts (§28.4) — item count is invariant across densities. leadRationale + cut[] are reasoning UI ONLY and must NEVER appear on the document (§11) — this is gated by pdf.js text extraction. Fabrication validation (§6.3) is untouched (server-side, upstream of rendering). [ESCAPED-BUG COMPENSATION from E7-A4, ledger v3-016]: A4 swapped the resume DOM for a pdf.js canvas, so the applications e2e no longer DOM-asserts the specific tailored content (it now proves canvas-painted + RESUME_TOKEN in server JSON). The content-in-order proof currently lives only in A1 unit extraction (fixture data). E7-C MUST add an end-to-end check that extracts text from the REAL generated/downloaded PDF and asserts the live-tailored content (RESUME_TOKEN) present in order, reclaiming content fidelity at the output layer.",
      "acceptance": "DECOMPOSE FIRST. Spec §28.8-C oracle: growing fixture content walks comfortable→compact with item count INVARIANT across densities (contrast check); overflow reports the true page count and never cuts; strict-template extraction-order invariant runs green in CI.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E7-C1a",
      "title": "Fit ladder engine — density multipliers + fitToPages (item-count invariant) + extraction helper + CI invariant",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/client/document/registry.ts",
        "src/client/document/fit.ts",
        "src/client/document/extractText.ts",
        "src/client/document/renderResume.ts",
        "test/fit.test.ts",
        "test/document-extraction-invariant.test.ts"
      ],
      "origin": "decomposed from E7-C1 (spec §28.4 fit ladder + §28.6 CI invariant)",
      "context": "Build the §28.4 fit ladder ENGINE (pure/Node-testable; UI wiring is E7-C1b). react-pdf reports the true page count, so fitting is EXACT. In src/client/document/registry.ts add per-template DENSITY MULTIPLIERS to each TemplateManifest — a { comfortable:1; standard:<~0.94>; compact:<~0.88> } record of scale factors applied to type size / lineHeight / page gaps (comfortable = exactly as set). In src/client/document/fit.ts (new): applyDensity(format: DocumentFormat, density: Density, multipliers) → a scaled DocumentFormat (body.size, heading implied via size, lineHeight, page.sectionGap/marginY scaled by the multiplier) with a HARD FLOOR of 9.5pt body size (never scale below 9.5). fitToPages({resume, profile, format, paper, targetPages}) → { density: Density; pageCount: number; fits: boolean } — render at comfortable; if pageCount > targetPages re-render at standard, then compact; FIRST density whose pageCount ≤ targetPages wins; if compact still exceeds, return { density:'compact', pageCount:<true count>, fits:false } (overflow — the UI ticket surfaces the honest actions; NEVER cut). Get pageCount from the rendered PDF (renderToBuffer → pdf.js numPages via the extractText helper's loader, or react-pdf). CRITICAL: the item COUNT in the document is IDENTICAL across all three densities — the ladder only rescales type, never drops items. In src/client/document/extractText.ts (new): extractPdfText(buffer: Buffer|Uint8Array) → string[] (the pdf.js legacy text items IN ORDER) — a shared helper reused by the ATS view (E7-C1c) and the invariant test. renderResume.ts may gain a small page-count helper if useful. Locked (cite oracle.md §28): PDFs come ONLY from @react-pdf/renderer — browser printing REJECTED (§28.0). Templates are CODE. The renderer NEVER cuts (§28.4) — item count in the document is INVARIANT across densities; density scales type size/line-height/gaps ONLY, never the set of items. leadRationale + cut[] are reasoning UI ONLY, NEVER on the document (§11, gated by pdf.js extraction). Fabrication validation (§6.3) untouched. Density is AUTO-ONLY (derived by the fit ladder) and is NEVER persisted (§28.4).",
      "acceptance": "- `bun run check` + `bun run test` pass; `bun run build` ok\n- LADDER WALK CONTRAST: a SMALL fixture fits 1 page at 'comfortable'; a LARGER fixture (more/longer items) is forced to 'standard'; a still-larger one to 'compact' — fitToPages returns the escalating density (not always the same)\n- ITEM-COUNT INVARIANT (never-cut, §28.4): rendering the SAME resume at comfortable/standard/compact — assert EVERY selected item.text is PRESENT in the extraction at ALL THREE densities (the same set of items survives; none dropped). Use presence-of-each-item, NOT raw pdf.js run-count (run counts shift with line-wrapping — a fragile check).\n- 9.5pt FLOOR: applyDensity never produces body.size < 9.5, even at compact with an aggressive multiplier\n- density NOT persisted: fitToPages RETURNS the density (a pure computation); it does not write it to any store/format\n- CI EXTRACTION-ORDER INVARIANT (test/document-extraction-invariant.test.ts): for a strict template, extractPdfText over the generated PDF contains the profile header + EVERY selected item.text in EXACT TailoredResume order (index-increasing), and the leadRationale/cut[] sentinel strings are ABSENT (§28.6/§11)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-05, committed): scope CLEAN (registry.ts, fit.ts, extractText.ts + 2 tests; renderResume.ts declared-but-untouched). check 0 / lint clean (154) / build ok / vitest 42 files 298 tests GREEN single run. fit.ts GENUINE: fitToPages actually re-renders each density (renderResumeToBuffer) and measures real pdf.js numPages, first ≤target wins, overflow→{compact,trueCount,fits:false} never cutting; applyDensity scales body.size(9.5 floor via Math.max)/lineHeight/marginY/sectionGap, immutable spread. Density RETURNED not persisted. Tests are real contrasts: LADDER WALK 20-item→comfortable, 22→standard, 24→compact (all 1pg), 40→compact/2pg/fits:false; ITEM-COUNT INVARIANT = every item.text present at all 3 densities (presence-of-each, not fragile run-count); density-not-persisted (input byte-identical + same ref); comfortable=passthrough; 9.5 floor (10*0.3→9.5). CI extraction-order invariant: strict template, profile+every item.text index-increasing, leadRationale/cut sentinels ABSENT. Per-template densityMultipliers {comfortable:1,standard:0.94,compact:0.88}. Gaming read: clean."
    },
    {
      "id": "E7-C1b",
      "title": "Fit into preview + fit chip + overflow actions (UI)",
      "status": "done",
      "depends_on": [
        "E7-C1a"
      ],
      "files": [
        "src/client/components/FitChip.tsx",
        "src/client/components/DocumentPreview.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/document/download.ts",
        "src/client/queries/useApplications.ts",
        "test/fit-ui.test.tsx"
      ],
      "origin": "decomposed from E7-C1 (spec §28.4 fit UI/overflow)",
      "context": "Wire the E7-C1a fit ladder into the live preview + download, and surface the honest fit/overflow UI (§28.4). DocumentPreview must render at the FITTED density: call fitToPages({resume, profile, resolvedFormat, paper, targetPages}) and render the document at the returned density (apply applyDensity to the format before renderResumeDocument). download.ts likewise renders the downloaded PDF at the fitted density so the file MATCHES the preview. FitChip.tsx (new): a small chip showing \"Fits N page · <density>\" (e.g. \"Fits 1 page · compact\") from the fitToPages result. ApplicationDetail.tsx: show the FitChip; on OVERFLOW (fits:false — content exceeds compact at targetPages) show the TRUE page count and offer the TWO honest actions: (a) allow 2 pages → set application.targetPages=2 via useUpdateApplication; (b) re-tailor to a tighter budget (a prompt/hook toward E7-D — for now a button that surfaces the intent; the budget wiring is E7-D1). NEVER cut. targetPages resolution: locked app uses lockedFormat.paper + its targetPages. Density is auto — do NOT add a density control or persist it. Locked (cite oracle.md §28): PDFs come ONLY from @react-pdf/renderer — browser printing REJECTED (§28.0). Templates are CODE. The renderer NEVER cuts (§28.4) — item count in the document is INVARIANT across densities; density scales type size/line-height/gaps ONLY, never the set of items. leadRationale + cut[] are reasoning UI ONLY, NEVER on the document (§11, gated by pdf.js extraction). Fabrication validation (§6.3) untouched. Density is AUTO-ONLY (derived by the fit ladder) and is NEVER persisted (§28.4).",
      "acceptance": "- `bun run check` + `bun run test` pass; `bun run build` ok\n- FIT CHIP: renders \"Fits <n> page(s) · <density>\" reflecting the fitToPages result for the application's format+targetPages (unit/e2e)\n- OVERFLOW CONTRAST: a fixture that overflows compact at targetPages=1 shows the TRUE page count (>targetPages) and the allow-2-pages action; clicking it PUTs targetPages=2 (and the content is NEVER truncated — item count unchanged)\n- the preview + download render at the SAME fitted density (the downloaded file matches what the chip claims)\n- no density control is exposed and no density value is persisted",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-05, committed): scope CLEAN (FitChip.tsx, ApplicationDetail.tsx, test/fit-ui.test.tsx; download/DocumentPreview/useApplications declared-but-untouched — already accepted format/targetPages). check 0 / lint clean (156) / build ok. Full vitest 300/302 with 2 FAILs = api.applications-format + api.applications-tailor (server-spawning, 32-36s under contention) — CONFIRMED [v3-010]-class env flake: both PASS in isolation 13/13 in 6.46s; C1b touches only client UI, cannot regress server integration. Fit wiring GENUINE: useFit computes fitToPages once (unconditional before early returns, hooks-safe), fittedFormat=applyDensity(resolvedFormat,fit.density,mult) passed to BOTH ResultView format + downloadResumePdf format (preview==download, unit-asserted identical). FitChip 'Fits N page(s) · density'. Overflow (!fit.fits): true pageCount + 'Nothing was cut' + Allow-2-pages→mutate{targetPages:2} (only when targetPages===1); re-tailor-tighter is a disabled button noting E7-D1 (flagged). NO density control (queryByRole combobox/slider density → absent), no density persisted. Gaming read: clean."
    },
    {
      "id": "E7-C1c",
      "title": "\"What the ATS sees\" extraction view + plain-text export + real-PDF content-fidelity e2e (A4 escaped-bug)",
      "status": "done",
      "depends_on": [
        "E7-C1a"
      ],
      "files": [
        "src/client/components/AtsView.tsx",
        "src/client/document/plainText.ts",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/document/download.ts",
        "test/ats-view.test.tsx",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "decomposed from E7-C1 (spec §28.6 ATS view/plain-text + ledger [v3-016] escaped-bug compensation)",
      "context": "Build the §28.6 \"What the ATS sees\" honesty view + plain-text export, AND discharge the E7-A4 escaped-bug (ledger [v3-016]). AtsView.tsx (new): a preview tab/toggle that runs extractPdfText (E7-C1a) over the application's ACTUAL generated export and shows the extracted text exactly as an ATS parser reads it (OpenResume-style round-trip) — this is Lede \"showing its work about the file\", the sibling to the reasoning panel showing its work about the judgment. plainText.ts (new): derive a plain-text export from the same extraction (or from TailoredResume in the same order) for pasting into application form fields; wire a \"plain text\" download alongside the PDF download in download.ts. ApplicationDetail.tsx: add the ATS-view tab + plain-text export action (this file also carries E7-C1b's fit UI — coordinate; both are additive to the detail page). ESCAPED-BUG COMPENSATION (ledger [v3-016] — MANDATORY): E7-A4 swapped the resume DOM for a pdf.js canvas, so the applications e2e no longer DOM-asserts the specific tailored content (it proves canvas-painted + RESUME_TOKEN in server JSON only). ADD to test/e2e/applications.spec.ts a step that takes the REAL generated/downloaded PDF and runs extractPdfText over it, asserting the live-tailored content (the RESUME_TOKEN and the selected items) is PRESENT IN ORDER — reclaiming content fidelity at the OUTPUT layer (§28.6 mechanism), not just server JSON. Locked (cite oracle.md §28): PDFs come ONLY from @react-pdf/renderer — browser printing REJECTED (§28.0). Templates are CODE. The renderer NEVER cuts (§28.4) — item count in the document is INVARIANT across densities; density scales type size/line-height/gaps ONLY, never the set of items. leadRationale + cut[] are reasoning UI ONLY, NEVER on the document (§11, gated by pdf.js extraction). Fabrication validation (§6.3) untouched. Density is AUTO-ONLY (derived by the fit ladder) and is NEVER persisted (§28.4).",
      "acceptance": "- `bun run check` + `bun run test` pass; `bun run build` ok\n- ATS VIEW: for a fixture resume, AtsView shows the extracted text (profile + every selected item, in order); the leadRationale/cut sentinels are ABSENT (§11)\n- PLAIN-TEXT EXPORT: produces the resume content as plain text in TailoredResume order (contrast: contains every selected item.text, excludes leadRationale/cut)\n- REAL-PDF CONTENT-FIDELITY e2e (escaped-bug, MANDATORY): the applications.spec extracts text from the REAL generated PDF and asserts the live-tailored RESUME_TOKEN content is present IN ORDER — the content-in-order proof now lives at the output layer, not only A1's fixture unit test\n- the applications e2e stays GREEN end-to-end (chromium)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-05, committed): scope CLEAN (AtsView.tsx, plainText.ts, ApplicationDetail.tsx, download.ts, ats-view.test.tsx, applications.spec.ts). check 0 / lint clean (159) / build ok / ats-view unit 4/4 / playwright applications 1/1 (the real-PDF e2e). Full vitest 305/306 — the 1 FAIL (api.applications-format, 46s under contention) CONFIRMED flake, 7/7 in 6.27s isolated; C1c is client-only. REAL-PDF e2e (4a) is GENUINE: captures the actual Download-PDF file via waitForEvent('download')→readFileSync→extractPdfText, asserts RESUME_TOKEN + SECOND_TOKEN present AND in order (firstIdx<secondIdx). Console allowlist is TIGHT (exact-text net::ERR_FILE_NOT_FOUND, scoped to downloadInFlight window only). ATS view + plainText exclude leadRationale/cut. FIXED an escaped bug in-scope: downloadResumePdf used Node-only renderToBuffer (browser build throws) → switched to pdf().toBlob(). Gaming read: clean."
    },
    {
      "id": "E7-C2",
      "title": "REPAIR: fit ladder uses Node-only renderToBuffer → broken in the browser; make it browser-safe + prove via e2e",
      "status": "done",
      "depends_on": [
        "E7-C1c"
      ],
      "files": [
        "src/client/document/fit.ts",
        "src/client/document/renderResume.ts",
        "src/client/document/extractText.ts",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/components/DocumentPreview.tsx",
        "test/fit.test.ts",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "repair: E7-C1a/C1b fit ladder + extraction use Node-only renderToBuffer/pdfjs-legacy; silently broken in the browser (fitToPages throws → useFit swallows → chip never renders, density never applied). Uncovered by the E7-C1c real-PDF e2e which caught the SAME class in downloadResumePdf.",
      "context": "ESCAPED BUG (escaped-bug rule — fix AND strengthen the check). E7-C1c proved that @react-pdf/renderer's BROWSER build stubs renderToBuffer/renderToStream/renderToFile to THROW ('Node specific API'); only pdf(doc).toBlob() is implemented in the browser. src/client/document/fit.ts's fitToPages STILL calls renderResumeToBuffer (→ renderToBuffer) to measure page count, and src/client/document/renderResume.ts's renderResumeToBuffer is Node-only. So in the REAL browser: fitToPages THROWS → E7-C1b's useFit catch swallows it → fit stays null → the FitChip NEVER renders and the fitted density is NEVER applied to the live preview/download. This passed CI only because vitest runs under Node (where renderToBuffer works). FIX: make the page-count render browser-safe. fit.ts must obtain the rendered PDF via a path that works in the real browser — pdf(renderResumeDocument(args)).toBlob() → arrayBuffer → pdf.js numPages — while KEEPING the Node unit tests (test/fit.test.ts) green (use an import.meta.env.SSR branch like src/client/document/fonts.ts already does, OR a shared browser-safe helper in renderResume.ts that both environments can call; renderResumeToBuffer may stay for Node callers/tests). extractText.ts must load pdf.js in a way that works in BOTH the Node test process and the browser (the legacy build + a workerless getDocument is fine; verify the browser path). Also HARDEN useFit in ApplicationDetail.tsx: a genuine render error must be DISTINGUISHABLE from 'genuinely needs >targetPages' — do not blanket-swallow so a broken render silently hides the whole feature again (log/surface it; a null fit from a real throw is a bug, not 'no overflow'). DocumentPreview.tsx is in scope in case the fit render should be shared with the preview's own pdf() render to avoid a double render. STRENGTHEN THE CHECK (the escaped-bug rule's teeth): test/e2e/applications.spec.ts must assert — in the REAL browser (chromium/applications project) — that the FitChip actually RENDERS with its 'Fits N page · <density>' text after tailor (proving fitToPages ran in the browser, not threw-and-swallowed). This is the regression guard the unit tests structurally cannot provide. Locked (oracle.md §28): PDFs ONLY from @react-pdf/renderer; browser printing REJECTED (§28.0); renderer NEVER cuts (§28.4); leadRationale/cut never on the document (§11); density auto-only, never persisted.",
      "acceptance": "- `bun run check` + `bun run lint` + `bun run build` pass\n- `bunx vitest run` full suite green (test/fit.test.ts Node path STAYS green; known api.applications-* contention flake excepted — confirm in isolation)\n- BROWSER FIT PROOF (the strengthened check): `bunx playwright test --project=applications` GREEN and the applications.spec asserts the FitChip's 'Fits <n> page · <density>' text is VISIBLE in the real browser after tailoring (fitToPages ran in-browser — no swallowed throw). A blank/absent chip must FAIL.\n- fit.ts render path no longer calls a Node-only API on the browser branch (grep/read: the browser render uses pdf().toBlob(), not renderToBuffer)\n- useFit distinguishes a real render error from a legitimate overflow (a thrown render does not masquerade as fit:null/no-overflow)\n- item-count invariant + ladder-walk unit tests unchanged and green (§28.4 never-cut preserved)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, 2026-07-05, committed 5aff4f3): scope CLEAN (fit.ts, renderResume.ts, extractText.ts, ApplicationDetail.tsx, applications.spec.ts; DocumentPreview declared-but-untouched). check 0 / lint clean (159) / build ok. Full vitest 305/306 (the 1 FAIL = api.applications-format 60s contention timeout, CONFIRMED 7/7 in 6.94s isolated). BROWSER FIT PROOF (the strengthened check): playwright applications GREEN 3/3 in isolation (13.4/27.2/12.4s) + in the all-projects run 10/10 — the new assertion `getByText(/^Fits \\d+ pages? · (comfortable|standard|compact)$/).toBeVisible()` passes in real chromium (builder verified it FAILS against pre-fix code → the guard genuinely catches the escaped bug). FIX GENUINE: fit.ts pageCountAt branches on import.meta.env.SSR — Node keeps renderResumeToBuffer, browser uses renderResumeToBlob (pdf().toBlob()) → countPages via loadPdfDocument; extractText.ts adds a pdf.js main-thread worker fallback (globalThis.pdfjsWorker) so getDocument works in-browser (was hard-throwing 'No workerSrc'); useFit now surfaces a render throw (role=alert 'Couldn't measure…') distinct from a legit fits:false overflow. Node unit tests unchanged & green (fit.test 8/8: ladder walk, item-count invariant §28.4, 9.5pt floor, density-not-persisted). Gaming read: clean. NOTE the initial gate run showed the applications spec failing — reproduced as pure EXTERNAL CONTENTION (playwright stacked with a heavy full-vitest on a loaded box); 3/3 clean in isolation."
    },
    {
      "id": "E7-D1",
      "title": "[COARSE — decompose at pickup, KEY-GATED] Length budget → selection (§28.5)",
      "status": "decomposed",
      "depends_on": [
        "E7-C2"
      ],
      "files": [],
      "origin": "spec §28.5/§28.8-D",
      "context": "COARSE PLACEHOLDER — decompose at pickup. KEY-GATED model-quality feature (follows the T014 pattern). Scope: the server derives a content budget from (paper, targetPages, format, ladder) and rides it on the USER MESSAGE exactly like per-app context (§6.2 / ledger [v3-001]) — prompt.ts STAYS FROZEN; no budget ⇒ user message byte-identical to today ⇒ keyless fixtures replay unchanged. Budget-driven cuts land in cut[] with rationales. Locked (cite oracle.md §28): PDFs are produced ONLY by @react-pdf/renderer — browser printing (window.print()/print.css) is REJECTED, not an interim or fallback (§28.0, rejected 2026-07-05). Templates are CODE (react-pdf components), never HTML/CSS or user-authored. The renderer NEVER cuts (§28.4) — item count is invariant across densities. leadRationale + cut[] are reasoning UI ONLY and must NEVER appear on the document (§11) — this is gated by pdf.js text extraction. Fabrication validation (§6.3) is untouched (server-side, upstream of rendering).",
      "acceptance": "DECOMPOSE FIRST. Spec §28.8-D oracle: empty budget ⇒ byte-identical user message (fixture-replay guard, KEYLESS); with a live key, a 1-page budget over the over-complete seed library produces MORE cut[] entries than a 2-page budget, each with a rationale (KEY-GATED, recorded honestly like T014).",
      "attempts": [],
      "evidence": "decomposed into E7-D1a (keyless machinery) + E7-D1b (key-gated eval harness, DEFERRED); see ledger [v3-029]."
    },
    {
      "id": "E7-D1a",
      "title": "Length budget → selection: keyless machinery (derive + transport)",
      "status": "done",
      "depends_on": [
        "E7-C2"
      ],
      "files": [
        "src/server/tailor/budget.ts",
        "src/server/tailor/engine.ts",
        "src/server/routes/applications.ts",
        "test/budget.test.ts",
        "test/engine.test.ts",
        "test/api.applications-tailor.test.ts"
      ],
      "origin": "decomposed from E7-D1 (spec §28.5 / §28.8-D, keyless half)",
      "context": "Implement the KEYLESS machinery for spec §28.5 \"length budget → selection\": the server derives a content budget from (paper, targetPages, format) and rides it on the model USER MESSAGE, EXACTLY the way per-app `context` already does (the E6-A3 precedent). The live \"budget changes selection\" claim is model-quality = KEY-GATED and handled by a SEPARATE ticket (E7-D1b) — do NOT attempt to prove it here; there is no provider key in this env.\n\nLOCKED (cite .ailoop/oracle.md §28 — never re-litigate): prompt.ts SYSTEM_PROMPT is FROZEN — do NOT edit it; the budget rides the `prompt` (user message) field only. PDFs are produced ONLY by @react-pdf/renderer; browser printing is REJECTED. Templates are CODE. The renderer NEVER cuts (§28.4). leadRationale + cut[] never reach the document (§11). validateNoFabrication (§6.2/§6.3) is server-side, entries-only, and MUST NOT receive the budget — a number that traces only to the budget string must still throw FabricationError.\n\nEXACT PATTERN TO MIRROR (per-app context transport, already shipped):\n- src/server/tailor/engine.ts:26-30 `buildUserPrompt(jd, context?)`: base = `Tailor for this job description:\\n\\n${jd}`; if `!context` returns base UNCHANGED (byte-identical — `!` catches undefined/null/\"\"); else appends `\\n\\nTailoring context (guides emphasis; not a source of facts):\\n${context}`.\n- engine.ts:58-66 ProviderEngine.attempt sends buildUserPrompt in the `prompt` field; SYSTEM_PROMPT+library go in `system` (untouched).\n- engine.ts:92-95 FixtureEngine.decide IGNORES context (`_context`) and keys replay on hashKey(jd, entries) only (evalcore.ts:12-16 — {jd, entries} only). Context is EXCLUDED from the key.\n- engine.ts:133-147 tailor(engine, jd, entries, layout, baseSummary?, context?) threads context to engine.decide; validateNoFabrication(resume, entries, baseSummary) gets entries only.\n- src/server/routes/applications.ts:216-223 the /api/applications/:id/tailor route calls tailor(...) with existing.context. paper (settingsRow.paper), targetPages (existing.targetPages, 1|2), and the effective format (existing.format ?? settingsRow.defaultFormat) are all in scope in that handler.\n\nDO EXACTLY THIS:\n1. NEW src/server/tailor/budget.ts — a PURE server-side function `deriveContentBudget({paper, targetPages, format})` -> string. Heuristic (§28.5:869 — the fit ladder absorbs estimation error): estimate lines-per-page x chars-per-line at `standard` density from DocumentFormat typography (body.size, body.lineHeight) + page margins + Paper dimensions, scale by targetPages, phrase to the model as an APPROXIMATE bullets/words budget (e.g. \"Aim for roughly N bullets (~M words) total so the resume fits T page(s)...\"). Use a small LOCAL density-multiplier constant; do NOT import the client fit ladder — src/client/document/fit.ts and registry.ts pull in react-pdf/pdf.js and are NOT safely importable from src/server. Reuse only `DocumentFormat`, `Paper` from @shared/types.\n2. engine.ts — add optional `budget?: string | null` to buildUserPrompt (append a clearly-labelled budget block AFTER the context block, SAME `!budget` early-return guard so no/empty budget is byte-identical), to TailorEngine.decide, ProviderEngine.decide+attempt, FixtureEngine.decide (IGNORE it — underscore, do NOT add to hashKey), and tailor(). Do NOT modify hashKey (evalcore.ts) and do NOT pass budget to validateNoFabrication.\n3. applications.ts — in the tailor route, derive the budget via deriveContentBudget from the in-scope paper/targetPages/effective-format and pass it as the new tailor() arg (after context).\n\nNOTE (expected, not a bug): in production the route now ALWAYS derives a budget, so ProviderEngine's live user message changes — that is the intended key-gated behavior. The keyless suite stays green because FixtureEngine ignores the budget (keys on jd+entries), so recorded fixtures replay unchanged and the T014 flip path is undisturbed (prompt.ts frozen; budget excluded from the key). Same reasoning as ledger [v3-001] for context.",
      "acceptance": "BASELINE (oracle.md, every ticket): `bun run check` exit 0; `bun run lint` clean (bun run lint:fix/format to fix); `bun run build` ok; full keyless suite green — `vitest run --passWithNoTests` then `playwright test` (chromium). Do NOT run the docker e2e (phase-close only).\nNEW TESTS (keyless — all green under the baseline):\n1. buildUserPrompt byte-identity (extend test/engine.test.ts): buildUserPrompt(jd), (jd, null), (jd, \"\"), and (jd, ctx) with NO budget arg each return EXACTLY today's string (baseline; context path unchanged). With a budget, the block is appended AFTER any context block and contains the budget text.\n2. ProviderEngine prompt spy (extend test/engine.test.ts, mirroring the existing generateObjectMock block): no budget -> callArgs.prompt === buildUserPrompt(jd[, ctx]); with a budget -> prompt contains the derived budget text and still starts with the base+context prefix.\n3. deriveContentBudget CONTRAST (new test/budget.test.ts, pure/keyless): for the same paper+format, targetPages=1 yields a NON-EMPTY budget naming an approximate count STRICTLY SMALLER than targetPages=2 (a 1-page budget must be tighter). This is the anti-gaming teeth — a derivation returning \"\" or a constant FAILS this. Also assert letter vs a4 and/or tighter typography shifts the number in the sane direction.\n4. Route threading (extend test/api.applications-tailor.test.ts using its existing fake/spy engine): tailoring an application whose format implies a 1-page budget passes a budget to the engine whose text matches deriveContentBudget for that app's paper/targetPages/format.\n5. Fixtures replay UNCHANGED: existing FixtureEngine replay tests and recorded-decision fixtures still resolve (hashKey untouched) — full keyless suite stays green with NO fixture edits.\n6. validateNoFabrication still entries-only: budget never threaded into it (call site unchanged); existing fabrication tests stay green.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, not builder self-report) — all green:\ncheck: tsc both tsconfigs exit 0; lint: biome 161 files clean; build: vite exit 0 (1m14s).\n3 changed test files verbose: 40/40 pass — buildUserPrompt byte-identity (undefined/null/\"\", context path, budget-after-context ordering via indexOf), ProviderEngine prompt-spy (no-budget==buildUserPrompt(jd[,ctx]); budget present in prompt), deriveContentBudget contrast (1-page STRICTLY < 2-page bullets+words; a4>letter; smaller body size ->larger; not constant/empty), route threading (budget matches deriveContentBudget for app paper/targetPages/format; 2-page distinctly larger), RED-TEAM #1 updated (empty context still byte-identical prefix, budget appends).\nfull vitest serial (maxForks=1, dodges documented contention flake): 45 files / 323 tests pass.\nplaywright: 10/10 (chromium+auth+applications; docker project NOT run — phase-close only per [v3-004]).\nSCOPE (git status): only the 6 declared files touched (budget.ts+budget.test.ts new; engine.ts/applications.ts/engine.test.ts/api.applications-tailor.test.ts modified). No fixture edits.\nGAMING READ clean: deriveContentBudget is a real heuristic (monotone in targetPages/paper-area/margins/body-size, not constant); buildUserPrompt keeps the `!budget` early-return so no/empty budget is byte-identical and the budget block appends AFTER context; hashKey (evalcore.ts) untouched; validate.ts untouched, validateNoFabrication still called (resume, entries, baseSummary) — budget never reaches grounding.\ncommitted: E7-D1a (see git log)."
    },
    {
      "id": "E7-D1b",
      "title": "Length budget → selection: key-gated eval harness (DEFERRED, T014 pattern)",
      "status": "done",
      "depends_on": [
        "E7-D1a"
      ],
      "files": [
        "scripts/eval-budget.ts"
      ],
      "origin": "decomposed from E7-D1 (spec §28.8-D, key-gated half)",
      "context": "Build the KEY-GATED eval harness for spec §28.8-D, mirroring scripts/eval.ts (the T014 harness). This env has NO GOOGLE_GENERATIVE_AI_API_KEY, so the LIVE comparison is DEFERRED and will NOT be run this epic — recorded honestly, like T014. Deliver a runnable harness (so it CAN be run when a key exists) + a keyless refuse-without-key guard. Do NOT weaken any check to \"pass\" without a key.\n\nMIRROR scripts/eval.ts EXACTLY in shape (read it): reads GOOGLE_GENERATIVE_AI_API_KEY and, if absent, prints a clear key-gated refusal and process.exit(1) — NEVER falls back to FixtureEngine.\n\nTHIS harness (scripts/eval-budget.ts): import deriveContentBudget from ../src/server/tailor/budget (delivered by E7-D1a), ProviderEngine+tailor from ../src/server/tailor/engine, SEED_ENTRIES from ../src/server/seed. Construct a 1-page budget and a 2-page budget for a fixed paper+format via deriveContentBudget, run tailor() live (ProviderEngine, gemini-2.5-flash) over the over-complete seed library for a single fixed JD once per budget, and assert: cut[].length for the 1-page budget > cut[].length for the 2-page budget, and every cut carries a non-empty `reason`. Report PASS/FAIL like eval.ts and process.exit accordingly.\n\nLOCKED (cite oracle.md §28): prompt.ts frozen; validateNoFabrication untouched; PDFs only via @react-pdf/renderer. Do NOT edit engine.ts/budget.ts here — only add the script.",
      "acceptance": "BASELINE: `bun run check` exit 0 (the script type-checks against the real budget+engine imports); `bun run lint` clean. build/vitest/playwright stay green (this ticket adds ONLY scripts/eval-budget.ts and touches no product code — assert no regression).\nKEYLESS refuse-without-key guard (machine-checkable, run it): invoking the script with GOOGLE_GENERATIVE_AI_API_KEY unset exits NON-ZERO and prints a clear \"key-gated, refusing\" message; it must NOT fall back to FixtureEngine and must NOT silently pass.\nDEFERRED (documented, NOT run — no key in this env): the live assertion that the 1-page budget yields more cut[] entries than the 2-page budget, each with a rationale. The coordinator records this deferral in the ledger like T014; do NOT fabricate a result.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator) — all keyless gates green:\ncheck: tsc both tsconfigs exit 0; lint: biome 162 files clean.\nscript type-checks against its REAL import tree (deriveContentBudget/ProviderEngine/tailor/SEED_ENTRIES/DEFAULT_FORMAT@src/shared/format) via tsconfig.server.json extension: exit 0 — proves the harness is wired to real machinery, not stubs.\nREFUSE-WITHOUT-KEY guard (run by coordinator, key unset): prints the key-gated refusal and exits 1; constructs NO FixtureEngine, no silent pass.\nSCOPE: only scripts/eval-budget.ts new; zero product-code change (build/vitest unchanged from E7-D1a HEAD f929a67, 45 files/323 tests).\nGAMING READ clean: real harness mirroring scripts/eval.ts — key-gate first (no fallback), 2x tailor() with budget1/budget2 over SEED_ENTRIES, asserts resume1.cut.length > resume2.cut.length AND every cut.reason non-empty, PASS/FAIL logging + main().catch. No fabricated result.\nDEFERRED (honest, no key in env): the LIVE 1-page>2-page cut-count assertion was NOT run — mirrors the T014 deferral. Harness is runnable the moment a GOOGLE_GENERATIVE_AI_API_KEY exists.\ncommitted: E7-D1b (see git log)."
    },
    {
      "id": "E8-A1",
      "title": "Templates: classic + compact (single-column, ATS-strict)",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/client/document/templates/classic.tsx",
        "src/client/document/templates/compact.tsx",
        "src/client/document/registry.ts",
        "test/document-templates.test.ts",
        "test/document-extraction-invariant.test.ts",
        "test/fit.test.ts"
      ],
      "origin": "spec §28.2 roster (fixed 2026-07-05) / §28.8-E; oracle Phase 7/E8",
      "context": "Add TWO new single-column templates to the registry (spec §28.2's launch roster, fixed 2026-07-05): `classic` — centered profile header (name centered, contact line beneath), hairline horizontal rule under each section heading; `compact` — one-line header (name left, contact right on the same row), tighter section treatment (smaller heading scale / reduced gaps via the template's own composition, NOT by mutating DocumentFormat).\n\nPATTERN TO MIRROR: src/client/document/templates/strict.tsx (read it first) — a template is a function (props: TemplateProps) => ReactElement composing ProfileHeader/SummarySection/SectionBlock etc. from ../sections.tsx into a react-pdf <Document><Page>. Register each in src/client/document/registry.ts's TEMPLATES with manifest { id, name, description, layout: 'single', atsGrade: 'strict', densityLadder: ['comfortable','standard','compact'], densityMultipliers: { comfortable: 1, standard: 0.94, compact: 0.88 }, render }. The declared 'strict' grade must be EARNED: both new templates must pass the §28.6 extraction-order invariant (index-increasing item order in pdf.js extraction).\n\nComposition levers available WITHOUT new features: react-pdf styles in the template file (alignment, rules via border, row layout for the compact header, heading size/letter-spacing scale factors applied to the format-derived styles). If sections.tsx's shared renderers need a narrow composition hook (e.g. an optional headerVariant prop or style override param), extending sections.tsx is IN scope — declare it by adding the file ONLY if needed and keep every existing template's output byte-stable (existing render/extraction tests must pass unchanged). Do NOT fork per-template copies of section renderers.\n\nLOCKED (cite .ailoop/oracle.md — never re-litigate): PDFs are produced ONLY by @react-pdf/renderer; browser printing (window.print()/print.css) is REJECTED. Templates are CODE (react-pdf components), never HTML/CSS or user-authored; ALL templates render every section through the shared section renderers (src/client/document/sections.tsx) — they differ in COMPOSITION only (header treatment, sidebar, rules, heading style), never in features. The renderer NEVER cuts (§28.4): item count is invariant across densities. leadRationale + cut[] never reach the document (§11) — gated by pdf.js text extraction. Any browser-executed render MUST use pdf().toBlob(), never renderToBuffer (react-pdf's browser build stubs it — the E7-C2 escaped-bug lesson). No tag-scoring, no LLM-checks-LLM, no hiring-status vocabulary (cross-cutting invariants).\n\nEXTEND the existing test files (read them first): test/document-templates.test.ts (per-template render/manifest assertions), test/document-extraction-invariant.test.ts (parameterize the strict extraction-order + leadRationale/cut-absent invariant over ALL atsGrade==='strict' templates instead of just 'strict'), test/fit.test.ts (item-count-invariant across densities parameterized over every template in TEMPLATES).",
      "acceptance": "BASELINE (oracle.md): `bun run check` exit 0; `bun run lint` clean; `bun run build` exit 0; `bunx vitest run --passWithNoTests` green; `bunx playwright test` (chromium, non-docker) green. Do NOT run the docker e2e.\nNEW/EXTENDED TESTS (keyless, all green):\n1. Registry: TEMPLATES now has 4 entries (strict, classic, compact, sidebar-left); classic + compact declare layout 'single' + atsGrade 'strict'.\n2. For classic AND compact: fixture resume renders to PDF; pdf.js extraction contains the profile header fields AND every selected item's text; leadRationale/cut[] sentinel strings ABSENT (extend the existing invariant test to iterate all strict templates).\n3. For classic AND compact: extraction order strictly index-increasing (§28.6) — the earned-strict proof.\n4. Item-count invariance: for EVERY template in TEMPLATES (now 4), rendering the fixture resume at each density in its ladder yields the same per-item presence (never cuts, §28.4) — parameterize the existing fit/never-cut guard over the registry.\n5. CONTRAST (anti-gaming — the new templates must actually implement their composition, not just differ trivially): via pdf.js page-1 text-item geometry — (a) classic: the profile NAME text is horizontally CENTERED (mean-x within ~15% of the page midline) while strict's name is left-anchored (mean-x in the left third); (b) compact: the name and the contact line share one header ROW (y-coordinates within one line-height of each other) while in strict/classic the contact sits BELOW the name (distinct y). A copy-paste template that only changes the manifest FAILS both.\n6. All existing tests pass UNCHANGED (strict + sidebar-left output byte-stable; if you extended sections.tsx, prove no existing template's rendered bytes changed).",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator) — all green: check exit 0; biome 164 files clean; build exit 0;\nfull vitest SERIAL 45 files / 335 tests (12 new vs pre-ticket 323); playwright 10/10 non-docker.\nSCOPE: declared files + sections.tsx (explicitly permitted composition hook — ProfileHeader variant 'left'|'centered'|'inline', default 'left' preserves existing markup; contactItems extraction render-identical).\nGAMING READ clean: geometry contrast tests are REAL pdf.js transform assertions (strict name mean-x < pageWidth/3; classic |mean-x - midline| <= 15%; compact name+contact same row y-gap <= ~line-height vs strict/classic stacked) — a manifest-only copy fails. Earned-strict extraction-order parameterized over all strict templates (strict/classic/compact); never-cut invariant parameterized over all 4 registry templates; leadRationale/cut absent for new templates.\nRESIDUAL (flagged, benign): compact.tsx reuses applyDensity from fit.ts, closing an import cycle registry->compact->fit->renderResume->registry. Benign — applyDensity is called at render time only, nothing crosses the cycle at module init; green in Node, browser e2e, and vite build. If it ever bites (HMR/TDZ), extract applyDensity to a leaf module as a mechanical repair.\nBuilder-caught env issue: a stale pre-existing tsx-watch dev server squatted port 8787 and failed its first e2e run with ERR_CONNECTION_REFUSED; killed -> 10/10. Not a code defect.\ncommitted: E8-A1 (see git log)."
    },
    {
      "id": "E8-A2",
      "title": "Templates: banner + sidebar-right (roster complete: 6)",
      "status": "done",
      "depends_on": [
        "E8-A1"
      ],
      "files": [
        "src/client/document/templates/banner.tsx",
        "src/client/document/templates/sidebar-right.tsx",
        "src/client/document/registry.ts",
        "test/document-templates.test.ts",
        "test/document-extraction-invariant.test.ts",
        "test/fit.test.ts",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "spec §28.2 roster (fixed 2026-07-05) / §28.8-E; oracle Phase 7/E8",
      "context": "Complete the template roster to 6 (spec §28.2, fixed 2026-07-05) with TWO templates: `banner` — single-column with a full-bleed header band tinted format.colors.primary (name/contact set on the band in a contrasting ink; section headings tinted primary), layout 'single', atsGrade 'strict' (a filled rect is a drawing op — text extraction order is unaffected; the invariant test EARNS the grade); `sidebar-right` — the mirror of the existing sidebar-left template (sidebar column on the RIGHT), layout 'sidebar-right', atsGrade 'good'.\n\nPATTERN: src/client/document/templates/strict.tsx and sidebar.tsx (read both). E8-A1 (already merged) added classic.tsx + compact.tsx — follow the same registry/manifest shape: densityLadder ['comfortable','standard','compact'], densityMultipliers { comfortable: 1, standard: 0.94, compact: 0.88 }. For sidebar-right, prefer extracting/reusing sidebar.tsx's composition with a mirrored column order over copy-paste — but do NOT change sidebar-left's rendered output (byte-stable guard).\n\nsrc/client/document/registry.ts's effectiveAtsGrade already caps any layout !== 'single' at 'good' — sidebar-right needs no special-casing; assert it.\n\nLOCKED (cite .ailoop/oracle.md — never re-litigate): PDFs are produced ONLY by @react-pdf/renderer; browser printing (window.print()/print.css) is REJECTED. Templates are CODE (react-pdf components), never HTML/CSS or user-authored; ALL templates render every section through the shared section renderers (src/client/document/sections.tsx) — they differ in COMPOSITION only (header treatment, sidebar, rules, heading style), never in features. The renderer NEVER cuts (§28.4): item count is invariant across densities. leadRationale + cut[] never reach the document (§11) — gated by pdf.js text extraction. Any browser-executed render MUST use pdf().toBlob(), never renderToBuffer (react-pdf's browser build stubs it — the E7-C2 escaped-bug lesson). No tag-scoring, no LLM-checks-LLM, no hiring-status vocabulary (cross-cutting invariants).\n\nEXTEND the same test files E8-A1 extended (they are parameterized over the registry now — adding the 2 registry entries should light them up automatically; verify and fill any gap).",
      "acceptance": "BASELINE (oracle.md): `bun run check` exit 0; `bun run lint` clean; `bun run build` exit 0; `bunx vitest run --passWithNoTests` green; `bunx playwright test` (chromium, non-docker) green. Do NOT run the docker e2e.\nNEW/EXTENDED TESTS (keyless, all green):\n1. Registry: 6 templates total; ≥2 single-column strict; BOTH sidebar orientations present (layout 'sidebar-left' and 'sidebar-right') — the §28.2 roster shape, asserted.\n2. banner: extraction contains profile + every item, strictly index-increasing order (§28.6 earned-strict), leadRationale/cut absent. The primary-tint must come from format.colors.primary: rendering banner with two different primary values yields different PDF bytes (the tint is live, not hardcoded).\n3. sidebar-right: extraction contains profile + every item (presence, not strict order); leadRationale/cut absent; effectiveAtsGrade(sidebar-right, any format) === 'good'.\n4. MIRROR CONTRAST (anti-gaming): sidebar-left and sidebar-right render pairwise non-identical output for the same fixture resume, AND their sidebar content sits on opposite halves — assert via pdf.js text-item x-coordinates (sidebar-section text mean-x < page midline for left, > for right). A sidebar-right that is a re-labeled sidebar-left FAILS.\n5. Item-count invariance across densities for all 6 templates (parameterized guard from E8-A1 now covers 6).\n6. All existing tests pass UNCHANGED; sidebar-left's rendered bytes are unchanged (byte-stable guard).",
      "attempts": [
        {
          "n": 1,
          "failed": "independent re-verify: playwright applications project RED (in isolation too, not the contention flake). strict-mode violation at applications.spec.ts:229 — getByRole button /Sidebar/ now resolves to 2 cards (Sidebar, Sidebar Right).",
          "hypothesis": "roster growth to 6 made an existing e2e selector ambiguous. Builder missed it by running only --project=chromium (8 tests), skipping the applications+auth projects — an acceptance shortcut.",
          "fixNote": "disambiguate the selector in test/e2e/applications.spec.ts (added to declared files) so it uniquely matches the sidebar-left card (e.g. name /^Sidebar ATS/ or exact accessible-name prefix), keeping the assertion semantics (templateId sidebar-left). Then run the FULL non-docker suite: bunx playwright test (all projects), plus vitest. No product-code change expected."
        }
      ],
      "evidence": "ACCEPTED on attempt 2. Attempt 1 failed the INDEPENDENT re-verify: builder ran only --project=chromium; the full run had the applications e2e RED (strict-mode selector collision — /Sidebar/ matched both Sidebar and the new Sidebar Right card; reproduced in isolation, NOT the contention flake). Fix: /^Sidebar ATS/ accessible-name prefix in 2 places, assertion semantics untouched (templateId 'sidebar-left' + aria-pressed persistence).\nINDEPENDENT re-verify round 2 (coordinator) — all green: check 0; lint clean (166 files); build 0 (attempt-1 evidence, no product change since); vitest SERIAL 45 files / 347 tests; playwright FULL non-docker 10/10 including applications.\nSCOPE: banner.tsx + sidebar-right.tsx new; sidebar.tsx (extracted renderSidebarComposition(props, side) — left keeps original composition order/paddings, byte-stable); sections.tsx (undefined-preserving ink prop for the band); registry.ts (2 manifests); 2 unit test files + the declared e2e selector fix.\nGAMING READ clean: banner tint is LIVE format.colors.primary (two primaries => different bytes, tested) with luminance-based contrastInk; sidebar mirror is genuine (mean-x opposite halves of midline, tested; pairwise non-identical bytes); registry roster shape asserted (6, >=2 strict single, both orientations); parameterized never-cut + extraction invariants auto-picked-up the 2 new templates.\ncommitted: E8-A2 (see git log)."
    },
    {
      "id": "E8-B1",
      "title": "Live mini-render thumbnails in the template picker",
      "status": "done",
      "depends_on": [
        "E8-A2"
      ],
      "files": [
        "src/client/document/thumbnail.tsx",
        "src/client/document/sampleResume.ts",
        "src/client/components/TemplatePicker.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "test/template-thumbnails.test.tsx",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "spec §28.2 live-preview decision (2026-07-05) / §28.8-E; oracle Phase 7/E8",
      "context": "Replace the TemplatePicker's text-only cards with LIVE mini-render thumbnails (spec §28.2, decided 2026-07-05: previews are live renders of THIS application's tailored resume, never static images).\n\nMACHINERY TO MIRROR: src/client/components/DocumentPreview.tsx (read it first) — usePDF({ document: renderResumeDocument({resume, profile, paper, format}) }) then paint page 1 onto a canvas via lazily-imported pdfjs-dist (GlobalWorkerOptions.workerSrc set from the bundled worker URL; render failure leaves the canvas blank rather than crashing — keep that posture). Browser render MUST go through the pdf()/usePDF blob path, never renderToBuffer (E7-C2 lesson).\n\nBUILD: (1) src/client/document/thumbnail.tsx — a TemplateThumbnail component: props {resume, profile, paper, format, templateId}; renders page 1 at small scale (~0.35) into a canvas; renders LAZILY (only when mounted/visible — the picker is inside a card that always mounts, so gate on visibility or first paint via IntersectionObserver or simple mount-time is acceptable given only 6) and SERIALIZED (a module-level render queue so 6 thumbnails don't render concurrently); cache the rendered blob/dataURL per (templateId + JSON of format-minus-templateId + paper + resume identity) in a module-level Map so re-opens repaint instantly. (2) src/client/document/sampleResume.ts — a small deterministic SAMPLE_RESUME (TailoredResume) + SAMPLE_PROFILE constant with obviously-generic content (e.g. 'Alex Sample'), used ONLY when the application has no tailored resume; the card then shows a visible 'Sample content' Badge. (3) TemplatePicker.tsx — each card gains the thumbnail (canvas) above the existing name/description/ATS badge; card semantics (aria-pressed select, effectiveAtsGrade badge, caveat, readOnly) unchanged. ApplicationDetail passes the resume/profile/paper the picker needs (it already has them — see DocumentPreview usage).\n\nLOCKED (cite .ailoop/oracle.md — never re-litigate): PDFs are produced ONLY by @react-pdf/renderer; browser printing (window.print()/print.css) is REJECTED. Templates are CODE (react-pdf components), never HTML/CSS or user-authored; ALL templates render every section through the shared section renderers (src/client/document/sections.tsx) — they differ in COMPOSITION only (header treatment, sidebar, rules, heading style), never in features. The renderer NEVER cuts (§28.4): item count is invariant across densities. leadRationale + cut[] never reach the document (§11) — gated by pdf.js text extraction. Any browser-executed render MUST use pdf().toBlob(), never renderToBuffer (react-pdf's browser build stubs it — the E7-C2 escaped-bug lesson). No tag-scoring, no LLM-checks-LLM, no hiring-status vocabulary (cross-cutting invariants).\n\nTESTING HONESTY (jsdom cannot paint pdf.js — the A4/B1e/C1c posture): unit tests (test/template-thumbnails.test.tsx, jsdom) cover the pure parts — cache key derivation (format-minus-templateId: two formats differing only in templateId share nothing; differing in primary color miss), queue serialization (two concurrent thumbnail renders run one-at-a-time — assert via instrumented fake render fn), sample fallback selection (no resume ⇒ SAMPLE_RESUME + badge rendered). The REAL paint proof lives in the e2e (extend test/e2e/applications.spec.ts): after tailoring, the picker shows a painted (non-blank) canvas per template card; two different templates' canvases differ pixelwise; changing colors.primary in the design panel repaints a thumbnail with different pixels (the anti-stock-image proof — use the page's real canvas.toDataURL from the browser context and compare); an application that has NOT been tailored shows the 'Sample content' badge on its picker cards.\n\nE2E note: canvas paint is async — poll/expect.toPass with generous timeout rather than fixed sleeps; the existing applications.spec.ts has patterns for waiting on the pdf.js preview.",
      "acceptance": "BASELINE (oracle.md): `bun run check` exit 0; `bun run lint` clean; `bun run build` exit 0; `bunx vitest run --passWithNoTests` green; `bunx playwright test` (chromium, non-docker) green. Do NOT run the docker e2e.\nNEW TESTS (all green):\n1. Unit (jsdom): cache key excludes templateId's own card identity correctly (same format+paper+resume across two templateIds ⇒ two distinct cache entries; changing colors.primary ⇒ cache miss; identical inputs ⇒ hit), render queue is serialized (concurrent mounts render sequentially), no-resume ⇒ SAMPLE_RESUME used + 'Sample content' badge in the DOM.\n2. E2E (real browser, chromium): after tailoring, every template card's thumbnail canvas is PAINTED (non-blank — toDataURL differs from a blank canvas of the same size); thumbnails of two different templates differ pixelwise; changing the primary color in the design panel causes a thumbnail's pixels to change (poll until repaint). Untailored application: picker cards show the 'Sample content' badge.\n3. Existing picker behavior unchanged: selecting a card still sets format.templateId only (existing tests/e2e stay green); readOnly (locked) still disables selection.\n4. No new PDF path: grep-level check — no renderToBuffer call in any src/client file added/changed by this ticket (browser renders go through usePDF/pdf().toBlob).",
      "attempts": [
        {
          "n": 1,
          "failed": "builder died twice mid-verification (transient API error, then session limit) — implementation complete, e2e never run by builder. Coordinator ran the gates: check/lint/build/vitest 358/358 green; playwright applications spec RED deterministically (3/3 retries, in isolation) at the (4b-color) primary-repaint poll, spec line ~330: expect.poll(thumbnailDataUrl).not.toBe(before) times out.",
          "hypothesis": "the poll uses the default 5s expect timeout (no expect block in playwright.config.ts) while a primary-color change invalidates ALL SIX thumbnail cache keys; the serialized render queue + requestIdleCallback (timeout 1000ms) deferral per card makes sidebar-left repaint later than 5s. The check is correct; its deadline is not. Same class as the paint helper, which already uses { timeout: 15000 }.",
          "fixNote": "give the (4b-color) repaint poll an explicit { timeout: 15000 } (and optionally scroll the sidebar-left card into view first) — do NOT weaken the assertion (pixels must still change). Then run the FULL bunx playwright test (all non-docker projects) + confirm applications passes 3x or in isolation."
        }
      ],
      "evidence": "ACCEPTED on attempt 2 (unusual path: builder died twice mid-verification — transient API error, then session limit — with implementation complete; coordinator ran the verification, found ONE deterministic e2e red, diagnosed it, resumed the builder for the fix).\nAttempt-1 red: the (4b-color) primary-repaint poll used the default 5s expect timeout while a color change re-renders ALL SIX thumbnails through the serialized queue (+ up-to-1s idle deferral each) — check right, deadline wrong. Fix: explicit { timeout: 15000 } (assertion strength unchanged); builder also switched pdf.js to bytes-direct getDocument({data}) (no object URL) eliminating the revoke race for thumbnails.\nINDEPENDENT re-verify (coordinator, final tree) — all green: check 0; lint clean (169 files); build 0; vitest SERIAL 46 files / 358 tests (11 new thumbnail unit tests: cache-key axes incl. templateId-excluded-from-format, queue maxActive===1 + rejection non-blocking, sample fallback + badge, readOnly); playwright FULL non-docker 10/10 (untailored: exactly 6 'Sample content' badges; tailored: 0 badges, all 6 canvases painted (opaque non-white + off-default dims), strict vs sidebar-left differ pixelwise, primary-color change repaints — the anti-stock-image proof).\nGAMING READ clean: render path is pdf().toBlob()/renderResumeToBlob + pdf.js bytes-direct; grep renderToBuffer over changed client files = 1 comment, 0 call sites. Allowlist widenings JUDGED LEGITIMATE (both exact-match): 409 scoped pre-login (documented setup->409->login probe; retries need it); net::ERR_FILE_NOT_FOUND is react-pdf usePDF's internal previous-blob-url revoke race (pre-existing, E7-C1b had it download-scoped; six serialized renders made it fire at every swap point) — each step keeps independent POSITIVE pixel/byte assertions so the allowlist cannot mask a real render failure; real asset/API failures log status-code texts and still fail.\nRESIDUAL (flagged): react-pdf usePDF's revoke race logs harmless console noise in production too; cosmetic, library-internal. Candidate cosmetic cleanup ticket if it ever matters.\ncommitted: E8-B1 (see git log)."
    },
    {
      "id": "E8-C1",
      "title": "Dedicated template gallery view (full-screen browse)",
      "status": "done",
      "depends_on": [
        "E8-B1"
      ],
      "files": [
        "src/client/components/TemplateGallery.tsx",
        "src/client/components/TemplatePicker.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "test/template-gallery.test.tsx",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "spec §28.2 gallery decision (2026-07-05) / §28.8-E; oracle Phase 7/E8",
      "context": "Add the dedicated gallery view (spec §28.2, decided 2026-07-05): a full-screen browse surface — a shadcn Dialog or Sheet opened from the Design card (e.g. a 'Browse templates' button next to the inline picker) — showing ONE LARGE live preview per template (reuse E8-B1's TemplateThumbnail at a larger scale; same cache, so already-rendered thumbnails upgrade cheaply or re-render at gallery scale — either is fine as long as renders stay serialized) with the template name, description, and the SAME effectiveAtsGrade badge + Workday/Taleo caveat the picker shows. Selecting a card sets format.templateId ONLY (mirror TemplatePicker's onChange contract), closes the gallery, and the inline picker + document preview reflect the choice.\n\nIA/§26 guard: the gallery is NOT a new route — it is a dialog within ApplicationDetail (a route would orphan under the no-orphan-routes invariant and has no resume to preview). readOnly (locked application) shows the gallery read-only: cards visible, selection disabled — same posture as TemplatePicker's readOnly.\n\nLOCKED (cite .ailoop/oracle.md — never re-litigate): PDFs are produced ONLY by @react-pdf/renderer; browser printing (window.print()/print.css) is REJECTED. Templates are CODE (react-pdf components), never HTML/CSS or user-authored; ALL templates render every section through the shared section renderers (src/client/document/sections.tsx) — they differ in COMPOSITION only (header treatment, sidebar, rules, heading style), never in features. The renderer NEVER cuts (§28.4): item count is invariant across densities. leadRationale + cut[] never reach the document (§11) — gated by pdf.js text extraction. Any browser-executed render MUST use pdf().toBlob(), never renderToBuffer (react-pdf's browser build stubs it — the E7-C2 escaped-bug lesson). No tag-scoring, no LLM-checks-LLM, no hiring-status vocabulary (cross-cutting invariants).\n\nDesign language: follow the softer-SaaS conventions already in the app (tinted bg, white cards, soft shadows, 8px radius — see existing components; reuse Card/Badge/Dialog primitives from ./ui).\n\nTESTING (same jsdom-honesty posture as E8-B1): unit (test/template-gallery.test.tsx, jsdom) — gallery lists all 6 registry templates with name/badge; clicking a card calls onChange with {...format, templateId} and ONLY templateId changed; readOnly disables selection. E2E (extend test/e2e/applications.spec.ts): open gallery from the Design card, 6 cards visible with painted canvases (reuse E8-B1's paint helpers), select 'sidebar-right', gallery closes, the inline picker shows sidebar-right selected, RELOAD the page and assert the selection persisted (format round-trip through the API).",
      "acceptance": "BASELINE (oracle.md): `bun run check` exit 0; `bun run lint` clean; `bun run build` exit 0; `bunx vitest run --passWithNoTests` green; `bunx playwright test` (chromium, non-docker) green. Do NOT run the docker e2e.\nNEW TESTS (all green):\n1. Unit (jsdom): gallery renders one card per registry template (6) with name + effectiveAtsGrade badge (sidebar cards show 'good' + caveat); selecting calls onChange({...format, templateId: id}) with every other format field untouched (deep-equal); readOnly blocks selection.\n2. E2E (real browser): 'Browse templates' opens the gallery; all 6 cards show painted (non-blank) canvases; selecting sidebar-right closes the gallery, updates the inline picker selection AND the main document preview; after a page reload the application still has templateId 'sidebar-right' (persisted via the format API).\n3. Locked application: gallery opens read-only, selection has no effect (e2e or unit).\n4. No new route added (client router untouched — the no-orphan-routes §26 invariant stays green); existing picker/design-panel tests unchanged.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, final tree) — all green: check 0; lint clean (171 files); build 0; vitest SERIAL 47 files / 363 tests (5 new gallery unit tests: 6 cards + badges/caveat, sample fallback, onChange {...format, templateId} deep-equal others-untouched, readOnly no-op); playwright FULL non-docker 10/10.\nE2E (4d) gallery scenario: Browse templates opens a dialog; all 6 cards painted (paint helper scoped to the dialog Locator — pre-empting the inline-picker data-template-id ambiguity, the exact strict-mode class that bit E8-A2); selecting Sidebar Right PUTs templateId==='sidebar-right' with earlier (4b) color (#14532d) and font (arimo) fields asserted UNTOUCHED; gallery closes; inline picker aria-pressed reflects it; persists across reload. Slotted after (4b)/(4c) and before template-agnostic re-tailor/lock so it disturbs nothing.\nGAMING READ clean: gallery is a Dialog (router untouched — §26 no-orphan-routes); reuses TemplateThumbnail with a backward-compatible scale prop correctly folded into the cache key; no renderToBuffer call sites; no weakened assertions/allowlists; TemplatePicker untouched (gallery owns its trigger).\nMINOR inefficiency noted (not a defect): the blob cache keys on scale though PDF bytes are scale-independent — two scales re-render the same PDF once each. Cosmetic.\ncommitted: E8-C1 (see git log)."
    },
    {
      "id": "E9-F0a",
      "title": "DocumentFormat v2 (all §31.2 axes) + v1→v2 migration, additive shared layer",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/shared/format-v2.ts",
        "src/shared/types.ts",
        "test/format-v2.test.ts"
      ],
      "origin": "spec §31.1/§31.2/§31.6 F0 (E9 intake [v3-038])",
      "context": "Epic E9 (spec §31, read it in full — /workspace/spec.md lines 939-1133) retires the six code templates for ONE parameterized engine; DocumentFormat v2 is the only varying input. THIS ticket is the pure shared layer, ADDITIVE ONLY — do not change the existing DocumentFormat type or any consumer; the cutover is E9-F0d. Create src/shared/format-v2.ts exporting: (1) type DocumentFormatV2 covering EVERY §31.2 axis with the EXACT enums/bounds (Document: pageFormat a4|letter, dateFormat = the 12 listed presets; Layout: columns one|two|mix, headerPosition top|left|right, sidebarWidthPct 25-40, per-section column/position assignment, manual page-break tokens; Type scale: bodySize 9-12 + name +4..+12 / title 0..+4 / sectionHeading 0..+3 / entryHeader 0..+2 offsets; Spacing: lineHeight 1.15-1.5, elementSpacing 0-4, marginsMm {x:10-28, y:10-28}; Entries; Headings: 8 styles + capitalize|uppercase + icons none|outline|filled; Fonts: body FontId + name slot 'same-as-body'|display FontId; Colors: area full-page|header|border, mode single|multi {text,background,accent}, border {size s|m|l, sides}, accentPlacement 9 boolean toggles; Header; Photo (carry v1 + crop/zoom fields); Links; Footer; Per-section display per §31.2 last bullet incl. level display text|dots|bar + 5 renamable labels — labels are FORMAT, level values are CONTENT and NOT in this type). Field naming is yours; values/bounds are the spec's contract — no extra values, no omissions. (2) zod schema formatV2Schema enforcing every bound (parse rejects out-of-range). (3) DEFAULT_FORMAT_V2 (migration of today's DEFAULT_FORMAT). (4) migrateFormat(v1: DocumentFormat): DocumentFormatV2 — pure, deterministic, idempotent-safe via isFormatV2() discriminator; maps each of the six templateIds (strict/classic/compact/banner/sidebar-left/sidebar-right — read src/client/document/templates/*.tsx + registry.ts for their actual look) to the v2 axis values reproducing that template's look (e.g. classic ⇒ headerAlignment center + heading underline-rule; banner ⇒ colors.area header; sidebar-left ⇒ columns two + headerPosition left + sidebarWidthPct matching the current template's width; compact ⇒ one-row header). marginX/marginY pt → mm (1pt=0.352778mm, round 1dp, clamp 10-28). Preserve typography/colors/photo/sections. Also migrate templateId → presetId string. Keep pt-vs-mm and every conversion in ONE table-like place so F0b can import the same constants. Frozen (oracle.md 'Phase 8' locks): bounded axes only; level VALUES excluded from format; §31.2 deferrals (custom date strings etc.) NOT typed in. RED-TEAM SHARPENING ([v3-042]): presetId is PROVENANCE, not a render input — DocumentFormatV2 may carry an optional presetId?: string used ONLY by UI to highlight which preset the current axes came from; the engine must never read it (F0b enforces by grep) and no axis behavior may key on it. Migration fixtures must NOT be authored by pasting migrateFormat output (circularity): anchor load-bearing values to the template sources.",
      "acceptance": "- bun run check → 0\n- bun run lint → 0\n- bun run build → 0\n- vitest run --passWithNoTests green (serial fallback if parallel contention flakes)\n- playwright test (chromium) green — full suite, no skips introduced\n- test/format-v2.test.ts: fixture-EXACT migration — for each of the six v1 template default configs (templateId + DEFAULT_FORMAT fields), migrateFormat returns the exact expected v2 object (toEqual on full literal fixtures, not spot fields)\n- idempotence: isFormatV2 discriminates; migrating an already-v2 value is a no-op or type-rejected (state which, test it)\n- zod bounds: for EVERY numeric axis a parse just-inside passes and just-outside fails (sidebarWidthPct 24/25/40/41, bodySize 8/9/12/13, lineHeight 1.14/1.5/1.51, marginsMm 9/10/28/29, elementSpacing -1/0/4/5, offsets at each stated bound); every enum axis rejects an un-specced value (e.g. dateFormat 'DD-MM-YYYY', headingStyle 'shadow')\n- dateFormat enum has EXACTLY the 12 spec values, headingStyle EXACTLY the 8, accentPlacement EXACTLY 9 toggles (test asserts counts + literal sets)\n- existing suite untouched and green (additive ticket — zero existing-file behavior change; types.ts may only re-export)\n- EVERY enum axis pinned by literal-set test (not just the three): entries structure(2), date&location placement(3) + order, subtitle(2), listStyle(2), per-element font styles(3 each), heading capitalization(2), heading icons(3), color area(3), color mode(2), border size(3), header alignment(2), details arrangements(3), separator(3), contact icon styles(7), name/title weight(2), title position(2), section layout(5: grid|rows|compact|bubble|level), level display(3), grid columns 1-4 — counts AND literal values asserted\n- zod parse REJECTS an object missing each top-level group (one test per group, loop is fine)\n- migration anchor assertions as NAMED LITERALS traceable to template source (independent of the full-object fixtures): sidebar presets' sidebarWidthPct equals the width constant in templates/sidebar.tsx; classic ⇒ headerAlignment 'center'; compact ⇒ the one-row header value; banner ⇒ colors.area 'header'",
      "attempts": [],
      "evidence": "Independent re-verify 2026-07-07: check 0 · lint 0 (biome 173 files) · build 0 (NODE_OPTIONS=--max-old-space-size=1024; unbounded build SIGKILLs on clean HEAD too — env OOM, mechanical amendment [v3-043]) · vitest 48 files/467 tests green (+104 new, 363 pre-existing intact) · scope: git diff = exactly {format-v2.ts new, types.ts +5 re-export, format-v2.test.ts new} · gaming read clean: fixtures hand-authored with per-field source-line comments (not migrateFormat output), all enum axes literal-set pinned, v1 FontId roster (6) verified subset of BODY_FONT_IDS. Judgment: migrateFormat intentionally does NOT stamp presetId (customized v1 formats are not presets); presets.ts self-identifies in F0b."
    },
    {
      "id": "E9-F0b",
      "title": "Engine core: one composition renders the four single-column presets (strict/classic/compact/banner)",
      "status": "done",
      "depends_on": [
        "E9-F0a"
      ],
      "files": [
        "src/client/document/engine/",
        "src/client/document/presets.ts",
        "test/engine-single-column.test.ts"
      ],
      "origin": "spec §31.1/§31.6 F0 (E9 intake [v3-038])",
      "context": "THE epic risk (spec §31.6 F0): ONE react-pdf composition, parameterized by DocumentFormatV2 (E9-F0a, done — src/shared/format-v2.ts), must reproduce the retired templates' looks. This ticket: the engine core + the four SINGLE-COLUMN presets. Build src/client/document/engine/ (new dir, your structure — e.g. index.tsx composing header/body from format) rendering a full react-pdf Document from { resume, profile, paper, format: DocumentFormatV2 }, consuming the v2 axes needed for single-column parity: headerAlignment/one-row header, heading treatments used by the four looks, colors.area header (banner band with auto-contrast ink — port the luminance logic from templates/banner.tsx), type scale, spacing, margins. Reuse the SHARED section renderers (src/client/document/sections.tsx) — the engine composes, sections render (registry pattern, oracle.md E7 locks). Axes the four presets don't exercise (two-column, borders, footers, per-section displays...) may be unimplemented BUT must not crash: unhandled axis values render as the default look for now (F1-F4 land them); document which in a code comment at the engine entry. src/client/document/presets.ts: export the six preset v2 configs = migrateFormat over each v1 template default (import the fn; do NOT hand-author drifted copies); this ticket TESTS the four single-column ones. ADDITIVE: templates/*.tsx and registry.ts stay untouched (cutover is E9-F0d); the engine ships alongside. Density ladder: apply the existing fit.ts multipliers to the v2 type/spacing axes uniformly (§31.3: ladder scales v2 axes). Frozen: react-pdf only; renderer never cuts; leadRationale/cut never rendered; one engine — no per-preset render functions, presets are DATA. RED-TEAM SHARPENING ([v3-042]): the engine must be a genuine parameterization, NOT a dispatcher over the six looks — no imports of templates/ or registry.ts inside engine/, no reading of format.presetId, and off-diagonal axis combinations must work (that is what distinguishes an engine from six templates in a trenchcoat). COORDINATOR NOTE post-F0a ([v3-043]): migrateFormat deliberately does not stamp presetId; presets.ts must set presetId on each of its six configs itself (they genuinely ARE presets). Reuse F0a exports: TEMPLATE_V2_OVERLAYS, DEFAULT_FORMAT_V2, the conversion-constants table, formatV2Schema.",
      "acceptance": "- bun run check → 0\n- bun run lint → 0\n- bun run build → 0\n- vitest run --passWithNoTests green (serial fallback if parallel contention flakes)\n- playwright test (chromium) green — full suite, no skips introduced\n- test/engine-single-column.test.ts, patterned on test/document-extraction-invariant.test.ts + document-templates.test.ts (read both first):\n- for EACH of the 4 presets rendered BY THE ENGINE: pdf.js extraction contains profile header + every item.text in strict index-increasing order; SENTINEL leadRationale/cut strings absent\n- geometry contrasts THROUGH THE ENGINE: classic-preset name mean-x ≈ page horizontal center while strict-preset name is left-anchored (assert both, contrast not existence); compact-preset name and first contact fragment share a text-line y while strict's do not; banner-preset render includes a header-band paint with the format's accent + contrast ink (port/adapt the existing banner tint assertion)\n- never-cut: for each of the 4 presets, item count in extraction is invariant across the full density ladder on a growing fixture\n- presets.ts: six configs exist, EACH deep-equals { ...migrateFormat(<its v1 default>), presetId: <id> } (toEqual — presets are migrations plus self-identity, not forks)\n- engine is one composition: grep-style assertion or structural test that presets carry NO render functions (data only)\n- existing suite (old templates) untouched and green\n- grep guards: `grep -rn \"templates/\\|registry\" src/client/document/engine/` → 0 hits; `grep -rn \"presetId\" src/client/document/engine/` → 0 hits\n- OFF-DIAGONAL composition test: strict preset with ONLY colors.area flipped to 'header' → band paint present AND name still left-anchored AND extraction order still index-increasing (a six-look dispatcher cannot produce this combination)\n- density is REAL, not vacuous: the growing fixture actually exercises the ladder (fitToPages resolves 'compact' for the large fixture — fit.test.ts lines 62-84 pattern) AND page count at forced 'compact' < at 'comfortable'; item-count invariance asserted at EACH forced density\n- unhandled-axis smoke: for every enum axis of DocumentFormatV2, each value applied over DEFAULT_FORMAT_V2 renders to a nonempty PDF buffer (no throw)\n- test/engine-single-column.test.ts imports the engine (src/client/document/engine/) and has ZERO imports of registry.ts or templates/ (renders must go through the engine, not the old path)",
      "attempts": [],
      "evidence": "Independent re-verify 2026-07-07: check 0 · lint 0 (180 files) · build 0 (bounded heap [v3-043]) · vitest 49 files/518 tests · playwright 10/10 · scope: exactly engine/ (5 files) + presets.ts + test/engine-single-column.test.ts · grep guards re-run clean (no templates//registry/presetId in engine/) · gaming read: genuine parameterization (band wraps any header — off-diagonal is structural, not special-cased); coordinator independently REPRODUCED the ibm-plex-mono fontkit crash on the untouched v1 strict path (vitest probe: sans renders, mono dies in TTFGlyph._getMetrics) — builder claim TRUE, exclusion legitimate, escaped-bug rule applied as E9-R1."
    },
    {
      "id": "E9-F0c",
      "title": "Engine two-column: sidebar left/right presets + sidebar width axis",
      "status": "done",
      "depends_on": [
        "E9-F0b"
      ],
      "files": [
        "src/client/document/engine/",
        "src/client/document/presets.ts",
        "test/engine-two-column.test.ts",
        "test/fixtures/pre-e9-formats/"
      ],
      "origin": "spec §31.1/§31.6 F0 (E9 intake [v3-038])",
      "context": "Extend the E9-F0b engine (src/client/document/engine/) with two-column layout: columns 'two', headerPosition 'left'|'right' (sidebar side), sidebarWidthPct 25-40 honored as real geometry. Reproduce the retired sidebar templates' look (read templates/sidebar.tsx + sidebar-right.tsx: which sections go sidebar vs main, contact placement, width) so the sidebar-left and sidebar-right presets in src/client/document/presets.ts render at parity through the ONE engine. Column assignment: derive the same section split the v1 templates used, expressed via the v2 per-section placement axis with a sensible default (spec §31.2 Layout: per-section column assignment; the presets pin the v1 split). 'mix' (band + two columns) is F1, NOT here — but don't paint yourself into a corner: the column machinery should not assume the header always spans a column. Still ADDITIVE (templates/registry untouched; cutover is E9-F0d). Frozen: one engine (no separate two-column composition function forked from the single-column path — parameterize); never cuts; react-pdf only. RED-TEAM SHARPENING ([v3-042]): also CAPTURE the pre-cutover ground truth for F0d — commit test/fixtures/pre-e9-formats/: for each of the six templates, the v1 format JSON exactly as today's code would store it (serialized through the CURRENT zod/API shape), plus one lockedFormat wrapper object and one settings.defaultFormat value. These are the REAL pre-E9 row shapes F0d's migration test must consume; F0d is forbidden from editing them. ESCAPED-GAP ADDITION ([v3-044]): the committed pre-e9 fixture set MUST include at least one v1 format with NON-EMPTY sections (e.g. { skill: { columns: 2 } }) and one with a non-default photo config — F0a's migrateFormat currently ignores v1 sections.columns (all its fixtures had sections: {}), and F0d's migration work must be forced to face that by real fixtures.",
      "acceptance": "- bun run check → 0\n- bun run lint → 0\n- bun run build → 0\n- vitest run --passWithNoTests green (serial fallback if parallel contention flakes)\n- playwright test (chromium) green — full suite, no skips introduced\n- test/engine-two-column.test.ts:\n- sidebar-left preset via engine: mean-x of sidebar-section text < mean-x of main-section text; sidebar-right preset: the inequality FLIPS (the E8 sidebar mean-x contrast, now through one engine)\n- sidebarWidthPct is real geometry: same preset at 25 vs 40 moves the main-column left edge in the stated direction by a measured amount (assert direction + nonzero delta, not exact pt)\n- extraction over both sidebar presets contains profile + EVERY selected item.text (presence for two-column; order not required — they are not strict)\n- never-cut across the full ladder for both presets\n- both presets === migrateFormat(<their v1 defaults>) (extends the F0b toEqual test to all six)\n- existing suite untouched and green\n- test/fixtures/pre-e9-formats/ committed, produced via the CURRENT (pre-cutover) format schema — six template defaults + a lockedFormat wrapper + a settings.defaultFormat, each parsing under today's zod schema (asserted in a test)\n- sidebar width delta magnitude: the 25%→40% main-column edge shift is within ±25% of the expected 0.15 × content-width in pt (not merely nonzero)\n- density non-vacuous for both sidebar presets (same fitToPages-resolves-compact + page-count-shrinks pattern as F0b)\n- test/engine-two-column.test.ts imports the engine only (zero registry/templates imports)",
      "attempts": [],
      "evidence": "Independent re-verify 2026-07-07: composite chain exit 0 — check 0 · lint 0 (191 files) · build 0 (bounded heap) · vitest green (50 files/542 per builder, chain exit 0) · playwright 10/10 · scope: exactly engine/document.tsx + test/engine-two-column.test.ts + test/fixtures/pre-e9-formats/ (10 files) · gaming read: in-place columns branch of the ONE composition (no fork), sidebarWidthPct = content-width geometry with ±25% magnitude bound, mean-x contrast flips left/right, fixtures parse under TODAY'S documentFormatZ incl. non-empty-sections {skill:{columns:2}} + non-default-photo. Honest gap documented in-code: columns two + header band renders bandless (unhandled off-diagonal, F3 wires it)."
    },
    {
      "id": "E9-F0d",
      "title": "Cutover: presets-through-one-engine replace templates; stored v1 formats migrate",
      "status": "decomposed",
      "depends_on": [
        "E9-F0c"
      ],
      "files": [
        "src/shared/types.ts",
        "src/shared/format.ts",
        "src/shared/format-v2.ts",
        "src/shared/schema.ts",
        "src/client/document/",
        "src/client/components/DesignPanel.tsx",
        "src/client/components/TemplatePicker.tsx",
        "src/client/components/TemplateGallery.tsx",
        "src/client/components/AtsView.tsx",
        "src/client/components/DocumentPreview.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/components/LayoutEditor.tsx",
        "src/server/",
        "test/",
        "drizzle/"
      ],
      "origin": "spec §31.1 migration + §31.6 F0 close (E9 intake [v3-038])",
      "context": "The F0 cutover, LARGE AND FLAGGED: if this exceeds one session return tooBig with a proposed split (suggested seam: (1) client render-path cutover + template deletion + test reparameterization, (2) server/API stored-format migration + e2e) rather than half-building. Goal: DocumentFormatV2 becomes THE DocumentFormat everywhere; the engine (src/client/document/engine/, done E9-F0b/c) is the ONLY render path; src/client/document/templates/ is DELETED; registry.ts becomes a PRESET registry (six presets from presets.ts with id/name/description/layout/atsGrade metadata — keep the declared-grade fields and effectiveAtsGrade semantics; the pure atsGrade(format) function is F5, do NOT build it here). renderResume/thumbnail/fit/download/plainText/AtsView/picker/gallery/DesignPanel operate on (resume, profile, paper, formatV2) with UNCHANGED user-facing behavior (picker still lists six choices with live thumbnails + badges; gallery unchanged; §28.3 panel knobs map onto v2 fields). STORED DATA: applications.format, applications.lockedFormat.format, settings.defaultFormat hold v1 JSON — migrate via migrateFormat at the persistence boundary (choose: lazy on read OR one-shot boot migration; state the choice and test it), idempotent, so a pre-E9 database round-trips to valid v2 with look preserved; locked apps stay LOCKED (migrating lockedFormat does not unlock or mutate locked TailoredResume snapshots — §28.1 byte-stability). Rename outward-facing 'template' vocabulary to 'preset' where cheap (registry API, picker labels) but do NOT churn routes/DB columns for naming alone. Retire test/document-templates.test.ts etc. by REPARAMETERIZING over Object.values(PRESETS) — the E7/E8 invariant suite must survive, not be deleted (oracle.md Phase 7 checks all still apply, now over presets-through-one-engine). Frozen: react-pdf only; one engine; never cuts; §28.1 byte-stability; §26 IA (no new top-level routes). RED-TEAM SHARPENING ([v3-042]): reparameterization must not weaken the suite — record the pre-cutover vitest test count for the document/fit/extraction suites and hold it as a floor; the must-survive assertions are ENUMERATED (below), not implied; migration tests consume the E9-F0c-committed test/fixtures/pre-e9-formats/ VERBATIM (your diff must not touch that directory). The F0b off-diagonal composition test must survive the cutover (it is the standing anti-trenchcoat guard). ESCAPED-GAP REPAIR ([v3-044], escaped-bug rule): E9-F0a's migrateFormat drops v1 sections.columns (a stored v1 format with skills in 2 columns migrates to gridColumns 1 — look NOT preserved). F0d must extend migrateFormat: v1 sections {<section>: {columns: N>1}} → sectionDisplay skillsLanguages/interests { layout: 'grid', gridColumns: N } (skill/language/interest sections; other sections' columns have no v2 axis — document the drop honestly if any) AND strengthen format-v2.test.ts with non-empty-sections fixtures (the check that let it escape). Also: if E9-R1 (ibm-plex-mono repair) is done by pickup, delete legacyAdapt.ts's temporary mono exclusion so the full registered roster passes through; its per-face render smoke protects this. PRE-CUTOVER SUITE FLOOR (pinned by coordinator 2026-07-07, post-R1): the document/engine/fit/format suites (test/document-*.test.ts, test/engine-*.test.ts, test/fit.test.ts, test/format.test.ts, test/format-v2.test.ts, test/page-model.test.ts) = 11 files / 257 tests on the pre-cutover tree. Post-cutover count over the same glob must be >= 257.",
      "acceptance": "- bun run check → 0\n- bun run lint → 0\n- bun run build → 0\n- vitest run --passWithNoTests green (serial fallback if parallel contention flakes)\n- playwright test (chromium) green — full suite, no skips introduced\n- grep guards: src/client/document/templates/ does not exist; zero imports of it; zero references to the old per-template render functions\n- the FULL reparameterized invariant suite green: for every preset — extraction contains profile + every item (strict presets: index-increasing order), sentinels absent, never-cut across ladder; effectiveAtsGrade honesty (sidebar-right ⇒ good, photo shown ⇒ good) — i.e. every oracle.md Phase-7/E8 check now passes over PRESETS\n- migration: a fixture pre-E9 sqlite row (v1 format JSON in applications.format + lockedFormat + settings.defaultFormat) reads back as schema-valid v2 with the mapped preset look; migration is idempotent (second read/run identical); locked TailoredResume snapshot bytes UNCHANGED by migration and by any subsequent v2 format PUT\n- format PUT round-trip: API accepts v2 (zod), rejects out-of-bounds (one 400 case tested), persists across restart\n- playwright e2e: full existing suite 10/10 green (gallery: 6 painted pairwise-different thumbnails, selection persists, sample badges — unchanged behavior over presets)\n- §28.1 byte-stability guard green (format change never mutates stored TailoredResume)\n- suite floor: vitest test count over test/{document-*,engine-*,fit*,format*}.test.ts AFTER cutover ≥ the pre-cutover count (record both numbers in evidence; --reporter=json or the summary line)\n- grep guard: no new .skip/.only/.todo anywhere under test/ (diff-scoped)\n- must-survive assertions enumerated, each present and green post-cutover: centered-classic mean-x, one-row-compact shared y, sidebar mean-x contrast BOTH sides, banner tint + contrast ink, sentinel absence, strict extraction order, never-cut per preset, OFF-DIAGONAL composition (F0b), §28.1 byte-stability\n- migration test consumes test/fixtures/pre-e9-formats/ files unchanged (git diff for E9-F0d touches zero paths under test/fixtures/pre-e9-formats/)",
      "attempts": [],
      "evidence": "tooBig at pickup (no code touched): renderResume/fit still dispatch through registry on legacy format; 7 components consume legacy shape; documentFormatZ + DB $type v1-shaped so a client-only cutover cannot round-trip the live app/e2e. Split refined: schema/zod/DB-type swap travels with the client cutover (F0d-1); old-data migration + fixture suite separate (F0d-2). Coordinator accepted [v3-047]."
    },
    {
      "id": "E9-F1",
      "title": "COARSE (decompose at pickup): design view shell (§31.2b) + layout axes",
      "status": "decomposed",
      "depends_on": [
        "E9-F0d2"
      ],
      "files": [
        "src/client/",
        "test/"
      ],
      "origin": "spec §31.2b + §31.2 Layout + §31.6 F1",
      "context": "COARSE EPIC — the coordinator decomposes this at pickup into fine tickets; do not dispatch as-is. Scope: the dedicated design view (child route /applications/:id/design entered from Design card + gallery, pinned pdf.js live preview right / scrolling jump-nav panel left in FlowCV group order, ~300ms debounced repaint with busy indicator, locked apps read-only, narrow-viewport Design/Preview tabs, fit chip + page boundaries visible; existing §28.3 controls move in) PLUS the Layout group axes: columns one|two|mix (mix = full-width header band + two-column body), header position, sidebar width slider, per-section column/position assignment (extends LayoutEditor per §13), draggable manual page-break tokens. Oracle: oracle.md Phase 8 F1 line.",
      "acceptance": "oracle.md Phase 8 'F1' checks + full baseline. Decompose before dispatch.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E9-F1a",
      "title": "Design view shell: /applications/:id/design + pinned multi-page preview + debounced persist + locked + responsive tabs",
      "status": "done",
      "depends_on": [
        "E9-F0d2"
      ],
      "files": [
        "src/client/main.tsx",
        "src/client/App.tsx",
        "src/client/components/DesignView.tsx",
        "src/client/components/DocumentPreview.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/components/TemplateGallery.tsx",
        "test/e2e/design.spec.ts",
        "test/design-view.test.tsx"
      ],
      "origin": "decomposed from E9-F1 (coordinator [v3-052]); spec §31.2b + §31.6 F1 (shell half)",
      "context": "DECOMPOSED FROM E9-F1 ([v3-052]). Build ONLY the design-view SHELL + preview. The Layout-group axis CONTROLS are E9-F1b and the page-break token is E9-F1c — DO NOT add any layout-axis controls here. Oracle: oracle.md Phase 8 'F1' line (shell half).\n\nWHAT EXISTS (verified 2026-07-07):\n- Router: src/client/main.tsx uses createBrowserRouter; routes are children of path '/' -> <App/> (main.tsx:26-41). Existing children: applications, applications/:id (-> ApplicationDetailRoute -> <ApplicationDetail applicationId={id}/>), library, settings, and a catch-all { path:'*', element:<Navigate to='/applications' replace/> } (main.tsx:38, the §26 no-orphan-routes SPA fallback).\n- App.tsx wraps children in <LoginGate><AppShell>{Outlet}</AppShell>; it special-cases /settings to render components/SettingsView ahead of the Outlet (App.tsx:11-17). The design view flows through the normal Outlet.\n- ApplicationDetail.tsx renders a 'Design' <Card> (:243-275) containing <TemplateGallery/> (a radix Dialog opened by a 'Browse templates' button), an inline <TemplatePicker/>, and <DesignPanel format onChange readOnly/> (:273). It computes resolvedFormat (lockedFormat.format -> application.format -> settings.defaultFormat -> DEFAULT_FORMAT_V2, :109), isLocked (:108), density/fit, and renders <FitChip fit={fit}/> (:288, from ./FitChip). Persistence today: handleFormatChange (:151) fires updateApplication.mutate({id, input:{format:next}}) IMMEDIATELY on every change — NO debounce.\n- DocumentPreview.tsx: renders the REAL pdf via @react-pdf usePDF and paints ONLY page 1 onto a <canvas> via lazily-imported pdfjs-dist (DocumentPreview.tsx:33 doc.getPage(1), scale 1.5). Has busy ('Rendering preview…' :88) + error (:81) states. NO page boundaries (single page), NO debounce, NO fit chip inside it. Props: { resume, format?, density? }; pulls profile+settings internally.\n- Persistence API: PUT /api/applications/:id { format } via useUpdateApplication() (src/client/queries/useApplications.ts:39) which invalidates ['applications'] + ['applications',id]. Read via useApplication(id) key ['applications',id].\n- Type: DocumentFormatV2 + DEFAULT_FORMAT_V2 in src/shared/format-v2.ts.\n- e2e harness: test/e2e/applications.spec.ts drives login -> create fixture-JD application -> tailor -> current renders -> reload persists -> lock, against the keyless FixtureEngine webServer (playwright.config.ts, chromium). Reusable helpers under test/e2e/helpers.\n\nBUILD:\n1. Add route '/applications/:id/design' as a real child under '/' in main.tsx (a real route so a deep-link AND a browser refresh at that URL resolve to the SAME view via the SPA fallback, NEVER the '*' redirect to /applications). It renders a new <DesignView applicationId={id}/> (src/client/components/DesignView.tsx), reading :id the way ApplicationDetailRoute does.\n2. DesignView layout: a scrolling controls panel on the LEFT hosting the EXISTING <DesignPanel format onChange readOnly/> VERBATIM (do not add axes), a PINNED pdf.js live preview on the RIGHT, and <FitChip/> visible near the preview. On a narrow viewport (~<768px) collapse to Design / Preview TABS.\n3. Persistence: DesignView's onChange must DEBOUNCE ~300ms before PUT (coalesce rapid knob changes into one write) via useUpdateApplication(); the debounced value must survive a reload (round-trips the API). Do NOT break ApplicationDetail's existing path.\n4. Preview repaint: the pinned preview re-renders on every (debounced) format change, showing the busy indicator during render.\n5. Page boundaries: extend DocumentPreview (or DesignView's preview host) to render EVERY page (pdfDoc.numPages), each page a separate canvas with a VISIBLE separation (gap/border/shadow) between pages. An overflowing fixture must show >=2 stacked page canvases.\n6. Locked: when isLocked, EVERY control is disabled (DesignPanel/gallery/picker readOnly) but the preview stays LIVE (renders lockedFormat).\n7. Entry points: the 'Design' card in ApplicationDetail AND the TemplateGallery must offer navigation to '/applications/:id/design' (e.g. an 'Open design view' link/button). Both land on the route.\n\nLocked decisions (cite oracle.md, never re-litigate): react-pdf is the ONLY PDF path (no window.print/print.css); bounded axes only; one engine. DO NOT add columns/header-position/sidebar-width/section-placement/page-break controls (E9-F1b/c). Touch ONLY declared files (manifest/lockfile exempt for deps).",
      "acceptance": "oracle.md Phase 8 'F1' line (shell half) + full baseline: `bun run check` (exit 0); `NODE_OPTIONS=--max-old-space-size=1024 bun run build` (exit 0, per [v3-043]); `bun run lint` (exit 0); `vitest run --passWithNoTests` green; `playwright test` chromium green.\n- test/design-view.test.tsx (RTL): DesignView renders DesignPanel; onChange is DEBOUNCED (fake timers — N rapid changes within 300ms => exactly ONE updateApplication mutate after the window); locked => controls disabled AND preview host still mounts.\n- test/e2e/design.spec.ts (chromium, keyless FixtureEngine webServer; follow applications.spec.ts + helpers): login -> create fixture-JD application -> tailor -> open /applications/:id/design from the Design card -> preview canvas PAINTS (non-blank, E8-B1 pixel pattern) -> change a DesignPanel control -> preview REPAINTS (pixel-diff vs before) -> RELOAD the deep URL -> the changed format PERSISTS (control reflects it; preview matches) -> the deep URL opened by DIRECT navigation (SPA fallback) renders the design view, NOT a redirect to /applications -> a LOCKED application's design view shows disabled controls with a live preview -> NO uncaught console errors.\n- Overflowing fixture => >=2 page canvases with visible separation (assert >=2 page-canvas elements).\n- vitest total does not drop below the F0-close floor (258); run `vitest run` and confirm the count did not regress; no new .skip/.only/.todo.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, branch e9-f1a merged ff to main @5a16417): check exit 0; NODE_OPTIONS=--max-old-space-size=1024 build exit 0 (14.02s); lint exit 0 (biome 190 files clean); vitest 588 pass /53 files (1 flake: ats-view.tsx timeout under full-suite load, UNTOUCHED file, passes isolated in 621ms = [v3-008] host-pressure class, not a regression); playwright 11/11 incl new design.spec.ts. Guards: no window.print/print.css, no .only/.skip/.todo added. Scope: all declared + playwright.config.ts (coordinator-ENDORSED necessary touch — a new e2e spec must be wired into a project to run; builder shared the applications project rather than a 4th server that destabilizes this host). Gaming read CLEAN: DesignView has a real 300ms trailing-edge debounce (clearTimeout coalesce + draftFormat immediate echo), locked short-circuits handleChange, narrow->tabs, sticky preview, fit chip, gallery+card entry points; DocumentPreview.allPages genuinely loops doc.numPages painting one bordered canvas per page (default false keeps ResultView/AtsView single-page); design.spec.ts asserts non-blank canvas pixels + deep-URL fresh-goto resolves to the view (not redirect) + font-PUT 200 fonts.body:arimo + repaint + reload-persist + locked-disabled-with-live-preview."
    },
    {
      "id": "E9-F1b",
      "title": "Layout-group controls in DesignPanel: columns · header position · sidebar width · per-section placement (UI over already-wired engine axes)",
      "status": "done",
      "depends_on": [
        "E9-F1a"
      ],
      "files": [
        "src/client/components/DesignPanel.tsx",
        "test/design-panel.test.tsx"
      ],
      "origin": "decomposed from E9-F1 (coordinator [v3-052]); spec §31.2 Layout + §31.6 F1 (layout-axes half)",
      "context": "DECOMPOSED FROM E9-F1 ([v3-052]). Add the Layout-GROUP controls to DesignPanel. The design-view shell (route/preview/persistence/locked/tabs) is E9-F1a (already built; DesignPanel is now hosted inside <DesignView>). The manual page-break token is E9-F1c. Oracle: oracle.md Phase 8 'F1' line (layout-axes half, MINUS page-break).\n\nWHAT EXISTS (verified 2026-07-07):\n- DesignPanel.tsx is a PURE controlled component: props { format: DocumentFormatV2; onChange:(next)=>void; readOnly? } (DesignPanel.tsx:191). It reads/writes format immutably and calls onChange; a `set` helper (:200) is readOnly-guarded. It renders Typography/Color/Page/Photo/Structure groups of selects/steppers/swatches (NEVER free text). Add a LAYOUT group in the same idiom.\n- The ENGINE ALREADY RENDERS these axes (src/client/document/engine/document.tsx, landed E9-F0c) — so this ticket is UI WIRING only, NO engine edit: layout.columns 'one'|'two'|'mix' (:157; one=single-col, two=sidebar+main, mix=full-width band+two cols); layout.headerPosition 'left'|'right' picks the sidebar side (:115; 'top' falls back to left for columnar; for columns 'one' the header is on top); layout.sidebarWidthPct 25-40 real geometry (resolveColumnGeometry :126); layout.sectionPlacement per-section {column:'main'|'sidebar'} (:105, unset => 'main').\n- Type + zod: src/shared/format-v2.ts LayoutV2 (:36-42); sidebarWidthPct bounded .min(25).max(40) (:309-318); ColumnsMode/HeaderPosition/SectionColumn enums (:32-34); the Section enum supplies sectionPlacement keys.\n- The engine geometry + never-cut PROOF already exists and passes in test/engine-two-column.test.ts (mean-x per side; sidebarWidthPct 25<->40 delta ~0.15*content-width; never-cut ladder). DO NOT duplicate render proofs; they already cover these axes. This ticket proves the CONTROLS write the right axis.\n- DesignPanel unit tests live in test/design-panel.test.tsx.\n\nBUILD — add a 'Layout' group to DesignPanel (same select/stepper idiom, all readOnly-guarded, bounded — NO free-text/raw-CSS per §31.1):\n- columns: one|two|mix (select) -> layout.columns.\n- header position: top|left|right (select) -> layout.headerPosition.\n- sidebar width: stepper/slider 25-40 (%) -> layout.sidebarWidthPct; only meaningful when columns!=='one' — disable/hide for columns 'one'.\n- per-section column assignment: for each Section, main|sidebar -> layout.sectionPlacement[section]={column}; only shown when columns!=='one'. NOTE: this is the per-app FORMAT axis layout.sectionPlacement — NOT settings.layout (that global section-order/visibility store is edited by LayoutEditor.tsx; keep the two distinct, do not touch LayoutEditor).\n\nDO NOT: edit the engine (axes already render), add page-break controls (E9-F1c), or touch settings.layout/LayoutEditor.tsx. Touch ONLY declared files.",
      "acceptance": "oracle.md Phase 8 'F1' line (layout controls) + full baseline (`bun run check`; `NODE_OPTIONS=--max-old-space-size=1024 bun run build`; `bun run lint`; `vitest run --passWithNoTests`; `playwright test` chromium).\n- test/design-panel.test.tsx (RTL, extend): a Layout group renders; changing columns => onChange with layout.columns in {one,two,mix}; header position => layout.headerPosition in {top,left,right}; sidebar width => layout.sidebarWidthPct within [25,40]; a per-section select => layout.sectionPlacement[section].column in {main,sidebar}. Sidebar-width + per-section controls are disabled/absent when columns==='one' and enabled when 'two'/'mix'. readOnly => NO onChange fires on interaction.\n- The already-green engine geometry + never-cut suite (test/engine-two-column.test.ts) stays green (these controls feed exactly those axes; render proof lives there, not duplicated here).\n- vitest total not below the F0-close floor (258); no new .skip/.only/.todo; new behavior tested.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, branch e9-f1b merged ff to main): check exit 0; bounded build exit 0 (13.31s); lint exit 0 (biome 190 clean); vitest 596 pass/53 files (588 floor + 8 new Layout-group tests, NO flake this run); playwright 11/11 FULL suite (all projects incl design.spec.ts which drives DesignPanel controls in the design view — F1b controls do not break the shell); engine-two-column.test.ts 24/24 untouched. Scope CLEAN: only DesignPanel.tsx + design-panel.test.tsx (both declared). Guards: no print/only/skip. Gaming read CLEAN: idiomatic Select/NumberStepper/FieldRow, all readOnly-guarded + bounded (typed option lists matching ColumnsMode/HeaderPosition/SectionColumn enums; sidebarWidthPct min25/max40; no free text), sidebar-width+section-placement gated on columns!==one, sectionPlacement written distinctly from settings.layout (LayoutEditor untouched/not imported), NO engine edit, NO page-break control (correctly deferred to F1c). Header position offers all three top|left|right."
    },
    {
      "id": "E9-F1c",
      "title": "Manual page-break token: engine seam (document.tsx) + DesignPanel per-section toggle + never-cut proof",
      "status": "done",
      "depends_on": [
        "E9-F1b"
      ],
      "files": [
        "src/client/document/engine/document.tsx",
        "src/client/components/DesignPanel.tsx",
        "test/engine-page-break.test.ts",
        "test/design-panel.test.tsx"
      ],
      "origin": "decomposed from E9-F1 (coordinator [v3-052]); spec §31.2 Layout page-break + §31.6 F1 never-cut",
      "context": "DECOMPOSED FROM E9-F1 ([v3-052]). Wire the manual PAGE-BREAK token — the ONE F1 layout axis the engine does not yet render — plus its DesignPanel control. Shell = E9-F1a; other layout controls = E9-F1b (both built; DesignPanel now has a Layout group). Oracle: oracle.md Phase 8 'F1' line (page-break + never-cut).\n\nWHAT EXISTS (verified 2026-07-07):\n- Type: src/shared/format-v2.ts LayoutV2.manualPageBreaks: Section[] (:41-42) — 'force a page break immediately before this section'. Already in the v2 type + zod (:317). DEFAULT is [].\n- The ENGINE DOES NOT render it: src/client/document/engine/document.tsx header comment lists layout.manualPageBreaks under 'AXES NOT YET WIRED' (:28-31: 'render as sections.tsx one existing look — never a crash'). No break token is emitted anywhere. This ticket lands that seam.\n- Engine composition: EngineDocument (document.tsx) maps resume.sections into <SectionBlock> (imported from ../sections) across column modes one (:196-222) / two (:224-274) / mix (:279-291). react-pdf supports the `break` prop on any View/element to force it onto a new page.\n- NEVER-CUT (§28.4/§31): item count in the document is invariant across density AND layout. A manual break INCREASES page count but must NOT drop or duplicate items.\n- Proof harness pattern: test/engine-two-column.test.ts (renderToBuffer via the engine -> extractPdfText -> assert every item.text present; pdf.js for page count) and test/engine-single-column.test.ts / test/page-model.test.ts. Reuse their fixtures/helpers idiom.\n- DesignPanel's Layout group (added in E9-F1b) is where the page-break control goes.\n\nBUILD:\n1. document.tsx: render a REAL page boundary immediately before every section whose `section` is in format.layout.manualPageBreaks (react-pdf `break` on that section's wrapper). Keep every other axis unchanged; must render without crashing in ALL column modes (one/two/mix); for columnar modes the break applies within the relevant column flow. Update the 'AXES WIRED/NOT YET WIRED' header comment to move manualPageBreaks to WIRED.\n2. DesignPanel Layout group: a per-section 'page break before' TOGGLE writing layout.manualPageBreaks (add the Section on, remove off). readOnly-guarded; bounded (toggle, not free text).\n\nDO NOT: cut/drop items to fit (never-cut is inviolable), touch other axes/files. Touch ONLY declared files.",
      "acceptance": "oracle.md Phase 8 'F1' line (page-break + never-cut) + full baseline (`bun run check`; `NODE_OPTIONS=--max-old-space-size=1024 bun run build`; `bun run lint`; `vitest run --passWithNoTests`; `playwright test` chromium).\n- test/engine-page-break.test.ts (NEW, engine -> renderToBuffer -> pdf.js): a fixture that fits on ONE page with manualPageBreaks:[] renders numPages===1; adding a mid-document section to manualPageBreaks renders numPages>=2 AND that section's content appears on a LATER page than the section before it (per-page extraction via getPage(n).getTextContent(); assert cross-page ordering) — proving the break forces a REAL boundary, not a no-op. Cover >= columns 'one' AND 'two'.\n- NEVER-CUT: with a manual break applied, extraction still contains EVERY selected item.text (no item dropped/duplicated) — item set invariant vs the no-break render.\n- test/design-panel.test.tsx: a per-section 'page break before' toggle adds/removes the Section in layout.manualPageBreaks; readOnly => no onChange.\n- vitest total not below the F0-close floor (258); no new .skip/.only/.todo; new behavior tested.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, branch e9-f1c merged ff to main = the merged tree). check/lint 0; bounded build 0 (11.41s); vitest 608/608 FIRST run no flake (596 floor +12 F1c; whole engine/document/fit/format suite green => the break-segment restructuring of the shared render path regressed NOTHING); playwright FULL 10 pass +1 flaky(design.spec passed on retry, [v3-008] host class) exit 0. Scope clean (4 declared). Guards clean. Gaming read CLEAN: with empty manualPageBreaks splitIntoBreakSegments => one segment => one row => BYTE-IDENTICAL to pre-ticket render (why engine-two-column 24/24 + full suite stayed green); single-column uses react-pdf break prop directly, columnar reconstructs at row grain because react-pdf ignores a break nested in a fitting row (real @react-pdf/layout splitNodes constraint, root-caused by builder); header/summary stay on first row only. engine-page-break.test.ts is sharp: numPages===1 no-break baseline proves the break CAUSES page 2 (not overflow), pageOf per-page extraction proves cross-page ordering, never-cut compares per-marker occurrence counts (drops AND dups), parameterized columns one/two + mix no-crash."
    },
    {
      "id": "E9-F2",
      "title": "COARSE (decompose at pickup): type scale · spacing · entries · headings · fonts roster · date formats",
      "status": "decomposed",
      "depends_on": [
        "E9-F1c"
      ],
      "files": [
        "src/client/",
        "src/shared/",
        "test/",
        "package.json"
      ],
      "origin": "spec §31.2 (Type/Spacing/Entries/Headings/Fonts/Document-dates) + §31.6 F2",
      "context": "COARSE EPIC — decompose at pickup. Scope: type-scale offsets (name/title/heading/entry-header, exact §31.2 bounds), spacing (line height, element spacing 0-4, paired mm margins), Entries group (structure, date&location right|left|split + order, subtitle line, list style bullet|hyphen, per-element font styles, body indent), Headings (8 treatments + capitalization + per-heading icons none|outline|filled), the LOCKED font roster (oracle.md Phase 8 intake decisions: 31 body + 8 name-slot @fontsource OFL faces, name slot 'same-as-body' default, self-hosted only), and the 12 date-format presets applied by the shared section renderers. Every control lands in the F1 design view's matching group. Oracle: Phase 8 F2 line.",
      "acceptance": "oracle.md Phase 8 'F2' checks + full baseline. Decompose before dispatch.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E9-F2a",
      "title": "Font roster 31 body + 8 name-slot faces + name-slot render path",
      "status": "done",
      "depends_on": [
        "E9-F1c"
      ],
      "files": [
        "src/client/document/fonts.ts",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/document/engine/document.tsx",
        "src/client/document/sections.tsx",
        "src/client/components/DesignPanel.tsx",
        "test/document-fonts.test.ts",
        "package.json"
      ],
      "origin": "decomposed from E9-F2",
      "context": "E9 phase F2 (design engine v2). The locked font roster (oracle.md Phase 8 intake [v3-038..040]) is 31 body + 8 name-slot faces, all @fontsource OFL except ibm-plex-mono from @ibm/plex-mono ([v3-051] — do NOT touch that face). The v2 schema ALREADY declares the full roster: BODY_FONT_IDS (31, src/shared/format-v2.ts:102-137), NAME_DISPLAY_FONT_IDS (8, :140-149), fonts.name = same-as-body|NameFontId (:150). But only 6 body faces register and NO name face is wired.\n\nDO: (1) Register all 31 body + 8 name faces for react-pdf: extend FONT_MANIFEST (fonts.ts:74-93) and the browser literal-URL table BROWSER_FONT_URLS (fonts.ts:105-154 — Vite needs a literal string per new URL(...), so add explicit Regular+Bold entries per face). Add @fontsource deps to package.json. (2) Remove the 6-face fallback: legacyAdapt.ts:24-35 LEGACY_FONT_IDS collapses unknown faces to ibm-plex-sans; every roster face must resolve to itself. (3) Wire the name-slot render path: fonts.name (default same-as-body) is unwired (document.tsx:44); the profile name Text (sections.tsx ~:47) must render in the name font when set, else the body font — thread through legacyAdapt + document/sections. (4) DesignPanel body-font picker offers only 6 faces (DesignPanel.tsx FONT_IDS :41) — offer all 31; add a name-slot picker (same-as-body + the 8 name faces).\n\nLocked (oracle.md): design-only, bounded axes, self-hosted fonts (NO CDN), snapshots byte-identical, roster FIXED (exactly 31+8, no more/fewer).\n\nTESTS: the parameterized per-face smoke test/document-fonts.test.ts:124-138 (describe.each over FONT_FACES, punctuation-heavy body+heading render) auto-covers new faces; :41-49 asserts FONT_FACES keys EXACTLY equal the registered set — both must cover all 39. Add a render test: setting fonts.name to a name face changes PDF bytes vs same-as-body.",
      "acceptance": "oracle.md Phase 8 F2 font line: the fixed 31+8 roster registers and renders (self-hosted, no CDN). Executable:\n- full baseline (bun run check / NODE_OPTIONS=--max-old-space-size=1024 bun run build / bun run lint / vitest + playwright chromium) green\n- test/document-fonts.test.ts: FONT_FACES == registered set == 39 faces (31 body + 8 name); per-face describe.each smoke green for all 39\n- render test: fonts.name = a name face changes PDF bytes vs same-as-body\n- no CDN URL introduced (self-hosted assets only)",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f2a, merged ff to main @3567d1c). check exit 0; NODE_OPTIONS=1024 build exit 0 (39 faces emitted); lint exit 0 (191 files); vitest 643/643 (incl document-fonts 47 tests: 39-face describe.each smoke + BOTH-direction roster equality line47 Object.keys(FONT_FACES)===[...BODY,...NAME] so exactly 39 no more/fewer + name-font distinct-bytes test); playwright exit 0 (10 passed, 1 retry-absorbed design.spec.ts contention flake, [v3-041] class non-regression). Scope clean (only declared files + bun.lock/package.json). No CDN. Gaming read clean: resolveFont now identity (6-face fallback removed), resolveNameFont threaded as ProfileHeader nameFontFamily prop, test ALL_FONT_IDS DERIVED from schema roster constants (cannot drift)."
    },
    {
      "id": "E9-F2b",
      "title": "Type-scale offsets (4) wired + spacing distinctness tests",
      "status": "done",
      "depends_on": [
        "E9-F2a"
      ],
      "files": [
        "src/client/document/sections.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/components/DesignPanel.tsx",
        "test/engine-single-column.test.ts",
        "test/document-format-render.test.ts"
      ],
      "origin": "decomposed from E9-F2",
      "context": "E9 phase F2. Type-scale offsets are declared (src/shared/format-v2.ts TypeScaleV2 :45-51; bounds: bodySize 9-12, nameOffset +4..+12, titleOffset 0..+4, sectionHeadingOffset 0..+3, entryHeaderOffset 0..+2) but only bodySize is wired (legacyAdapt.ts:67). The 4 offsets are NOT applied — sections.tsx hardcodes name fontSize:20 (:47), sectionLabel body.size+1 (:71), groupHeading body.size (:82); document.tsx:39-41 lists them unwired.\n\nDO: wire the 4 offsets into rendered sizes (likely in legacyAdapt.ts, producing per-role sizes the sections consume): name = bodySize+nameOffset; profile title/subtitle = bodySize+titleOffset; section heading = bodySize+sectionHeadingOffset; entry/group header = bodySize+entryHeaderOffset. Schema already enforces §31.2 bounds. Add the 4 offset controls to the DesignPanel Typography block (:226-291, which today has only body size + line height).\n\nSpacing (lineHeight, elementSpacing 0-4, marginsMm.x/y) is ALREADY fully wired (legacyAdapt.ts:69,72-81) with UI — this ticket only ADDS its distinctness tests, does NOT re-implement it.\n\nLocked: design-only, bounded, snapshots byte-identical.\n\nTESTS (E8-A1 contrast pattern — distinct-bytes idiom at test/document-format-render.test.ts:104-121; geometry helper page1Geometry at test/engine-single-column.test.ts:119-167; the parameterized AXIS_CASES block :399-682 today only asserts nonempty): assert each of the 4 offsets moves the MEASURED font size in the stated direction (higher value => larger rendered size, via extraction geometry, not just byte-diff), and lineHeight/marginsMm.x/marginsMm.y/elementSpacing each measurably change geometry.",
      "acceptance": "oracle.md Phase 8 F2 type/spacing lines: offsets/margins/line-height move measured geometry in the stated direction and honor §31.2 bounds. Executable: full baseline green; the 4 type-scale offsets each render a measurably larger size at a higher value (geometry assertion); spacing axes (lineHeight, elementSpacing, marginsMm.x/y) each measurably change geometry; DesignPanel exposes the 4 offset controls.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f2b, merged ff to main @dcf5a3d). check/build/lint exit 0; vitest 651/651; playwright 11/11 (no flake). Acceptance: TYPE_SCALE_CASES assert extracted transform[0] rendered pt == bodySize+offset exactly AND max>min for all 4 offsets + distinct bytes; 4 spacing geometry tests (lineHeight/elementSpacing/marginsMm.x/y) each move measured geometry. Scope clean (4 files, no manifest change). Gaming read clean: typeScaleSizes typed on toLegacyFormat return (survives density spread), sections fallback reproduces exact prior hardcoded sizes (back-compat), new title render CONDITIONAL on profile.headline (no empty-line regression), titleOffset non-vacuous. Headline render is spec-implied title-axis completion; stored snapshots untouched."
    },
    {
      "id": "E9-F2c",
      "title": "Headings: 8 treatments + capitalization + none|outline|filled icons",
      "status": "done",
      "depends_on": [
        "E9-F2b"
      ],
      "files": [
        "src/client/document/sections.tsx",
        "src/client/components/DesignPanel.tsx",
        "test/engine-headings.test.ts"
      ],
      "origin": "decomposed from E9-F2",
      "context": "E9 phase F2. Headings axis declared (src/shared/format-v2.ts HeadingsV2 :80-99): HEADING_STYLES is an 8-value array (:81-90 — underline, boxed, outline-short-rule, rules-above-below, accent-bar, plain, thin-underline, tick-marks), capitalization (capitalize|uppercase), icons (none|outline|filled). Only the underline treatment + uppercase render today — sectionLabel (sections.tsx:69-79) hardcodes borderBottomWidth:0.75 + textTransform:uppercase + color primary; document.tsx:43-44 lists style-beyond-underline/capitalization/icons unwired.\n\nDO: implement all 8 heading treatments as visually distinct renderings of the SECTION label, the capitalize|uppercase branch, and none|outline|filled per-heading icon rendering, in sections.tsx. Add the heading controls (treatment select, capitalization toggle, icon style) to DesignPanel. NOTE: the existing DesignPanel Heading weight control binds header.nameWeight/titleWeight (the profile header), NOT headings.* — keep them distinct.\n\nLocked: design-only, bounded, snapshots byte-identical.\n\nTESTS (new file test/engine-headings.test.ts; distinct-bytes idiom at test/document-templates.test.ts:322-336 / document-format-render.test.ts:104-121): parameterize over HEADING_STYLES and assert all 8 produce PAIRWISE-distinct PDF bytes; capitalize vs uppercase changes extracted heading text case; each of none|outline|filled changes bytes.",
      "acceptance": "oracle.md Phase 8 F2 heading line: 8 heading styles pairwise-distinct bytes. Executable: full baseline green; all 8 HEADING_STYLES render pairwise-distinct bytes; capitalize vs uppercase changes extracted heading text; none|outline|filled icons each change bytes; DesignPanel exposes treatment+capitalization+icon controls.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f2c, merged ff to main). check/build/lint 0; vitest 657/657 (incl new engine-headings.test.ts 6 tests: 8 HEADING_STYLES pairwise-distinct bytes, capitalize/uppercase EXACT-item extraction case flip (EXPERIENCE present/Experience absent), icons none/outline/filled pairwise-distinct + never-cut, axis-cardinality==8, DEFAULT_FORMAT_V2 fallback parity underline/uppercase/none). playwright exit 0 (10 passed +1 retry-absorbed design.spec flake). library-crud full-run failures were host contention — isolated rerun 8/8 clean, NON-regression. Scope clean (4 files). Gaming read clean: 8 treatments are genuine distinct View compositions (box/rules/thin+thick underline/accent-bar/short-rule/tick-marks/plain), icons are Views (no glyph dep), plain drops accent color, threaded via typed headingsConfig extra-property (same seam as F2a/b), fallback reproduces exact pre-ticket look."
    },
    {
      "id": "E9-F2d",
      "title": "Structured group-header seam + 12 date-format presets (F2 risk slice)",
      "status": "done",
      "depends_on": [
        "E9-F2c"
      ],
      "files": [
        "src/client/document/sections.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/document/formatDate.ts",
        "src/server/tailor/assemble.ts",
        "src/shared/types.ts",
        "src/shared/schema.ts",
        "src/client/components/DesignPanel.tsx",
        "test/format-date.test.ts",
        "test/document-format-render.test.ts",
        "src/server/routes/backup.ts",
        "test/api.export-import.test.ts"
      ],
      "origin": "decomposed from E9-F2",
      "context": "E9 phase F2 — THE F2 RISK slice; sequenced before the entries UI (E9-F2e depends on it). Goal: the 12 date-format presets (src/shared/format-v2.ts DATE_FORMATS :13-26; document.dateFormat :29,:307) render a date distinctly. TODAY no date helper exists and dates are NOT applied — a group header is an opaque composed string TailoredGroup.heading (src/shared/types.ts:117), built in assemble() from rule.groupBy(entry.meta) (assemble.ts:69-85). Library entries carry structured role/period/location (schema.ts:16-18) but period is a FREEFORM string; item-level date? fields (schema.ts:48,57,68) are freeform too.\n\nDO: (1) INVESTIGATE the data flow first (assemble.ts, the SECTIONS registry @shared/sections, the fixtures) and decide where a parseable/structured date lives. Carry structured header parts (e.g. title/subtitle/date/location) on TailoredGroup ADDITIVELY — the existing heading string MUST remain so stored snapshots and FROZEN fixtures without structured fields still render (§28.1/§31.1: stored TailoredResume byte-identical across format changes; a snapshot missing structured fields falls back to heading). This is server-assembled structure (§5), NOT a model-schema or content change. Update the TailoredResume zod (schema.ts) additively/optionally so persisted snapshots still validate. (2) Add a pure formatDate(value, preset) helper (new src/client/document/formatDate.ts) covering all 12 DATE_FORMATS; apply it in sections.tsx to the structured date. (3) Add the date-format select to DesignPanel.\n\nHARD CONSTRAINT: this must NOT require a content-schema change beyond §31 single allowed touch (meta.level, reserved for F4). Deriving structured parts in assemble() from EXISTING entry.meta is allowed; changing what the user/model provides is NOT. If 12-formats-render-distinctly genuinely requires new content fields, STOP and return { blocked: true, reason } with the exact contradiction — do NOT half-build.\n\nLocked: design-only, snapshots byte-identical, NO tag/level scoring.\n\nTESTS: new test/format-date.test.ts unit-tests the pure helper over all 12 presets; a fixture date rendered under each of the 12 DATE_FORMATS produces DISTINCT extracted text; a snapshot-stability test proves changing dateFormat leaves a stored snapshot byte-identical (existing byte-stability guard must stay green).",
      "acceptance": "oracle.md Phase 8 F2 date line: 12 date formats each render a fixture date distinctly. Executable: full baseline green; formatDate is a pure unit-tested helper over all 12 presets; a fixture date renders distinct extracted text under all 12 DATE_FORMATS; changing dateFormat does NOT mutate stored snapshots (byte-stability guard green); DesignPanel exposes the date-format select. If blocked on a content-schema contradiction, returns { blocked, reason } with no partial build.",
      "attempts": [
        {
          "n": 1,
          "failed": "none (tooBig on a scope-contract gap, NOT a build failure)",
          "hypothesis": "coordinator omitted src/client/document/engine/legacyAdapt.ts — the extra-property seam EVERY prior F2 axis (F2a/b/c) used to cross renderResume->document->legacyAdapt->sections; ticket also carried a caveat telling the builder not to touch it",
          "fixNote": "added legacyAdapt.ts to declared files; removed the do-not-touch caveat; re-dispatch as ONE whole ticket (NOT the proposed F2d-1/F2d-2 split, which would make a helper+fields ticket whose 12-format render acceptance is unverifiable alone)"
        }
      ],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f2d, merged ff to main). check/build/lint 0; vitest 667/667; playwright 11/11 (no flake). DATE FORMAT test: 12 presets -> Set of extracted texts size===12 (mutually distinct, non-vacuous) + never-cut; SNAPSHOT STABILITY: JSON byte-identical before/after 2 dateFormat renders; format-date.test.ts 7/7 (12 presets + ordinal 11-13 + parseHeadingDate ISO/natural/unparseable); backup round-trip: headingParts survives export->import (deep-equal, non-vacuous). Scope clean (10 files ⊆ declared; schema.ts unused). Content-schema UNTOUCHED (headingParts derived in assemble() from existing entry.meta; TailoredGroup additive; heading fallback for old snapshots). §27 backup gap (tailoredResumeZ default-strip) FIXED in same ticket. NOTE (transitional, F2e owns): headingParts composes title·subtitle·date uniformly => new project headers gain role+date (was name-only), education gains date, experience swaps role/company order vs old groupBy; no byte-identity/test regression (old snapshots render via heading)."
    },
    {
      "id": "E9-F2e",
      "title": "Entries group: placement/order/subtitle/list/font-styles/indent/structure over the F2d seam",
      "status": "done",
      "depends_on": [
        "E9-F2d"
      ],
      "files": [
        "src/client/document/sections.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/server/tailor/assemble.ts",
        "src/shared/types.ts",
        "src/server/routes/backup.ts",
        "src/client/components/DesignPanel.tsx",
        "test/engine-entries.test.ts",
        "test/api.export-import.test.ts"
      ],
      "origin": "decomposed from E9-F2",
      "context": "E9 phase F2 — FINAL slice; builds on E9-F2d's structured group-header seam (now on main). Implement the full Entries axis group (src/shared/format-v2.ts EntriesV2 :60-78): structure (full-width|columns), dateLocationPlacement (right|left|split), dateLocationOrder (date-first|location-first), subtitlePlacement (same-line|below), listStyle (bullet|hyphen), subtitleFontStyle/dateFontStyle/locationFontStyle (normal|bold|italic), bodyIndent (bool).\n\nWHAT F2d ALREADY LANDED (read before building):\n- TailoredGroup.headingParts {title, subtitle?, date?} (src/shared/types.ts) derived in assemble()'s headingPartsFromMeta (src/server/tailor/assemble.ts) from existing entry.meta. NO location field yet — YOU add it.\n- sections.tsx resolveGroupHeadingText(group, format): composes title·subtitle·date when headingParts present, formats date via formatDate + document.dateFormat, falls back to raw group.heading when headingParts absent (back-compat for pre-F2d snapshots — PRESERVE this fallback).\n- The extra-typed-property seam on legacyAdapt.ts toLegacyFormat (typeScaleSizes/headingsConfig/nameFontFamily/dateFormat) — thread entries.* the SAME way.\n- backup.ts tailoredResumeZ round-trips headingParts (§27) — if you add `location` to headingParts you MUST add it to tailoredResumeZ too + a round-trip assertion (test/api.export-import.test.ts), or export->import silently strips it.\n\nDO:\n1. Add `location?: string` to TailoredGroupHeadingParts (types.ts) + headingPartsFromMeta (assemble.ts, from meta.location where the section has it — experience/education/project) + backup.ts tailoredResumeZ (§27 round-trip) + a round-trip test.\n2. FINALIZE the entry-header composition (F2d flagged this as YOUR domain): render title/subtitle/date/location as SEPARATE elements (not one joined string) so dateLocationPlacement (right|left|split), dateLocationOrder, subtitlePlacement (same-line|below), and the per-field font styles (subtitle/date/location normal|bold|italic) all apply. Keep the raw-`heading` fallback for headingParts-less snapshots.\n3. listStyle switches the ItemRow bullet glyph (sections.tsx:~202 hardcoded); bodyIndent indents the item body; structure toggles the entry internal full-width vs columns. NOTE EntriesV2.structure (per-entry internal layout) is DISTINCT from layout.columns (page columns, F1b) — do not conflate.\n4. Add the Entries control group to DesignPanel.\n\nThread entries.* through legacyAdapt (extra typed property). Locked: design-only, bounded axes, snapshots byte-identical, never-cut (item count invariant).\n\nTESTS (new file test/engine-entries.test.ts; E8-A1 contrast pattern via renderResumeToBuffer + pdf.js extraction/geometry, page1Geometry helper at test/engine-single-column.test.ts:119-167): each EntriesV2 axis value measurably distinct — dateLocationPlacement right|left|split moves the date/location measured x-position; dateLocationOrder swaps order in extraction; subtitlePlacement same-line|below changes geometry; listStyle bullet|hyphen changes the extracted bullet glyph; each of the 3 font styles changes bytes; bodyIndent moves the body x; structure changes geometry. never-cut: item count invariant across each axis. Back-compat: a group with only `heading` (no headingParts) still renders via the fallback.",
      "acceptance": "oracle.md Phase 8 F2 entries line (every enum value measurably distinct, E8-A1 pattern). Executable: full baseline green (check/NODE_OPTIONS build/lint/vitest/playwright chromium+auth+applications); each EntriesV2 axis value renders measurably distinct (geometry/extraction, parameterized); never-cut (item count invariant across the group); heading-string fallback still renders when headingParts absent; if `location` added to headingParts it round-trips export->import intact (§27, non-vacuous test); DesignPanel exposes the Entries control group.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f2e, merged ff to main). check/build/lint 0; vitest 681/681; playwright 11/11 (no flake). engine-entries.test.ts 13/13: dateLocationPlacement Set(date-x).size===3, dateLocationOrder extraction-index swap, subtitlePlacement distinct y, listStyle exact glyph (bullet has •, hyphen has - and NOT •), 6 font-style CONTRAST cases, bodyIndent shifts body x w/ bullet x unchanged, structure distinct geometry, back-compat RAWHEADINGFALLBACK — never-cut (ITEMONE+ITEMTWO) asserted in EVERY case. location round-trips export->import (non-vacuous). Scope clean (8 files, all declared). Content-schema untouched (location additive on headingParts, derived from experience/education meta.location; project has none). RESIDUAL flagged: fontStyle italic renders as skewX(-12deg) faux-italic — fonts.ts roster has no italic assets and fonts.ts is out of scope; a real, distinct, asset-free axis (true italic assets = optional later refinement). structure:columns + dateLocationPlacement off-diagonal renders sensible default (date/location own column) — matches F1 off-diagonal convention."
    },
    {
      "id": "E9-F3",
      "title": "COARSE (decompose at pickup): colors subsystem · header group · links · footer · photo crop",
      "status": "decomposed",
      "depends_on": [
        "E9-F2e"
      ],
      "files": [
        "src/client/",
        "test/"
      ],
      "origin": "spec §31.2 (Colors/Header/Links/Footer/Photo) + §31.6 F3",
      "context": "COARSE EPIC — decompose at pickup. Scope: color area full-page|header|border, mode single|multi (text/background/accent hexes, curated swatches + free hex), auto-contrast ink generalized from the banner logic, border page-frame (size s|m|l, per-side toggles — extraction-neutrality CI test REQUIRED, the [v3-038] promoted row), 9 accent-placement toggles, Header group (alignment, 3 detail arrangements, separator icon|bullet|bar, 7 contact icon styles, name/title weight + position), Links toggles, Footer toggles (page numbers/email/name/custom line), photo crop/zoom at upload. IMAGE BACKGROUNDS ARE REJECTED (scope tripwire). Oracle: Phase 8 F3 line.",
      "acceptance": "oracle.md Phase 8 'F3' checks + full baseline. Decompose before dispatch.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E9-F3a",
      "title": "Colors subsystem: area (full-page|header|border) + mode + background + generalized auto-contrast ink + two-column band fix",
      "status": "done",
      "depends_on": [
        "E9-F2e"
      ],
      "files": [
        "src/client/document/engine/document.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/document/sections.tsx",
        "src/client/components/DesignPanel.tsx",
        "test/engine-single-column.test.ts",
        "test/engine-two-column.test.ts"
      ],
      "origin": "decomposed from E9-F3",
      "context": "E9 phase F3 (design engine v2). Colors axis (src/shared/format-v2.ts): colors.area full-page|header|border (ColorArea :155, enum :355), colors.mode single|multi (:156,:356), colors.text/background/accent hexes (:174-176). TODAY only area==header renders a filled band (document.tsx: hasBand :208, band style :230-234 rendered :272-275); full-page/border areas + colors.background + mode:multi are UNWIRED (document.tsx AXES-NOT-WIRED :46-61). Auto-contrast ink EXISTS but band-only: contrastInk(hex) luminance gt 0.6 ? #111 : #fff at document.tsx:84-99, applied only when hasBand (ink prop :209,:253 threaded to ProfileHeader which overrides name/contact/link/title colors sections.tsx:369-376).\nCARRIED RESIDUAL to FIX here: columns two + area header renders BANDLESS (document.tsx:217 bandAtPageTop = hasBand AND columnsMode not two; two-col header folds into main column :322-324 with no band). Wire the band for the two-column layout.\nDO: render colors.area full-page (page background = colors.background) and coordinate area:border with F3b (the drawn frame is F3b; here area:border may just render page bg); wire colors.background as page/section bg; generalize auto-contrast ink beyond the band (when the effective background is dark, flip ink) so colors.mode/area drive ink, not band-only (E8-A2 luminance generalized); FIX the two-column band. Add DesignPanel controls: area select, mode select, background ColorField (reuse ColorField + COLOR_SWATCHES + hex guard at DesignPanel.tsx:145-154,272-316,744-760). Thread colors.background/mode/area via legacyAdapt extra typed property (prior seam pattern) or document.tsx reads format directly.\nLocked (oracle.md): design-only, bounded axes, IMAGE BACKGROUNDS REJECTED (scope tripwire), snapshots byte-identical, self-hosted. Extraction TEXT must stay unchanged when only colors change (colors paint, never reorder/cut text).\nTESTS (E8-A2 luminance oracle: expectedInk + page1FillColors op-list fill extractor at test/engine-single-column.test.ts:152-173; band+ink assertions :266-276,:344-361): multi-mode auto-contrast ink verified (dark background flips ink, via page1FillColors); header band + full-page background keep EXTRACTION text green (index-order + content unchanged vs default); two-column area:header now paints a band (page1FillColors shows the accent fill in the two-col render, test/engine-two-column.test.ts).",
      "acceptance": "oracle.md Phase 8 F3 colors lines. Executable: full baseline green (check / NODE_OPTIONS build / lint / vitest / playwright chromium+auth+applications); multi-mode auto-contrast ink verified (E8-A2 luminance generalized: dark bg flips ink, asserted via fill-color extraction); header band + full-page background keep extraction TEXT green (order+content unchanged, colors paint only); columns two + area header now renders a band (fill-color asserted in the two-column render); DesignPanel exposes area/mode/background controls; no image-background path introduced (scope tripwire).",
      "attempts": [
        {
          "n": 1,
          "failed": "spec-conformance (baseline green but violates a locked decision)",
          "hypothesis": "builder painted colors.area full-page/border background UNCONDITIONALLY (document.tsx pageBackground, no mode gate) and added a test enshrining single-mode dark-bg. SPEC.md:1014 (single = accent over black-on-white) + :1093 (single mode over white) lock single mode to a WHITE page background; painted page bg is a MULTI-mode feature. Also breaks F5 atsGrade premise (single-mode-over-white is strict-eligible).",
          "fixNote": "Gate the full-page/border PAGE background on colors.mode===multi ONLY; single mode => page stays white (no paint) regardless of area. Multi => paint colors.background + auto-contrast ink (as already built). Leave the header BAND (area:header) UNCHANGED — the banner strict preset uses it in single mode (registry.ts:74), F5 owns the atsGrade reconciliation. Fix the single-mode test to assert WHITE bg (no authored-dark paint). Cite SPEC.md:1014/:1093."
        }
      ],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f3a @5f020d8, merged ff to main; attempt 2 after spec-conformance fix). check/build/lint 0; vitest 684/684; playwright 11/11 (no flake). resolvePageBackground gates full-page/border page bg on mode===multi (single => white page always, spec.md:1014/:1093); header band untouched (banner single-mode band preserved, F5 owns atsGrade reconciliation); generalized auto-contrast ink flips document-wide over a painted multi-mode bg; two-column area:header now paints a band. Tests non-vacuous: multi asserts dark fill present + ink #fff + text preserved; single asserts dark bg ABSENT from fills + colors.text present (white page). Scope clean (4 files, legacyAdapt/sections untouched via read-format-directly). No image bg (scope tripwire honored)."
    },
    {
      "id": "E9-F3b",
      "title": "Border page-frame: size s|m|l + per-side toggles, drawn first+last (extraction-order neutral CI test)",
      "status": "done",
      "depends_on": [
        "E9-F3a"
      ],
      "files": [
        "src/client/document/engine/document.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/components/DesignPanel.tsx",
        "test/engine-single-column.test.ts"
      ],
      "origin": "decomposed from E9-F3",
      "context": "E9 phase F3. Border page-frame axis (src/shared/format-v2.ts): colors.border = { size s|m|l, sides:{top,right,bottom,left booleans} } (:177-180, BorderSize :157, enum :360-363; default size:s all-sides-false baseFromV1:515). GREENFIELD: colors.border is NOT rendered anywhere today (only heading-decoration borders exist in sections.tsx, unrelated).\nDO: render a page frame in document.tsx (Page-level) whose per-side presence comes from colors.border.sides and thickness from size (s|m|l e.g. 0.5|1|2pt), colored by colors.accent. CRITICAL ([v3-038] intake: border PROMOTED to ATS-neutral because a frame drawn first AND last leaves extraction order intact): draw the frame so it does NOT perturb text extraction order; the promoted-row CI test is REQUIRED. Add DesignPanel border controls (size select + 4 per-side toggles). Thread colors.border via the prior seam.\nLocked (oracle.md): design-only, bounded, snapshots byte-identical, IMAGE frames REJECTED (a drawn vector frame is fine; an image frame is a scope tripwire).\nTESTS: border sides/size MEASURABLE (page1Geometry/page1FillColors at test/engine-single-column.test.ts:126-173: a wider size or an added side changes measured frame geometry/fill); EXTRACTION-ORDER INVARIANT ([v3-038] promoted row, REQUIRED): a full 4-side max-size border leaves extraction index-increasing and content-identical to no-border (the pattern at test/engine-single-column.test.ts:175-204 / test/document-extraction-invariant.test.ts) — this earns border its ATS-neutral grade.",
      "acceptance": "oracle.md Phase 8 F3 border line. Executable: full baseline green; border sides/size measurable (frame geometry/fill changes with size and per-side toggles); EXTRACTION-ORDER INVARIANT CI test green (4-side max-size border leaves extraction index-increasing and content-identical to no-border, the [v3-038] promoted-row test); DesignPanel exposes border size + 4 per-side toggles; no image-frame path (scope tripwire).",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f3b @c304501, merged ff to main). check/build/lint 0; vitest 689/689 (incl border describe: no-side/single-side geometry, side-count 1->2->4, size s-vs-l thickness, and the REQUIRED [v3-038] extraction-order invariant: full 4-side max-size l border => borderedText===noBorderText content-identical + index-increasing vs no-border). playwright: full run 10 passed +1 design.spec failure = documented host-contention flake (TypeError Failed to fetch / fitToPages threw / standardFontDataUrl — [v3-041]/[v3-053] class); ISOLATED rerun exit 0 (flaky, retry-recovered) => NON-regression (border controls do not touch fetch/fit). Border = absolute filled rects (no Text => extraction-neutral structurally), thickness s/m/l 0.5/1/2pt, accent-colored, rendered in BOTH single-column and columnar Page branches (two .map sites). Scope clean (3 files; legacyAdapt untouched via read-format-directly). No image-frame (scope tripwire honored)."
    },
    {
      "id": "E9-F3c",
      "title": "Header group (wrapped arrangement, separator, 7 contact-icon styles, title position/weight) + Links (underline/accentColor/icon)",
      "status": "done",
      "depends_on": [
        "E9-F3b"
      ],
      "files": [
        "src/client/document/sections.tsx",
        "src/client/document/engine/document.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/components/DesignPanel.tsx",
        "test/engine-single-column.test.ts"
      ],
      "origin": "decomposed from E9-F3",
      "context": "E9 phase F3. HEADER group (src/shared/format-v2.ts HeaderV2 :201-209): alignment left|center (WIRED via resolveVariant document.tsx:106-110), detailsArrangement stacked|single-row|wrapped (only single-row->inline WIRED; stacked=default-left; WRAPPED unwired), separator icon|bullet|bar (UNWIRED), contactIconStyle 7 values CONTACT_ICON_STYLES none-frame/circle-filled/circle-outline/rounded-filled/rounded-outline/square-filled/square-outline (:188-196, UNWIRED), nameWeight/titleWeight normal|bold (nameWeight WIRED legacyAdapt:46-48; titleWeight currently collapses onto the same field — make it INDEPENDENT), titlePosition same-line|below (UNWIRED). LINKS group (LinksV2 :222): underline, accentColor, icon booleans — profile links render today (sections.tsx:389-393 Link color colors.primary) but ALL THREE toggles UNWIRED.\nDO (all in ProfileHeader, sections.tsx): implement detailsArrangement wrapped (contact line wraps) vs stacked vs single-row; separator glyph between contact items (icon | bullet dot | bar |); the 7 contact-icon styles as View shapes (like F2c heading icons, no font-glyph dependency; none-frame = no icon); titlePosition same-line|below relative to name; INDEPENDENT titleWeight (thread separately, stop collapsing onto nameWeight). Links: underline adds textDecoration, accentColor toggles colors.primary vs colors.text, icon renders a small link glyph/View. Add DesignPanel controls for all. This ticket ADDS the contact-icon and link-icon element classes that E9-F3d (accent-placement) will color.\nLocked (oracle.md): design-only, bounded axes (ship ALL enum values: 3 arrangements, 3 separators, 7 contact styles), snapshots byte-identical.\nTESTS (E8-A1 contrast + page1Geometry at test/engine-single-column.test.ts:126-147): each header axis value measurably distinct (wrapped vs stacked vs single-row geometry; each of 3 separators changes bytes/glyph; 7 contact-icon styles pairwise-distinct bytes; titlePosition same-line vs below changes name/title relative geometry; independent titleWeight changes title bytes without changing name weight); each links toggle changes bytes (underline adds decoration, accentColor flips link color, icon adds a glyph); extraction text (contact values) unchanged across styling.",
      "acceptance": "oracle.md Phase 8 F3 header/links coverage (E8-A1: every enum value measurably distinct). Executable: full baseline green; detailsArrangement 3 values geometry-distinct; separator 3 values distinct; 7 contact-icon styles pairwise-distinct bytes; titlePosition same-line|below distinct; titleWeight independent of nameWeight; links underline/accentColor/icon each change bytes; extraction contact values unchanged; DesignPanel exposes all header + link controls.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f3c @e7589d0, merged ff to main). check/build/lint 0; vitest 703/703; playwright exit 0 (10 passed +1 retry-absorbed applications.spec host-contention flake). New headerConfig/linksConfig legacyAdapt seam. Tests rigorous+non-vacuous: detailsArrangement wrapped drops links below (geometry) + 3-way pairwise bytes; separator bullet/bar/icon pairwise-distinct + bar glyph | in extraction/icon not; ALL 7 contactIconStyle pairwise-distinct bytes (double-loop); titlePosition same-line vs below geometry; titleWeight independence (title bold w/o name); links underline/accentColor/icon each change bytes. Adds contact-icon + link-icon element classes for F3d. Scope clean (5 files). RESIDUAL (minor UX): DesignPanel keeps the legacy Heading-weight control (dual-writes name+title, to avoid touching out-of-scope design-panel.test.tsx) alongside a new independent Title-weight control — engine-level independence (the oracle target) IS achieved."
    },
    {
      "id": "E9-F3d",
      "title": "Accent-placement: 9 toggles each gate accent on ONLY its element class (extraction text unchanged)",
      "status": "done",
      "depends_on": [
        "E9-F3c"
      ],
      "files": [
        "src/client/document/sections.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/components/DesignPanel.tsx",
        "test/engine-single-column.test.ts"
      ],
      "origin": "decomposed from E9-F3",
      "context": "E9 phase F3. AccentPlacementV2 (src/shared/format-v2.ts :159-169, zod :364-374): EXACTLY 9 booleans (asserted format-v2.test.ts:668-682): name, title, headings, headingRules, headerIcons, levelIndicators, dates, entrySubtitles, linkIcons. TODAY accent (colors.accent -> colors.primary) is HARDCODED per element in sections.tsx, independent of these flags (headings :121, rules :194-252, links :171 always accent; name/title/dates/subtitles always colors.text). ZERO toggles read.\nDO: make each of the 9 flags GATE whether colors.accent (vs colors.text) applies to its element class in sections.tsx: name; title (profile title/headline); headings (section labels); headingRules (heading decoration borders/bars); headerIcons (the contact-icon Views from F3c); dates (entry date); entrySubtitles (entry subtitle); linkIcons (the link icon from F3c); levelIndicators (skill/language level display — that element is built in F4; wire the flag to its future color path defensively so it is a NO-OP-until-F4, and NOTE this honestly; its full test defers to F4). Thread accentPlacement via legacyAdapt extra property. Add DesignPanel accent-placement controls (9 toggles).\nLocked (oracle.md): design-only, bounded, snapshots byte-identical. CRITICAL oracle semantics: a toggle must change bytes ONLY in its own element class and leave EXTRACTION TEXT unchanged (accent is a color, never a text/structure change).\nTESTS (page1FillColors fill-extractor + Buffer.compare at test/engine-single-column.test.ts:152-173): for each of the 8 F3c-available toggles (levelIndicators deferred to F4), flipping it flips its element class fill (accent<->text) with OTHER elements' fills unchanged and extracted TEXT identical. Parameterize.",
      "acceptance": "oracle.md Phase 8 F3 accent line: each of the 9 accent-placement toggles changes bytes ONLY in its element class, extraction text unchanged. Executable: full baseline green; for each toggle (levelIndicators noted deferred to F4 — its element lands there), flipping it flips its element class fill accent<->text with other classes' fills and all extracted text unchanged (fill-color extraction, parameterized); AccentPlacementV2 still exactly 9 keys; DesignPanel exposes the 9 toggles.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f3d @f3f394c, merged ff to main). check/build/lint 0; vitest 711/711; playwright 11/11 (no flake). All 9 AccentPlacementV2 flags gate colors.accent vs colors.text per element class in sections.tsx (threaded via legacyAdapt seam); 8 tested, levelIndicators documented no-op-until-F4 (its element lands there). Test rigor (non-vacuous, per-toggle from ALL-OFF baseline with distinctive ACCENT_HEX #ff00ff): Buffer.compare differs; offColors lacks accent, onColors has it (via page1FillColors OR new page1StrokeColors for outline-icon strokes); extractPdfText onText===offText (accent is color-only, extraction text unchanged — THE oracle semantic). AccentPlacementV2 still exactly 9 keys. linkIcons decoupled from links.accentColor (text vs icon); headingRules governs all heading decoration fills/strokes as one shared color independent of headings text gate. Scope clean (4 files)."
    },
    {
      "id": "E9-F3e",
      "title": "Footer: page numbers + email + name + custom line (react-pdf fixed render, paginates)",
      "status": "done",
      "depends_on": [
        "E9-F3d"
      ],
      "files": [
        "src/client/document/engine/document.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/components/DesignPanel.tsx",
        "test/engine-single-column.test.ts",
        "test/document-format-render.test.ts"
      ],
      "origin": "decomposed from E9-F3",
      "context": "E9 phase F3. FooterV2 (src/shared/format-v2.ts :225-230, zod :397-405): pageNumbers, email, name booleans + customText string (max 200, no newlines). GREENFIELD: NO footer in document.tsx today; no react-pdf fixed/render footer anywhere.\nDO: add a Page-level react-pdf footer using the standard View fixed render={({pageNumber,totalPages}) => ...} pattern in BOTH the columnar and non-columnar Page branches of document.tsx. footer.pageNumbers renders pageNumber / totalPages; footer.email renders profile.email; footer.name renders profile.name; footer.customText renders that line. Compose the enabled parts. Thread footer via legacyAdapt or read format directly (document.tsx has the format). Add DesignPanel footer controls (3 toggles + custom-text input honoring no-newline/max-200).\nLocked (oracle.md): design-only, bounded, snapshots byte-identical, never-cut (footer is fixed chrome; must not consume item space or cut content).\nTESTS: footer text RENDERS (extraction contains enabled parts, e.g. profile.email when footer.email on, the customText when set) and PAGE NUMBERS PAGINATE (a tall multi-page fixture shows N / M with correct totalPages on each page; assert page 2 footer reads 2 / M). Footer off => none of those strings present. Never-cut: item count invariant footer on vs off.",
      "acceptance": "oracle.md Phase 8 F3 footer line: footer text renders and page numbers paginate. Executable: full baseline green; enabled footer parts (email/name/customText) appear in extraction, disabled absent; page numbers paginate (multi-page fixture: each page footer shows correct N / totalPages); never-cut (item count invariant footer on vs off); DesignPanel exposes footer toggles + custom-text (no-newline/max-200 enforced).",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f3e @aeab207, merged ff to main). check/build/lint 0; vitest 716/716; playwright exit 0 (10 passed +1 retry-absorbed design.spec host flake). Footer via <Text fixed render={({pageNumber,totalPages})=>}> positioned absolute over the bottom margin (never consumes flow => never-cut), wired into BOTH Page branches; reads format.footer directly; ABSENT when nothing enabled (no preset snapshot changed). Tests non-vacuous: email/name via occurrence-DELTA over the header (avoids header false-positive); customText presence/absence; PAGINATION on growingResumeFixture(24)+strict => 2 real pages, page1=1/2 page2=2/2; never-cut item-count invariant footer on vs off. DesignPanel: 3 toggles + maxLength=200 newline-stripped custom-text. Scope clean (3 files; legacyAdapt untouched, read-format-directly)."
    },
    {
      "id": "E9-F3f",
      "title": "Photo crop/zoom: focal crop + zoom render + size/shape/crop/zoom DesignPanel controls",
      "status": "done",
      "depends_on": [
        "E9-F3e"
      ],
      "files": [
        "src/client/document/sections.tsx",
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/components/DesignPanel.tsx",
        "test/engine-single-column.test.ts"
      ],
      "origin": "decomposed from E9-F3",
      "context": "E9 phase F3. PhotoV2 (src/shared/format-v2.ts :212-219, zod :385-391): hidden, size(32-160), shape(circle|rounded|square), crop:{x,y}(0-100 focal %), zoom(1-2). TODAY legacyAdapt:159 threads only {hidden,size,shape}; sections.tsx renders a plain Image src style photo (:401,414,427) with size+shape only. crop/zoom UNWIRED (document.tsx:59). Only DesignPanel Show-photo (photo.hidden) control exists; size/shape have NO control either.\nDO: render crop/zoom on the photo Image (sections.tsx) using react-pdf Image objectFit + objectPosition (crop.x/y as focal %) and a zoom scale (zoom 1-2 enlarges within the fixed frame, clipped by the shape borderRadius/overflow). Thread photo.crop/zoom (and size/shape which render but have no UI) via legacyAdapt. Add DesignPanel photo controls: size (32-160), shape (circle|rounded|square), crop focal x/y (0-100), zoom (1-2).\nSCOPE NOTE (do NOT expand): a client photo-UPLOAD entry point does NOT exist today (profile.photoUrl is server-accepted but no upload UI). Building an upload/file-picker component is OUT OF SCOPE and NOT in the F3 oracle — test crop/zoom with a FIXTURE photoUrl (a small data: URI is fine). If you believe upload is required, return blocked with reasoning rather than building a file-upload feature.\nLocked (oracle.md): design-only, bounded axes, IMAGE BACKGROUNDS/FRAMES REJECTED (a profile photo is fine; a decorative image bg is not), snapshots byte-identical.\nTESTS (page1Geometry + Buffer.compare at test/engine-single-column.test.ts:126-173, fixture data-URI photo + a profile with photoUrl set, photo.hidden=false): crop focal x/y change the rendered image objectPosition (distinct bytes / measured position); zoom 1 vs 2 changes bytes; size changes measured image dimensions; each shape distinct. Photo hidden=true => no image.",
      "acceptance": "oracle.md Phase 8 F3 photo-crop coverage (E8-A1: each value measurably distinct). Executable: full baseline green; crop focal x/y change rendered position; zoom 1..2 changes bytes; size changes measured dimensions; 3 shapes distinct; DesignPanel exposes size/shape/crop/zoom controls; tested via fixture photoUrl (no upload-UI built, out of scope).",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on e9-f3f @5078bf5, merged ff to main). check/build/lint 0; vitest 723/723 (my clean run — the builders 719/720 fit-ui.test.tsx failure did NOT reproduce, confirmed flake/non-regression; fit-ui not in F3f scope); playwright 11/11 (no flake). Photo crop/zoom: clipping frame (overflow:hidden + shape borderRadius) + oversized size*zoom Image re-centered via negative margins (enlarge-then-clip) + objectFit:cover + objectPosition from crop.x/y; one buildPhotoNode seam feeds all 3 ProfileHeader variants. Config threaded via new photoConfig extra property (legacy photo type untouched => scope-clean). Tests non-vacuous (non-square fixture PNGs so crop actually moves): hidden gate, frame size tracks photo.size, 3 shapes pairwise-distinct bytes, crop.x/crop.y each distinct image transform, zoom 1 vs 2 distinct bytes, never-cut. Scope clean (4 files). Upload-UI correctly OUT of scope (fixture data-URI photo). No image-bg (scope tripwire honored)."
    },
    {
      "id": "E9-F4",
      "title": "COARSE (decompose at pickup): per-section displays + skill/language levels (§31.4)",
      "status": "decomposed",
      "depends_on": [
        "E9-F3f"
      ],
      "files": [
        "src/client/",
        "src/shared/",
        "src/server/",
        "test/",
        "drizzle/"
      ],
      "origin": "spec §31.2 last bullet + §31.4 + §31.6 F4",
      "context": "COARSE EPIC — decompose at pickup. Scope: per-section display axes (skills/languages layout grid|rows|compact|bubble|level, grid columns 1-4, level display text|dots|bar with 5 renamable labels; interests same minus level; experience title/employer order + group-promotions toggle; summary in-header + show-heading; education degree/school order) PLUS the ONE schema touch: optional meta.level 1-5 on skill-class Library entries (§4.3 registry + EntryEditor + zod), levels are CONTENT (values) vs FORMAT (labels), unleveled entries fall back to rows — level NEVER invented. TRIPWIRE: level-scoring = failed ticket; the anti-scoring control test (tailoring invariant to level values) is REQUIRED. Level dots/bars extraction-neutrality CI test required ([v3-038] promoted row). Oracle: Phase 8 F4 line.",
      "acceptance": "oracle.md Phase 8 'F4' checks + full baseline. Decompose before dispatch.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E9-F4a",
      "title": "Content schema: numeric meta.level 1–5 on skill-class entries + EntryEditor + data migration + anti-scoring control",
      "status": "done",
      "depends_on": [
        "E9-F3f"
      ],
      "files": [
        "src/shared/types.ts",
        "src/shared/schema.ts",
        "src/shared/sections.ts",
        "src/client/components/SectionMetaFields.tsx",
        "src/client/components/EntryEditor.tsx",
        "drizzle/",
        "test/schema.test.ts",
        "test/remaining-sections.test.tsx",
        "test/entry-editor.test.tsx",
        "test/tailor-level-invariance.test.ts"
      ],
      "origin": "decomposed from E9-F4 (spec §31.4 — the one content-schema touch)",
      "context": "F4 CHILD 1 of 4 (the CONTENT-schema touch — §31.4). The design-format side of F4 (SectionDisplayV2, levelDisplay/levelLabels axes) ALREADY EXISTS in src/shared/format-v2.ts and is migrated/validated; this ticket does NOT touch format-v2.ts. This ticket changes ONE Library content field so the numeric level VALUES that later render tickets display can exist.\n\nLOCKED (cite oracle.md §31.4): skill/language levels are CONTENT (the numeric value), the 5 renamable labels are FORMAT (already in format-v2). Tailoring IGNORES levels — level-scoring = FAILED ticket (extends the §1 tag-scoring tripwire).\n\nCOLLISION RESOLUTION (coordinator decision [v3-072], user-approved 2026-07-07): today `meta.level` is free-text `z.string().min(1).max(120).optional()` on BOTH skill and language (src/shared/schema.ts:89,102; type src/shared/types.ts:59,61). REPURPOSE it to numeric: `meta.level: z.number().int().min(1).max(5).optional()` on skill AND language (§31.4 numeric 1–5). This is a breaking value change, handled deterministically:\n1. types.ts EntryMeta: skill `level?: number`, language `level?: number`.\n2. schema.ts skillMetaZ + languageMetaZ: `level: z.number().int().min(1).max(5).optional()`.\n3. src/shared/sections.ts: language's `metaText: (m) => m.level ?? \"\"` (line 66) currently PRINTS the free-text level on the resume. The numeric level is now shown by the (later) levelDisplay render, NOT as metaText — so REMOVE the level from language metaText (language then has no metaText; the language name lives in facts, same as interest). Do not invent a new metaText.\n4. DATA MIGRATION (establishes the first data-migration precedent — drizzle/ has only DDL so far). `meta` is JSON text in SQLite (entries.meta = text mode:json). Existing rows may hold a STRING level that the new numeric zod would reject on next read/edit. Add a new drizzle migration (bump the journal) that clears string levels: `UPDATE entries SET meta = json_remove(meta, '$.level') WHERE section IN ('skill','language') AND json_type(meta, '$.level') = 'text';`. All current levels are strings so this clears them (the product has real user data — memory: real multi-user product). Numeric levels (none yet) are left intact by the json_type guard. Verify the migration is included and runs on boot.\n5. CLIENT entry editor: SectionMetaFields.tsx META_FIELDS has skill `[{category},{level}]` and language `[{level}]` rendered as free-text <Input> (MetaFieldDescriptor = {key,label,required}). Level must become a BOUNDED 1–5 picker (extend MetaFieldDescriptor with an enum/number-range variant, or a dedicated level control), NOT a free-text input. EntryEditor.tsx cleanMeta/validate must coerce/validate the numeric level (empty ⇒ omit the optional field — unleveled entries are valid and MUST stay possible; level NEVER invented).\n\nANTI-SCORING CONTROL (REQUIRED by oracle F4 line, this ticket owns it): tailoring is currently blind to meta.level (verified: nothing in src/server/tailor/ reads it). Add test/tailor-level-invariance.test.ts: two libraries identical EXCEPT skill/language level values ⇒ tailor()/assemble produce byte-identical selection + order (mirror the existing structure-ignores-tags test pattern — find it via `grep -rn \"tags\" test/ | grep -i identical/invariant` or in assemble/engine tests). This is the level-scoring tripwire guard.\n\nUpdate test/remaining-sections.test.tsx (uses `level: \"fluent\"` for language — change to numeric or drop, and drop any assertion that 'fluent' renders as language metaText — it no longer does). Update test/schema.test.ts + test/entry-editor.test.tsx for the numeric level. SEED_ENTRIES (src/server/seed.ts) has NO level usage — no seed change needed. prompt.ts is NOT edited (level is not a prompt/emphasis signal) ⇒ the honesty-note T014/T013 re-run trigger does NOT fire.\n\nOUT OF SCOPE (later F4 children): rendering the level display (dots/bars/labels) — F4b; DesignPanel level controls — F4d. Do NOT touch format-v2.ts, sections.tsx (the .TSX renderer), legacyAdapt.ts, DesignPanel.tsx.",
      "acceptance": "Baseline (oracle.md, bun toolchain): `bun run check`, `NODE_OPTIONS=--max-old-space-size=1024 bun run build`, `bun run lint`, `vitest run --passWithNoTests` + `playwright test` (chromium) all green.\n- meta.level is numeric int 1–5 optional on skill AND language (types.ts + schema.ts); a string level value FAILS entryInput/entryImport parse; a numeric 3 passes; omitted level passes (unleveled valid).\n- data migration present + runs on boot; a DB seeded with a skill/language entry whose meta.level is a string 'fluent' has that level cleared (json_remove) so the row parses under the new schema.\n- language section no longer renders level as metaText (sections.ts registry).\n- EntryEditor: level is a bounded 1–5 control (not free text); saving with no level omits the field.\n- test/tailor-level-invariance.test.ts GREEN and NON-VACUOUS: two libraries differing ONLY in level values produce identical tailor selection+order (level-scoring tripwire).\n- new behavior has new tests; no baseline regression; scope: only declared files.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, clean tree, main) GREEN: bun run check exit 0; NODE_OPTIONS=--max-old-space-size=1024 bun run build exit 0 (built 11.25s); bun run lint exit 0 (197 files, no fixes); vitest 743/743 pass (58 files, no timeouts in clean run); playwright 11/11 pass (chromium+auth+applications). Scope clean: only declared files touched (types.ts/schema.ts/sections.ts/SectionMetaFields.tsx/EntryEditor.tsx/drizzle 0004+journal/4 test files). Gaming read PASS: meta.level string->number(1-5) on skill+language; language metaText removed; migration drizzle/0004_clear_string_levels.sql json_remove w/ json_type=text guard, registered idx4 in _journal.json; EntryEditor bounded <select> 1-5 + Not-set, omits when unset (never invents); anti-scoring control test/tailor-level-invariance.test.ts NON-VACUOUS (2 libs differ only in level -> identical resume; 3rd test proves order is rank/sortKey-driven not level-driven)."
    },
    {
      "id": "E9-F4b",
      "title": "Render: skills/languages/interests display (grid|rows|compact|bubble|level) + level display (text|dots|bar) + levelIndicators accent gate live",
      "status": "done",
      "depends_on": [
        "E9-F4a"
      ],
      "files": [
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/document/sections.tsx",
        "src/client/document/engine/document.tsx",
        "test/engine-section-display.test.ts"
      ],
      "origin": "decomposed from E9-F4 (spec §31.2 last bullet skills/languages/interests + §31.4 level display + §31.6 F4)",
      "context": "F4 CHILD 2 of 4 (RENDER — the label-section displays + numeric level). Depends on F4a (numeric meta.level). THE HEADLINE GAP: format.sectionDisplay is fully defined/migrated/validated but the render engine IGNORES it entirely — toLegacyFormat (src/client/document/engine/legacyAdapt.ts:203) sets `sections: {}` and emits NO sectionDisplay config, so skills/languages/interests currently render as one hardcoded 'rows' look. This ticket wires the skills/languages + interests display axes through the engine.\n\nSEAM PATTERN (follow the existing ones exactly): each axis group in legacyAdapt.ts has a `resolveXConfig` fn + exported `XRenderConfig` type + a passthrough field on toLegacyFormat's return (see resolveEntriesConfig / EntriesRenderConfig ~136-140, and accentPlacementConfig). ADD `resolveSectionDisplayConfig` + `SectionDisplayRenderConfig` + a `sectionDisplayConfig` field; sections.tsx reads it off `format`.\n\nAXES TO RENDER (from format.sectionDisplay, defined in src/shared/format-v2.ts):\n- skillsLanguages.layout: 'grid'|'rows'|'compact'|'bubble'|'level'. rows = today's stacked look (baseline). grid = gridColumns (1–4) columns (the existing GroupBlock itemsGrid machinery at sections.tsx ~1027 is reachable via columns>1 — reuse it, do NOT add a second grid path). compact = denser inline. bubble = pill/chip style. level = show each item with its numeric level (see below).\n- skillsLanguages.gridColumns 1–4 (already editable in DesignPanel; make it actually change columns).\n- skillsLanguages.levelDisplay: 'text'|'dots'|'bar' — how a level renders: text = the renamable label (levelLabels[level-1]); dots = level filled of 5 dots; bar = level/5 filled bar.\n- skillsLanguages.levelLabels: [5 strings] — the display text for text mode (FORMAT, from format).\n- interests.layout: 'grid'|'rows'|'compact'|'bubble' (NO level) + interests.gridColumns 1–4.\n\nLEVEL RULES (oracle F4 line, §31.4): the numeric meta.level (1–5) is CONTENT. UNLEVELED entries render level-less — the 'level' LAYOUT falls back to 'rows' for an entry with no level; NEVER invent a level (no default number). A mixed library (some leveled, some not) renders each per its own level presence.\n\nlevelIndicators ACCENT GATE — complete the F3d residual: format.colors.accentPlacement.levelIndicators is currently a WIRED NO-OP (sections.tsx DEFAULT_ACCENT_PLACEMENT ~147-161 documents 'built in E9-F4'; the gate helper is accentOrText / resolveAccentPlacement ~163-186). Now that the level display element exists, make levelIndicators LIVE: when true, dots/bars/level-text paint colors.accent; when false, colors.text. Test it.\n\nEXTRACTION-NEUTRALITY CI TEST (REQUIRED — [v3-038] promoted ATS row: 'level display ANY = neutral'): dots and bars are DRAWINGS; they must NOT perturb pdf.js text extraction ORDER or CONTENT. Add test/engine-section-display.test.ts asserting: for a leveled fixture, extractPdfText (src/client/document/extractText.ts) is byte-identical across levelDisplay='text' vs 'dots' vs 'bar' EXCEPT the label text that text-mode legitimately adds (assert dots and bars add NO text vs a no-level render; assert extraction ORDER index-increasing unchanged). Follow the op-extractor pattern inline in test/engine-single-column.test.ts (page1FillColors/page1StrokeColors/renderEngineToBuffer).\n\nPER-VALUE DISTINCTNESS (E8-A1 contrast pattern, parameterized): each layout value (grid/rows/compact/bubble/level) and gridColumns 1..4 produces measurably distinct bytes/geometry; levelDisplay text/dots/bar distinct. Use growingResumeFixture (test/engine-two-column.test.ts:102) / SEED_ENTRIES for fixtures; add leveled skill fixtures (some with meta.level, some without — to prove the fallback).\n\nNEVER-CUT (§28.4): item count invariant across densities for every new layout. OUT OF SCOPE: experience/summary/education display (F4c), DesignPanel controls (F4d), format-v2.ts, DesignPanel.tsx. Only declared files.",
      "acceptance": "Baseline (oracle.md) all green (check/build/lint/vitest+playwright chromium).\n- skillsLanguages.layout grid|rows|compact|bubble|level each renders + is measurably distinct (parameterized contrast); gridColumns 1–4 measurably change column count; interests layout+gridColumns render.\n- levelDisplay text uses levelLabels[level-1]; dots = level-of-5; bar = level/5; each distinct.\n- UNLEVELED entry in 'level' layout renders via 'rows' fallback, level-less; NO invented level (test with a mixed fixture).\n- levelIndicators accent toggle: level indicators paint accent when true, text when false (from-baseline test).\n- test/engine-section-display.test.ts EXTRACTION-NEUTRALITY: dots/bars add no extraction text + order index-increasing unchanged vs text mode's legitimate label ([v3-038] promoted-row CI test) — GREEN.\n- never-cut item-count invariant across densities for each layout; no baseline regression; only declared files.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, combined tree main) GREEN: check 0; build 0 (11.43s); lint 0 (198 files); vitest 774/775 — the 1 failure is boot.smoke.test.ts 20s TIMEOUT under concurrent load, CONFIRMED flake (passes 3/3 in 7s isolated); playwright 11/11. Scope clean vs declared (legacyAdapt.ts + client/document/sections.tsx + test/engine-section-display.test.ts; document.tsx untouched). Gaming read PASS: seam resolveSectionDisplayConfig/SectionDisplayRenderConfig/sectionDisplayConfig mirrors linksConfig; grid reuses existing itemsGrid path (no 2nd grid); levelDisplay text=levelLabels[level-1], dots/bar pure Views (zero text children); levelIndicators accent gate live via accentOrText in buildStyles. Tests NON-VACUOUS: grid column-count measured by distinct bullet x-positions (1-4); 5 layouts pairwise-distinct + never-cut; dots/bar add fills but ZERO extraction text vs level-less baseline (extraction-neutrality, promoted row); unleveled-in-level==rows by geometry+fills+strokes+text equality. KNOWN GAP (builder-flagged, owned by NEW ticket E9-F4b2): meta.level not yet threaded entry->assemble->TailoredItem, so item.level is undefined in the real pipeline until F4b2."
    },
    {
      "id": "E9-F4b2",
      "title": "Thread meta.level entry->assemble->TailoredItem so the level display is reachable + relax anti-scoring test to selection/order invariance",
      "status": "done",
      "depends_on": [
        "E9-F4b"
      ],
      "files": [
        "src/shared/types.ts",
        "src/server/tailor/assemble.ts",
        "test/assemble.test.ts",
        "test/tailor-level-invariance.test.ts"
      ],
      "origin": "repair: F4b flagged meta.level never reaches TailoredItem (assemble.ts out of F4b scope) => level display renders nothing for real resumes; escaped-gap repair of my F4 under-specification",
      "context": "F4 REPAIR ticket. F4b built + tested the skill/language LEVEL DISPLAY render (dots/bars/text) behind an `item.level` seam, but the value never flows there: assemble() (src/server/tailor/assemble.ts) builds TailoredItem from the decision + entries and DROPS meta.level, so in the real pipeline every item.level is undefined and the level display always falls back to level-less. Thread the CONTENT level onto the assembled item so the feature is reachable end-to-end.\n\nDO: (1) src/shared/types.ts — TailoredItem gains optional `level?: number`. (2) src/server/tailor/assemble.ts — when building items for the skill AND language sections, look up the source entry by entryId and copy entry.meta.level onto the assembled item (ONLY skill/language — the sections whose EntryMeta carries level; NEVER invent a level; entries without one leave item.level undefined). This is what F4b's sections.tsx resolveItemLevel(item) reads. (3) SNAPSHOT SELF-CONTAINMENT (spec §27): level must ride IN the snapshot (snapshots hold full content, never entry-ID references) — copying it at assemble time satisfies this; editing/deleting the Library entry later leaves a stored snapshot byte-identical. Confirm existing §27/§28.1 byte-stability + snapshot-integrity tests stay green (this is CONTENT in a NEW tailor, never a mutation of a stored one).\n\nCRITICAL — RELAX THE ANTI-SCORING TEST (test/tailor-level-invariance.test.ts, created by F4a): it currently asserts `expect(resumeA).toEqual(resumeB)` for two libraries differing ONLY in level values. Once level is copied onto items that FULL deep-equality is WRONG — level now legitimately appears in the output. The real oracle (oracle.md F4 line) is that level never changes SELECTION or ORDER, NOT that the whole output is invariant. Rewrite the first assertion to compare SELECTION + ORDER only: the sequence of entryIds per section/group must be identical across the two libraries (strip/ignore the level field). KEEP it non-vacuous (the two libs still differ in level; order still rank/sortKey-driven). The existing 3rd test (order is decision/sortKey-driven, not level-driven) stays as the positive guard. Level-scoring is STILL a failed ticket.\n\n(4) validateNoFabrication is unaffected (level is a number from meta, not item text) — do not change validate; confirm a level on an item does not trip fabrication. (5) test/assemble.test.ts — new test: a skill/language entry with meta.level=N yields an assembled item with level=N; an entry without it yields item.level undefined; a NON-leveled section (e.g. experience) never gets a level.\n\nDECLARED FILES ONLY: src/shared/types.ts, src/server/tailor/assemble.ts, test/assemble.test.ts, test/tailor-level-invariance.test.ts. DO NOT touch sections.tsx, legacyAdapt.ts, format-v2.ts, DesignPanel.tsx, prompt.ts, validate.ts.",
      "acceptance": "Baseline (oracle.md) all green (check/build/lint/vitest+playwright chromium; suite floor 775).\n- TailoredItem has optional level; assemble copies entry.meta.level onto skill/language items (test), undefined when the entry has none (never invented), never onto non-leveled sections.\n- test/tailor-level-invariance.test.ts rewritten to SELECTION+ORDER invariance (entryId sequences identical across level-differing libs), still non-vacuous; level-scoring guard intact.\n- §27 snapshot-integrity + §28.1 byte-stability tests stay green; validateNoFabrication unaffected.\n- no baseline regression; only declared files.",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, main) GREEN: check/build/lint 0; vitest 778/778 (clean run, no flake; floor 775 -> +3); playwright 11/11 (1 retry-absorbed design.spec.ts sandbox font-fetch flake, exit 0). Scope clean (4 declared files). assemble.ts: toGroup item map adds level: entryLevel(r.entry.meta); helper entryLevel switches on meta.section, returns meta.level for skill/language ONLY, undefined otherwise — copied from the already-resolved Resolved{item,entry} pair (no new lookup), satisfying §27 snapshot self-containment. types.ts: TailoredItem gained level?: number. 3 new assemble tests (level copied for skill/language present+absent; never on experience; not fabrication). Invariance test assertion-1 rewritten to compare entryId sequences per section/group (level stripped) — selection+order invariance; non-vacuous tests 2 (libs differ in level) + 3 (order rank/sortKey-driven) kept. validate.ts untouched; §27/§28.1 snapshot+byte-stability tests green in the full run."
    },
    {
      "id": "E9-F4c",
      "title": "Render: experience (title/employer order + group-promotions) + summary (as-part-of-header + show-heading) + education (degree/school order)",
      "status": "done",
      "depends_on": [
        "E9-F4b"
      ],
      "files": [
        "src/client/document/engine/legacyAdapt.ts",
        "src/client/document/sections.tsx",
        "test/engine-section-order.test.ts"
      ],
      "origin": "decomposed from E9-F4 (spec §31.2 last bullet experience/summary/education + §31.6 F4)",
      "context": "F4 CHILD 3 of 4 (RENDER — the narrative-section display toggles). Depends on F4b (shares the sectionDisplayConfig seam in legacyAdapt.ts + sections.tsx; serialized after F4b to avoid conflict). Extends the SAME resolveSectionDisplayConfig/SectionDisplayRenderConfig seam F4b added — add experience/summary/education to it. AXES (from format.sectionDisplay, defined in src/shared/format-v2.ts):\n- experience.order: 'title-first'|'employer-first'. Today the experience heading groups by `${company} · ${role} · ${period}` (employer-first). title-first swaps to role-leading. See how the experience group heading renders (sections.tsx headingPartsFromMeta / GroupBlock; registry groupBy in src/shared/sections.ts is the CURRENT order — the axis overrides the display order, NOT the registry).\n- experience.groupPromotions: boolean. When true, multiple roles at the SAME employer group under one employer header (promotions shown as sub-entries); when false, each role is its own group (today's look). Use a fixture with two roles at one company to prove the difference.\n- summary.asPartOfHeader: boolean — summary renders inside/under the ProfileHeader block vs as its own section (today: own section, SummarySection sections.tsx:856). summary.showHeading: boolean — show the 'Summary' section label (today: no label).\n- education.order: 'degree-first'|'school-first' (today groupBy `${school} · ${degree}` = school-first). degree-first leads with the degree.\n\nPER-VALUE DISTINCTNESS (E8-A1 pattern): each toggle measurably changes bytes/geometry/extraction order. Add test/engine-section-order.test.ts (op-extractor + extractPdfText pattern from engine-single-column.test.ts). For order swaps, assert the extraction text ORDER of the two parts flips. For summary.asPartOfHeader, assert the summary text's position moves relative to the header. For groupPromotions, assert group COUNT changes (2 roles/1 company ⇒ 1 group when true, 2 when false) with item count invariant (§28.4 never-cut).\n\nOUT OF SCOPE: skills/languages/interests + level display (F4b, already merged), DesignPanel controls (F4d), format-v2.ts, DesignPanel.tsx. Only declared files.",
      "acceptance": "Baseline (oracle.md) all green.\n- experience.order title-first vs employer-first: extraction order of role vs employer flips (measured).\n- experience.groupPromotions true vs false: group count changes for a 2-roles-1-company fixture; item count invariant (never-cut).\n- summary.asPartOfHeader moves the summary relative to the header (measured); summary.showHeading adds/omits the section label (extraction).\n- education.order degree-first vs school-first flips degree/school order (measured).\n- test/engine-section-order.test.ts non-vacuous + green; no baseline regression; only declared files.",
      "attempts": [],
      "evidence": "Committed a0d6525 (user drove it locally after killing the first builder). Coordinator INDEPENDENT re-verify: commit scope clean (legacyAdapt.ts + sections.tsx + engine-section-order.test.ts only); its 14 tests green (order flips, groupPromotions group-count + never-cut, summary gap/label, education flip); seam widened Pick->full SectionDisplayV2. BUT surfaced a latent F0 baseline regression (engine-entries F2e subtitlePlacement) — F4c was committed WITHOUT the full suite (builder killed at that step). Root cause NOT in F4c: baseFromV1 defaulted experience.order/education.order to the swap value (employer-first/school-first) keyed off groupBy, while the pre-E9 render leads with role/degree. Fixed by the [v3-076] repair (commit 51551a9). F4c render code itself is correct."
    },
    {
      "id": "E9-F4d",
      "title": "DesignPanel controls for all F4 sectionDisplay axes (layout/levelDisplay/levelLabels/experience/summary/education) + unit + e2e",
      "status": "done",
      "depends_on": [
        "E9-F4c",
        "E9-F4b2"
      ],
      "files": [
        "src/client/components/DesignPanel.tsx",
        "test/design-panel.test.tsx",
        "test/e2e/design.spec.ts"
      ],
      "origin": "decomposed from E9-F4 (spec §31.2b design view — Sections group controls + §31.6 F4)",
      "context": "F4 CHILD 4 of 4 (DESIGN PANEL controls). Depends on F4b+F4c (controls must target axes that actually render, so the e2e repaint proof is meaningful) via F4c. Today DesignPanel.tsx (~1547 lines) exposes ONLY skillsLanguages.gridColumns + interests.gridColumns (the 'Section columns' group ~1251-1292 via GridColumnsSelect). Every other sectionDisplay axis is persisted-default-only with NO UI. Add controls following the EXISTING pattern exactly: a control group `<div className=\"flex flex-col gap-3\"><p className=\"text-sm font-medium\">GroupTitle</p>...</div>` with FieldRow + reusable primitives (EnumSelect, NumberStepper, GridColumnsSelect ~1518, ColorField, FontSelect); the `set(next)` helper (~455) writes `onChange({...format, sectionDisplay:{...format.sectionDisplay, group:{...}}})`. Option constant tables live at top ~79-200 (follow COLUMNS_MODE_OPTIONS style).\n\nADD controls for: skillsLanguages.layout (grid|rows|compact|bubble|level), skillsLanguages.levelDisplay (text|dots|bar), skillsLanguages.levelLabels (5 editable text inputs, each 1–40 chars per formatV2Schema), interests.layout (grid|rows|compact|bubble), experience.order + experience.groupPromotions, summary.asPartOfHeader + summary.showHeading, education.order. Keep it in the 'Sections' jump-nav group (§31.2b control order ends with Sections). Per §31.2b, a per-section control group SHOULD appear only when that section has content (FlowCV rule) — follow whatever gating the panel already uses for content-aware groups; if none exists, showing all is acceptable (don't invent a config).\n\nPERSISTENCE: unchanged — DesignView.tsx debounces (~300ms) then PUTs via updateApplication/updateSettings; server validates with formatV2Schema (src/shared/schema.ts:176 documentFormatZ). No new persistence code; just drive format through onChange.\n\nREAD-ONLY: a locked application opens the design view with every control disabled (existing readOnly prop) — your new controls MUST honor readOnly like the siblings.\n\nTESTS: extend test/design-panel.test.tsx (unit — each new control writes the right format path; design-panel.test.tsx currently has NO sectionDisplay coverage). Extend test/e2e/design.spec.ts (chromium): change a new Sections control (e.g. skillsLanguages.layout) ⇒ the pinned pdf.js preview REPAINTS (pixel-diff, E8-B1 pattern already in design.spec.ts) AND the PUT persists across reload; a locked app's Sections controls are disabled. NO uncaught console errors.\n\nOUT OF SCOPE: the EntryEditor level control (F4a, done), render logic (F4b/F4c, done), format-v2.ts. Only declared files.",
      "acceptance": "Baseline (oracle.md) all green (incl. playwright chromium).\n- DesignPanel exposes controls for skillsLanguages.layout+levelDisplay+levelLabels(×5), interests.layout, experience.order+groupPromotions, summary.asPartOfHeader+showHeading, education.order; each writes the correct format.sectionDisplay path (unit test, non-vacuous).\n- controls honor readOnly (disabled for a locked app).\n- e2e (design.spec.ts): changing a Sections control drives the rendered PDF — proven by a RELOAD-forced remount pixel-diff (experience.order flip over the experience-only seed) + PUT persists across reload; locked ⇒ disabled; no uncaught console errors. [AMENDED, human-authorized, ledger v3-079: original said \"repaints the preview (pixel-diff)\" assuming a live-repaint the product never had — pre-existing DocumentPreview.tsx bug, tracked by repair ticket E9-F4d2 which restores the LIVE-repaint assertion.]\n- no baseline regression; only declared files.",
      "attempts": [
        {
          "n": 1,
          "failed": "e2e (test/e2e/design.spec.ts) — the new pixel-diff repaint assertion timed out 3x (initial+2 retries), NOT a flake. check/build/lint/unit(32/32) all GREEN; product code correct.",
          "hypothesis": "The e2e pixel-diff control choice is wrong for THIS fixture. The e2e uses the server default SEED_ENTRIES (src/server/seed.ts) which contains ONLY 6 experience entries — NO skill/language content. So changing sectionDisplay.skillsLanguages.layout rows->bubble is a VISUAL NO-OP (no skills to render) => preview never repaints => poll times out. The DesignPanel control itself works (PUT persisted 200 with layout=bubble); only the visual-proof control is mis-chosen. ALSO flagged: builder made an OUT-OF-SCOPE edit to spec.md (invented a whole new §32 voice-sources/cover-letters feature); coordinator reverted it — do NOT touch spec.md.",
          "fixNote": "In test/e2e/design.spec.ts step (5b), drive the pixel-diff on a control whose axis affects a section that HAS content in the seed fixture. Use experience.order: snapshot previewDataUrl, change the \"Experience order\" combobox (e.g. to \"Title first\" — DEFAULT_FORMAT_V2 experience.order is \"employer-first\", so this flips the heading = guaranteed pixel change over 6 experience entries), assert the PUT persists format.sectionDisplay.experience.order===\"title-first\" AND the preview pixel-diff changes, AND after reload the \"Experience order\" combobox still reads \"Title first\". (summary.showHeading is an acceptable alternative — the decision fixture has a non-empty summary.) Keep the read-only assertions as-is (skillsLanguages combobox disabled is fine — it only checks disabled state, not repaint). Do NOT touch spec.md or any file outside the 3 declared. Re-run the FULL gate (check/build/lint/unit + this e2e) and return the structured result."
        },
        {
          "n": 2,
          "failed": "e2e repaint assertion again — but now root-caused. Live pixel-diff on .document-preview canvas cannot pass: even a body-font change does not repaint in place.",
          "hypothesis": "PRE-EXISTING product bug, OUTSIDE F4d scope: the main design preview never live-repaints on ANY format change. Confirmed by TWO independent investigations (builder + coordinator Explore agent): @react-pdf usePDF render effect has empty deps (react-pdf.browser.js:347) => renders once on mount; DocumentPreview.tsx:144 never calls usePDF s update() fn and RenderedPreview has no format-keyed remount; the multi-page PdfPages host recreates canvases only on remount. No pre-existing test ever asserted the main preview live-repaints (applications.spec.ts uses expectCanvasPainted non-white checks; the E8-B1 pixel-diff runs on the THUMBNAIL canvas, a different thumbnail.tsx pipeline that DOES repaint). So F4d s acceptance (\"changing a Sections control repaints the preview (pixel-diff)\") assumed a live-repaint capability the product has never had.",
          "fixNote": "ESCALATED — semantic acceptance decision required (see ledger [v3-078]). Builder final e2e proves the axis via a RELOAD-forced remount pixel-diff (experience.order employer-first->title-first over the experience-only §22 seed) + PUT persistence + locked-disabled + no console errors = the honest codebase-consistent oracle. Awaiting human: (A) accept reload-based oracle + spawn repair ticket for the live-repaint bug that asserts LIVE pixel-diff (escaped-bug rule); (B) expand F4d to fix DocumentPreview.tsx now; (C) block on the preview bug first."
        }
      ],
      "evidence": "Committed 31280cd (user drove it locally). Coordinator INDEPENDENT re-verify: commit scope clean (DesignPanel.tsx + design-panel.test.tsx + design.spec.ts only); adds real Sections controls (skillsLanguages layout/levelDisplay/levelLabels, interests layout, experience order/groupPromotions, summary asPartOfHeader/showHeading, education order) via the existing EnumSelect/set pattern; 167-line unit test + e2e repaint/persist coverage; not gamed. 3 of its default-dependent tests (design-panel unit x2 + design.spec e2e) updated in the [v3-076] repair to select the non-default order value. Green in the full baseline."
    },
    {
      "id": "E9-F4c-r1",
      "title": "Repair: correct v1→v2 migration default for experience/education order (latent F0 bug surfaced by F4c)",
      "status": "done",
      "depends_on": [
        "E9-F4c"
      ],
      "files": [
        "src/shared/format-v2.ts",
        "test/format-v2.test.ts",
        "test/design-panel.test.tsx",
        "test/e2e/design.spec.ts"
      ],
      "origin": "repair: F4c baseline regression (engine-entries F2e subtitlePlacement) — escaped because F4c was committed without the full suite (builder interrupted)",
      "context": "baseFromV1 keyed experience.order/education.order off the groupBy KEY order (company·role, school·degree) => 'employer-first'/'school-first', but assemble's headingParts render title=role / title=degree FIRST. Dormant until F4c consumed the axis; then the default heading render silently flipped and the F2e subtitlePlacement test regressed (swapped subtitle stopped moving). Fix A (user-approved): default -> title-first/degree-first, matching the pre-E9 display; update the 3 tests that hardcoded the old default to select the non-default value.",
      "acceptance": "engine-entries F2e green again; full baseline green (804/804 vitest; all e2e specs pass isolated); default now matches pre-E9 render (fixture-exact intent, §31.6 F0).",
      "attempts": [],
      "evidence": "Coordinator-applied (surgical, fully diagnosed), committed 51551a9. check/lint/build 0; vitest 804/804; every playwright spec passes isolated (design.spec + applications.spec flake only under full concurrent load — shared server, pre-existing sandbox pattern). engine-entries 13/13. Escaped-bug closure: the corrected default is now GATED by the render (engine-entries only passes at title-first; engine-section-order exercises both values explicitly), so the value can't silently drift from the display again."
    },
    {
      "id": "E9-F5",
      "title": "COARSE (decompose at pickup): user presets + atsGrade(format) + tested classification table",
      "status": "decomposed",
      "depends_on": [
        "E9-F4d"
      ],
      "files": [
        "src/client/",
        "src/shared/",
        "src/server/",
        "test/"
      ],
      "origin": "spec §31.1 presets + §31.5 + §31.6 F5",
      "context": "COARSE EPIC — decompose at pickup. Scope: settings.presets[] (named DocumentFormatV2 snapshots), 'Save current design as preset' in the design view, preset gallery over the full v2 space (six migrated + a handful curated from the new axes), pure atsGrade(format) → strict|good implementing the TESTED classification table (oracle.md Phase 8 intake decisions [v3-038]: border ANY + level display ANY are NEUTRAL — promoted by probe; strict still requires columns one, no photo, heading icons none, single-mode-over-white). The grade function's unit table must EQUAL the CI-observed classification from the F3/F4 axis extraction tests; every shipped preset's badge matches its tested grade; per-cause caveat UI. Oracle: Phase 8 F5 line.",
      "acceptance": "oracle.md Phase 8 'F5' checks + full baseline. Decompose before dispatch.",
      "attempts": [],
      "evidence": null
    },
    {
      "id": "E9-F5a",
      "title": "Pure atsGrade(format) + downgrade-causes + tested unit table; effectiveAtsGrade delegates",
      "status": "done",
      "depends_on": [
        "E9-F4d"
      ],
      "files": [
        "src/client/document/registry.ts",
        "test/ats-grade.test.ts"
      ],
      "origin": "decomposed from E9-F5 (spec §31.5 + F5 line) — the pure grade fn + classification table; unblocks F5c/F5d",
      "context": "Lede resume-tailoring app. Extract a PURE atsGrade(format: DocumentFormatV2): AtsGrade ('strict'|'good') in src/client/document/registry.ts, keyed ONLY on the format's own axes, implementing the SHIPPED ATS classification table locked at intake (oracle.md Phase 8 [v3-038]). Refactor the existing effectiveAtsGrade(manifest, format) (registry.ts:98-101) to DELEGATE to it (both UI call sites go through effectiveAtsGrade; keep its signature). Also export atsGradeCauses(format): string[] returning the human-readable reasons a format is downgraded to 'good' (for the F5c caveat UI).\n\nTHE SHIPPED TABLE (oracle [v3-038]) — atsGrade === 'strict' IFF ALL hold; otherwise 'good':\n  - layout.columns === 'one' AND layout.headerPosition === 'top' (a left/right header column = sidebar = good)\n  - photo.hidden === true (no photo shown)\n  - headings.icons === 'none'\n  - background is single mode over white: colors.mode === 'single' AND colors.area is not a full-page/header background fill AND the effective page background is white. Read ColorsV2 in src/shared/format-v2.ts (~l.171-182) for the exact area/mode/background-hex semantics; cross-check test/engine-single-column.test.ts l.501-540 (single-mode stays white; a full-page dark bg is the downgrading case).\nGRADE-NEUTRAL axes — MUST NOT affect the grade (PROMOTED to neutral by the intake probe [v3-038]): colors.border (any size/sides), sectionDisplay levelDisplay (text|dots|bar), contact icons, footer. NOTE these are extraction-ORDER-neutral yet still grade-neutral; do NOT confuse with backgrounds, which are extraction-order-neutral but STILL DOWNGRADE the grade per the kept spec row.\n\nGROUND-TRUTH ANCHOR: the six built-in presets (src/client/document/presets.ts PRESETS + registry.ts PRESET_MANIFESTS l.34-82) already declare a grade. Your function MUST satisfy: for every PresetId, atsGrade(PRESETS[id]) === PRESET_MANIFESTS[id].atsGrade — strict for strict/classic/compact/banner, good for sidebar-left/sidebar-right. Derive the exact axis conditions so this holds AND matches the [v3-038] table above. This equality is the F5 oracle line 'every shipped preset's badge matches its tested grade'.\n\nLocked: bounded axes only; ONE render engine; no tag/level scoring. Do not change any preset's data or the engine. AtsGrade type already lives at registry.ts:20 — reuse it (only two product grades exist; 'neutral' is a CI-test classification, NOT a product grade).",
      "acceptance": "- Baseline (oracle.md): bun run check exit0; NODE_OPTIONS=--max-old-space-size=1024 bun run build exit0; bun run lint exit0; bun run vitest run --passWithNoTests green; bunx playwright test --project=chromium green.\n- NEW test/ats-grade.test.ts (non-vacuous unit table):\n  (a) per-axis: from a strict baseline format, toggling colors.border (any), levelDisplay dots/bar, contact icons, footer does NOT change grade (stays strict); setting layout.columns to two|mix, OR photo.hidden=false, OR headings.icons to outline|filled, OR a full-page/dark background, OR headerPosition left|right EACH flips grade to 'good'.\n  (b) for ALL six presets: atsGrade(PRESETS[id]) === PRESET_MANIFESTS[id].atsGrade.\n  (c) atsGradeCauses returns a non-empty reason list exactly when grade is 'good', and [] when strict; each downgrading axis above produces a distinct cause string.\n- effectiveAtsGrade delegates to atsGrade (no regression at its two existing call sites; document-format-render.test.ts / document-templates.test.ts stay green).\n- Only declared files touched.",
      "attempts": [],
      "evidence": "Built in worktree (build-phase workflow wf_7e4e23e9-a2c), MERGED to main f4fe82f. Coordinator INDEPENDENT merged-tree re-verify on /workspace GREEN: check/lint exit0; NODE_OPTIONS=1024 build exit0; vitest 829/829 (804 floor + 25 new ats-grade.test.ts); playwright FULL 11/11 no-retry incl design.spec.ts 11.9s + applications.spec.ts. Diff is PURE logic in registry.ts (no new imports): atsGrade(format) + atsGradeCauses(format) + hasColoredPageBackground helper; effectiveAtsGrade now delegates to atsGrade(format). Table matches [v3-038]: strict IFF columns one + headerPosition top + photo.hidden + icons none + no colored page bg (multi mode over full-page/border); border/levelDisplay/contact-icons/footer grade-neutral; all six presets atsGrade===manifest.atsGrade. Scope clean. Gaming clean. NOTE: the workflow Gate FALSELY reported design.spec red — a STALE-DIST artifact (gate built in a worktree cwd with font-path ENOENT); a clean bun run build on /workspace makes design.spec pass 3/3 deterministically."
    },
    {
      "id": "E9-F5b",
      "title": "settings.presets[] data model — zod + DB column + drizzle migration + server plumbing",
      "status": "done",
      "depends_on": [
        "E9-F4d"
      ],
      "files": [
        "src/shared/schema.ts",
        "src/server/db/schema.ts",
        "src/server/routes/settings.ts",
        "drizzle/",
        "test/settings-presets.test.ts",
        "test/api.profile-settings.test.ts"
      ],
      "origin": "decomposed from E9-F5 (spec §31.1 presets) — the persistent user-preset data model; backend only",
      "context": "Lede resume-tailoring app (Fastify + better-sqlite3 + Drizzle, single-user). Add persistent USER PRESETS: named DocumentFormatV2 snapshots the user saves. Backend + data model ONLY (no UI — that is F5c/F5d).\n\n1) TYPE + ZOD (src/shared/schema.ts): add UserPreset = { id: string(min1), name: string(min1), format: DocumentFormatV2 }. Add presets to settingsInput (schema.ts:261-268) as an optional array of UserPreset, each format validated with the existing documentFormatZ (= formatV2Schema, schema.ts:178). Keep it OPTIONAL/defaulted so existing PUT payloads without presets still validate.\n2) DB COLUMN (src/server/db/schema.ts:58-76): add a presets json column to the settings singleton table, typed UserPreset[], defaulting to [].\n3) MIGRATION (drizzle/): add the next migration adding the presets column, following the EXACT pattern of existing files (drizzle/0003_*.sql, 0004_*.sql + their drizzle/meta/_journal.json + snapshot json). Prefer generating via drizzle-kit if package.json has a generate script; else hand-write the .sql + update meta/_journal.json + add the snapshot json consistent with siblings. Migration MUST apply on boot — boot.smoke.test.ts must stay green.\n4) SERVER (src/server/routes/settings.ts): include presets in currentSettings() (l.24-36, default [] when null/legacy) and accept it in PUT /api/settings (settingsInputWithFormat, l.22). GET returns presets; PUT round-trips them. Do NOT touch the BYOK key path.\n\nLocked stack (frozen): Drizzle migrations-on-boot, better-sqlite3 single-file, singleton settings via id=1 CHECK. Bounded axes only. NO hiring-status/tracker field. presets carry ONLY {id,name,format} — no scoring, no extra status.",
      "acceptance": "- Baseline (oracle.md): check/build/lint exit0; vitest green (incl boot.smoke — migration applies on a fresh DATA_DIR boot); playwright chromium green.\n- NEW test/settings-presets.test.ts (keyless integration): PUT /api/settings with a presets array persists it; GET returns it; a preset's format validates as DocumentFormatV2; a PUT OMITTING presets still validates (backward-compat). Presets survive a DB reopen / fresh app instance.\n- Legacy row with null presets column resolves to [] (no crash).\n- Only declared files touched (drizzle/ new migration + meta is in-scope).",
      "attempts": [
        {
          "n": 1,
          "failed": "integration: stale-base MIGRATION COLLISION + out-of-scope touch",
          "hypothesis": "The build-phase workflow worktree branched at 78a9be5 (34 commits behind main); the F5b worker did NOT fast-forward to main (unlike the F5a worker) because its declared files predate E9. It ran drizzle-kit generate on the stale base and produced drizzle/0004_nebulous_silver_surfer.sql — but main ALREADY has drizzle/0004_clear_string_levels.sql (E9-F4a meta.level migration). Two 0004 migrations => broken migration sequence / boot failure. It also touched test/api.profile-settings.test.ts (the GET /api/settings allowed-keys guard Set) which was undeclared — a legit cascade the ticket under-declared. The code itself was correct: check/build/lint/vitest(370) + 7 acceptance tests all green in-worktree.",
          "fixNote": "RE-DISPATCH on the CURRENT main working tree (NOT a worktree). The migration MUST be numbered 0005 (main's latest is 0004_clear_string_levels). Regenerate the .sql + drizzle/meta/0005_snapshot.json + the drizzle/meta/_journal.json entry via `bunx drizzle-kit generate` AGAINST CURRENT MAIN so the snapshot reflects the real current schema (including the 0004 meta.level column). test/api.profile-settings.test.ts is now a DECLARED file — add 'presets' to its allowed-keys Set. Everything else per the original context."
        }
      ],
      "evidence": "Attempt 2 (single Sonnet on main), committed 467871f. Coordinator INDEPENDENT re-verify GREEN: check/lint/build exit0; vitest 835/835 (829 floor +6 settings-presets); playwright 10 passed + 1 retry-absorbed design.spec font-fetch flake (green ISOLATED 1/1 20s; client bundle byte-identical to pre-F5b so the flake is environmental, not a regression); suite exit0. Migration is 0005_magical_eddie_brock (ALTER TABLE settings ADD presets text) — exactly one 0004 (no collision); boot.smoke 3/3 confirms it applies on a fresh DATA_DIR. schema.ts userPresetZ{id,name,format} optional on settingsInput; db presets json column nullable -> [] at route; GET/PUT round-trip (tested byte-equal + backward-compat + 400 on bad format/missing id-name + reopen persistence + legacy-null->[]). api.profile-settings allowed-keys guard learns 'presets'. Scope clean (6 declared files + 2 new drizzle files). Gaming clean. NOTE (latent, non-blocking): drizzle-kit wanted a full-table-recreate (buggy INSERT referenced new col on old table); builder hand-wrote the ALTER but left meta/0005_snapshot.json as generated — a future drizzle-kit generate MAY see a phantom diff; harmless at runtime (migrator runs the .sql, not the snapshot)."
    },
    {
      "id": "E9-F5c",
      "title": "Curated new-axis presets + per-cause ATS caveat UI (picker + gallery)",
      "status": "done",
      "depends_on": [
        "E9-F5a"
      ],
      "files": [
        "src/client/document/presets.ts",
        "src/client/document/registry.ts",
        "src/client/components/TemplatePicker.tsx",
        "src/client/components/TemplateGallery.tsx",
        "test/document-templates.test.ts",
        "test/engine-single-column.test.ts",
        "test/migration-v1-formats.test.ts",
        "test/template-gallery.test.tsx"
      ],
      "origin": "decomposed from E9-F5 (F5 line: curated new-axis presets + per-cause caveat UI)",
      "context": "Lede resume-tailoring app. Two deliverables, both building on E9-F5a (pure atsGrade + atsGradeCauses now in registry.ts).\n\n1) CURATED NEW-AXIS PRESETS: ship a handful (3-5) of NEW built-in presets beyond the six migrated (strict/classic/compact/banner/sidebar-left/sidebar-right), showcasing new v2 axes (accent placement, tinted sidebar, distinctive heading style + name font, level dots, etc.). Add to PRESET_IDS/PRESETS (presets.ts) and PRESET_MANIFESTS (registry.ts) following the existing shape; each new preset is a DocumentFormatV2 with a declared manifest.atsGrade that MUST equal atsGrade(PRESETS[id]) (the F5a invariant). Composition-only: every preset renders through the one engine + shared renderers; NO new render path.\n\n2) PER-CAUSE ATS CAVEAT UI: in TemplatePicker.tsx and TemplateGallery.tsx, when effectiveAtsGrade is 'good', surface the SPECIFIC causes via atsGradeCauses(format) (the picker pattern) — replace/augment the single ATS_CAVEAT constant so each downgrade cause (photo shown, two-column, heading icons, colored background) is listed. Keep the badge (strict/good) driven by effectiveAtsGrade.\n\nLocked: bounded axes only; ONE engine; no tag/level scoring; a declared 'strict' grade is EARNED via the §28.6 extraction-order CI invariant — any NEW preset you declare 'strict' MUST pass test/document-extraction-invariant.test.ts (STRICT_PRESET_IDS: header + every item in exact content order). If unsure, declare a showcase preset 'good'.",
      "acceptance": "- Baseline (oracle.md): check/build/lint exit0; vitest green; playwright chromium green.\n- Roster grows by the curated presets; for EVERY preset (old+new) atsGrade(PRESETS[id]) === manifest.atsGrade, AND every atsGrade:'strict' preset passes the extraction-order invariant (test/document-extraction-invariant.test.ts).\n- test/document-templates.test.ts updated: new presets render (extraction contains profile + every selected item; leadRationale/cut strings absent) — parameterized over PRESET_IDS so new ids are covered automatically.\n- Caveat UI: a 'good' format shows the per-cause list from atsGradeCauses (not a generic sentence); a 'strict' format shows no caveat. (Assert via component test surface or a small causes-wiring unit assertion; e2e lands in F5d.)\n- Only declared files touched.",
      "attempts": [
        {
          "n": 1,
          "failed": "blocked (correctly): under-declared files — roster growth cascades into 3 hardcoded-count tests",
          "hypothesis": "Growing PRESETS/PRESET_IDS/PRESET_MANIFESTS beyond 6 breaks 3 tests that hard-code the 6-preset roster and were NOT in the declared files: (1) test/engine-single-column.test.ts 'six configs exist' asserts PRESET_IDS===the 6 AND every PRESETS[id] deep-equals migrateFormat({...DEFAULT_FORMAT,templateId:id}); (2) test/migration-v1-formats.test.ts iterates PRESET_IDS and reads a per-id pre-e9 v1 fixture (only 6 exist -> ENOENT for new ids); (3) test/template-gallery.test.tsx hardcodes count/badge/disabled-button = 6. Builder wrote ZERO code and surfaced it (same class as F5b's under-declared cascade).",
          "fixNote": "RE-DISPATCH with those 3 test files ADDED to declared files. GENERALIZE ONLY the roster-SIZE/COUNT assertions (derive from Object.keys(PRESET_MANIFESTS).length / PRESET_IDS.length dynamically). DO NOT weaken the v1->v2 MIGRATION-EXACTNESS invariant: the SIX legacy migrated ids (strict/classic/compact/banner/sidebar-left/sidebar-right) MUST still deep-equal migrateFormat({...DEFAULT_FORMAT,templateId:id}) with presetId stamped — that is an F0 locked check; keep it, just restrict that particular assertion + the v1-fixture loop to the 6 LEGACY ids (new v2-native presets have no v1 predecessor and are validated as plain DocumentFormatV2 instead). Builder's vetted design (carry forward): 'signature' (strict — accent-bar heading, space-grotesk name, accentPlacement, level dots: all ATS-neutral), 'ledger' (strict — thin-underline headings, split date/location, bodyIndent, circle-filled contact icons, oswald name), 'frame' (strict — colors.area 'border' single-mode 4-side frame, boxed headings, archivo-black name: single-mode border stays strict per the F5a table), 'spotlight' (good — two-column sidebar variant, distinct from sidebar-left/right). The atsGrade(PRESETS[id])===manifest.atsGrade invariant + the extraction-order CI invariant for any 'strict' preset are the HARD gates — if a look can't hold 'strict', declare it 'good'."
        }
      ],
      "evidence": "Attempt 2 (single Sonnet on main), committed fe26924. Coordinator INDEPENDENT re-verify GREEN: independent atsGrade spot-check signature/ledger/frame=strict, spotlight=good (all === manifest); check/lint/build exit0; vitest 864/864 (835 floor +29); playwright FULL 11/11 FIRST-TRY no-retry (design.spec.ts 10.1s + applications.spec.ts 13.1s — no regression from the TemplatePicker/TemplateGallery + applyPreset change). 4 v2-native presets added off DEFAULT_FORMAT_V2. applyPreset WIDENED to project the new identity axes (headings.style/fonts.name/contactIconStyle/colors.border+accentPlacement/entries.dateLocationPlacement+bodyIndent/levelDisplay) so a curated preset rewrites panel state (spec §31.1) — no-op for the 6 legacy (they sit at the default baseline on those axes); JUDGED spec-aligned + necessary, not a regression (suite green, incl generalized template-gallery). Per-cause caveat UI added beneath the pinned generic ATS_CAVEAT. CASCADE handled cleanly: migration-exactness deep-equal PRESERVED, scoped to LEGACY_PRESET_IDS (new presets assert formatV2Schema); roster-size + gallery counts generalized. spotlight sidebarWidthPct=32 (reused legacy safe width after a fit.test hyphenation catch). Scope: 8 declared files. Gaming/regression read clean."
    },
    {
      "id": "E9-F5d",
      "title": "Save-current-as-preset UI + saved-presets gallery + e2e round-trip",
      "status": "done",
      "depends_on": [
        "E9-F5b",
        "E9-F5c"
      ],
      "files": [
        "src/client/components/DesignView.tsx",
        "src/client/components/TemplateGallery.tsx",
        "src/client/hooks/queries.ts",
        "test/e2e/design.spec.ts",
        "test/template-gallery.test.tsx"
      ],
      "origin": "decomposed from E9-F5 (F5 line: save-current-as-preset -> gallery -> applies -> round-trips reload)",
      "context": "Lede resume-tailoring app. Wire user-presets end-to-end on top of F5b (settings.presets API) and F5c (gallery caveat UI + curated presets).\n\n1) SAVE-CURRENT-AS-PRESET: add a 'Save current design as preset' affordance in DesignView.tsx controls (~l.134-157, near the gallery button). Capture the current in-memory displayFormat into a named UserPreset and persist via the settings API (PUT /api/settings with the appended preset). Use the settings query/mutation hook (src/client/hooks/useSettings.ts — extend it with a save-preset mutation or reuse updateSettings). Prompt for/derive a name.\n2) SAVED-PRESETS GALLERY: extend TemplateGallery.tsx (currently maps only PRESET_MANIFESTS l.95) to ALSO render the user's saved presets (settings.presets), each selectable via the same applyPreset/onChange contract, showing the effectiveAtsGrade badge. Applying a saved preset sets the format from the saved snapshot.\n\nVERIFY THE FILE LIST FIRST: if useSettings lives at a different path than src/client/hooks/useSettings.ts, do NOT touch an undeclared file — return { blocked, reason } so the coordinator can re-scope.\n\nLocked: bounded axes only; ONE engine; presets carry only {id,name,format}; no tracker/scoring fields. window.print/print.css remain rejected.",
      "acceptance": "- Baseline (oracle.md): check/build/lint exit0; vitest green; playwright chromium green.\n- NEW/EXTENDED e2e in test/e2e/design.spec.ts (keyless FixtureEngine webServer): in the design view, change knobs -> 'Save as preset' with a name -> the preset APPEARS in the gallery -> RELOAD -> the saved preset is STILL there (persists server-side) -> selecting it APPLIES its format (observable design change). No uncaught console errors (keep the documented blob-url allowlist).\n- Applying a saved preset shows its effectiveAtsGrade badge.\n- Only declared files touched.",
      "attempts": [],
      "evidence": "Single Sonnet on main, committed c2b3c0f. Coordinator independent re-verify GREEN: check/lint/build exit0; vitest 869/869 (864 floor +5 saved-presets gallery tests); playwright FULL 11/11 first-try no-retry incl design.spec.ts 20.6s (now carries the save->appears->diverge->RELOAD->persists->apply round-trip). e2e is non-vacuous: reload + fresh server fetch proves persistence; apply asserts app PUT round-trips fonts.body=arimo (the stored snapshot). TemplateGallery savedPresets section applies preset.format DIRECTLY (not applyPreset). queries.ts casts useSettings result to expose presets (server has returned it since F5b; api.ts type left untouched, out of scope) — LATENT residual: api.ts SettingsResponse narrower than reality. Scope clean (5 declared files). Gaming clean."
    },
    {
      "id": "E9-R1",
      "title": "Repair: ibm-plex-mono vendored font crashes fontkit on multi-word text (latent since E7) + per-face render smoke",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/client/document/fonts.ts",
        "test/document-fonts.test.ts",
        "package.json",
        "bun.lock"
      ],
      "origin": "repair: escaped defect confirmed during E9-F0b re-verify (pre-exists on v1 strict template; every epic's acceptance missed it)",
      "context": "CONFIRMED defect (coordinator-reproduced 2026-07-07, vitest env): rendering ANY multi-word text with typography.body.family 'ibm-plex-mono' via the existing v1 path (renderResumeToBuffer, strict template) crashes fontkit: 'Offset is outside the bounds of the DataView' in TTFGlyph._getCBox/_getMetrics via LayoutEngine advanceWidth. 'ibm-plex-sans' renders fine. The face is registered in src/client/document/fonts.ts from @fontsource/ibm-plex-mono woff files; the design panel offers it, so a user picking IBM Plex Mono breaks every render. Diagnose the asset path first (likely fixes, in preference order: register a different file format/subset from the same package — fontkit handles TTF more robustly than some woffs; or bump/replace the @fontsource package; or swap the mono face's source). Do NOT remove the face from the roster (it is in the locked E9 roster and in v1's shipped six) and do NOT touch src/client/document/engine/ (another ticket owns it — its temporary exclusion of ibm-plex-mono gets lifted at cutover once this lands). ESCAPED-BUG RULE — this ticket must also strengthen the check that let it through: test/document-fonts.test.ts gains a per-face RENDER smoke: for EVERY registered FontId, renderResumeToBuffer over a fixture whose summary + items contain multi-word text (spaces force fontkit word-wrap layout) at body AND heading roles returns a nonempty buffer. That test must FAIL on the current ibm-plex-mono asset before your fix and pass after (state both observations in evidence).",
      "acceptance": "- bun run check → 0; bun run lint → 0; NODE_OPTIONS=--max-old-space-size=1024 bun run build → 0\n- bunx vitest run --passWithNoTests green; npx playwright test green (10/10)\n- new per-face render smoke covers EVERY id in FONT_FACES (parameterized, not hand-listed) with multi-word body + heading text; demonstrated red on the unfixed asset, green after\n- ibm-plex-mono renders a real multi-word PDF (the coordinator's probe scenario passes)\n- no face removed from the roster; no engine/ changes",
      "attempts": [],
      "evidence": "Independent re-verify 2026-07-07: coordinator's original crash probe (mono + multi-word via v1 strict) PASSES post-fix · check 0 · lint 0 · build 0 (11 mono woff2 assets bundled) · vitest 50 files/554 · playwright 10/10 · scope: exactly fonts.ts + document-fonts.test.ts · diff read: principled format field on FontManifest threaded through BOTH resolution paths (createRequire + Vite URL), stale woff2 comment corrected; per-face smoke is parameterized over FONT_FACES keys × body/heading roles; builder demonstrated red-then-green (woff crash stack captured pre-fix). Root cause: malformed space-glyph glyf/loca in @fontsource/ibm-plex-mono woff (all published versions); sibling woff2 clean."
    },
    {
      "id": "E9-F0d1",
      "title": "Cutover: presets-through-one-engine replace templates (client + live schema)",
      "status": "done",
      "depends_on": [],
      "files": [
        "src/shared/types.ts",
        "src/shared/format.ts",
        "src/shared/format-v2.ts",
        "src/shared/schema.ts",
        "src/client/document/",
        "src/client/components/DesignPanel.tsx",
        "src/client/components/TemplatePicker.tsx",
        "src/client/components/TemplateGallery.tsx",
        "src/client/components/AtsView.tsx",
        "src/client/components/DocumentPreview.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/components/LayoutEditor.tsx",
        "src/server/db/schema.ts",
        "src/server/routes/",
        "test/",
        "package.json",
        "bun.lock"
      ],
      "origin": "decomposed from E9-F0d (spec §31.1/§31.6 F0 close)",
      "context": "PART 1 of the F0 cutover (E9-F0d split, coordinator-accepted [v3-047]). The engine (src/client/document/engine/, E9-F0b/c) already composes single+two-column v2 axes; this ticket makes it THE ONLY render path and makes the live app speak DocumentFormatV2 end-to-end. registry.ts is currently the six-template lookup (TEMPLATES/getTemplate/render fns); renderResume.ts and fit.ts dispatch through it on legacy DocumentFormat; thumbnail.tsx/download.ts/plainText.ts and 7 components (DesignPanel 435 lines, TemplatePicker, TemplateGallery, AtsView, DocumentPreview, ApplicationDetail 370, LayoutEditor) consume the legacy shape. src/shared/schema.ts documentFormatZ and src/server/db/schema.ts $type<DocumentFormat> are v1-shaped and MUST move to v2 in lockstep (validator + compile-time type swap only — NO drizzle data-migration file; old-row migration is E9-F0d2's job: this ticket may gate reads via isFormatV2 or treat the DB as freshly-v2; e2e boots a fresh DB so this verifies end-to-end). registry.ts becomes a PRESET registry: six presets (from presets.ts) with id/name/description/layout/atsGrade metadata; effectiveAtsGrade adapted to v2 (photo.hidden===false || layout.columns!=='one' ⇒ good); the pure atsGrade(format) over the whole config is F5 — do NOT build it. renderResume/fit/thumbnail/download/plainText operate on v2 via the engine; the 7 components operate on v2 fields with UNCHANGED user-facing behavior (picker still 6 choices + live thumbnails + badges; §28.3 panel knobs map onto v2 fields: body font/size/lineHeight, accent/text colors, margins pt→mm UI, sectionGap→elementSpacing, photo, per-section columns→sectionDisplay). Delete src/client/document/templates/. ALSO ON THIS RENDER PATH (do here): (a) [v3-044] escaped-gap repair — migrateFormat maps v1 sections {<s>:{columns:N>1}} → sectionDisplay skillsLanguages/interests {layout:'grid', gridColumns:N} for skill/language/interest (other sections' columns have no v2 axis — comment the drop honestly), strengthened format-v2.test.ts consuming test/fixtures/pre-e9-formats/non-empty-sections.json VERBATIM; (b) E9-R1 landed — remove legacyAdapt.ts's temporary ibm-plex-mono exclusion (per-face render smoke protects it); you own sections.tsx now — keep the adapter seam or teach sections.tsx v2 directly, whichever is cleaner, but ONE composition + one shared renderer set stands. Rename outward-facing 'template' vocabulary to 'preset' where cheap; do NOT churn routes/DB columns for naming. SUITE REPARAMETERIZATION (anti-lobotomy [v3-042]): the E7/E8 invariant suite SURVIVES over Object.values(PRESETS) through the engine. FORBIDDEN: test/fixtures/pre-e9-formats/ (zero diff there), .ailoop/, spec.md, drizzle/ migration files. Frozen: react-pdf only; ONE engine; never cuts; §28.1 byte-stability; §26 IA; leadRationale/cut never rendered.",
      "acceptance": "- bun run check → 0; bun run lint → 0; NODE_OPTIONS=--max-old-space-size=1024 bun run build → 0\n- bunx vitest run --passWithNoTests green (serial fallback if contention flakes); npx playwright test → 10/10 green\n- grep guards (outputs in evidence): src/client/document/templates/ deleted; `grep -rn \"templates/\" src/ test/` → 0; zero refs to StrictTemplate/SidebarTemplate/SidebarRightTemplate/ClassicTemplate/CompactTemplate/BannerTemplate; no new .skip/.only/.todo under test/ (diff-scoped)\n- suite floor: post-cutover count over test/document-*.test.ts + test/engine-*.test.ts + test/fit.test.ts + test/format.test.ts + test/format-v2.test.ts + test/page-model.test.ts >= 257 (pre-cutover pin; report the number)\n- MUST-SURVIVE assertions present and green: centered-classic mean-x; one-row-compact shared y; sidebar mean-x contrast both sides; banner tint + contrast ink; sentinel absence; strict extraction order per strict preset; never-cut across ladder per preset; off-diagonal composition guard; §28.1 byte-stability guard\n- documentFormatZ → formatV2Schema at the API boundary; DB $type → DocumentFormatV2; format PUT accepts v2 and rejects >=1 out-of-bounds case (400 tested)\n- [v3-044] repair: non-empty-sections.json fixture (verbatim) migrates to skillsLanguages {layout:'grid', gridColumns:2} (tested); non-default-photo.json migrates photo-preserved (tested)\n- ibm-plex-mono exclusion gone from engine font resolution; per-face smoke still green\n- effectiveAtsGrade honesty: sidebar presets ⇒ good; any preset + shown photo ⇒ good\n- playwright e2e UNCHANGED behavior: gallery 6 painted pairwise-different thumbnails, selection persists across reload, sample badges, color-change repaint\n- cross-cutting invariants green (no orphan routes; not-a-tracker grep; context-not-facts)",
      "attempts": [],
      "evidence": "Independent re-verify 2026-07-07 (builder died at session limit post-build, pre-final-confirmation; coordinator ran verification per [v3-035] precedent): check 0 · lint 0 · build 0 (bounded heap) · vitest 50 files/555 · playwright 10/10 · grep guards: templates/ deleted, 0 refs to old render fns, 0 new skips · pre-e9 fixtures 0 diff · SUITE FLOOR 258 >= 257 · must-survive verified by diff-read (document-templates reparameterized over PRESETS with contrast blocks intact; engine-single-column untouched so the off-diagonal guard stands; e2e adapted shape-only, assertions intact) · [v3-044] repair tested on verbatim fixtures (skill columns:2 -> skillsLanguages grid/2) · registry = display metadata only, no render fns. DEVIATION ACCEPTED (verified true by coordinator probes): ibm-plex-mono exclusion STAYS — @fontsource woff crashes on spaces, woff2 crashes on colon (complementary defects; both reproduced isolated) -> E9-R1 incomplete, E9-R2 seeded. Judgment endorsed: gallery preset-selection preserves stylistic axes (keeps E8's user-facing contract; F5 owns preset-apply semantics)."
    },
    {
      "id": "E9-F0d2",
      "title": "Stored v1 format migration (old data) + migration test suite",
      "status": "done",
      "depends_on": [
        "E9-F0d1"
      ],
      "files": [
        "src/server/",
        "src/shared/format-v2.ts",
        "test/",
        "drizzle/"
      ],
      "origin": "decomposed from E9-F0d (spec §31.1 migration)",
      "context": "PART 2 of the F0 cutover (E9-F0d split [v3-047]). After E9-F0d1 the live app speaks v2 end-to-end, but pre-existing DB rows (applications.format, applications.lockedFormat.format, settings.defaultFormat) may hold v1 JSON written before the cutover. Wire migrateFormat (src/shared/format-v2.ts — migration wiring only, no new axis logic) into the persistence boundary. RECOMMENDED: lazy-on-read (migrateFormat applied when a row loads; idempotent via isFormatV2; no boot-time table rewrite) — state your choice and why in evidence. Migration tests consume test/fixtures/pre-e9-formats/ VERBATIM (your diff must not touch that directory). Locked applications stay LOCKED: migrating lockedFormat does not unlock or mutate locked TailoredResume snapshot bytes (§28.1). Frozen: react-pdf only; ONE engine; §28.1 byte-stability.",
      "acceptance": "- bun run check → 0; bun run lint → 0; NODE_OPTIONS=--max-old-space-size=1024 bun run build → 0\n- bunx vitest run --passWithNoTests green (serial fallback if contention flakes); npx playwright test → 10/10 green\n- every file under test/fixtures/pre-e9-formats/ (VERBATIM — git status shows zero changes there) migrates to schema-valid v2 preserving look: six template defaults → their preset configs (toEqual vs presets.ts modulo presetId), non-empty-sections → skillsLanguages grid/2, non-default-photo → photo preserved shown/square/size, lockedFormat wrapper + settings.defaultFormat cases covered\n- idempotence: second migration pass identical\n- integration: a pre-E9 sqlite row (v1 JSON in applications.format + lockedFormat + settings.defaultFormat, inserted raw) reads back through the API as schema-valid v2; locked TailoredResume snapshot bytes UNCHANGED by migration and by a subsequent v2 format PUT\n- format PUT round-trip persists across server restart\n- cross-cutting invariants + full baseline green",
      "attempts": [],
      "evidence": "Independent re-verify 2026-07-07: check 0 · lint 0 · build 0 (bounded heap) · vitest 52 files/582 · playwright 10/10 · scope: exactly routes/applications.ts + routes/backup.ts + 2 new test files; pre-e9 fixtures 0 diff · gaming read: one exported read-boundary helper applied at every client-exposing site; integration tests insert RAW v1 via drizzle into all three storage sites on a LOCKED app, read through the real API (schema-valid v2, locked stays locked, locked content toEqual-stable across migrating read + v2 PUT + fresh-instance restart, isFormatV2 pass-through asserted). Endorsed: lazy-on-read choice; latent pre-existing leak fixed (tailor/lock consumed existing.format ungated); export gated too. FLAGGED GAP accepted as E9-F0d3: POST /api/import rejects pre-cutover backup FILES (v2-only zod)."
    },
    {
      "id": "E9-R2",
      "title": "Repair: ibm-plex-mono asset — BOTH vendored @fontsource files are defective (woff: spaces, woff2: colon); source a clean asset + punctuation-bearing per-face smoke",
      "status": "done",
      "depends_on": [
        "E9-F0d2"
      ],
      "files": [
        "src/client/document/fonts.ts",
        "src/client/document/engine/legacyAdapt.ts",
        "test/document-fonts.test.ts",
        "package.json",
        "bun.lock"
      ],
      "origin": "repair: E9-R1 incomplete — colon-crash escaped its smoke (coordinator-verified during E9-F0d1 re-verify)",
      "context": "CONFIRMED (coordinator probes 2026-07-07, isolated react-pdf/fontkit renders): @fontsource/ibm-plex-mono latin-400 .woff crashes fontkit ('Offset is outside the bounds of the DataView') on ANY multi-word text (space glyph, the E9-R1 finding); the SAME package's .woff2 (5.2.7, latest) renders spaces fine but crashes on a bare colon ':'. Complementary defects — no vendored @fontsource asset for this face is safe for resume prose. The engine currently fail-safes by excluding ibm-plex-mono in src/client/document/engine/legacyAdapt.ts (falls back to the default face — the picker offers Mono but renders Sans: crash-safe, dishonest). FIX FOR REAL: source a clean IBM Plex Mono asset — preferred: the official IBM '@ibm/plex-mono' npm package (OFL, TTF/woff/woff2 variants; verify whichever variant you register renders spaces AND colons AND full resume prose through fontkit BEFORE wiring); alternatives: another @fontsource major version, or vendoring the TTF from IBM's GitHub release into the repo (document provenance + license). Wire it through fonts.ts's per-face manifest (the R1 'format' field generalizes to a per-face source override if needed) for BOTH the createRequire (node) and Vite URL (browser) paths, then DELETE the legacyAdapt.ts exclusion so Mono genuinely renders. ESCAPED-BUG RULE (second escape of the same class — the R1 smoke lacked ordinary punctuation): the per-face render smoke fixture text must include realistic resume punctuation for EVERY face — at minimum colon, comma, hyphen, parentheses, slash, digits, ampersand (e.g. 'Led team: shipped 3 services (2019-2023), cut costs 40% & latency 2x — a/b tested'), at body AND heading roles. Demonstrate the strengthened smoke RED on the current woff2 asset (colon) before your fix, GREEN after.",
      "acceptance": "- bun run check → 0; bun run lint → 0; NODE_OPTIONS=--max-old-space-size=1024 bun run build → 0 (mono assets present in dist)\n- bunx vitest run --passWithNoTests green; npx playwright test 10/10\n- strengthened per-face smoke (punctuation fixture, parameterized over the registry, body + heading roles) demonstrated RED on the unfixed woff2 (colon crash captured), GREEN after\n- ibm-plex-mono exclusion DELETED from legacyAdapt.ts; a resume with spaces + colons + digits renders through the engine with fonts.body 'ibm-plex-mono' (tested)\n- new dependency (if any) is OFL and pinned; provenance documented in fonts.ts\n- no face removed; existing tests untouched and green",
      "attempts": [],
      "evidence": "Independent re-verify 2026-07-07: check 0 · lint 0 · build 0 (IBMPlexMono-{Regular,Bold}.woff in dist/assets) · vitest 52/584 (isolated forks pool, maxForks=2) · playwright 10/10 (one contention-flake fail on applications spec in the concurrent run, PASSED isolated + on full re-run — known [v3-041] host flake, not R2) · scope: exactly fonts.ts + legacyAdapt.ts + document-fonts.test.ts + package.json + bun.lock; pre-e9 fixtures 0 diff · coordinator crash probe: mono renders full resume punctuation (colons/parens/%/&/digits/dash/slash) through the real engine pipeline (buf>1000 bytes); license verified OFL-1.1 (SIL, IBM Corp, RFN Plex); @fontsource/ibm-plex-mono removed from package.json AND bun.lock · gaming read: files-override on manifest for the non-@fontsource path shape, both SSR+browser paths wired, exclusion deleted, byte-diff regression guard proves mono renders as itself not fallback."
    },
    {
      "id": "E9-F0d3",
      "title": "Backup-file compatibility: POST /api/import accepts pre-cutover (v1-format) backup files",
      "status": "done",
      "depends_on": [
        "E9-F0d2"
      ],
      "files": [
        "src/server/routes/backup.ts",
        "src/shared/schema.ts",
        "test/api.export-import.test.ts",
        "test/"
      ],
      "origin": "repair: gap flagged honestly by E9-F0d2 (spec §31.1 content-preserving migration + §2/§27 full-instance backup)",
      "context": "E9-F0d2 migrated the three sqlite storage sites but deliberately left POST /api/import untouched and flagged it: the import body validator is v2-only (documentFormatZ is now formatV2Schema-shaped), so a backup FILE exported before the E9 cutover — whose applications[].format / lockedFormat.format / settings.defaultFormat hold v1 JSON — is REJECTED. That breaks the standing Phase-5 oracle's spirit (export→wipe→import round-trips a full instance) for pre-E9 exports. Fix at the import boundary: accept v1 format values in the import body (zod union v1-shape|v2 with a preprocess through the existing migrateFormat/resolveStoredFormat from src/shared/format-v2.ts — reuse, do not re-derive) and persist v2. The v1 leg of the union should be structural (has templateId, no formatVersion) — isFormatV2 is the discriminator. In-test, construct a pre-cutover backup JSON by embedding the test/fixtures/pre-e9-formats/ files VERBATIM (read-only — zero diff under that dir) into a backup envelope: applications with format + lockedFormat (locked content present) + settings.defaultFormat. Import must restore them as schema-valid v2 with look preserved, locked apps still locked, locked TailoredResume content intact. Current-version (v2) import round-trip must stay green (the standing export-import tests). Frozen: §28.1; locked stays locked; no new axis logic.",
      "acceptance": "- bun run check → 0; bun run lint → 0; NODE_OPTIONS=--max-old-space-size=1024 bun run build → 0\n- bunx vitest run --passWithNoTests green; npx playwright test 10/10\n- new test: a pre-cutover backup envelope built from the verbatim pre-e9 fixtures imports successfully; GET after import returns schema-valid v2 (formatV2Schema.parse), look preserved (strict fixture → migrateFormat expectation), locked app locked with content toEqual-intact; settings.defaultFormat migrated\n- v2 backup round-trip (export→wipe→import) still green (standing tests untouched or extended, never weakened)\n- a malformed format (neither valid v1 shape nor v2) still 400s (tested)\n- zero diff under test/fixtures/pre-e9-formats/",
      "attempts": [],
      "evidence": "Independent re-verify 2026-07-07: check 0 · lint 0 · build 0 · vitest 52 files/584 green SERIAL (an overlapping-run contention flake produced 5 spurious failures when coordinator and builder suites raced — re-run clean; the standing [v3-041] flake mode, loadavg 33-56 during overlap) · playwright 10/10 · scope: exactly schema.ts + backup.ts + api.export-import.test.ts; pre-e9 fixtures 0 diff · gaming read: v1 zod resurrected structurally as import-compat only (commit-1ddcaca bounds), union transform delegates to resolveStoredFormat (zero re-derived logic), live write paths stay v2-only; tests embed fixtures verbatim, assert 2xx + migrateFormat-equal look + locked intact + settings migrated + malformed 400."
    },
    {
      "id": "E9-F4d2",
      "title": "Fix design-view preview LIVE-repaint on format change (usePDF.update / format-key remount) + assert live pixel-diff",
      "status": "done",
      "depends_on": [
        "E9-F4d"
      ],
      "files": [
        "src/client/components/DocumentPreview.tsx",
        "test/e2e/design.spec.ts"
      ],
      "origin": "repair: pre-existing live-repaint bug (DocumentPreview usePDF.update unused) surfaced by E9-F4d e2e; escaped-bug rule (ledger v3-078/v3-079) — strengthen the repaint check from reload-based to LIVE",
      "context": "REPAIR of a pre-existing bug F4d surfaced (do NOT re-do F4d — its controls are shipped/done). THE BUG (confirmed by two independent investigations, file:line): the main design-view preview never repaints in place on a format change. DocumentPreview.tsx line ~144 destructures only `const [instance] = usePDF({document: renderResumeDocument({resume,profile,paper,format,density})})` but @react-pdf/renderer 4.5.1's usePDF render effect has an EMPTY dependency array (node_modules/@react-pdf/renderer/lib/react-pdf.browser.js ~347) so it calls updateContainer ONCE on mount; a changed document prop is ignored. usePDF returns an update(newDoc) fn (react-pdf.browser.js ~357) that is never called, and RenderedPreview has no format-keyed key to remount. In the DESIGN VIEW (allPages=true -> PdfPages/renderAllPagesInto) canvases are recreated only on remount. So DesignView passes the new in-memory displayFormat straight to the preview (DesignView.tsx ~162) but pixels change only on a full reload. THE FIX (pick the cleanest; only DocumentPreview.tsx): call usePDF's returned update() in an effect keyed on the rendered document, OR add a format-hash key to remount RenderedPreview, OR adopt thumbnail.tsx's renderResumeToBlob+manual-paint approach (thumbnail.tsx already repaints live and sidesteps the blob-url revocation race by handing pdf.js raw bytes). PRESERVE the documented blob-url revocation-race console-error allowlist in design.spec.ts (lines ~70-93). Keep single-page mode (ResultView/AtsView/ApplicationDetail) working. STRENGTHEN THE CHECK (escaped-bug rule): change design.spec.ts step (5b) back to a LIVE (NO reload between snapshot and change) before/after previewDataUrl pixel-diff on a format change that affects page 1 (experience.order flip, or body font) — assert the canvas pixels change WITHOUT a reload; keep the reload-persistence and locked-disabled assertions too.",
      "acceptance": "Baseline (oracle.md) all green incl. playwright chromium.\n- LIVE repaint: in design.spec.ts, snapshot previewDataUrl, change a format control (experience.order or body font), and the .document-preview canvas pixel-diff changes WITHOUT any page reload (expect.poll not.toBe, 15s).\n- reload persistence + locked-disabled assertions still pass; documented console-error allowlist preserved; no uncaught console errors.\n- single-page preview (ResultView/ApplicationDetail) still renders (no regression).\n- only declared files (DocumentPreview.tsx, test/e2e/design.spec.ts).",
      "attempts": [],
      "evidence": "INDEPENDENT re-verify (coordinator, on main working tree) GREEN — NOT the builder self-report. bun run check exit0; NODE_OPTIONS=1024 bun run build exit0 (11.19s); bun run lint exit0 (199 files); vitest 804/804 (65s, no flakes); playwright FULL suite 11/11 first-try no-retry incl [applications] design.spec.ts 11.8s (the NEW live no-reload previewDataUrl pixel-diff on experience.order flip PASSES) + applications.spec.ts 11.8s (no cross-spec regression). Also design.spec.ts ISOLATED under --project=applications 1 passed 11.0s. Scope clean: only src/client/components/DocumentPreview.tsx + test/e2e/design.spec.ts. Fix = react-pdf usePDF [instance,update] + effect calling update(renderResumeDocument) keyed on resume/profile/paper/format/density (react-pdf PDFViewer pattern); test step (5b)->live expect.poll not.toBe(before) with reload-persistence kept as (6c). NOTE: builder wrongly claimed the e2e was un-runnable here (pdf.js fake-worker) — that was a wrong project filter (--project=chromium finds no design test); under --project=applications it paints and passes."
    }
  ]
}