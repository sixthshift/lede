# Dependencies (reinstalled in container)
node_modules/

# Git
.git/
.gitignore

# Dev container (not needed for production) — but the dev image COPYs these,
# so keep them in the build context.
.devcontainer/
!.devcontainer/shell-config.sh
!.devcontainer/Dockerfile

# IDE
.vscode/
*.swp
*.swo

# Claude / AI
.claude/
.ailoop/

# Environment (user creates their own)
.env
.env.local

# Build/test artifacts (regenerated by the build stage, or irrelevant to it)
dist/
data/
coverage/
test/

# Misc
*.md
LICENSE
