T003 — INDEPENDENT RE-VERIFY (coordinator-run on branch v3-t003 @ aadbcb8, baseSha 5c37a2c)

SCOPE (git diff --name-only 5c37a2c..aadbcb8): ZERO src/
  playwright.config.ts        (ONLY the additive testMatch: /(applications|design|mutation-probe)\.spec\.ts/)
  test/e2e/mutation-probe.spec.ts (new)

GAMING READ:
  - config change is exactly the additive testMatch for the new spec (protocol A: permitted). No project/server/other change.
  - probe uses HARD-FAILING `await expect(expectResumeCanvasPainted(page)).rejects.toThrow()` (line 50) as the pass condition;
    the following try/catch only re-runs to log the caught error message (comment states it doesn't affect pass/fail) + a toBeTruthy guard.
  - injection is TEST-SIDE only: page.addStyleTag({content:'.document-preview canvas{display:none}'}) removed via styleTag.evaluate(el=>el.remove()).
  - builder's neuter-sanity: neutering the injection made the test go RED on all 3 attempts ('promise resolved instead of rejected') — the contrast really bites.

BASELINE GATE (authoritative, ports clear):
  check 0 · lint 0 (235 files) · build 0 · vitest 1049 passed
  chromium 8 · auth 1 · applications 10 (applications 8 + design 1 + mutation-probe 1; markers injected->caught->restored all logged)

VERDICT: DONE. The measuring instrument for later phases is proven to bite.
