T030 — INDEPENDENT RE-VERIFY (branch v3-t030 @ 4730329, baseSha 3cbfc9d)
SCOPE: server/routes/applications.ts + shared/types.ts + client/api.ts + ApplicationCard.tsx + 3 tests + applications.spec.ts. (client/api.ts + api tests + applications-ui fixture = legitimate CASCADES of the CO-1 type/route change; ApplicationsView untouched — declared superset. Declared files updated.)
CO-1 (human-approved) applied correctly: LIST_COLUMNS adds letterGenState + `locked: sql<boolean>\`(locked IS NOT NULL)\`.mapWith(Boolean)` — derived boolean, NEVER the heavy snapshot; confined to the projection (no new column/table/route). ApplicationSummary = Omit<Application,'current'|'locked'> & {locked:boolean}; client ApplicationListItem aliases it (no dup type).
RED-TEAM #4 STRENGTHENED not weakened: old `expect(listRow).not.toHaveProperty('locked')` → `expect(listRow.locked).toBe(false)` + still asserts `current` undefined (heavy snapshot omitted §9). New api.applications-lock.test.ts proves the list `locked` boolean flips across lock/unlock.
PROTOCOL A: applications.spec +1 dashboard test; 0 skip/only; 409 x15, flag-voice x9 intact.
DASHBOARD test (OQ4a pill contrast): untailored vs tailored apps → server genState/letterGenState/locked asserted via request.get; cards' resume pills DIFFER + match own row; Locked badge visible on locked card, count 0 on unlocked; letter pill iff letter (untailored → no letter pill, all variants checked); NOT-A-TRACKER sweep asserts no Applied/Interviewing/Offer/Rejected text. No thumbnail, only token pills/badges.
BASELINE (authoritative, exit codes): check 0 · lint 0 · build 0 · vitest 1053 passed + 1 FLAKE (fit-ui.test.tsx §28.4 — react-pdf timing under load; PASSES 4/4 isolated, exit 0; unrelated to T030) · chromium EXIT 0 (14) · auth EXIT 0 (1) · applications EXIT 0 (20).
VERDICT: DONE.
