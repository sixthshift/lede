T011 — INDEPENDENT RE-VERIFY (coordinator-run on branch v3-t011 @ 2465f43, baseSha 56487fa) — RISKIEST TICKET

SCOPE (git diff --name-only 56487fa..2465f43): client-only, NO server/shared/data-model
  src/client/components/ApplicationDetail.tsx  (declared)
  src/client/App.tsx                           (declared)
  test/e2e/applications.spec.ts                (declared)
  test/e2e/helpers/workspace.ts                (declared)
  src/client/components/WorkspaceShell.tsx     (UNDECLARED — accepted: 1-line h-screen->h-full to fill the frame)
  src/client/components/AppShell.tsx           (UNDECLARED — accepted: +31/-4 fullBleed prop for the workspace route, "every other route untouched")
  src/client/styles/app.css                    (UNDECLARED — accepted: +29/-8 canvas max-width + result-view column stack so preview fits the narrow pane)
  JUDGMENT: all 3 undeclared files are client-only and squarely in-scope for "render /applications/:id inside WorkspaceShell"; coordinator under-declared at intake. Not scope creep, not gaming. Declared files updated to match.

PROTOCOL A (applications.spec.ts): 8 original titles preserved + exactly 3 ADDED (protocol C / protocol B / drawer), 0 removed/renamed; no skip/fixme/only; 409 x10 preserved; flag-voice x6 preserved.

GAMING READ of the 3 new tests — all protocols GENUINELY implemented (not stubbed):
  - Protocol C: editor-pane + active canvas both toBeInViewport({ratio:0.9}); canvasBox.width>=320; distinctColorCount(canvas)>1 (polled); elementFromPoint occlusion guard (2 uses); same-editor-DOM-node expando marker (__t011EditorMarker) survives the resume->letter in-pane switch; asserted for resume AND letter.
  - Protocol B: [aria-modal="true"] count 0; no element bbox > 0.5 * viewport area; real un-forced page.click() on "Plain text" underlying control; asserted at 1280 AND in the <1280 drawer-open state.
  - Drawer: at 1024x720 preview starts non-co-visible; "Show preview" toggle reveals a canvas with distinctColorCount>1.

BASELINE GATE (authoritative on branch tree, ports clear):
  check 0 · lint 0 · build 0 · vitest 1053 passed
  chromium 8 · auth 1 · applications 13 (applications.spec 11 [8 preserved + 3 new] + design 1 + mutation-probe 1)

VERDICT: DONE. Core epic risk retired — co-visible workspace expressed as passing machine-checkable contracts with all v2 behavior green.
