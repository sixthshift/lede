T012 — INDEPENDENT RE-VERIFY (branch v3-t012 @ 9d8e208, baseSha 2465f43)

SCOPE (git diff --name-status): client-only, no server/shared/data-model
  M ApplicationDetail.tsx (declared) · D DesignView.tsx (declared, deleted outright) · M main.tsx (declared, redirect) · M design.spec.ts (declared) · M workspace.ts (declared)
  M ResultView.tsx        (UNDECLARED — accepted: allPages passthrough so workspace preview keeps DesignView's multi-page host)
  M TemplateGallery.tsx   (UNDECLARED — accepted: removed dead applicationId/"Open design view" link pointing at dropped route)
  R085 test/design-view.test.tsx -> test/application-detail-design.test.tsx (unit test RE-HOMED per no-deletion rule)
  JUDGMENT: undeclared files in-scope for design-fold/route-drop; declared files corrected.

NO-DELETION / re-home verified:
  - unit: old design-view.test.tsx 4 it() (renders DesignPanel / debounces->1 PUT / locked disabled / multi-page) === new application-detail-design.test.tsx SAME 4 it() (describe renamed). No coverage lost.
  - e2e design.spec: title changed ("design view: deep link..." -> "design controls: reachable in-workspace...") because the /design route it named is dropped — legitimate re-home, NOT hide-by-rename. 4 behaviors re-homed: deep-link->REDIRECT, debounced persistence, preview host, locked read-only. expect( in design.spec 42->50 (INCREASED). No skip/fixme/only. Total test/** non-comment expect = 2316 (non-decreasing).

KEY CONTRACTS (in migrated design.spec):
  - redirect: goto('/applications/<id>/design') -> toHaveURL('/applications/<id>') SAME id + workspace-shell + company marker visible (main.tsx DesignRedirect <Navigate replace/>). 8 toHaveURL/redirect refs.
  - persist: font PUT body format.fonts.body==='arimo' asserted + page.reload() re-check (server round-trip, not pixels). 20 reload/arimo/PUT refs.
  - locked: 6 toBeDisabled() across axis controls + save-preset + ATS after lockFinal.

BASELINE (authoritative, ports clear): check 0 · lint 0 · build 0 · vitest 1053 · chromium 8 · auth 1 · applications 13.
VERDICT: DONE.
