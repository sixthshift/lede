T014 — INDEPENDENT RE-VERIFY (branch v3-t014 @ dbb2716, baseSha 1b61413) — closes Phase 1

SCOPE: TEST-ONLY (test/e2e/applications.spec.ts + test/e2e/helpers/workspace.ts; 0 src). Cannot alter lock behavior — only observes it.

PROTOCOL A: +2 tests (sentinel-reaches-document, locked sweep); 0 skip/fixme/only; 409 count 10->15; flag-voice 6->9.

GAMING READ:
  - sentinel contrast (un-gameable): resume edit -> "Plain text" export .toContain(resumeSentinel) AND pixelDiffFraction(resumeCanvas)>0.002 (floor, ~10-100x cursor-blink noise). Letter edit -> cover-letter PDF extractPdfText .toContain(letterSentinel) AND letter canvas pixelDiffFraction>0.002. Long distinctive sentinels. Modality (aria-modal 0 + no >50% overlay) asserted WHILE field toBeFocused() (mid-edit).
  - CORE LOCK GUARANTEE intact: resume-part PATCH 409 (line 1685) + letter-part PATCH 409 (1700) when locked, both affordances toBeDisabled().
  - HONEST behavior mapping (builder verified in source, coordinator accepts as PRESERVED v2 contract): design-axis 'Body font' toBeDisabled() when locked but general application PUT route not lock-gated (no 409 asserted — matches actual); motivation NOT disabled when locked (JobPanel has no locked prop), PUT 200s + persists across reload; voice-flag stays enabled + real 200 when locked. This matches the established contract enforced by pre-existing applications.spec test #5 ("locked disables every affordance and 409s"), which has been green through v2 and all of Phase 1 — i.e. "every affordance" = the document-content (resume/letter) affordances. The redesign PRESERVES this; T014 (test-only) did not change it.

BASELINE (authoritative, ports clear): check 0 · lint 0 · build 0 · vitest 1053 · chromium 8 · auth 1 · applications 17 (applications.spec 15 [13+2] + design 1 + probe 1).
VERDICT: DONE.
