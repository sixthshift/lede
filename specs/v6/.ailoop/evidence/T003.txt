T003 — independent re-verify (coordinator, on main, baseSha e0d17a4)

SCOPE (git delta, subtracting T001+T002+WIP) — PASS
  = src/server/tailor/evalcore.ts (M), src/shared/signal-coverage.ts (new), test/signal-coverage.test.ts (new) — exactly the 3 declared files.

GAMING READ — CLEAN
  src/shared/signal-coverage.ts (client-safe, only `import type {TailoredResume}` — no node:*):
    - tokenize: moved verbatim from evalcore.
    - rationaleReferencesPhrase(rationale, phrase): tokenize(phrase).some(tok len>=4 && rationaleLower.includes(tok)) — the exact ANY->=4-char-token primitive; the documented limit lives here (comment discloses it).
    - uncoveredSignals(resume): candidates = [...new Set([...weights, ...hardRequirements])] (dedup, weights-first stable order, roleLevel EXCLUDED); ledeRationales = group.leadRationale per group (non-blank only); uncovered = candidates referenced by NO lede rationale. Reads ONLY group.leadRationale — never non-lede rationale, never item.text.
  evalcore.ts: rationaleReferencesSignal now delegates to rationaleReferencesPhrase over [roleLevel, ...weights, ...hardRequirements]; local tokenize DELETED; imports from @shared/signal-coverage. flipPredicate public behavior unchanged (still includes roleLevel). Genuine single source.

TEST QUALITY (test/signal-coverage.test.ts) — all sharpened cases present, EXACT-array assertions:
  weights one-lede -> ['API versioning']; second lede -> []; hardRequirements present/absent; roleLevel excluded (not.toContain + exact array); lede-only (non-lede item names signal -> still uncovered); text-not-rationale (lede .text names it, not leadRationale -> still uncovered); dedup (same phrase both arrays -> once); documented-limit ('container orchestration' vs 'container deposit tracking' sharing only 'container', one named -> both covered). Imports tokenize (single-source proof).

FAST TIER (authoritative, coordinator re-run)
  bun run check -> 0; bun run build -> 0 (LOAD-BEARING: proves no node:* leaked into the client bundle via the shared module); bun run lint -> 0
  bunx vitest run -> VITEST_EXIT=0: Test Files 85 passed (85); Tests 1119 passed (1119). Fully clean — no flakes this run (builder's lone fit-ui flake did not recur).

VERDICT: ACCEPT T003. P1 not yet drained (T004 remains: ReasoningPanel surfacing + e2e).
