T32 — independent re-verify (coordinator) — ACCEPT
baseSha 9b75724 == main HEAD; branch = base + 1 commit 7a82be4 (clean ff-merge to main).

SCOPE (git diff --name-only 9b75724..branch): exactly 3 declared files —
  src/server/routes/applications.ts, src/shared/schema.ts, test/api.applications-letter-edit.test.ts. No overflow.

BASELINE (re-run on merged main 7a82be4, verifier full-suite un-scoped):
  bun run check   -> exit 0 (tsc client + server)
  bun run build   -> exit 0 (13.27s)
  bun run lint    -> exit 0 (biome, 227 files, no fixes)
  bunx vitest run -> 77 files / 1005 tests PASS, 0 fail, ~50s (+18 vs T31's 987)

GAMING READ (diff): CLEAN.
  - letterPartPatchZ .strict() outer + all 3 path variants (greeting|body|closing); groundedOn unrepresentable
    in the text PATCH; applyLetterPartPatch preserves body paragraph's groundedOn (only text replaced), immutable.
  - INSERT: insertLetterParagraph ALWAYS constructs {text, groundedOn:[]}; never reads client-supplied groundedOn
    (schema declares it optional for shape-compat but route forces []) — laundering vector closed at construction;
    test asserts stored value is [].
  - POSITION: splice at position (0..len inclusive, >len -> null -> 400); REMOVE index-addressed via URL param,
    coerced, out-of-range -> null -> 400.
  - BLANK: route constructs {greeting:'',body:[],closing:''} with ZERO engine reference (no decide/decideLetter
    call path); test injects a spy engine that throws if called and asserts zero invocations. letterGenState left
    at "untailored" (non-generated).
  - All 4 routes: 409-locked checked BEFORE any mutation; write only letterCurrent+updatedAt; resume `current`,
    letterPrevious, letterGenState untouched. Tests use real POST create + real POST /lock; 18 tests incl.
    groundedOn-preserve, forced-[] on insert, positional insert/remove by exact text, spy-no-call blank, locked->409
    on every route. No hardcoding, no weakened/deleted tests.
