T12 — Zod: motivation + letter snapshot + backup round-trip
Independent re-verify on branch ailoop/T12, baseSha 59cf308.

=== SCOPE (git diff --name-only 59cf308..HEAD) — CLEAN, 3 declared files ===
src/server/routes/backup.ts
src/shared/schema.ts
test/api.export-import.test.ts

=== BASELINE ===
bun run check    -> exit 0
bun run build    -> exit 0 (✓ built in 10.36s)
bun run lint     -> exit 0 (biome: Checked 214 files, No fixes applied)
bunx vitest run  -> 67 files, 928 tests PASS (exit 0)  [+8 vs T11]

=== ACCEPTANCE (test/api.export-import.test.ts, verbose, non-vacuous) ===
✓ applicationCreate: motivation > accepts well-formed
✓ applicationCreate: motivation > accepts its absence (optional)
✓ applicationCreate: motivation > rejects an empty motivation
✓ applicationCreate: motivation > rejects a motivation over 4000 chars
✓ applicationCreate: motivation > rejects a non-string motivation (proves bounded z.string, not z.any)
✓ POST /api/import: coverLetterZ > paragraph with groundedOn key deleted -> 4xx (rejected)
✓ POST /api/import: coverLetterZ > paragraph with groundedOn: [] -> accepted (required key, empty allowed)
✓ round-trip: motivation + letterCurrent survive export -> wipe -> import
    (server logs confirm genuine wipe: DELETE app-round-trip then GET -> 404 BEFORE import restores;
     motivation + letterCurrent deep-equal restored into the same row)

=== GAMING READ ===
coverLetterZ = LetterDecisionZ reused directly (no divergent letter shape re-declared).
motivation = z.string().min(1).max(4000).optional().nullable() on BOTH applicationCreate + applicationUpdate.
applicationImport letter* fields are .optional() for pre-letter-epic backup compat; PRESENT values still
fully validated (coverLetterZ.nullable). Import upsert already spreads the full parsed row -> writes them.
settings/secrets remain excluded from export/import (unchanged).

VERDICT: accept. Merged --ff-only into main.
