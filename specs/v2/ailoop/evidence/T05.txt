T05 — Record Phase 0 cover-letter fixtures (KEY-GATED) — COORDINATOR-RUN, attempt 4 (human-authorized past 3-cap), baseSha 0f3610e
ACCEPTED. Merged --ff-only into main (231a08a).

RECORDING (live gemini-2.5-flash): all 3 JDs recorded on attempt 1/5 (retry loop unused this run). Files:
  test/fixtures/letters/{platform-sdk,rules-engine,frontend-rewrite}.json + manifest.json + eval-transcript.md.
PROVENANCE: manifest model gemini-2.5-flash, provider google, recordedAt ISO, usage total 9344 tokens, per-JD
  attempts count (all 1). Real SDK usage, script-written.

INDEPENDENT KEYLESS RE-VERIFY (coordinator, over the RECORDED fixtures, run twice incl. post-biome-format):
  - lead-flip: TRUE all 3 (platform-sdk→cloudcase-platform-sdk, rules-engine→cloudcase-rules-engine,
    frontend-rewrite→cloudcase-frontend-rewrite).
  - letterFlipContrast: TRUE — unions are distinct singletons {platform-sdk}/{rules-engine}/{frontend-rewrite};
    every pairwise diff non-empty both directions (superset failure from attempt 1 GONE after the focus repair).
  - validateLetterNoFabrication: clean all 3 (rules-engine now grounds '~30k'/'~50%' verbatim after T02R2).
SCOPE: only the 5 fixture files. Baseline: check 0; vitest 916 (fixtures are data; suite unaffected; T16 will add
  the permanent keyless flip/contrast/grounding test).
VERDICTS (judged, in eval-transcript.md): each letter leads on its JD-distinct entry with a focused, on-target
  argument; grounded solely in the cited entry; numbers verbatim. Phase 0 core risk (is a grounded letter any good?)
  RETIRED.

RESIDUAL: record-letter-fixtures.ts emits JSON.stringify (multi-line) output; biome (pre-commit hook) collapses
  single-element groundedOn arrays — so a re-record must be `biome check --write`-formatted before commit (the hook
  enforces this). Cosmetic; JSON semantics identical; FixtureEngine is whitespace-agnostic.
