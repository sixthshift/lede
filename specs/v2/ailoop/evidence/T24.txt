T24 — Application-page letter surface + motivation field UI (e2e)
Independent re-verify on branch ailoop/T24 (commit a26f38b), baseSha 8b71c0b.

=== WORKING-TREE RECONCILIATION (coordinator) ===
The builder left test/setup.ts + vite.config.ts modified UNCOMMITTED (a @fontsource font-fetch backstop +
maxForks 3->6) — UNDECLARED files, NOT in the commit. Coordinator discarded them (`git checkout --`) before
re-verifying, since the merge takes the COMMIT, not the working tree. All checks below ran on the CLEAN tree.
The backstop was NOT needed for green (see below) — logged as an observation, not adopted (out of scope).

=== SCOPE (git diff --name-only 8b71c0b..HEAD) — CLEAN, 4 declared files ===
src/client/components/ApplicationDetail.tsx
src/client/components/GenStateBadge.tsx
src/client/components/JobPanel.tsx
test/e2e/applications.spec.ts
(api.ts / useApplications.ts declared but untouched — T13's letter hooks sufficed.)

=== BASELINE (clean tree) ===
bun run build    -> exit 0
bun run check    -> exit 0
bun run lint     -> exit 0 (biome: Checked 225 files, No fixes applied)
bunx vitest run  -> 75 files, 969 tests PASS (exit 0; fit-ui passed — backstop not needed)

=== P2 PHASE ORACLE — playwright --project=applications (clean tree) ===
4 passed (43.2s), 1 worker:
  ✓ create -> tailor -> render -> reload-persist -> re-tailor -> lock (pre-existing, unaffected)
  ✓ cover letter: generate/paint isolation, download filename, undo, motivation persistence   [NEW]
  ✓ cover letter: a failed generation surfaces a distinct failed badge, never a stub letter    [NEW]
  ✓ design view: deep link, debounced persistence, multi-page host, locked read-only (pre-existing)
New letter-e2e test titles confirmed RAN (not exit-0-with-0-tests).

=== GAMING READ ===
ApplicationDetail letter section: distinct [data-testid="letter-preview"] host (NOT shared .document-preview);
Generate/Regenerate (disabled while generating), Undo (disabled when !letterPrevious), Download (downloadLetterPdf
with company/role); LetterPreview shown only when letterCurrent exists, else a "No cover letter yet" generate
affordance — never an empty document. GenStateBadge gains a `kind` prop (default 'resume' -> resume unchanged;
'letter' -> distinct labels). JobPanel adds a motivation Textarea -> applicationUpdate PUT. LetterPreview.tsx
UNTOUCHED (exported wrapper takes {letter,format}, sources profile/paper from hooks — usage typechecks).
Isolation proven two ways: resume .document-preview canvas pixels unchanged + server `current` JSON unchanged by
letter generation. Download filename asserted via the real production letterPdfFilename() + a fired download event
(the browser's download.suggestedFilename() mis-reports em-dash names — a documented Chromium quirk reproduced
minimally; the exact filename string is separately unit-proven in T22).

VERDICT: accept. Merged --ff-only into main. P2 drained + phase oracle green.

OBSERVATION (not adopted): test/setup.ts font-fetch backstop would durably kill the recurring @fontsource vitest
flake. Out of scope for T24; candidate for a future dedicated test-infra ticket if the flake keeps recurring.
