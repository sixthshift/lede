T23 — Letter preview component (pdf.js)
Independent re-verify on branch ailoop/T23, baseSha 63c169a.

=== SCOPE (git diff --name-only 63c169a..HEAD) — CLEAN, 3 declared files ===
src/client/components/DocumentPreview.tsx  (pre-authorized: pure-additive helper export)
src/client/components/LetterPreview.tsx (new)
test/letter-preview.test.tsx (new)

=== BASELINE ===
bun run build    -> exit 0 (✓ built in 13.70s)
bun run check    -> exit 0
bun run lint     -> exit 0 (biome: Checked 225 files, No fixes applied)
bunx vitest run  -> 75 files, 969 tests PASS (exit 0; fit-ui passed this run)

=== ACCEPTANCE (test/letter-preview.test.tsx, RTL/jsdom) ===
✓ calls renderLetterDocument with the EXACT {letter, profile, paper, format} props (deep-equal, input-bound)
✓ calls renderLetterDocument AGAIN when the letter prop changes (proves the update()-in-useEffect re-render path)
✓ calls renderLetterDocument AGAIN when the format prop changes
✓ shows the loading state while usePDF is loading (positive DOM: 'Rendering preview…' + .letter-preview__loading)
✓ paints a letter-scoped <canvas class='letter-preview__canvas'> once usePDF ready with a url

=== GAMING READ ===
DocumentPreview.tsx change is PURE-ADDITIVE: PdfCanvas gains an optional className prop defaulting to
'document-preview__canvas' (its original value) + is exported; every existing resume call site omits the prop ->
unchanged behavior (design-view.test.tsx canvas assertions still pass). LetterPreview mirrors RenderedPreview:
usePDF({document: renderLetterDocument(...)}) + explicit update() keyed on [letter,profile,paper,format] (not
usePDF's inert empty-dep effect); profile/paper from useProfile()/useSettings(); letter-scoped class. Re-render
assertions are the real anti-cheat (a version relying on usePDF's empty-dep effect would fail #2/#3).

VERDICT: accept. Merged --ff-only into main.
