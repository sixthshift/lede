T02 — Letter prompt + engine path (decideLetter) — INDEPENDENT RE-VERIFY (coordinator), branch ailoop/T02, baseSha 4c81865
ACCEPTED. Merged --ff-only into main.

SCOPE (git diff --name-only 4c81865..ailoop/T02): CLEAN — exactly the 3 declared files:
  src/server/tailor/engine.ts, src/server/tailor/letter-prompt.ts (new), test/letter-engine.test.ts (new)
prompt.ts UNTOUCHED (git diff empty) — the frozen resume SYSTEM_PROMPT is intact.

BASELINE (full, coordinator-run on the branch):
  bun run check → 0 ; bun run lint → 0 ; build → 0
  bunx vitest run (FULL) → 65 files / 894 passed, 0 failed (877 + 17 new letter-engine tests).
  NOTE: builder saw test/fit-ui.test.tsx flake in ITS full run; my independent full run had 0 failures —
  confirmed the documented @fontsource full-suite flake (CLAUDE.md), NOT a T02 regression (T02 touches no font/fit code).

GAMING READ: clean.
  - letter-prompt.ts: LETTER_SYSTEM_PROMPT enforces the fact-lock (claims/numbers trace to groundedOn entries'
    facts; never invent/strengthen; groundedOn = real entry ids). buildLetterUserPrompt guards motivation/context/voice
    so absent inputs give a byte-identical prefix (returns base JD-only) — the fixture-stability seam. Voice left as a
    guarded seam for the later ticket, as instructed.
  - engine.ts: decideLetter on the interface + both engines. ProviderEngine mirrors decide() (retry-once,
    LetterDecisionZ, LETTER_SYSTEM_PROMPT+renderLibrary, buildLetterUserPrompt). FixtureEngine.decideLetter keys on
    hashKey(jd,entries), loads DEFAULT_LETTER_FIXTURES_DIR, NoFixtureError on miss. Clean generic loadFixturesFrom<T>
    refactor (internal to engine.ts, not scope creep).
  - test: hardcoded-literal baseline (not self-derived), TWO fixtures under DIFFERENT keys resolving strictly by
    hashKey (not first-found), motivation-block-append contrast with unchanged prefix. Non-vacuous.

ACCEPTANCE: all met (buildLetterUserPrompt byte-identity + append; NoFixtureError on empty/missing dir; key-match).
