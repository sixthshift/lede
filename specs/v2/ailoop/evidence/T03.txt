T03 — Letter assemble + grounding validator (tailorLetter) — INDEPENDENT RE-VERIFY, branch ailoop/T03, baseSha 18e5a61
ACCEPTED. Merged --ff-only into main.

SCOPE (diff --name-only 18e5a61..ailoop/T03): CLEAN — src/server/tailor/letter.ts (new), test/letter-validate.test.ts (new).
validate.ts NOT modified — its already-exported FabricationError/extractNumbers/hasNumberToken were reused.

BASELINE (full, coordinator-run): check 0 / lint 0 / build 0 / vitest 66 files 900 passed, 0 failed (894 + 6 new).
fit-ui did not flake in my run (builder saw it, isolated-confirmed 4/4 — documented @fontsource flake).

GAMING READ: clean.
  - validateLetterNoFabrication is PER-CITATION: cited = paragraph.groundedOn.map(byId), blob = ONLY those cited
    entries' facts; a number in an uncited entry does NOT ground it (the discriminating case). Unknown entry id throws
    FabricationError. Signature (letter, entries) — no motivation/context param (fact-lock by construction).
  - assembleLetter is a light pass-through; groundedOn untouched.
  - tailorLetter: decideLetter → assembleLetter → validate → return.
  - test: per-citation discriminator (uncited-X throws, cited-X passes), unknown-entry instanceof FabricationError,
    assembleLetter toEqual pass-through, end-to-end tailorLetter over a temp-dir FixtureEngine with a grounded number.

ACCEPTANCE: all met.
