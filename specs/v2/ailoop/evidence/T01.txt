T01 — Letter decision schema + domain types — INDEPENDENT RE-VERIFY (coordinator), branch ailoop/T01, baseSha 298ea1d
ACCEPTED. Merged --ff-only into main.

SCOPE (git diff --name-only 298ea1d..ailoop/T01): CLEAN — exactly the 3 declared files:
  src/shared/schema.ts, src/shared/types.ts, test/letter-schema.test.ts

BASELINE (full, coordinator-run on the branch):
  bun run check                         → exit 0
  NODE_OPTIONS=--max-old-space-size=1024 bun run build → exit 0 (built in 11.38s)
  bun run lint                          → exit 0 (203 files, no fixes)
  bunx vitest run (FULL)                → 64 files / 877 passed (was 870; +7 = the new letter-schema tests)

ACCEPTANCE:
  - LetterDecisionZ accepts valid letter + a paragraph with groundedOn:[] — pass
  - rejects: missing closing (destructured out); groundedOn KEY deleted (delete op, not undefined); empty body min(1) — all pass
  - LetterDecision/CoverLetter HAND-DECLARED in types.ts (no z.infer, no any/unknown) — confirmed by diff read
  - @ts-expect-error on a LetterDecision literal missing closing — present

GAMING READ: clean. Schema matches spec (greeting/body[text,groundedOn].min(1)/closing). Reject tests are real
(destructure/delete/empty), non-vacuous. Types hand-declared with correct required `closing`.

CAVEAT (verifier-confirmed, → mechanical oracle amendment, ledger 0006): `bun run check` typechecks only src/**
(tsconfig include = src/**/*, src/server/**/*); test/** is NOT typechecked and vitest has no typecheck step. So the
@ts-expect-error clause is NOT baseline-enforced. The TYPE is genuinely correct (verified by inspection + the builder's
out-of-band scratch-tsconfig proof), so T01's intent holds — but future negative-type assertions are dev-time guards,
not CI-enforced. See oracle.md amendment.
