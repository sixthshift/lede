{
  "project": "lede-v2-cover-letters",
  "caps": {
    "maxAttempts": 3,
    "thrash": 2,
    "chunk": 20
  },
  "tickets": [
    {
      "id": "T01",
      "title": "Letter decision schema + domain types",
      "status": "done",
      "phase": "P0",
      "depends_on": [],
      "files": [
        "src/shared/schema.ts",
        "src/shared/types.ts",
        "test/letter-schema.test.ts"
      ],
      "origin": "spec Phase 0 (letter decision schema)",
      "context": "[DONE — see .ailoop/evidence/T01.txt] Lede v2 adds cover letters from the same honest engine. This is the letter's OUTPUT CONTRACT — the model returns judgment only, the server assembles (oracle.md locked decisions).\n\nEXISTS: The resume decision contract is `TailorDecisionZ` in src/shared/schema.ts:306-323 (flat: {signals, summary, items[{entryId,text,rank,leadRationale?}], cut[]}) with its domain mirror `TailorDecision` in src/shared/types.ts:105-117. jdSignalsZ at schema.ts:300-304.\n\nBUILD, mirroring that shape:\n1. src/shared/schema.ts: add `LetterDecisionZ` = z.object({ greeting: z.string(), body: z.array(z.object({ text: z.string(), groundedOn: z.array(z.string()) })).min(1), closing: z.string() }). This is the LIGHTLY-STRUCTURED letter the model returns (locked decision: greeting / body paragraphs[] with groundedOn / closing). Each body paragraph carries `groundedOn: entryId[]` — the entries whose facts it draws on (loud default: same traceability shape as the resume's entryId).\n2. src/shared/types.ts: add the domain mirror `LetterDecision` type and a `CoverLetter` type for the ASSEMBLED/stored letter (same three parts; body paragraphs keep groundedOn). Keep it parallel to TailorDecision/TailoredResume.\n\nDO NOT: add a route, engine, validator, or migration (later tickets). No fact-source leakage — groundedOn holds entry ids only. Touch ONLY declared files.",
      "acceptance": "Baseline (oracle.md) + new test/letter-schema.test.ts:\n- LetterDecisionZ.parse ACCEPTS a valid letter {greeting, body:[{text,groundedOn:['e1']}], closing}; ACCEPTS a paragraph with groundedOn:[] (the hand-added case).\n- REJECTS a letter missing `closing`; one whose body paragraph has the `groundedOn` KEY DELETED (not merely undefined); one with empty body (min(1)).\n- The `LetterDecision`/`CoverLetter` domain types are HAND-DECLARED (grep-forbid `any`/`unknown` in those declarations — a `type X = any` alias is a fail) and a value missing `closing` FAILS to type-check as `LetterDecision`, proven via `// @ts-expect-error`.\n- `bun run check` clean.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T01.txt"
    },
    {
      "id": "T02",
      "title": "Letter prompt + engine path (decideLetter on both engines)",
      "status": "done",
      "phase": "P0",
      "depends_on": [
        "T01"
      ],
      "files": [
        "src/server/tailor/letter-prompt.ts",
        "src/server/tailor/engine.ts",
        "test/letter-engine.test.ts"
      ],
      "origin": "spec Phase 0 (letter prompt + engine path behind TailorEngine split)",
      "context": "[DONE — see .ailoop/evidence/T02.txt] Add the letter generation path behind the existing engine split (oracle.md: engine-split ProviderEngine/FixtureEngine; the model call is the ONLY key-dependent step).\n\nEXISTS (src/server/tailor/engine.ts): `TailorEngine` interface :18-25 with `decide(jd, entries, context?, budget?)`. `buildUserPrompt(jd, context?, budget?)` :34-45 assembles the USER message (JD + optional context block + optional budget block), guarding byte-identity when context/budget absent. `ProviderEngine.decide` :58-89 calls Vercel AI SDK `generateObject({ model, schema: TailorDecisionZ, system: SYSTEM_PROMPT + renderLibrary(entries), prompt: buildUserPrompt(...) })`; retries once. `FixtureEngine.decide` :113-132 keys on `hashKey(jd, entries)` ONLY (context/budget ignored), loads test/fixtures/decisions/*.json, throws NoFixtureError if none. `renderLibrary(entries)` is in prompt.ts:157-163. The FROZEN resume SYSTEM_PROMPT is prompt.ts:30-113 — DO NOT touch prompt.ts.\n\nBUILD:\n1. NEW src/server/tailor/letter-prompt.ts: a `LETTER_SYSTEM_PROMPT` constant (frozen-style: the honest-engine framing — judgment + grounding over Library + JD, never invention; return greeting/body[]/closing with each body paragraph's groundedOn = the entry ids whose FACTS it draws on; voice/motivation/context guide phrasing and are NEVER fact sources). Export `buildLetterUserPrompt(jd, motivation?, context?, voice?)` that assembles the USER message: base JD, then optional clearly-labelled blocks for motivation ('why you, why this role' — authored intent, not a fact source), context, and voice exemplars — EACH guarded so an absent input yields a byte-identical prefix (mirror engine.ts:34-45). NOTE: voice wiring is finalized in T43; here just leave the guarded seam (absent voice ⇒ no voice block).\n2. src/server/tailor/engine.ts: extend `TailorEngine` with `decideLetter(jd, entries, motivation?, context?, voice?): Promise<LetterDecision>`. ProviderEngine.decideLetter mirrors decide() but uses schema: LetterDecisionZ, system: LETTER_SYSTEM_PROMPT + renderLibrary(entries), prompt: buildLetterUserPrompt(...). FixtureEngine.decideLetter keys on `hashKey(jd, entries)` (motivation/voice ignored for fixture MATCHING, exactly as decide() ignores context) but loads from a letters dir: add `DEFAULT_LETTER_FIXTURES_DIR = path.join(process.cwd(), 'test/fixtures/letters')`; on miss throw NoFixtureError with the recorded scenario names.\n\nDO NOT: touch prompt.ts (frozen). No validation/assemble here (T03). No routes. Touch ONLY declared files.",
      "acceptance": "Baseline + new test/letter-engine.test.ts:\n- buildLetterUserPrompt(jd) with NO motivation/context/voice equals a HARDCODED LITERAL string written in the test (NOT a value derived by calling the function under test — that is a tautology); buildLetterUserPrompt(jd,'my motivation') equals that same literal with a labelled motivation block APPENDED (the no-input prefix unchanged).\n- FixtureEngine.decideLetter throws NoFixtureError (code 'no_fixture') when test/fixtures/letters is empty/missing.\n- KEY-MATCH (not first-found): seed a temp letters dir with TWO fixtures under DIFFERENT keys; decideLetter(jd,entries) returns the one whose key === hashKey(jd,entries), NOT the other.\n- prompt.ts untouched — enforced mechanically by the independent scope check (prompt.ts is NOT a declared file for this ticket; any touch fails the ticket).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T02.txt"
    },
    {
      "id": "T03",
      "title": "Letter assemble + grounding validator (tailorLetter orchestration)",
      "status": "done",
      "phase": "P0",
      "depends_on": [
        "T01",
        "T02"
      ],
      "files": [
        "src/server/tailor/letter.ts",
        "test/letter-validate.test.ts"
      ],
      "origin": "spec Phase 0 (grounding validation); oracle.md fact-lock",
      "context": "[DONE — see .ailoop/evidence/T03.txt] The fact-lock for letters: every number in a letter must trace to the FACTS of the entries its paragraph cites — never to motivation/context/voice (oracle.md: grounding generalizes but never weakens; the grounding set stays `entries`).\n\nEXISTS: The resume validator `validateNoFabrication(resume, entries, baseSummary?)` in src/server/tailor/validate.ts:33-64 throws `FabricationError` (validate.ts:6-11) — builds a byId entry map, extracts number tokens (NUMBER_TOKEN_RE :18) from each item.text, requires each number to appear in that item's own entry's `facts`. The resume orchestrator `tailor(engine, jd, entries, layout, baseSummary?, context?, budget?)` is engine.ts:160-175 (decide → assemble → validateNoFabrication → return).\n\nBUILD — NEW src/server/tailor/letter.ts:\n1. `assembleLetter(decision: LetterDecision): CoverLetter` — the server-assembles step. The letter is lightly structured, so assembly is light: pass through greeting/body/closing into the stored CoverLetter shape; drop nothing; add nothing.\n2. `validateLetterNoFabrication(letter: CoverLetter, entries: Entry[]): void` — mirror validate.ts's number-grounding: build byId from `entries`; for each body paragraph, every groundedOn id MUST exist in byId (else throw FabricationError 'unknown entry <id>'); every number token in paragraph.text MUST appear in the FACTS of one of THAT paragraph's groundedOn entries (else throw FabricationError). Reuse validate.ts's number-token helpers by importing them (add named exports to validate.ts ONLY if not already exported — if that requires touching validate.ts, do it and declare it; otherwise re-implement the tiny regex locally). Motivation/context/voice are NOT passed in — they cannot be a grounding source by construction.\n3. `tailorLetter(engine, jd, entries, motivation?, context?, voice?): Promise<CoverLetter>` — decideLetter → assembleLetter → validateLetterNoFabrication → return.\n\nDO NOT: add routes (T13) or persistence. Touch ONLY declared files (add validate.ts to declared files IF you must export its number helpers).",
      "acceptance": "Baseline + new test/letter-validate.test.ts (per-paragraph scoping, not any-entry pool):\n- assembleLetter pass-through: expect(assembleLetter(decision)).toEqual({greeting,body,closing}) — no drop/mutation of groundedOn.\n- PER-CITATION DISCRIMINATOR: a number is in entry X's facts; a paragraph cites groundedOn:['Y'] (X UNCITED) and contains that number ⇒ throws FabricationError (proves per-citation grounding, not a library-wide pool). The SAME number in a paragraph citing ['X'] ⇒ passes.\n- a paragraph groundedOn a NONEXISTENT entry id ⇒ throws, asserted `err instanceof FabricationError` (not just message match).\n- SIGNATURE (verify by INSPECTION — test/** is NOT typechecked by the baseline, oracle amendment): validateLetterNoFabrication's signature takes NO motivation/context parameter (read/grep the signature); a `// @ts-expect-error` on a call passing one documents the intent. Voice/motivation cannot be a grounding source by construction.\n- tailorLetter over FixtureEngine + a hand fixture whose paragraph contains a number GROUNDED in its cited entry's facts (forces the number-check to actually run) returns a validated CoverLetter.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T03.txt"
    },
    {
      "id": "T04",
      "title": "Letter eval + record scripts + letter-flip predicate",
      "status": "done",
      "phase": "P0",
      "depends_on": [
        "T01",
        "T02",
        "T03"
      ],
      "files": [
        "scripts/eval-letter.ts",
        "scripts/record-letter-fixtures.ts",
        "src/server/tailor/evalcore.ts",
        "test/evalcore-letter.test.ts"
      ],
      "origin": "spec Phase 0 (eval over 3 v1 JDs; recorded fixtures)",
      "context": "[DONE — see .ailoop/evidence/T04.txt] The Phase 0 proving harness. KEY-GATED to RUN (T05), but the harness + the flip PREDICATE are keyless code, unit-tested here.\n\nEXISTS: src/server/tailor/evalcore.ts — `hashKey(jd, entries)` :12-16 (sha256 over jd + entries sorted by id), `CONTRAST_JDS` :22-56 (the three v1 §22 JDs: platform-sdk→cloudcase-platform-sdk, rules-engine→cloudcase-rules-engine, frontend-rewrite→cloudcase-frontend-rewrite), `flipPredicate(resume, targetId)` :65-88, `tagShuffle` :110-118. `SEED_ENTRIES` in src/server/seed.ts. scripts/eval.ts and scripts/record-fixtures.ts are the RESUME analogs to mirror (record-fixtures.ts:42-92 does live decide → assemble → validate → flip check → write fixture {key,name,decision} + manifest.json provenance {model, provider, recordedAt, fixtures[]}). Scripts run via `bun run scripts/<x>.ts`; key = GOOGLE_GENERATIVE_AI_API_KEY, model gemini-2.5-flash; refuse (exit 1) if key unset — never fall back to FixtureEngine.\n\nBUILD:\n1. src/server/tailor/evalcore.ts: add `letterFlipPredicate(letter: CoverLetter, targetEntryId)` returning whether the LEAD body paragraph's groundedOn INCLUDES targetEntryId; and `letterFlipContrast(letters: {name,letter}[])` returning whether each pairwise difference of the letters' groundedOn UNIONS is non-empty IN BOTH DIRECTIONS (no ground-on-everything letter passes by superset). These are the mechanical letter-flip checks (oracle.md Phase 0). Pure functions.\n2. NEW scripts/eval-letter.ts (mirror eval.ts): live ProviderEngine, for each CONTRAST_JD run tailorLetter over SEED_ENTRIES, assert letterFlipPredicate (lead paragraph groundedOn includes the target) AND letterFlipContrast across all three; print PASS/FAIL + a verdict line per letter (argument quality / motivation reflected — the judged half). Key-gate first.\n3. NEW scripts/record-letter-fixtures.ts (mirror record-fixtures.ts): live decideLetter per JD → assembleLetter → validateLetterNoFabrication → letterFlipPredicate; ONLY on pass write test/fixtures/letters/<name>.json = {key: hashKey(jd, SEED_ENTRIES), name, decision} and test/fixtures/letters/manifest.json = {model, provider, recordedAt, usage (prompt/completion/total tokens from the model response), fixtures:[{name,key,leadingEntryId}]}. NOTE: the resume record-fixtures.ts omits token usage — you MUST capture it here (the un-fakeable provenance signal, oracle.md); surface usage from the model response (decideLetter may need to return/expose it). Never hand-edit a fixture; a failing flip is reported, not forced.\n\nDO NOT: run the live scripts here (no key; that is T05). No fixtures committed by this ticket. Touch ONLY declared files.",
      "acceptance": "Baseline + new test/evalcore-letter.test.ts (pure-function contrast, keyless):\n- letterFlipPredicate: target in the LEAD paragraph's groundedOn ⇒ true; target present ONLY in a NON-LEAD (later) paragraph ⇒ FALSE (proves 'lead' is enforced, not any-paragraph); target absent ⇒ false.\n- letterFlipContrast: three identical-groundedOn letters ⇒ false; three letters whose groundedOn unions pairwise-differ BOTH directions ⇒ true; SUPERSET TRAP — letter A's union a strict superset of B's ⇒ FALSE (empty difference in the B→A direction).\n- the per-JD record/verify step is an IMPORTABLE pure(ish) function; a keyless unit test with a failing/mock engine asserts NO fixture is written when the flip/grounding check fails.\n- `bun run check` typechecks both new scripts.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T04.txt"
    },
    {
      "id": "T02R",
      "title": "Repair: focus the letter prompt so letters don't ground-on-everything",
      "status": "done",
      "phase": "P0",
      "depends_on": [
        "T02"
      ],
      "files": [
        "src/server/tailor/letter-prompt.ts",
        "test/letter-engine.test.ts"
      ],
      "origin": "repair: Phase 0 letter-flip contrast fails (T05 attempt 1) — the frontend-rewrite letter grounded on ALL 3 seed entries {frontend-rewrite,platform-sdk,rules-engine}, making the platform-sdk {platform-sdk} and rules-engine {rules-engine} unions strict SUBSETS ⇒ letterFlipContrast false (the spec's 'no ground-on-everything letter passes by superset' guard). Root cause: LETTER_SYSTEM_PROMPT does not push for FOCUSED, per-role grounding.",
      "context": "REPAIR of the letter prompt built in T02. A live Phase 0 recording (T05 attempt 1) showed all three letters flip on the LEAD paragraph, but the frontend-rewrite letter grounded across all 3 seed entries (SEED_ENTRIES = exactly cloudcase-frontend-rewrite / cloudcase-platform-sdk / cloudcase-rules-engine), so its groundedOn UNION was a superset of the other two letters' unions ⇒ the spec's letterFlipContrast (pairwise union difference non-empty BOTH directions) fails. 2 of 3 letters were already focused (platform-sdk: 1 para on platform-sdk; rules-engine: 2 paras on rules-engine); the frontend letter over-reached into the other JDs' lead entries.\n\nEXISTS: src/server/tailor/letter-prompt.ts `LETTER_SYSTEM_PROMPT` (T02) — fact-lock framing + output shape; it does NOT tell the model to stay focused. test/letter-engine.test.ts asserts buildLetterUserPrompt (USER message) byte-identity — UNAFFECTED by a SYSTEM-prompt change; check whether it also asserts any LETTER_SYSTEM_PROMPT substring and update only if so.\n\nBUILD — edit LETTER_SYSTEM_PROMPT ONLY (do NOT touch buildLetterUserPrompt — its byte-identity is a fixture-stability contract): add a FOCUS instruction. The model must build the letter's argument from the experience(s) GENUINELY most relevant to THIS role — typically ONE lead experience elaborated across paragraphs — and must NOT inventory the candidate's whole background or mention every project to seem broad. A second experience may be cited ONLY if directly relevant to this role's core ask. The set of entries a letter grounds on for one role should be visibly DIFFERENT from what it would ground on for a different role (distinct positioning, not a superset of everything). Keep the existing fact-lock/output sections intact.\n\nDO NOT: change buildLetterUserPrompt; change LetterDecisionZ/engine; weaken the fact-lock. Touch ONLY declared files. (The SUFFICIENT gate for this repair is T05's live re-record contrast, not this keyless ticket.) [DONE — see .ailoop/evidence/T02R.txt]",
      "acceptance": "Baseline (oracle.md) green (check/build/lint/vitest — test/fit-ui flake per oracle). AND:\n- LETTER_SYSTEM_PROMPT now contains an explicit FOCUS instruction (lead decisively with the most role-relevant experience; do NOT inventory the whole background / ground on everything; a second entry only if directly relevant; per-role grounding should differ) — assert via a substring/behavioral check in test/letter-engine.test.ts.\n- buildLetterUserPrompt output is UNCHANGED (its existing byte-identity assertions still pass — the repair touches only the SYSTEM prompt).\n- FACT-LOCK INTACT (red-team): the grounding language survives the edit — assert LETTER_SYSTEM_PROMPT still contains the never-invent / groundedOn-traceability instruction (the focus edit must ADD to, not replace, the fact-lock).\n- NOTE: this keyless ticket is NECESSARY, not SUFFICIENT. The real proof is T05's live re-record: letterFlipContrast must return true (no letter's groundedOn union is a superset of another's) with the lead-flip still holding.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T02R.txt"
    },
    {
      "id": "T04R",
      "title": "Repair: make letter-fixture recording robust to sampling variance (per-JD retry)",
      "status": "done",
      "phase": "P0",
      "depends_on": [
        "T04",
        "T02R"
      ],
      "files": [
        "scripts/record-letter-fixtures.ts"
      ],
      "origin": "repair: T05 attempt 2 failed on sampling variance — a live diagnostic proved the (focused) model reliably produces flipping, focused, grounded letters, but the record script fails the whole recording if a SINGLE JD's one call happens to slip (ungrounded opening paragraph, or a number placed in a paragraph that doesn't cite its source entry). The fix is recording robustness, not the model/prompt.",
      "context": "REPAIR of scripts/record-letter-fixtures.ts (built in T04). BACKGROUND: A coordinator diagnostic (3 live calls, post-T02R focus prompt) produced clean letters for all 3 JDs — each lead-flips and grounds on a SINGLE distinct entry ({platform-sdk}/{rules-engine}/{frontend-rewrite}), so letterFlipContrast=true and no fabrication. But a separate record run had 2/3 JDs slip (platform-sdk: body[0] groundedOn empty; rules-engine: number '30,000' in a paragraph not citing its entry). gemini-2.5-flash is stochastic; the record script currently makes ONE attempt per JD and reports failure on a slip.\n\nEXISTS: scripts/record-letter-fixtures.ts calls recordOneLetter (from evalcore.ts) once per JD; recordOneLetter returns {ok:true,decision} | {ok:false,reason} after decide→assemble→validateLetterNoFabrication→letterFlipPredicate. The LetterRecordEngine wrapper in the script captures per-call token usage. decideLetter already retries once on a THROW (schema/API error); a successful-but-nonflipping/ungrounded decision does NOT throw, so it is not retried.\n\nBUILD — edit scripts/record-letter-fixtures.ts ONLY:\n- Wrap the per-JD recording in a RETRY LOOP: call recordOneLetter up to K attempts (K=5) and keep the FIRST result whose ok===true (a real fresh model call each attempt, still gated by the SAME mechanical flip+grounding checks — this is not forcing/hand-editing; it tolerates sampling noise for a model that demonstrably passes).\n- If all K attempts for a JD fail, report that JD's failures (last reason + attempt count) and exit non-zero as today — do NOT write a fixture.\n- PROVENANCE honesty: record the KEPT letter's real token usage in the manifest (as today), and additionally record the number of attempts spent per JD (so the record is honest that retries happened). Do NOT fabricate usage.\n\nDO NOT: touch the prompt (letter-prompt.ts), the engine, validators, or evalcore's recordOneLetter (the retry belongs in the script). Do NOT weaken the flip/grounding checks. Touch ONLY scripts/record-letter-fixtures.ts. (Sufficient gate: T05's re-record — all 3 flip, contrast true, grounding clean, provenance real.)",
      "acceptance": "Baseline (oracle.md) green (check/build/lint/vitest; scripts/ typechecked out-of-band per oracle amendment — the verifier confirms record-letter-fixtures.ts still typechecks).\n- record-letter-fixtures.ts now retries each JD up to K=5 times, keeping the first result that passes recordOneLetter's flip+grounding checks; on all-fail it still exits non-zero and writes no fixture (the checks are NOT weakened — grep confirms letterFlipPredicate + validateLetterNoFabrication still gate each attempt).\n- manifest provenance still records real token usage + now an attempts-per-JD count.\n- NOTE: keyless-necessary only; the SUFFICIENT gate is T05's live re-record (all 3 lead-flip, letterFlipContrast=true, grounding clean, usage>0).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T04R.txt"
    },
    {
      "id": "T02R2",
      "title": "Repair: letter prompt must reproduce numbers VERBATIM from facts",
      "status": "done",
      "phase": "P0",
      "depends_on": [
        "T02R"
      ],
      "files": [
        "src/server/tailor/letter-prompt.ts",
        "test/letter-engine.test.ts"
      ],
      "origin": "repair: T05 attempt 3 — rules-engine failed all 5 retries deterministically. Fact 'rules engine ~30k lines' vs the model writing '30,000'; normalizeToken('30,000')='30000' has no token in '~30k' => FabricationError (the fact-lock correctly catching a shorthand->precise expansion). Human-approved fix (numbers verbatim); human authorized ONE T05 attempt past the 3-attempt cap.",
      "context": "REPAIR of the letter prompt (letter-prompt.ts). BACKGROUND: the fact-lock is MECHANICAL and token-exact (validate.ts normalizeToken strips commas but does NOT equate '30k' with '30,000'). A seed fact reads 'rules engine ~30k lines of unstructured rules'; the model kept writing '30,000' in the rules-engine letter, which does not trace to the '~30k' token => FabricationError, all 5 retries. platform-sdk + frontend-rewrite record clean. Human chose: harden the prompt to force verbatim numbers (keep the fact-lock strict; do NOT change validate.ts).\n\nEXISTS: src/server/tailor/letter-prompt.ts LETTER_SYSTEM_PROMPT (fact-lock §1, focus §3 from T02R). buildLetterUserPrompt (byte-identity contract — do NOT touch).\n\nBUILD — edit LETTER_SYSTEM_PROMPT ONLY: add an explicit instruction under/near the fact-lock: reproduce every number EXACTLY as it appears in the entry's facts, including approximations and shorthand — e.g. if a fact says '~30k', write '~30k', NOT '30,000' or '30000'; never expand, round, spell out, or reformat a number. The reader/validator matches number TOKENS against the facts verbatim, so a reformatted number reads as fabrication. Keep the existing fact-lock, focus, and output sections intact.\n\nDO NOT: change buildLetterUserPrompt; change validate.ts / the number matcher (human chose the prompt fix, not a matcher change); weaken any check. Touch ONLY declared files. (Sufficient gate: T05 re-record — all 3 flip, contrast true, grounding clean incl. rules-engine's '~30k'.)",
      "acceptance": "Baseline green (check/build/lint/vitest; fit-ui flake per oracle). AND:\n- LETTER_SYSTEM_PROMPT now contains an explicit VERBATIM-NUMBER instruction (reproduce numbers exactly as in facts incl. shorthand/approximations like '~30k'; do not expand/round/reformat) — assert via a substring check in test/letter-engine.test.ts.\n- buildLetterUserPrompt output UNCHANGED (existing byte-identity assertions pass).\n- FACT-LOCK + FOCUS instructions still present (the add does not replace them).\n- NOTE: keyless-necessary; SUFFICIENT gate is T05's live re-record (rules-engine must now ground its number, e.g. '~30k', and all 3 flip with contrast true).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T02R2.txt"
    },
    {
      "id": "T05",
      "title": "Record letter fixtures over the 3 JDs (KEY-GATED)",
      "status": "done",
      "phase": "P0",
      "depends_on": [
        "T04",
        "T02R",
        "T04R",
        "T02R2"
      ],
      "files": [
        "test/fixtures/letters/manifest.json",
        "test/fixtures/letters/platform-sdk.json",
        "test/fixtures/letters/rules-engine.json",
        "test/fixtures/letters/frontend-rewrite.json",
        "test/fixtures/letters/eval-transcript.md"
      ],
      "origin": "spec Phase 0 (recorded fixtures with provenance + verdict transcript)",
      "context": "KEY-GATED. Run the Phase 0 proving run with a live key and commit the recorded letter fixtures + provenance + verdict transcript. This RETIRES the epic's central risk: is a grounded letter any good? If the letters do not flip, the letter prompt (letter-prompt.ts, T02) is wrong — nothing downstream saves it (oracle.md Phase 0). This ticket runs SCRIPTS built in T04; it writes ONLY fixtures/transcript.\n\nRUN (needs GOOGLE_GENERATIVE_AI_API_KEY in /workspace/.env):\n1. `bun run scripts/record-letter-fixtures.ts` → writes test/fixtures/letters/*.json + manifest.json (provenance: model id, provider, recordedAt). Capture stdout.\n2. `bun run scripts/eval-letter.ts` → the mechanical flip assertions + a verdict line per letter. Save the full transcript (each letter's text + verdict line: argument quality, motivation reflected) to test/fixtures/letters/eval-transcript.md.\n\nIf the key is ABSENT: STOP and report — do NOT hand-author fixtures (a recording without live provenance does not count, oracle.md). This is the one escalation the loop allows here.\n\nDO NOT: edit prompt/engine/validator code (if the flip fails, that is a repair on T02, reported not forced). Touch ONLY declared files.",
      "acceptance": "- test/fixtures/letters/manifest.json provenance: model 'gemini-2.5-flash', provider 'google', a recordedAt that parses as a valid ISO date within the session window, AND TOKEN USAGE > 0 (the un-fakeable signal — absence AUTO-FAILS, oracle.md). 3 fixtures present.\n- Each of platform-sdk/rules-engine/frontend-rewrite.json is {key, name, decision:LetterDecision}.\n- LETTER-FLIP (mechanical, re-run keylessly by a test the verifier runs): lead body paragraph's groundedOn includes cloudcase-platform-sdk / cloudcase-rules-engine / cloudcase-frontend-rewrite respectively; pairwise groundedOn-union diffs non-empty BOTH directions (superset-safe).\n- validateLetterNoFabrication passes on all three recorded letters.\n- eval-transcript.md: each letter's text + a verdict line that NAMES the JD and at least one cited entryId it argues from (coordinator judges argument quality; a boilerplate 'PASS' fails). [DONE attempt 4, human-authorized past cap — see .ailoop/evidence/T05.txt]",
      "attempts": [
        {
          "n": 1,
          "failed": [
            "letter-flip-contrast"
          ],
          "hypothesis": "Lead-flip passed for all 3 (lead paragraph cites the JD target), and grounding validated. But letterFlipContrast=false: the frontend-rewrite letter grounded on ALL 3 seed entries {frontend-rewrite,platform-sdk,rules-engine}, so platform-sdk {platform-sdk} and rules-engine {rules-engine} unions were strict subsets (empty diff one direction) — the spec's 'no ground-on-everything letter passes by superset' guard. Root cause is upstream: LETTER_SYSTEM_PROMPT (T02) doesn't push focused per-role grounding.",
          "fixNote": "Repair via T02R (focus the letter prompt). Then re-record. Coordinator-run (key-gated); provenance+usage were real and correct — only the contrast failed."
        },
        {
          "n": 2,
          "failed": [
            "lead-flip",
            "fabrication"
          ],
          "hypothesis": "After T02R (focused prompt), a re-record had 2/3 JDs slip: platform-sdk body[0] groundedOn empty (ungrounded opening paragraph ⇒ lead-flip false); rules-engine number '30,000' in a paragraph not citing its source entry (fabrication). A coordinator diagnostic (3 fresh live calls) then produced CLEAN letters for all 3 (each lead-flips, grounds on a single distinct entry, contrast true). So the model reliably CAN produce good letters — the failure is sampling variance the single-attempt record script doesn't tolerate.",
          "fixNote": "Repair via T04R (per-JD retry loop in the record script, K=5, same mechanical gates). Then re-record."
        },
        {
          "n": 3,
          "failed": [
            "fabrication"
          ],
          "hypothesis": "With retry (T04R): platform-sdk + frontend-rewrite PASS on attempt 1 (clean, lead-flip, focused). rules-engine fails ALL 5 retries with the SAME deterministic error: fact says '~30k lines' but the model writes '30,000'; normalizeToken strips commas ('30,000'->'30000') and '~30k' has no '30000' token -> FabricationError. The fact-lock is working correctly (model expanded an approximate shorthand into a precise-looking number = strengthening). Root cause: the letter prompt does not instruct verbatim number reproduction from facts. NOT sampling (5/5 identical).",
          "fixNote": "Fix: letter-prompt hardening — reproduce numbers VERBATIM as they appear in facts (write '~30k', not '30,000'); do not expand/round/reformat. CAP REACHED (3/3) — escalated to human for approval to continue past cap with this deterministic fix."
        },
        {
          "n": 4,
          "failed": [],
          "hypothesis": "Human-authorized attempt past cap, after T02R2 (verbatim numbers). Re-record: all 3 JDs PASS on attempt 1; independent keyless verify — lead-flip TRUE all 3, letterFlipContrast TRUE (distinct singleton unions), grounding clean (rules-engine now grounds '~30k'). Provenance real (9344 tokens). PASSED.",
          "fixNote": "n/a — passed. Fixtures biome-formatted before commit (pre-commit hook)."
        }
      ],
      "evidence": ".ailoop/evidence/T05.txt"
    },
    {
      "id": "T11",
      "title": "DB columns (motivation + letter snapshots) + migration 0006 + types",
      "status": "done",
      "phase": "P1",
      "depends_on": [
        "T01"
      ],
      "files": [
        "src/server/db/schema.ts",
        "src/shared/types.ts",
        "drizzle/0006_letter_snapshots.sql",
        "drizzle/meta/_journal.json",
        "drizzle/meta/0006_snapshot.json",
        "test/boot.smoke.test.ts",
        "test/db.test.ts"
      ],
      "origin": "spec Phase 1 (schema additions + migration)",
      "context": "Add the persistence for motivation + letter drafts to the `applications` table (oracle.md: letter storage mirrors the resume lifecycle; motivation is a dedicated field beside context; letter has its own genState taxonomy).\n\nEXISTS: src/server/db/schema.ts:82-101 `applications` — `current`/`locked` json TailoredResume|null (:90-91), `genState` plain text default 'untailored' (:93 — comment lists the 4 states, NOT $type-narrowed), `currentMeta` json (:94-98), `context` (:87). Domain mirror `Application` in src/shared/types.ts:194-209 (genState union :205). Migrations: latest is drizzle/0005_magical_eddie_brock.sql; drizzle/meta/_journal.json lists idx 0-5; KNOWN RESIDUAL (CLAUDE.md): 0005_snapshot.json was generator-emitted vs hand-simplified SQL, and 0004_snapshot.json is absent — so `drizzle-kit generate` may show phantom diffs. Boot applies migrations via migrate() in src/server/db/index.ts:40-42 (migrationsFolder 'drizzle'); test/boot.smoke.test.ts is the fresh-DATA_DIR boot smoke.\n\nBUILD:\n1. src/server/db/schema.ts: add to `applications` — `motivation` text (nullable, like context); `letterCurrent` (letter_current) json CoverLetter|null; `letterPrevious` (letter_previous) json CoverLetter|null; `letterGenState` (letter_gen_state) text notNull default 'untailored' (comment: 'untailored'|'tailoring'|'tailored'|'failed'); `letterFailedReason` (letter_failed_reason) text nullable. Follow the existing json/text idioms (:90-98).\n2. src/shared/types.ts: extend `Application` (:194-209) with motivation, letterCurrent/letterPrevious (CoverLetter|null), letterGenState (same union as genState), letterFailedReason (string|null).\n3. Migration: HAND-WRITE drizzle/0006_letter_snapshots.sql as ALTER TABLE statements adding the 5 columns (avoid the phantom-diff trap — do not blindly run generate; if you run drizzle-kit generate, simplify its SQL to just these ALTERs). Append the 0006 entry to drizzle/meta/_journal.json and add drizzle/meta/0006_snapshot.json (minimal, matching the journal). The migration MUST apply cleanly on a fresh DATA_DIR at boot.\n\nDO NOT: add routes (T13), zod (T12), or the voiceSources column (T41, migration 0007). Touch ONLY declared files.",
      "acceptance": "Baseline + boot smoke:\n- fresh-DATA_DIR boot applies through 0006 AND the __drizzle_migrations bookkeeping table contains a 0006 entry (proves a real numbered migration, not an edited-0005 or ad-hoc boot-time ALTER); applications has columns motivation, letter_current, letter_previous, letter_gen_state, letter_failed_reason.\n- UPGRADE PATH: boot a DATA_DIR migrated only through 0005, insert an app row, then boot with the full folder; 0006 applies additively, the pre-existing row survives with the new columns at their defaults (no data loss).\n- SQL DEFAULTS (not test-supplied): insert a row OMITTING all five new fields; read back letter_gen_state === 'untailored' and letter snapshots null.\n- CONTRAST: a row with letterCurrent set round-trips the JSON (non-null).\n- PHANTOM-DIFF: `drizzle-kit generate` against a scratch copy after 0006 produces NO new migration (0006_snapshot.json matches schema.ts).\n- `bun run check` clean; existing migration-boundary tests green.",
      "attempts": [
        {
          "n": 1,
          "failed": [
            "full-test-suite"
          ],
          "hypothesis": "T11 under-declared its file footprint. test/db.test.ts:182 asserts the FULL applications column list via toEqual; adding the 5 letter/motivation columns necessarily breaks that exact-match enumeration. Builder was disciplined (did not touch undeclared file). The tests intent (no hiring-status; generation-state only, §198-199 not.toContain) is preserved — the new columns are generation-state + emphasis-input.",
          "fixNote": "Prior work is committed on branch ailoop/T11 at 78a4ea3 and is CORRECT — do NOT redo it. The ONLY remaining fix: test/db.test.ts (now declared) column-list toEqual at ~line 182 must include the 5 new columns (motivation, letter_current, letter_previous, letter_gen_state, letter_failed_reason) in schema/migration order. Keep the not.toContain(status)/(hiring_status) assertions intact — the not-a-tracker intent is unchanged. Then re-run FULL baseline (check/build/lint/vitest all green) and commit."
        }
      ],
      "evidence": ".ailoop/evidence/T11.txt"
    },
    {
      "id": "T12",
      "title": "Zod: motivation + letter snapshot + backup round-trip",
      "status": "done",
      "phase": "P1",
      "depends_on": [
        "T11",
        "T01"
      ],
      "files": [
        "src/shared/schema.ts",
        "src/server/routes/backup.ts",
        "test/api.export-import.test.ts"
      ],
      "origin": "spec Phase 1 (application schema additions)",
      "context": "Wire motivation + letter snapshots through the request/import zod contracts (oracle.md: export/import must carry new persisted columns or they are silently stripped on restore; secrets/settings stay excluded — unchanged).\n\nEXISTS: src/shared/schema.ts `applicationCreate` :281-288 and `applicationUpdate` :290-297 (context is z.string(1-4000); jobDescription 1-20000). src/server/routes/backup.ts hand-writes `tailoredResumeZ` :32-63, `lockedFormatZ` :69-73, and `applicationImport = applicationCreate.extend({...full row incl current/locked/genState/currentMeta})` :81-97 — the comment at :45-55 WARNS undeclared keys are stripped on restore.\n\nBUILD:\n1. src/shared/schema.ts: add `motivation` to BOTH applicationCreate and applicationUpdate — z.string().min(1).max(4000).optional().nullable() (authored intent; mirror context's bounds; motivation is optional — a letter can be generated without it).\n2. src/server/routes/backup.ts: add a `coverLetterZ` (greeting/body[{text,groundedOn}]/closing — reuse/parallel LetterDecisionZ shape) and extend `applicationImport` (:81-97) to carry motivation, letterCurrent (coverLetterZ.nullable), letterPrevious (coverLetterZ.nullable), letterGenState, letterFailedReason. Ensure the import upsert (:134-197) writes them.\n\nDO NOT: add generation routes (T13). Touch ONLY declared files.",
      "acceptance": "Baseline + test/api.export-import.test.ts (extend):\n- applicationCreate.parse ACCEPTS motivation and ACCEPTS its absence; REJECTS motivation as '' (empty), as a >4000-char string, and as a non-string (number) — proves bounded z.string(1,4000), not z.any().\n- coverLetterZ: a paragraph with the groundedOn KEY DELETED ⇒ safeParse fails; a paragraph with groundedOn:[] ⇒ accepted (required, empty allowed).\n- ROUND-TRIP with the row ACTUALLY WIPED: create an app with motivation + non-null letterCurrent → GET /api/export → DELETE the app (or reset DB) → POST /api/import → GET: motivation and letterCurrent deep-equal restored (attributable to import, not incidental persistence). settings/secrets stay excluded.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T12.txt"
    },
    {
      "id": "T13",
      "title": "Letter generate + undo routes, genState/failedReason, letterPrevious, motivation merge",
      "status": "done",
      "phase": "P1",
      "depends_on": [
        "T11",
        "T12",
        "T03"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/client/api.ts",
        "src/client/queries/useApplications.ts",
        "test/api.applications-letter.test.ts"
      ],
      "origin": "spec Phase 1 (API routes, letter lifecycle)",
      "context": "The letter generation lifecycle route, mirroring the resume /tailor route but INDEPENDENT (oracle.md: letter generation is an independent action; overwrite-on-regenerate + one-level undo; its own genState/failedReason). NOTE: this ticket does NOT build the cross-document 409 in-flight guard — that is T14.\n\nEXISTS: src/server/routes/applications.ts — `POST /api/applications/:id/tailor` :219-287 is the template: resolveEngine (:98-118, keyless FixtureEngine vs BYOK decrypt; no key ⇒ {error:'no_api_key'} 400 at :232-234), load entries/settings/profile, tailor(...), on success write current+genState:'tailored'+currentMeta (:264-276), on catch set genState:'failed' + mapTailorError (:277-286, mapTailorError :43-58). `POST /lock` :295-324, LIST_COLUMNS :122-133 (omits heavy current/locked), PUT merge :180-209 (:197-206 field merge). Client: src/client/api.ts application fns :112-158 (tailorApplication :140-143); src/client/queries/useApplications.ts hooks :17-89 (invalidate ['applications',id]).\n\nBUILD:\n1. applications.ts: `POST /api/applications/:id/generate-letter` — mirror /tailor: resolveEngine (no key ⇒ 400 no_api_key), load entries+profile, read motivation/context from the row, call `tailorLetter(engine, jobDescription, entries, motivation, context)` (voice comes in T43). SUCCESS: displace existing letterCurrent → letterPrevious, write letterCurrent = new letter, letterGenState 'tailored'. FAILURE (catch): leave letterCurrent/letterPrevious UNTOUCHED, set letterGenState 'failed' + letterFailedReason via a mapper mirroring mapTailorError (reuse mapTailorError's classification for the reason string). Generating a letter MUST NOT mutate `current` (resume).\n2. applications.ts: `POST /api/applications/:id/undo-letter` — swap letterCurrent ↔ letterPrevious (one-level, itself undoable once), mirroring the spec's resume-undo semantics but letter-only.\n3. applications.ts: add motivation to the PUT merge (:197-206). Add letterCurrent/letterPrevious to LIST_COLUMNS OMISSION (keep the list endpoint light).\n4. Client: add generateLetter(id) / undoLetter(id) to api.ts and useGenerateLetter / useUndoLetter to useApplications.ts (invalidate ['applications',id]).\n\nDO NOT: build the 409 in-flight guard (T14) or per-part editing (T31/T32) or lock-joint letter freezing (that is Phase 3's locked-409 + a small lock extension — keep lock untouched here). Touch ONLY declared files.",
      "acceptance": "Baseline + test/api.applications-letter.test.ts (fixture engine, non-vacuous):\n- generate-letter over a FixtureEngine → 200; letterCurrent is the fixture letter, letterGenState 'tailored'; `current` (resume) BYTE-EQUAL to a PRE-POPULATED value (set via a real /tailor first) before/after — isolation, not vacuous-null.\n- regenerate with a DISTINCT fixture (change jd/motivation so the letter differs): new letterCurrent deep-UNEQUAL to the prior; the prior moved to letterPrevious (a no-op regenerate must FAIL this).\n- undo-letter swaps letterCurrent↔letterPrevious back (exact-value).\n- FAIL PATH from a NON-NULL start: seed non-null letterCurrent/letterPrevious via a prior success, force a failure ⇒ both deep-equal the pre-failure snapshot; letterGenState 'failed'; TWO distinct injected error types ⇒ TWO DIFFERENT letterFailedReason values (proves mapTailorError classification, not a hardcoded string).\n- PUT motivation ⇒ a SEPARATE GET returns it (not the PUT's own echo).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T13.txt"
    },
    {
      "id": "T14",
      "title": "Cross-document 409 in-flight guard (blocking generation)",
      "status": "done",
      "phase": "P1",
      "depends_on": [
        "T13"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "test/api.applications-inflight.test.ts"
      ],
      "origin": "spec Phase 1 (one generation in flight across both documents ⇒ 409)",
      "context": "NET-NEW v2 locked decision (oracle.md): ONE generation in flight per application across BOTH documents; a second generate (resume /tailor OR letter /generate-letter) while one is in flight ⇒ 409. This machinery does NOT exist today (grep-confirmed: no server 409 in-flight guard; genState:'tailoring' is client-only). Intake decision 1: we build this fresh; we do NOT retrofit resume undo/previous.\n\nEXISTS: applications.ts /tailor :219-287 and /generate-letter (T13) each set genState/letterGenState to 'tailored' on success. resolveEngine :98-118; deps.engine injection seam (ApplicationsRoutesDeps :86-92) lets a test inject a blocking engine.\n\nBUILD (applications.ts):\n1. A per-application in-flight guard shared by BOTH generation routes. On entry to /tailor and /generate-letter: if EITHER genState==='tailoring' OR letterGenState==='tailoring' for this app, reply 409 {error:'generation_in_flight'} (or the existing convention — cite the lock 409 style). Otherwise set the relevant *_genState to 'tailoring' BEFORE the model call, and clear it to 'tailored'/'failed' after (in both success and catch paths). The guard must be a real server-side check on persisted/observable state, not client-only.\n2. The 'tailoring' write must be visible to a concurrent request — i.e. set it in the DB before awaiting the engine, so a second request observes it.\n\nTEST HARNESS: the in-flight state must be produced by a REAL held-open generation — inject (via deps.engine) a blocking FixtureEngine whose decide/decideLetter await a manually-resolved promise (a deferred). Fire request A (it sets 'tailoring' and blocks), then fire request B and assert 409, then resolve A's deferred and assert A completes 200. NEVER set genState:'tailoring' directly to fake in-flight.\n\nDO NOT: change the letter/resume result shapes; touch client. Touch ONLY declared files.",
      "acceptance": "Baseline + test/api.applications-inflight.test.ts:\n- The blocking engine signals 'decide() entered' (resolves a sync promise) BEFORE awaiting its deferred; the test fires B only after that signal (no TOCTOU flake). POST /tailor (A) blocks; concurrent POST /generate-letter (B) ⇒ 409 generation_in_flight; resolve A ⇒ A 200, state 'tailored'; a fresh /generate-letter ⇒ 200.\n- Symmetric: letter in flight ⇒ concurrent /tailor ⇒ 409.\n- CROSS-DOCUMENT FAILURE RELEASE: force A (/tailor) to FAIL ⇒ genState settles to 'failed' (not stuck 'tailoring') ⇒ an immediately-following /generate-letter ⇒ 200 (guard released on the failure path too).\n- 409 driven by the real held-open generation, never a pre-written 'tailoring'.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T14.txt"
    },
    {
      "id": "T15",
      "title": "Motivation isolation guard (resume message byte-identical)",
      "status": "done",
      "phase": "P1",
      "depends_on": [
        "T13"
      ],
      "files": [
        "test/motivation-isolation.test.ts"
      ],
      "origin": "spec Phase 1 (motivation isolation — the leak the dedicated field prevents)",
      "context": "Motivation is a dedicated field that must reach ONLY the letter pipeline — never the resume-selection emphasis (oracle.md locked decision; the reason motivation is NOT folded into context). This is a pure guard test over already-built code (T02 letter path, T13 route wiring, and the frozen resume buildUserPrompt at engine.ts:34-45 which takes context/budget but NOT motivation).\n\nBUILD — NEW test/motivation-isolation.test.ts (MUST drive the REAL route — a signature-only unit check on buildUserPrompt is INSUFFICIENT: it passes whether or not motivation is wired anywhere, and cannot catch a T13 route bug that concatenates motivation into context/jobDescription before calling tailor()):\n- Create TWO real persisted applications, IDENTICAL in every field (same jd, context, targetPages, format) EXCEPT one has motivation set (via PUT), one does not. Drive POST /api/applications/:id/tailor for both through a SPY FixtureEngine that captures decide()'s (jd, entries, context, budget) args; assert the captured resume-engine inputs are BYTE-IDENTICAL across the two apps.\n- CONTRAST: the spy's decideLetter args for the with-motivation app CONTAIN the motivation text; for the without-motivation app they do not (proves motivation is wired to the letter, isolated from the resume).\n\nDO NOT: modify production code — if the test reveals motivation IS leaking into the resume message, that is a bug to report against T13/T02, not to paper over. Touch ONLY declared files.",
      "acceptance": "Baseline + test/motivation-isolation.test.ts (through the REAL /tailor route, two identical-but-motivation apps):\n- captured resume-engine decide() args are BYTE-IDENTICAL across the two apps (motivation never reaches the resume path).\n- captured decideLetter args CONTAIN motivation for the with-motivation app and NOT for the without app (contrast — a bug folding motivation into resume context/jd would fail the first assertion).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T15.txt"
    },
    {
      "id": "T16",
      "title": "Letter grounding/fabrication pipeline tests + fixture acceptance",
      "status": "done",
      "phase": "P1",
      "depends_on": [
        "T05",
        "T03",
        "T12"
      ],
      "files": [
        "test/letter-pipeline.test.ts"
      ],
      "origin": "spec Phase 1 (fixture-driven pipeline tests)",
      "context": "The Phase 1 keyless proof that the recorded letters are contract-valid and the fabrication control holds end-to-end. Depends on T05 (recorded fixtures — KEY-GATED; if T05 has not run, this ticket cannot pass and must wait).\n\nEXISTS after prior tickets: LetterDecisionZ (T01), validateLetterNoFabrication + tailorLetter (T03), FixtureEngine.decideLetter loading test/fixtures/letters (T02), the recorded fixtures (T05). SEED_ENTRIES in src/server/seed.ts.\n\nBUILD — NEW test/letter-pipeline.test.ts:\n1. LetterDecisionZ.parse ACCEPTS each recorded fixture's `decision` (all three); REJECTS a copy whose body paragraph groundedOn is deleted.\n2. FABRICATION CONTRAST PAIR (the anti-laundering control): construct a CoverLetter paragraph containing a number that is NOT in any entry's facts (only in a motivation-like string) → validateLetterNoFabrication THROWS FabricationError; the SAME number placed in a paragraph whose groundedOn entry's facts contain it → PASSES.\n3. tailorLetter over FixtureEngine (SEED_ENTRIES + a recorded JD) returns a validated letter that matches the recorded decision's parts.\n\nDO NOT: touch production code. Touch ONLY declared files.",
      "acceptance": "Baseline + test/letter-pipeline.test.ts:\n- HARD PRECONDITION: fs.readdirSync('test/fixtures/letters') contains the 3 recorded fixtures + manifest; a missing/empty dir FAILS LOUDLY (never a silent skip/synthesize).\n- PROVENANCE AUTHENTICITY: manifest model === 'gemini-2.5-flash', usage total tokens > 0, a real timestamp — a zero-usage/placeholder-provenance fixture fails even if `decision` parses.\n- LetterDecisionZ.parse ACCEPTS each recorded decision; REJECTS a copy with a paragraph's groundedOn deleted.\n- FABRICATION, THREE CASES (per-citation, not library-pool): (a) number in no entry ⇒ throws; (b) number in the paragraph's cited entry facts ⇒ passes; (c) number in entry X's facts but the paragraph cites only Y (X uncited) ⇒ THROWS (closes the library-wide-pool laundering hole).\n- tailorLetter over FixtureEngine returns a validated letter matching the recorded decision's parts.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T16.txt"
    },
    {
      "id": "T21",
      "title": "Letter layout in the react-pdf engine + extraction-order invariant",
      "status": "done",
      "phase": "P2",
      "depends_on": [
        "T01",
        "T05"
      ],
      "files": [
        "src/client/document/engine/letter.tsx",
        "src/client/document/renderLetter.ts",
        "src/client/document/index.ts",
        "test/letter-render.test.ts",
        "test/letter-extraction-invariant.test.ts"
      ],
      "origin": "spec Phase 2 (minimal letter layout in the one engine)",
      "context": "Render the letter through the SAME react-pdf path as the resume (oracle.md: PDFs only via react-pdf; the preview IS the artifact; minimal letter layout — header identity + date + recipient/greeting + prose body + sign-off — sharing the application format's typography/colors; NO letter-specific design axes).\n\nEXISTS: The resume composition is src/client/document/engine/document.tsx `EngineDocument` :381-600 (react-pdf <Document title/author={profile.name}> → <Page size=PAGE_SIZE[paper]>). Render entrypoints: src/client/document/renderResume.ts (renderResumeDocument :26, renderResumeToBuffer :37, renderResumeToBlob :46) + engine/render.ts. Fonts registered in renderResume.ts. legacyAdapt.ts adapts DocumentFormatV2 → the sections' legacy DocumentFormat. Extraction: src/client/document/extractText.ts `extractPdfText(buffer)` :35-47; plainText.ts. Extraction invariant test pattern: test/document-extraction-invariant.test.ts (render via renderResumeToBuffer + extractPdfText, assert content in order, SENTINEL strings for leadRationale/cut absent).\n\nBUILD:\n1. NEW src/client/document/engine/letter.tsx: an `EngineLetter({letter: CoverLetter, profile, paper, format})` react-pdf Document — header (profile identity, title/author=profile.name), date, recipient/greeting, body paragraphs in order, sign-off. Reuse the format's typography/colors via the SAME legacyAdapt/font resolution the resume uses; do NOT invent new axes. There is NO leadRationale/cut on a letter — do not render any such field.\n2. NEW src/client/document/renderLetter.ts: renderLetterDocument(args) + renderLetterToBuffer + renderLetterToBlob mirroring renderResume.ts (register fonts).\n3. src/client/document/index.ts: export the new letter render entrypoints from the barrel.\n\nDO NOT: add design-panel axes for the letter (tripwire); touch the resume document. Touch ONLY declared files.",
      "acceptance": "Baseline + tests:\n- test/letter-extraction-invariant.test.ts uses a HAND-BUILT CoverLetter with >=3 distinct-marker body paragraphs (BODY_PARA_ONE/TWO/THREE); extractPdfText yields greeting, each paragraph, and closing with a strictly-increasing lastIdx loop (real order proof, not a 1-paragraph degenerate).\n- NO id leakage: build a paragraph whose groundedOn id string (e.g. 'cloudcase-platform-sdk') is NOT literal body prose; assert that id string is ABSENT from the extracted text (targets a field the type actually carries — the resume's leadRationale/cut sentinel check is vacuous for a letter, which has no such fields).\n- test/letter-render.test.ts: renderLetterToBuffer over a recorded fixture is a non-empty PDF; title/author = profile.name asserted against TWO distinct profiles (a hardcoded literal can't pass both).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T21.txt"
    },
    {
      "id": "T22",
      "title": "Letter download + filename + PDF title/author",
      "status": "done",
      "phase": "P2",
      "depends_on": [
        "T21"
      ],
      "files": [
        "src/client/document/download.ts",
        "test/letter-download.test.ts"
      ],
      "origin": "spec Phase 2 (download naming)",
      "context": "Letter download hygiene (oracle.md loud default: `<Name> — <Company> — <Role> — Cover Letter.pdf`; PDF title/author from profile — extends the v1 file-hygiene convention).\n\nEXISTS: src/client/document/download.ts — `slugFilename([name,company,role])` :34-40 joins with ' — ' → `<Name> — <Company> — <Role>.pdf`; `pdfFilename` :42; `downloadResumePdf(args)` :65-73 (pdf(renderResumeDocument(...)).toBlob() + anchor click); sanitizeSegment :23-30. renderLetterToBlob from T21.\n\nBUILD (download.ts):\n- `letterPdfFilename(name, company, role)` → `<Name> — <Company> — <Role> — Cover Letter.pdf` (reuse sanitizeSegment/slug helpers; append ' — Cover Letter').\n- `downloadLetterPdf(args)` mirroring downloadResumePdf but using renderLetterToBlob (title/author=profile.name already set in T21's document).\n\nDO NOT: change the resume filename. Touch ONLY declared files.",
      "acceptance": "Baseline + test/letter-download.test.ts (four-case, mirroring download.test.ts):\n- letterPdfFilename('Ada Lovelace','Acme','Staff Engineer') === 'Ada Lovelace — Acme — Staff Engineer — Cover Letter.pdf'; a SECOND distinct input yields a different filename; illegal chars (/\\:*?\"<>|) + diacritics sanitize to a SPECIFIC expected string; missing company/role omitted.\n- PROPERTY: stripping ' — Cover Letter' from letterPdfFilename's output reproduces pdfFilename's output for the same inputs (proves it reuses sanitizeSegment/slug, not a weaker parallel sanitizer).\n- downloadLetterPdf(args): a spy asserts renderLetterToBlob (NOT renderResumeToBlob) is called with the letter/profile/paper/format, and the created anchor's .download === letterPdfFilename(...).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T22.txt"
    },
    {
      "id": "T23",
      "title": "Letter preview component (pdf.js)",
      "status": "done",
      "phase": "P2",
      "depends_on": [
        "T21"
      ],
      "files": [
        "src/client/components/LetterPreview.tsx",
        "test/letter-preview.test.tsx",
        "src/client/components/DocumentPreview.tsx"
      ],
      "origin": "spec Phase 2 (pdf.js preview)",
      "context": "The letter's pdf.js preview — the preview IS the artifact (oracle.md), same render path as export.\n\nEXISTS: src/client/components/DocumentPreview.tsx — `RenderedPreview` :129-173 uses react-pdf `usePDF({document: renderResumeDocument(...)})` and repaints via update() keyed on inputs; `DocumentPreview` :175-210 pulls profile/paper from useProfile()/useSettings(); PdfCanvas/PdfPages render pdf.js pages. ResultView.tsx consumes it.\n\nBUILD — NEW src/client/components/LetterPreview.tsx:\n- Mirror RenderedPreview/DocumentPreview but for the letter: usePDF({document: renderLetterDocument({letter, profile, paper, format})}), repaint on [letter, profile, paper, format] changes, render page(s) via the same PdfCanvas/PdfPages approach (import or lift the shared canvas helpers — if you must export them from DocumentPreview.tsx, declare it; otherwise a local minimal canvas render is fine). Pull profile/paper from useProfile()/useSettings() like DocumentPreview.\n\nDO NOT: add the application-page wiring (T24) or editing (Phase 3). Touch ONLY declared files (add DocumentPreview.tsx to declared files ONLY if you export shared helpers from it).",
      "acceptance": "Baseline + test/letter-preview.test.tsx (RTL/jsdom):\n- MODULE-MOCK renderLetterDocument and assert LetterPreview calls it with the EXACT {letter,profile,paper,format} props it received (input-bound, not just 'a canvas mounts'); assert it is CALLED AGAIN when letter/format props change (re-render effect — the bug DocumentPreview documents having fixed).\n- Per usePDF state, a POSITIVE DOM outcome: mocked loading ⇒ loading text shown; mocked ready + fake url ⇒ a <canvas> with the letter-scoped testid/class present (not merely 'does not throw', which tolerates a silently-blank error-swallowed component).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T23.txt"
    },
    {
      "id": "T24",
      "title": "Application-page letter surface + motivation field UI (e2e)",
      "status": "done",
      "phase": "P2",
      "depends_on": [
        "T13",
        "T21",
        "T22",
        "T23"
      ],
      "files": [
        "src/client/components/ApplicationDetail.tsx",
        "src/client/components/JobPanel.tsx",
        "src/client/components/GenStateBadge.tsx",
        "src/client/api.ts",
        "src/client/queries/useApplications.ts",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "spec Phase 2 (application-page surface for generate/regenerate/undo + motivation)",
      "context": "Surface the letter on the application page + the motivation field, with e2e coverage in the `applications` playwright project (oracle.md: design.spec.ts + applications.spec.ts live in the applications project via testMatch /(applications|design)\\.spec\\.ts/; the applications server runs LEDE_TAILOR_ENGINE=fixture — keyless replay). COARSE ticket — decompose if a worker finds it larger than one session.\n\nEXISTS: src/client/components/ApplicationDetail.tsx — header action buttons :185-240 (Lock/Unlock, Tailor/Re-tailor :202-204, Download :205-239), GenStateBadge :181, renders JobPanel :244 + ResultView. JobPanel.tsx :101-110 has the context Textarea (mirror for motivation). GenStateBadge.tsx :7-19 maps genState→pill (replicate for letterGenState). Client fns/hooks from T13 (generateLetter/undoLetter).\n\nBUILD:\n1. ApplicationDetail.tsx: a letter section — Generate/Regenerate + Undo-letter buttons (wired to useGenerateLetter/useUndoLetter), a letter GenState badge, and the LetterPreview (T23) shown when letterCurrent exists; when it does NOT, show the generate affordance — NEVER an empty document. A 'Download cover letter' button (downloadLetterPdf, T22). CRITICAL: give the LetterPreview host a DISTINCT scope ([data-testid=\"letter-preview\"], NOT the shared .document-preview class) so an e2e canvas assertion cannot accidentally observe the RESUME canvas via .first().\n2. JobPanel.tsx: a motivation Textarea (mirror context :101-110) writing motivation through the existing applicationUpdate PUT.\n3. GenStateBadge.tsx: support a letterGenState variant (or a small sibling).\n4. e2e in test/e2e/applications.spec.ts (keep it in the applications project — do NOT add a new spec file unless you also widen the testMatch in playwright.config.ts): generate letter → preview canvas paints (expectCanvasPainted pattern from design.spec.ts) → download yields '<Name> — <Company> — <Role> — Cover Letter.pdf'; an app with no letter shows the generate affordance (no empty doc); motivation is enterable, persists across reload (fresh fetch), and reaches the letter request (assert via the fixture engine's received input or the persisted column).\n\nDO NOT: build editing (Phase 3) or voice flag (Phase 4). Touch ONLY declared files (add playwright.config.ts if you widen testMatch).",
      "acceptance": "Baseline + `bunx playwright test --project=applications` green, AND the verifier confirms the new letter-e2e test TITLES actually appear in the run (--list / JSON reporter) — exit-0 with 0 new tests run does NOT count:\n- letter preview host has a DISTINCT scope ([data-testid=\"letter-preview\"] canvas); generate ⇒ that canvas paints (non-white pixel); the RESUME canvas pixel data is UNCHANGED by letter generation (isolation).\n- download yields '<Name> — <Company> — <Role> — Cover Letter.pdf'.\n- no-letter state: a visible 'Generate' affordance IS present (getByRole) AND zero letter-preview canvases (count===0) — both, so absence-of-everything can't pass.\n- motivation: entered in the UI, persists across reload (fresh fetch). This e2e claim is scoped to PERSISTENCE only; motivation flow-through to decideLetter is proven by T15 (do not re-assert it vacuously via the fixture-matching path, which ignores motivation).\n- RTL unit: Undo-letter button disabled when letterPrevious is null; letter GenStateBadge renders 'failed'/'tailoring'/'tailored' distinctly.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T24.txt"
    },
    {
      "id": "T31",
      "title": "Per-part text PATCH (resume) + 400 on structural/non-text + locked 409",
      "status": "done",
      "phase": "P3",
      "depends_on": [
        "T11",
        "T12"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/shared/schema.ts",
        "test/api.applications-edit.test.ts"
      ],
      "origin": "spec Phase 3 (per-part edit API; structural change unrepresentable)",
      "context": "The edit API: per-part text PATCH where structural change is UNREPRESENTABLE BY CONSTRUCTION (oracle.md loud default: a part addressed by a stable path — summary; section/group/item position — carrying ONLY a string; unknown parts and non-string bodies ⇒ 400; over whole-snapshot PUT + diffing). Editing is text-level only on the resume; structure stays the model's (locked decision). Locked app ⇒ 409.\n\nEXISTS: applications.ts PUT :180-209, lock :295-324 (locked column set), resolveEngine/mapTailorError. TailoredResume shape in types.ts:149-154 (sections→groups→items[{entryId,text,level?}], summary). The edit mutates `current` IN PLACE (oracle.md: edits never displace snapshots).\n\nBUILD:\n1. src/shared/schema.ts: a `resumePartPatchZ` = a `.strict()` discriminated/path-addressed body carrying ONLY a string value — e.g. z.object({path: <strict union of {kind:'summary'} | {kind:'item',section,group,index}>, text: z.string()}).strict(). MUST be `.strict()` — default zod SILENTLY STRIPS unknown keys, so a structural field like entryId would be ignored (200) rather than rejected; strict makes structural change UNREPRESENTABLE, not merely runtime-ignored. groundedOn/entryId/rank/level/structure are absent from the schema.\n2. applications.ts: `PATCH /api/applications/:id/resume-part` — parse resumePartPatchZ; if the app is LOCKED ⇒ 409; resolve the addressed part in `current` and replace ONLY its text; if the path does not resolve (unknown/removed part) ⇒ 400; write `current` back in place (do NOT touch previous/locked/genState). Edits are NOT fabrication-validated (trusted authorship).\n\nDO NOT: allow adding/removing/reordering items (structural — the letter gets insert/remove in T32, the resume never does); touch the letter (T32). Touch ONLY declared files.",
      "acceptance": "Baseline + test/api.applications-edit.test.ts (well-formed structural attempts, .strict() enforced):\n- resumePartPatchZ is `.strict()`; z.infer<typeof resumePartPatchZ> has NO entryId/rank/groundedOn/level property (type-level assert).\n- PATCH {path:{kind:'item',...}, text:'new'} ⇒ 200; a FULL row diff shows ONLY that item's text changed (genState/currentMeta/locked/item-count/order/entryId all identical).\n- WELL-FORMED structural attempt {path:{kind:'item',...}, text:'valid', entryId:'x'} (valid text PLUS a structural key) ⇒ 400 (strict rejects, does not silently strip).\n- index one past a real group's end {index: items.length} ⇒ 400 AND item count unchanged (no silent append).\n- unknown/typo'd part ⇒ 400; non-string text (123) ⇒ 400.\n- PATCH on a LOCKED app (locked via the real POST /lock) ⇒ 409; GET shows the row byte-unchanged after.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T31.txt"
    },
    {
      "id": "T32",
      "title": "Letter paragraph PATCH + insert/remove + blank-letter creation",
      "status": "done",
      "phase": "P3",
      "depends_on": [
        "T31",
        "T13"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/shared/schema.ts",
        "test/api.applications-letter-edit.test.ts"
      ],
      "origin": "spec Phase 3 (letter paragraph insert/remove; blank letter)",
      "context": "The letter's editing surface — richer than the resume's because letter prose structure IS the user's (oracle.md locked decision: the letter allows paragraph insert/remove; a hand-added paragraph carries groundedOn:[] — authored, not validated; a blank letter can be created without a generation — an empty parts skeleton, no model call — the retroactive-import mechanics).\n\nEXISTS after T31: resumePartPatchZ + PATCH resume-part in applications.ts; letterCurrent column + generate-letter route (T13); CoverLetter shape (T01).\n\nBUILD (applications.ts + schema.ts):\n1. `PATCH /api/applications/:id/letter-part` — text-edit a letter part by a stable path (greeting; body paragraph index; closing), carrying only a string; unknown part / non-string ⇒ 400; LOCKED ⇒ 409; mutate letterCurrent in place.\n2. Letter paragraph INSERT/REMOVE — routes (or actions on the same PATCH surface) to insert a new body paragraph (text, groundedOn:[] — hand-authored) and remove a paragraph by index; LOCKED ⇒ 409.\n3. `POST /api/applications/:id/letter-blank` (or equivalent) — create a BLANK letterCurrent (empty greeting/body:[]/closing skeleton) WITHOUT any model call / engine resolution; letterGenState stays a non-generated value (e.g. 'untailored' or a hand-authored marker — pick one consistent with T13). This is the retroactive-import entry point.\n\nDO NOT: fabrication-validate hand edits; allow resume structural change (that stays T31's text-only). Touch ONLY declared files.",
      "acceptance": "Baseline + test/api.applications-letter-edit.test.ts:\n- PATCH letter-part {path:{kind:'body',index:0}, text:'x'} ⇒ that paragraph's text changes, groundedOn preserved.\n- INSERT with an explicit groundedOn:['real-id'] in the body ⇒ the stored paragraph is groundedOn:[] regardless (server forces [], or the field is absent from the strict schema) — closes the hand-authored-laundering vector.\n- POSITION (not count-only): start with >=2 distinct paragraphs; insert at index 1 ⇒ the paragraphs before/after are unchanged and the new one is exactly at index 1; remove index 0 ⇒ assert by exact text which remain, in order.\n- POST letter-blank on an app with NO letter, using an INJECTED SPY engine whose decide/decideLetter THROW if called ⇒ 200 with an empty skeleton and ZERO spy invocations (proves no model call — not merely 'works without a key'); then a PATCH edits it into content.\n- LOCKED (via the real POST /lock) ⇒ every letter route (patch/insert/remove/blank) ⇒ 409.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T32.txt"
    },
    {
      "id": "T33",
      "title": "Overwrite + one-level undo semantics tests (both documents)",
      "status": "done",
      "phase": "P3",
      "depends_on": [
        "T31",
        "T32",
        "T13"
      ],
      "files": [
        "test/api.applications-overwrite.test.ts"
      ],
      "origin": "spec Phase 3 (overwrite/undo semantics asserted not assumed)",
      "context": "Prove the overwrite + one-level undo contract end-to-end (oracle.md: re-tailor/regenerate overwrites wholesale, edits included; edit→undo→edited draft in previous, one re-undo restores; a subsequent generation overwrites previous and the edit is gone). Test-only ticket over already-built routes (T13 generate/undo, T31/T32 edits, existing /tailor).\n\nBUILD — NEW test/api.applications-overwrite.test.ts (fixture engine):\n1. Edit a resume item's text → re-tailor (/tailor) → the edit is GONE (fixture text is back) — overwrite asserted.\n2. Edit a letter paragraph → regenerate (/generate-letter) → the edit is GONE.\n3. Letter edit → undo-letter → the edited draft sits in letterPrevious (one re-undo via undo-letter restores it); THEN a fresh generate-letter overwrites letterPrevious and the edit is unrecoverable — the locked one-level semantics.\n\nDO NOT: touch production code. Touch ONLY declared files.",
      "acceptance": "Baseline + test/api.applications-overwrite.test.ts (non-vacuous):\n- edit a resume item ⇒ assert the edited text is PRESENT (pre-overwrite) ⇒ re-tailor ⇒ edited text GONE, fixture text back (two assertions, so a no-op PATCH fails).\n- edit a letter paragraph ⇒ present ⇒ regenerate ⇒ gone.\n- ONE-LEVEL UNDO from distinct non-null state: generate-letter TWICE with DISTINCT fixtures (distinct non-null letterCurrent/letterPrevious), edit current, then undo-letter ⇒ current becomes old-previous, previous becomes the edited draft (exact text both directions); undo again ⇒ edited draft restored to current.\n- UNRECOVERABLE: a fresh generate-letter overwrites letterPrevious; then undo-letter once ⇒ the edited text is in NEITHER letterCurrent NOR letterPrevious (recovery genuinely fails, not just 'a field's value moved').",
      "attempts": [],
      "evidence": ".ailoop/evidence/T33.txt"
    },
    {
      "id": "T34",
      "title": "Editing UI (in-place text) + locked read-only (e2e)",
      "status": "done",
      "phase": "P3",
      "depends_on": [
        "T31",
        "T32",
        "T24"
      ],
      "files": [
        "src/client/components/ResultView.tsx",
        "src/client/components/LetterPreview.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/api.ts",
        "src/client/queries/useApplications.ts",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "spec Phase 3 (text-level in-place editing UI; locked disables affordances)",
      "context": "In-place text editing on both drafts in the UI, and locked ⇒ read-only (oracle.md: text-level only on the resume; letter allows paragraph insert/remove; locked disables every edit affordance and the route 409s). COARSE — decompose if larger than one session.\n\nEXISTS: ResultView.tsx renders the resume; LetterPreview (T23) + ApplicationDetail letter surface (T24); PATCH routes (T31/T32); design.spec.ts locked-read-only pattern (readOnly gating). Client api/hooks from T13.\n\nBUILD:\n1. Resume item + summary in-place text editing (an editable affordance calling PATCH resume-part). Letter paragraph in-place editing + insert/remove buttons (PATCH letter-part + insert/remove routes). Add the client api fns + hooks for the PATCH/insert/remove routes.\n2. When the application is LOCKED, every edit/insert/remove affordance is disabled (read-only), mirroring design.spec.ts's locked handling.\n3. e2e in test/e2e/applications.spec.ts: edit a resume item's text + a letter paragraph → each persists across reload (fresh fetch, non-vacuous); the edited letter still renders (preview canvas paints — extraction sanity); a LOCKED app shows disabled affordances and a direct PATCH returns 409.\n\nDO NOT: allow resume structural editing in the UI (text-only). Touch ONLY declared files.",
      "acceptance": "Baseline + `bunx playwright test --project=applications` green:\n- type a RUN-UNIQUE marker into a resume item AND a letter paragraph; waitForResponse on the PATCH 200; reload; assert exact marker match AND inequality with the captured PRE-EDIT baseline.\n- edited ARTIFACT reflects the edit: previewDataUrl differs before/after the edit (pixel-diff) OR extractPdfText on the downloaded PDF contains the marker (not just 'a canvas paints', which stale content passes).\n- LOCKED (via the real POST /lock): individually assert `disabled` on the resume text-edit control, the letter text-edit control, the insert button, AND the remove button (four assertions); a WELL-FORMED PATCH ⇒ exact 409 and GET shows the row byte-unchanged.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T34.txt"
    },
    {
      "id": "T41",
      "title": "profile.voiceSources column + cap constant + migration 0007 + types",
      "status": "done",
      "phase": "P4",
      "depends_on": [
        "T11",
        "T01"
      ],
      "files": [
        "src/server/db/schema.ts",
        "src/shared/schema.ts",
        "src/shared/types.ts",
        "drizzle/0007_voice_sources.sql",
        "drizzle/meta/_journal.json",
        "drizzle/meta/0007_snapshot.json",
        "test/boot.smoke.test.ts",
        "src/client/document/sampleResume.ts",
        "test/api.profile-settings.test.ts"
      ],
      "origin": "spec Phase 4 (voiceSources bounded set)",
      "context": "The voice-source store: a bounded Profile-scope set (oracle.md: cap = 5, a CODE CONSTANT not user-configurable; each source {id, kind:'cover-letter'|'resume', text, at}; always prose). Depends on T11 to keep migration ordering (0006 then 0007).\n\nEXISTS: src/server/db/schema.ts profile :38-56 — `links` json array default [] (:47-50) is the bounded-JSON-array idiom to follow. src/shared/schema.ts profileInput :244-253 (`links: z.array(profileLinkZ).max(8)` :250 is the bounded-array precedent; userPresetZ :263-268 is the bounded-object precedent). src/shared/types.ts Profile :81-90. Migrations: T11 adds 0006; this is 0007.\n\nBUILD:\n1. schema.ts: add `voiceSources` (voice_sources) json column to profile, notNull default [] (follow links :47-50), typed VoiceSource[].\n2. src/shared/schema.ts: `voiceSourceZ` = z.object({ id: z.string(), kind: z.enum(['cover-letter','resume']), text: z.string(), at: z.number() }); add `voiceSources: z.array(voiceSourceZ).max(5)` to profileInput (the cap in zod is a SECONDARY guard; the primary cap is the code constant enforced at the flag route in T42). Export a `VOICE_SOURCES_CAP = 5` constant.\n3. src/shared/types.ts: add VoiceSource type + voiceSources to Profile.\n4. Migration drizzle/0007_voice_sources.sql (hand-written ALTER TABLE profile ADD voice_sources ...) + _journal.json 0007 entry + drizzle/meta/0007_snapshot.json.\n\nDO NOT: add the flag/delete routes (T42) or conditioning (T43). Touch ONLY declared files.",
      "acceptance": "Baseline + boot smoke:\n- fresh-DATA_DIR boot applies 0007; insert a profile row OMITTING voiceSources, SELECT, deep-equal the column to [] (proves the SQL JSON default resolves on READ, not just column-exists via pragma).\n- voiceSourceZ.parse REJECTS kind:'other' and any non-enum kind (keeps the cut 'other' kind out); ACCEPTS 'cover-letter'/'resume'.\n- profileInput ACCEPTS <=5 voiceSources, REJECTS 6 (zod .max(5)).\n- VOICE_SOURCES_CAP === 5 exported. (Cap ENFORCEMENT is T42 — this ticket's green must NOT be read as 'cap enforced'.)\n- migration-boundary/boot tests green; drizzle-kit generate no-op check on 0007.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T41.txt",
      "filesAmendment": "+sampleResume.ts +api.profile-settings.test.ts (mechanical: required Profile.voiceSources cascade; ledger 0064)"
    },
    {
      "id": "T42",
      "title": "Flag route (frozen copy) + delete + resume plain-text + locked-permitted + cap",
      "status": "done",
      "phase": "P4",
      "depends_on": [
        "T41",
        "T21"
      ],
      "files": [
        "src/server/routes/applications.ts",
        "src/server/routes/profile.ts",
        "src/shared/schema.ts",
        "src/client/document/plainText.ts",
        "test/api.voice-flag.test.ts",
        "src/shared/plainText.ts",
        "src/client/document/download.ts",
        "test/ats-view.test.tsx"
      ],
      "origin": "spec Phase 4 (flag → frozen copy; delete; cap; locked-permitted)",
      "context": "The ONLY door into voiceSources: flag an application output → a FROZEN COPY into profile.voiceSources (oracle.md: flagging copies, never points at mutable current; a voice source is always prose — flagging a resume freezes its plain-text extraction, flagging a letter freezes its prose; flagging is permitted on a LOCKED app; cap=5 rejects a 6th with a typed error; delete works; a number only in a voice source ⇒ FabricationError is proven end-to-end via T43's conditioning, but the flag itself just copies text).\n\nEXISTS: applications.ts (app row read; lock column). profile.ts PUT :26-44 (spreads validated input). voiceSourceZ + VOICE_SOURCES_CAP (T41). plainText.ts `plainText(resume, profile)` :19-31 (resume → 'what the ATS sees' prose; pure, no react-pdf — importable server-side). The letter's prose = its greeting/body/closing joined.\n\nBUILD:\n1. applications.ts: `POST /api/applications/:id/flag-voice` body {kind:'cover-letter'|'resume'}. For 'cover-letter': freeze the letter's prose (join letterCurrent parts). For 'resume': freeze plainText(current, profile) (import plainText server-side; if plainText is not server-importable as-is, make it a shared pure module and declare the move). Append {id, kind, text, at} to profile.voiceSources. If profile.voiceSources.length >= VOICE_SOURCES_CAP (5) ⇒ a typed error (e.g. 409/422 {error:'voice_cap'}). Flagging MUST work on a LOCKED app (locked blocks generation/editing, NOT flagging). The copy is FROZEN — it must not reference the mutable current.\n2. A delete route — `DELETE /api/profile/voice-sources/:vid` (in profile.ts) removes by id.\n\nDO NOT: add a Profile paste-in box (tripwire — flagging is the only door); auto-mine facts (tripwire). Touch ONLY declared files.",
      "acceptance": "Baseline + test/api.voice-flag.test.ts (frozen AFTER mutation, not just at flag time):\n- flag-voice kind:'cover-letter' ⇒ a source with the letter's prose lands; THEN edit/regenerate the letter ⇒ the source is BYTE-IDENTICAL (frozen).\n- flag-voice kind:'resume' ⇒ stored text EXACT-EQUAL to plainText(current, profile) (deep string equality, not .includes); THEN re-tailor the resume (overwrites current) ⇒ the source is UNCHANGED, still the OLD plainText (not the new current's) — proves a true snapshot, not a live reference.\n- locked-permitted: flagging returns 200 on a LOCKED app for BOTH kind:'resume' AND kind:'cover-letter'.\n- CAP: the 6th flag ⇒ exact 409 {error:'voice_cap'}; voiceSources.length stays exactly 5; the 6th text appears NOWHERE in the stored array (no silent ring-buffer evict).\n- DELETE by id removes a source (confirmed via a fresh GET /api/profile).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T42.txt",
      "filesAmendment": "+src/shared/plainText.ts (move target) +src/client/document/download.ts (importer) — plainText moved client->shared so the server flag route imports the SAME pure fn byte-identically (server tsconfig has no @client alias; server->client import is a layering violation). Pre-dispatch refinement, ledger 0069. | +test/ats-view.test.tsx (2nd plainText importer in test/, import-path-only cascade of the move; pre-amend grep missed test/; ledger 0071)"
    },
    {
      "id": "T43",
      "title": "Voice conditioning into both documents + fixture-replay byte-identity guard",
      "status": "done",
      "phase": "P4",
      "depends_on": [
        "T41",
        "T02",
        "T13"
      ],
      "files": [
        "src/server/tailor/engine.ts",
        "src/server/tailor/letter-prompt.ts",
        "src/server/routes/applications.ts",
        "test/voice-conditioning.test.ts"
      ],
      "origin": "spec Phase 4 (voice conditions both documents; fixture-replay guard)",
      "context": "Voice reaches the composition input of BOTH documents (oracle.md: voice conditions the resume tailor's composition AND the letter; excluded from grounding; rides the user message like context; ABSENT voice ⇒ byte-identical user message ⇒ existing decision fixtures replay untouched — the single most important stability guard).\n\nEXISTS: engine.ts buildUserPrompt(jd, context?, budget?) :34-45 (guards byte-identity when context/budget absent); ProviderEngine.decide/decideLetter; letter-prompt.ts buildLetterUserPrompt (T02, already has a guarded voice seam). applications.ts /tailor + /generate-letter (T13) load profile — voiceSources available there. validateNoFabrication grounding set stays `entries` (voice excluded — do NOT add voice to grounding).\n\nBUILD:\n1. engine.ts: add an optional `voice?` param to buildUserPrompt (resume) and to decide/tailor plumbing, appending a labelled voice-exemplar block ONLY when voice is present — guarded so that ABSENT voice yields a byte-identical string to today's output (the fixture-replay guard). Same for the letter path in letter-prompt.ts / decideLetter (finalize the T02 seam).\n2. applications.ts: pass profile.voiceSources (their `text`) into BOTH tailor(...) and tailorLetter(...) at generate time. Voice is style exemplars — passed as composition input, never into the grounding set.\n\nCRITICAL: the no-voice path MUST be byte-identical to the pre-epic composed message. BEFORE editing engine.ts, capture the CURRENT buildUserPrompt(jd,context,budget) output for a fixed input as a GOLDEN string literal committed into the test (or `git show HEAD:src/server/tailor/engine.ts` reference) — the test must diff against THAT golden, NOT against a value this same change generates (self-referential = tautology). NOTE: the 'existing fixtures still replay' check is a WEAK guard — FixtureEngine matches on hashKey(jd,entries) and never calls buildUserPrompt, so it passes even if buildUserPrompt is broken; it is a keying regression check ONLY, not the byte-identity proof. The byte-identity proof is the golden diff + the end-to-end captured-message assertion through the real route. Voice is excluded from validateNoFabrication.\n\nDO NOT: copy voice sentences (exemplars, not templates); add voice to grounding. Touch ONLY declared files.",
      "acceptance": "Baseline + test/voice-conditioning.test.ts:\n- GOLDEN byte-identity (NOT self-generated): with NO voice, buildUserPrompt(jd,context,budget) equals a golden string literal captured from PRE-T43 engine.ts, char-for-char — asserted SEPARATELY for the resume message AND the letter message (buildLetterUserPrompt).\n- the 'existing decision fixtures replay' check is labelled a KEYING regression only (FixtureEngine matches on hashKey, independent of buildUserPrompt) — NOT the byte-identity proof.\n- END-TO-END plumbing: set profile.voiceSources in the DB, POST /tailor AND /generate-letter through a SPY engine capturing the ACTUAL composed message the ROUTE sent; assert it CONTAINS the voice block (proves step-2 route plumbing on BOTH surfaces — not a buildUserPrompt called in isolation). With no voice sources, the captured message contains no voice block.\n- FABRICATION end-to-end (not a validate.ts signature check): a voice source with a number absent from all entries' facts + baseSummary, run through the ACTUAL tailor()/tailorLetter() orchestration (a fixture/spy decision echoing that number into item/paragraph text) ⇒ FabricationError thrown by the orchestrator — proves voice text never enters the entries/baseSummary grounding args.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T43.txt"
    },
    {
      "id": "T44",
      "title": "Voice UI: Profile management + flag affordance + retroactive-import e2e",
      "status": "done",
      "phase": "P4",
      "depends_on": [
        "T42",
        "T43",
        "T34"
      ],
      "files": [
        "src/client/components/ProfileEditor.tsx",
        "src/client/components/ApplicationDetail.tsx",
        "src/client/api.ts",
        "src/client/hooks/queries.ts",
        "src/client/queries/useApplications.ts",
        "test/e2e/applications.spec.ts"
      ],
      "origin": "spec Phase 4 (flag affordance; Profile-editor management; retroactive-import flow)",
      "context": "The voice UI: a 'use as a voice source' flag on outputs, and voiceSources manageable (list/delete) in the Profile editor (oracle.md: capture is a byproduct of normal use — edit → flag — never a form; the retroactive-import path is create app → blank letter → hand-author → flag). COARSE — decompose if larger than one session.\n\nEXISTS: ProfileEditor.tsx :64-81/166-222 — the links add/remove bounded-list pattern to REPLICATE for voiceSources (list existing, delete each; NO add-by-paste — flagging is the only add). ApplicationDetail.tsx letter+resume surfaces (T24/T34). api.ts/useApplications.ts (flag/delete hooks to add); queries.ts useProfile/useUpdateProfile :69-81.\n\nBUILD:\n1. ApplicationDetail.tsx: a 'Use as a voice source' flag button on the letter output and on the resume output → calls POST flag-voice (add the api fn + hook). Enabled even when locked (flagging is permitted locked).\n2. ProfileEditor.tsx: a voiceSources section listing each source (kind + a snippet) with a delete button — the links-list idiom (:166-222); NO paste-in add (tripwire).\n3. e2e in test/e2e/applications.spec.ts — the RETROACTIVE-IMPORT flow: create an application → create a blank letter (T32) → hand-author paragraphs (T34 editing) → flag it → assert the authored text is a voice source, byte-identical to what was typed (read it back via profile).\n\nDO NOT: add a Profile paste-in voice box (tripwire). Touch ONLY declared files.",
      "acceptance": "Baseline + `bunx playwright test --project=applications` green (new test titles confirmed present in the run):\n- retroactive-import e2e: CLICK the actual blank-letter UI affordance (not a DB/API backdoor) → hand-author paragraphs with distinctive whitespace/punctuation across MULTIPLE parts → flag → read profile.voiceSources via a FRESH server fetch (not the query cache) → EXACT-equal the test's own literal constant, including the exact part-join delimiter.\n- flag on a LOCKED app: actually CLICK the flag button ⇒ 200 / a new voice source appears (not just 'button enabled').\n- DELETE: after delete, reload + re-fetch /api/profile ⇒ the source is gone server-side (not just optimistic DOM removal).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T44.txt",
      "filesAmendment": "declared src/client/queries/queries.ts was an intake TYPO — real file is src/client/hooks/queries.ts (where useProfile/useUpdateProfile live); builder correctly touched the real file. Ledger 0076."
    },
    {
      "id": "T45",
      "title": "Voice-on/off eval recorded with provenance (KEY-GATED)",
      "status": "done",
      "phase": "P4",
      "depends_on": [
        "T43",
        "T42"
      ],
      "files": [
        "scripts/eval-voice.ts",
        "test/fixtures/voice/manifest.json",
        "test/fixtures/voice/transcript.md"
      ],
      "origin": "spec Phase 4 (the 'voice works' key-gated model-quality claim)",
      "context": "KEY-GATED. The 'voice works' proof recorded honestly (oracle.md: voice-on vs voice-off over a fixed JD + Library ⇒ DIFFERENT PHRASING with IDENTICAL selection and order — same entryId sets, same ranks; recorded with provenance like v1's T014; never inferred from a green suite).\n\nEXISTS: scripts/eval.ts + eval-budget.ts (the key-gated harness pattern: key-gate first, no FixtureEngine fallback). SEED_ENTRIES; CONTRAST_JDS; voice conditioning (T43); flag route (T42). Model gemini-2.5-flash; key GOOGLE_GENERATIVE_AI_API_KEY.\n\nBUILD:\n1. NEW scripts/eval-voice.ts: key-gate first. PIN temperature as low/deterministic as the provider allows for BOTH runs (so any phrasing difference is INPUT-attributable, not sampling noise — 'strings differ' between two live calls is nearly free and proves nothing) and record the temperature in provenance. Over a fixed JD + SEED_ENTRIES, run the resume tailor (and/or the letter) with NO voice vs WITH a fixed, deliberately-distinctive voice exemplar. Assert: phrasing differs AND selection+order IDENTICAL (full ORDERED entryId sequence + each item's rank match position-for-position — set equality would miss a reorder). The per-surface verdict line must AFFIRMATIVELY confirm the voice-on output adopts the exemplar's distinctive register (not merely 'text differs'). Provenance (model id, usage from the SDK response, timestamp, temperature) is WRITTEN BY THE SCRIPT, never hand-edited.\n2. RUN it (needs the key) and write test/fixtures/voice/manifest.json (provenance) + transcript.md (the voice-on/off outputs + verdict lines).\n\nIf the key is ABSENT: STOP and report — do NOT fake the recording (a recording without provenance does not count). Escalation allowed here.\n\nDO NOT: infer 'voice works' from the keyless suite; change selection/order behavior. Touch ONLY declared files.",
      "acceptance": "- scripts/eval-voice.ts compiles + key-gates (refuses without the key); pins low/deterministic temperature for both runs (recorded in provenance).\n- test/fixtures/voice/manifest.json provenance (model id, usage from the SDK response, timestamp, temperature) is WRITTEN BY THE SCRIPT (not hand-edited) — mirrors record-fixtures.ts.\n- transcript.md: voice-on vs voice-off with a verdict line that AFFIRMATIVELY confirms the voice-on output adopts the exemplar's distinctive register (not merely 'strings differ' — that is sampling noise).\n- selection/order IDENTICAL: the full ORDERED entryId sequence AND each item's explicit rank match position-for-position (not set equality).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T45.txt"
    },
    {
      "id": "T06",
      "title": "Keyless letter-flip contrast guard over committed fixtures (repair)",
      "status": "done",
      "phase": "P0",
      "depends_on": [
        "T05",
        "T16"
      ],
      "files": [
        "test/letter-flip-keyless.test.ts"
      ],
      "origin": "repair: P0 coverage-audit gap — flip/contrast never asserted keyless over test/fixtures/letters/*",
      "context": "COVERAGE-AUDIT REPAIR. Phase 0 requires the letter-flip contrast to replay KEYLESSLY, but no test asserts it over the committed fixtures. letterFlipContrast/letterFlipPredicate (src/server/tailor/evalcore.ts) are tested only on synthetic data (test/evalcore-letter.test.ts) and over the live model (scripts/eval-letter.ts, key-gated). manifest.leadingEntryId is read by no test. A hand-edited fixture could silently break the flip and the keyless suite stays green. Close the gap with a NEW keyless vitest test.",
      "acceptance": "NEW test/letter-flip-keyless.test.ts (keyless, READS the committed files, not inline data): (1) load test/fixtures/letters/{platform-sdk,rules-engine,frontend-rewrite}.json + manifest.json. (2) PER FIXTURE the LEAD body paragraph groundedOn includes manifest.leadingEntryId for that fixture, via letterFlipPredicate (platform-sdk to cloudcase-platform-sdk; rules-engine to cloudcase-rules-engine; frontend-rewrite to cloudcase-frontend-rewrite). (3) CONTRAST: letterFlipContrast over the 3 real fixtures letters is true (pairwise-non-empty unions BOTH directions; superset trap rejected). (4) NEGATIVE CONTROL, REQUIRED anti-vacuity: mutate a loaded COPY (swap a fixture lead groundedOn to the wrong entry; AND separately make one letter ground-on-everything) and assert letterFlipPredicate / letterFlipContrast FAIL on the mutated copy, proving the test catches a broken/laundered fixture. Baseline: bun run check exit 0, bunx vitest run all green.",
      "attempts": [],
      "evidence": ".ailoop/evidence/T06.txt"
    },
    {
      "id": "T07",
      "title": "Letter-preview typography pixel-diff (P2 coverage-audit repair)",
      "status": "done",
      "phase": "P2",
      "depends_on": [
        "T23",
        "T24"
      ],
      "files": [
        "test/e2e/applications.spec.ts"
      ],
      "origin": "repair: P2 coverage-audit WEAK — spec bullet 'changing typography visibly re-renders the letter preview (pixel-diff)' has no direct assertion; covered only by a mocked re-render + a real letter-canvas pixel-diff on a text-edit trigger + the resume typography pixel-diff (design.spec.ts). This asserts it directly on the letter preview.",
      "context": "COVERAGE-AUDIT REPAIR (low severity, behavior near-certain — same shared format engine the resume typography pixel-diff already exercises; this closes the LITERAL spec bullet). Add ONE new e2e to test/e2e/applications.spec.ts (the `applications` playwright project — design.spec.ts is also in it, but this belongs with the letter surface). SEAMS: ApplicationDetail.tsx renders BOTH the DesignPanel (format/typography controls) AND LetterPreview under [data-testid=\"letter-preview\"], both driven by the same resolvedFormat — so a typography change on that page re-renders the letter preview. Mirror the EXISTING patterns: (1) design.spec.ts changes typography via page.getByRole('combobox', { name: 'Body font' }).click() then selecting a font, awaiting the format PUT 200; (2) applications.spec.ts already pixel-diffs the letter canvas via [data-testid=\"letter-preview\"] canvas .evaluate(el => el.toDataURL()) and has expectLocatorCanvasPainted + createApplicationViaUi helpers + the generate-letter flow (see the existing T24/T34 letter tests in this file). Keyless (FixtureEngine).",
      "acceptance": "New e2e in test/e2e/applications.spec.ts, `applications` project green (new title appears in the run): (1) create app -> tailor -> generate letter; wait for [data-testid=\"letter-preview\"] canvas to PAINT (expectLocatorCanvasPainted — proves the before-capture is real content, not blank). (2) capture the LETTER canvas dataURL (letterBefore). (3) change a TYPOGRAPHY control in the DesignPanel (e.g. Body font combobox, mirroring design.spec.ts) and await the format PUT 200 — the ONLY change between captures is typography (NO letter regeneration/edit, NO content change). (4) poll [data-testid=\"letter-preview\"] canvas dataURL until it .not.toBe(letterBefore) with a timeout (the established pixel-diff pattern). ANTI-CHEAT (must all hold): the diff is asserted on the LETTER canvas ([data-testid=\"letter-preview\"]), NOT the resume .document-preview canvas; the trigger is a typography/format change, NOT a text edit or regenerate; the before-capture is a painted (non-blank) canvas. Baseline: bun run build (FIRST — stale dist fails the applications project), bun run check 0, bunx vitest run green, bunx playwright test --project=applications green (never run playwright projects concurrently; @fontsource flake -> isolated re-run).",
      "attempts": [],
      "evidence": ".ailoop/evidence/T07.txt"
    }
  ]
}
