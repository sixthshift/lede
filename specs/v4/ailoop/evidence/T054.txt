T054 — Preview zoom expand-in-place ≥xl ephemeral + frame-on-rerender (OQ6/F507). Re-verify branch t054-preview-zoom tip c6eb5a1, baseSha 501762b.
SCOPE (excl .ailoop): WorkspaceShell, DocumentPreview, preview-zoom.spec(new), playwright.config(reg). All declared. CLEAN.
BASELINE: check 0 · build 0 · lint 0 (264) · vitest 1077/1077. preview-zoom 4/4 + pane-arbitration/cohesion 14/14 on branch.
GAMING CLEAN: zoom = REAL layout width (PREVIEW_ZOOMED_WIDTH_CLASS "w-[clamp(608px,48vw,960px)]" ~1.6x, swapped for PREVIEW_PROPORTIONAL_WIDTH_CLASS) — NOT transform:scale; editor shrinks via flex, stays ≥240px, side-by-side (no overlay). EPHEMERAL: plain useState(false), NO localStorage/sessionStorage. Toggle (aria-pressed/aria-label ZoomIn/ZoomOut) renders ONLY in the coVisible (≥xl) branch. DocumentPreview keeps the page frame on re-render (no bare-text jump). Spec asserts: real reflow (editor layout width changed + elementFromPoint, transform:scale FAILS), ≥1.5× canvas, editor ≥240px, zero modality, restore-off; ephemeral via SPA navigate-away-back reset + no-storage-key snapshot; control querySelector===null@1279 vs present@1280; frame survives format-axis re-render. co-visibility (v3) intact. Full playwright at P4 gate.
