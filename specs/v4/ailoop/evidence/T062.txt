T062 — Library docked panels → sanctioned below-lg sheets (P5 modality-sweep repair). Re-verify branch t062-panel-sheets tip 3c45c1d, baseSha 187b242.
SCOPE (excl .ailoop): EntryEditor, LayoutEditor, ProfileEditor, panel-sheet.spec(new), playwright.config(reg). All declared. CLEAN.
BASELINE: check 0 · build 0 · lint 0 · vitest 1079/1079. panel-sheet 9 + docked-panel-bounds + cohesion + panel-craft 30/30.
GAMING CLEAN: each panel Content className `fixed inset-0 ... rounded-none [T043 anim] lg:inset-auto lg:bottom-6 lg:right-6 lg:max-h-[85vh] lg:w-[30rem/26rem] lg:max-w-[90vw] lg:rounded-xl` — REAL full-width sheet below lg (inset-0, not a shrunk floater), byte-identical docked at >=lg. modal={false} kept, NO aria-modal introduced (grep 0). T034 coarse 44px X-Close / T043 anim / T055 sticky-header preserved verbatim. panel-sheet.spec: 375+768 each panel is full-width sheet (bbox≈viewport width) + role=dialog no-aria-modal + Escape-closes + Close-closes + focus-managed; 1280 each panel docked (bbox≈w-30rem, NOT full-width). docked-panel-bounds (F103) green at 1280.
Fixes the T060 [0151] regression: the 3 panels no longer cover ~70% as floating overlays below lg — now sanctioned full-width sheets (OQ2). T060 sweep re-runs against this next.
