T011 — F109 contrast fixes — INDEPENDENT RE-VERIFY (coordinator), attempt 2
merged tree 96c87be (merge of branch worktree-agent-a2862cb07222232ba @0bdb513, baseSha 14e2c0f)

SCOPE — git diff --name-only 14e2c0f..0bdb513:
  src/client/styles/app.css, src/client/styles/tokens.css, test/ui-foundation.test.tsx
  => all declared (footprint expanded in attempt 2 to include ui-foundation.test.tsx). CLEAN.
GAMING READ: --ink-faint→--ink-soft on weight-bar/rationale labels; --success #15803d→#137337
  (lightness nudge within hue ~142°/72%, value not re-hue); accent-color:var(--accent) on :root.
  ui-foundation.test.tsx §12-verbatim assertion MOVED with the authorized change (still pins the
  full palette + no-Google-fonts + primary→accent); a real per-theme contrast suite ADDED (renders
  ReasoningPanel/Badge, toggles .dark, computes WCAG, hue-family guard, asserts no other test pins
  #15803d). Strengthened, not weakened. PASS.
BASELINE (full, coordinator-run on merged tree):
  check 0; lint 0; build 0
  bunx vitest run: 1071 passed / 1071 (ZERO flakes this run)
  playwright chromium 20; auth 1; applications 32 (incl. v3 cohesion.spec.ts)
ACCEPTANCE: ui-foundation F109 contrast suite + updated palette assertion all green within the 1071.
RATCHET: vitest 1061 -> 1071 (+10). playwright unchanged. Non-decreasing.
VERDICT: DONE.
