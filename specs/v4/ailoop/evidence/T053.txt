T053 — Settings split/center/≤1200px (F506). Re-verify branch t053-settings-split tip f9e5839, baseSha 39fdc3e.
SCOPE (excl .ailoop): SettingsView, settings.spec. All declared (NO DesignPanel edit needed — see reframe). CLEAN.
BASELINE: check 0 · build 0 · lint 0 (263) · vitest 1077/1077. settings 7/7 chromium (twice, stable).
REFRAME [0136]: the ≤1200px bound was ALREADY met by T041a's default-collapsed DesignPanel groups — format card measures 991px at default load (Provider 159.5, API key 210.5). So T053 = center column + assert bound + keep round-trips, all in SettingsView. Definition-of-done unchanged (bound kept + asserted strictly).
GAMING CLEAN: settings.spec ≤1200 test measures REAL per-card boundingBox().height (toBeLessThanOrEqual(1200), no scroller hides an oversized card). Centered: SettingsView mx-auto max-w-2xl; spec asserts |leftGap-rightGap|<=8 (real symmetric margin within the pane, rail-offset documented). Round-trips intact (provider/apiKey/format save+reload). Also fixed a T041a-collapse escape: Heading-weight Select in the now-collapsed Typography group → test expands it + reduced-motion for deterministic Radix open (same pattern as T041b's expandDesignGroup). rail-nav-format→"Default document format" heading (:64/:76) kept green.
