P3 PHASE-CLOSE GATE — GREEN on merged tree 3b586a1.
- build: exit 0
- playwright chromium: 25 passed
- playwright auth:      1 passed (harness fix [0083] + port-free)
- playwright applications: 109 passed + 1 flaky (applications.spec.ts:1392 rail-nav v3-T013 "preview
  must not remount") = 110. The :1392 failure is the PRE-EXISTING intermittent preview-remount flake
  (flagged flaky at the P2 gate [0084]); it PASSES cleanly on a fresh whole-file run (18/18 incl :1392)
  and in single-test isolation. Per the oracle flake discriminator (isolated/fresh pass = flake, not
  regression) it is not gate-blocking. 0 ERR_CONNECTION_REFUSED.
- tap-targets.spec.ts:240 (EntryEditor Close >=44px): FIXED [0110] — now waits for T043's zoom-in-95
  entry animation to settle before measuring (the control is 44px settled; test measured mid-animation).
- vitest: 1077/1077 (render-family float, per-file isolation clean).
- No docker at P3 (P1 + P5 only).
New P3 specs all green: toasts (8 enumerated + 2 failure-contrasts), export-busy, motion (band+reduced),
letter-empty, card-detail. v3 cohesion contracts green incl toast-non-modality.
NEW RATCHET FLOOR (P3 gate): vitest>=1077 · chromium>=25 · auth>=1 · applications>=110.
CARRIED RESIDUAL: applications.spec.ts:1392 rail-nav intermittent preview-remount flake (v3, predates v4)
→ T060/T061 (P5) to stabilize alongside the auth DATA_DIR residual.
