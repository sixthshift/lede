T030 — bottom tab bar below lg + rail hidden + section nav folds into editor (F301)
Independent re-verify by coordinator on MERGED tree (main HEAD 84e4d42), baseSha 90a1b80.

SCOPE (git diff --name-only 90a1b80..HEAD): playwright.config.ts, ApplicationDetail.tsx,
  NavTabs.tsx, WorkspaceShell.tsx, test/e2e/responsive-nav.spec.ts (+ .ailoop/ledger.md coordinator log).
  All ⊆ declared files + playwright registration allowlist. CLEAN.

FAST BASELINE (merged tree):
  check  → exit 0
  build  → exit 0
  lint   → exit 0 (Checked 250 files, no fixes)
  vitest → 83 files / 1077 tests passed (floor 1077; no render-family flake this run)

GAMING READ: CLEAN.
  - WorkspaceShell useIsBelowLg() (matchMedia, jsdom fallback = desktop) conditionally RENDERS
    rail<aside> XOR BottomTabBar — never both → querySelector===0 works both directions
    (below-lg rail absent; >=lg bottom-bar absent). Content panes get pb-14 clearing the h-14 bar.
  - NavTabs.BottomTabBar reuses the SAME TABS constant (no second nav model); flex-1 icon+label items.
  - ApplicationDetail folds a SECTIONS nav into the editor pane (lg:hidden, reachable below lg),
    distinct editor-section-nav-* testids so scroll-spy strict rail-nav-* locators still match one.
  - Spec responsive-nav.spec.ts is genuine: rail/bar ABSENCE asserted via count===0 (gone, not hidden);
    44px via real link boundingBox.height>=44; functional navigation clicks each tab + asserts URL;
    no-drawer via count===0; bar-covers-nothing via padding>=barHeight + elementFromPoint hit-test;
    section nav operability via design-heading scrollIntoView. No hardcoding/weakening.

Builder self-report (branch t030-responsive-nav): check/build/lint 0, vitest 1077, responsive-nav 5/5,
  regression sanity scroll-spy/chrome-merge/cohesion/card-bounds 22/22. Claim CONFIRMED by re-verify.

NOTE: full playwright (applications project incl. responsive-nav.spec) DEFERRED to P2 phase-close gate
  per cadence amendment [0033].
