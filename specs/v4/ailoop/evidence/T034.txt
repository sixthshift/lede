T034 — Coarse-pointer 44px tap targets (audited set) + single-column fields below sm (F305)
Independent re-verify on branch t034-tap-targets tip b3fecd4, baseSha 2e8ad9c (attempt 1 + structural resume).

SCOPE (git diff --name-only 2e8ad9c..HEAD, excl .ailoop): playwright.config.ts, ApplicationCard.tsx,
  ApplicationDetail.tsx, EntryEditor.tsx, NewApplication.tsx, SectionMetaFields.tsx, WorkspaceShell.tsx,
  tap-targets.spec.ts(new). All ⊆ declared (SectionMetaFields added [0079]). CLEAN.

FAST BASELINE: check 0 · build 0 · lint 0 (254 files) · vitest 1077/1077.

WHAT: 7 audited controls bumped to ≥44px short-axis via `[@media(pointer:coarse)]:min-h-[44px]`
  arbitrary-variant (Tailwind 3.4 has no coarse variant), applied to the REAL interactive element,
  gated to pointer:coarse so desktop rendering byte-unchanged. Bottom-tab-bar (h-14=56px) already
  passed, unmodified. Single-column-below-sm now via REAL responsive variant
  `grid-cols-1 sm:grid-cols-2` on SectionMetaFields' grid div (the earlier injected-<style> hack was
  removed at coordinator direction [0079] — structure now comes from the problem, not a scope workaround).

GAMING READ: CLEAN.
  - Coarse bumps on real elements, not container wrappers; excluded set NOT enlarged.
  - Anti-blanket-cheat guard: tap-targets.spec asserts a ui/select.tsx listbox OPTION (unowned, excluded)
    stays < 44px — a container-level blanket min-height would fail this (red-team). PASSES.
  - Spec confirms matchMedia('(pointer:coarse)').matches===true in every test's beforeEach (real coarse
    emulation via hasTouch+isMobile), measures each audited control's short-axis ≥44 on the REAL
    interactive locator (not a padded wrapper — structural: locator resolves to the button element).
  - Single-column test targets the real grid (getByTestId entry-meta-fields-grid): 1 track below sm,
    2 tracks at/above sm — genuine breakpoint swap, not a permanent collapse.
  - EntryEditor: 0 references to the removed hack (META_FIELDS_GRID/OVERRIDE_CSS/<style>).

Builder self-ran tap-targets.spec (applications) 10/10. Full playwright authoritatively at the P2 gate.
