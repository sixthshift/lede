// One editorial serif voice for a group's leadRationale — spec.md §11/§12.
// Reasoning UI only; never printed.

export function Callout({ text }: { text: string }) {
  return <p className="callout">{text}</p>;
}
